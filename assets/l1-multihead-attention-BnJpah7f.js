var e={id:`genai-l1-multihead-attention`,subjectId:`genai`,level:1,title:`Multi-Head Attention`,whyItMatters:`Splitting attention into heads costs exactly nothing — the parameter count is 4d² whether you use 1 head or 16. It buys the ability to attend to several different things at once, which one softmax cannot do.`,assumes:[`You have read *Self-Attention*`,`You can count parameters in a matrix`],estMinutes:18,sections:[{type:`intuition`,title:`One softmax can only look at one thing`,md:`Attention weights sum to 1, so a head has a fixed budget to spend. Resolving a pronoun and tracking the syntactic subject are two different searches, and one distribution cannot do both — weight given to one is taken from the other.

**Multi-head attention** runs several attention operations in parallel, each on its own slice of the vector, and concatenates the results.

The trick is that the slices are *carved out of the same d*, not added on top. With d = 512 and 8 heads, each head works in 64 dimensions. So you get eight independent attention patterns for **exactly the same parameter count** as one — which is why it is not a trade-off at all.`},{type:`code`,lang:`python`,title:`The parameter count is the same at every head count`,code:`d = 512
for h in [1, 2, 8, 16]:
    dk = d // h
    print('h=%2d  d_k=%3d  W_q,W_k,W_v: %d x %d each, %2d heads -> %s  (+ W_o %s)'
          % (h, dk, d, dk, h, f'{3*h*d*dk:,}', f'{d*d:,}'))
print('total, every h:  4 * d^2 = %s' % f'{4*d*d:,}')

# ---- real output ----
# h= 1  d_k=512  W_q,W_k,W_v: 512 x 512 each,  1 heads -> 786,432  (+ W_o 262,144)
# h= 2  d_k=256  W_q,W_k,W_v: 512 x 256 each,  2 heads -> 786,432  (+ W_o 262,144)
# h= 8  d_k= 64  W_q,W_k,W_v: 512 x 64 each,  8 heads -> 786,432  (+ W_o 262,144)
# h=16  d_k= 32  W_q,W_k,W_v: 512 x 32 each, 16 heads -> 786,432  (+ W_o 262,144)
# total, every h:  4 * d^2 = 1,048,576`,annotations:{3:`Each head's projection is d x d_k, and there are h of them — but h times d_k is d, so the product is d² regardless of how you split it.`,13:`786,432 at every head count, exactly. More heads means smaller heads, not more parameters.`,14:`W_o is the output projection that mixes the concatenated head outputs back together. It is essential: without it the heads would be concatenated and never combined, so each would only ever influence its own slice of the next layer.`}},{type:`code`,lang:`python`,title:`Two heads, the same input, genuinely different patterns`,code:`import numpy as np, math
np.random.seed(1)
X = np.random.randn(4, 8)

def softmax(x):
    e = np.exp(x - x.max(axis=-1, keepdims=True))
    return e / e.sum(axis=-1, keepdims=True)

for h in range(2):
    s = slice(h*4, (h+1)*4)
    Q = K = X[:, s]
    print('head %d:' % h, np.round(softmax(Q @ K.T / math.sqrt(4)), 3).tolist())

# ---- real output ----
# head 0: [[0.533, 0.166, 0.048, 0.253], [0.087, 0.802, 0.019, 0.092], [0.198, 0.148, 0.426, 0.228], [0.387, 0.267, 0.084, 0.262]]
# head 1: [[0.953, 0.042, 0.002, 0.002], [0.482, 0.344, 0.106, 0.069], [0.035, 0.142, 0.702, 0.121], [0.081, 0.21, 0.275, 0.434]]`,annotations:{9:`Each head sees a different slice of the same 8 dimensions. Here the slices are just the first and second halves; in a real model the learned projections decide what each head gets to look at.`,14:`Token 1: head 0 spreads its weight (0.533 / 0.166 / 0.048 / 0.253) while head 1 concentrates almost entirely on itself at 0.953. Same input, same token, two completely different readings.`,15:`Token 2 shows it the other way round: head 0 concentrates on token 2 at 0.802 while head 1 spreads across 0.482 and 0.344. Neither head is right — they are looking for different things, which is exactly the point.`}},{type:`note`,label:`What heads actually learn — and the caveat`,md:`Interpretability work has found heads that do specific, nameable jobs: attending to the previous token, tracking syntactic dependencies, matching brackets, and **induction heads** that find an earlier occurrence of the current token and copy what followed it — which is a large part of the mechanism behind in-context learning.

Two honest caveats. Most heads are not interpretable, and reading meaning into a head is easy and often wrong. And **many heads can be pruned with almost no loss** — the Michel et al. result found that a large fraction of heads can be removed at inference with negligible degradation, which suggests substantial redundancy rather than sixteen distinct specialists.

So the right summary is that multi-head attention provides *capacity for* multiple patterns, and training uses some of it.`},{type:`note`,label:`How many heads, and where the head dimension bites`,md:`The usual choice puts **d_k around 64**: GPT-2 small is d = 768 with 12 heads, LLaMA-2 7B is d = 4096 with 32 heads. Both land at exactly 64 per head.

That is not arbitrary. Too few dimensions per head and each head cannot represent a rich enough query — 8 dimensions is not much of a search. Too many and you are back to a single head with a single budget.

**More heads is not free in every sense.** Attention cost still scales with n² per head, and although the parameter count is fixed, the number of separate small matrix multiplications rises, which is less efficient on a GPU than one large one — the reason implementations do all heads in one batched operation rather than a loop.

The important variant to know is **grouped-query attention**, where several query heads share one key/value head. It leaves the query capacity intact while cutting the KV cache by the sharing factor, which is the dominant memory cost at long context — LLaMA-2 70B uses 64 query heads with 8 KV heads, an eightfold cache reduction.`}],quiz:[{question:`Why does one attention head struggle to do two jobs at once?`,options:[{text:`It has too few parameters`,explanation:`Parameter count is not the constraint.`},{text:`Its weights sum to 1, so it has a fixed budget — weight given to one target is taken from another`,explanation:`Correct, and that is exactly what multiple heads relieve.`},{text:`The softmax is too slow`,explanation:`Speed is unrelated.`},{text:`It can only see nearby tokens`,explanation:`A head sees the whole sequence.`}],correct:1},{question:`The parameter count came out at 786,432 for 1, 2, 8 and 16 heads. Why?`,options:[{text:`A coincidence of d = 512`,explanation:`It holds for any d.`},{text:`Each head's projection is d × d_k and h·d_k = d, so the product is d² regardless of the split`,explanation:`Correct — more heads means smaller heads, not more parameters.`},{text:`The heads share weights`,explanation:`Each head has its own projections.`},{text:`Only W_o was counted`,explanation:`W_o is listed separately at 262,144.`}],correct:1},{question:`What does W_o, the output projection, do?`,options:[{text:`Normalises the output`,explanation:`That is the norm layer's job.`},{text:`Mixes the concatenated head outputs — without it, each head would only ever influence its own slice of the next layer`,explanation:`Correct, which is why it is essential rather than decorative.`},{text:`Applies the softmax`,explanation:`The softmax is inside each head.`},{text:`Splits the input into heads`,explanation:`That happens with the input projections.`}],correct:1},{question:`On token 1, head 0 gave [0.533, 0.166, 0.048, 0.253] and head 1 gave [0.953, 0.042, 0.002, 0.002]. What is the reading?`,options:[{text:`Head 1 is more confident and therefore better`,explanation:`Neither head is right; they are looking for different things.`},{text:`Same input, same token, two completely different patterns — which is the entire purpose of having heads`,explanation:`Correct, and on token 2 the roles reverse.`},{text:`Head 0 is broken`,explanation:`A spread distribution is a valid pattern.`},{text:`The random seed differed`,explanation:`Both heads read the same X in the same run.`}],correct:1},{question:`What did the head-pruning results show?`,options:[{text:`Every head is essential`,explanation:`The opposite.`},{text:`A large fraction of heads can be removed at inference with negligible loss, suggesting substantial redundancy`,explanation:`Correct, which tempers the story that each head is a distinct specialist.`},{text:`Pruning heads improves accuracy`,explanation:`It is roughly neutral, not an improvement.`},{text:`Only the first layer's heads matter`,explanation:`Redundancy is spread across layers.`}],correct:1},{question:`What does grouped-query attention change?`,options:[{text:`It reduces the number of query heads`,explanation:`Query capacity is left intact.`},{text:`Several query heads share one key/value head, cutting the KV cache by the sharing factor — LLaMA-2 70B uses 64 query heads with 8 KV heads`,explanation:`Correct, and the KV cache is the dominant memory cost at long context.`},{text:`It removes the output projection`,explanation:`W_o remains.`},{text:`It makes attention linear in sequence length`,explanation:`The n² cost is unchanged.`}],correct:1}],interviewQuestions:[{question:`Why multiple attention heads?`,answer:`Because a single head has a fixed budget — its weights sum to 1 — so it cannot attend strongly to two different things at once, and resolving a pronoun and tracking a syntactic dependency are genuinely different searches. Multiple heads run several attention operations in parallel on different slices of the vector. The part worth stressing is that it is free: the slices are carved out of the same d, so with d = 512 the parameter count is 786,432 whether you use 1 head or 16 — h times d_k is always d. You get several independent attention patterns for exactly the cost of one.`,isCaseBased:!1},{question:`How do you choose the number of heads?`,answer:`By targeting a head dimension around 64, which is what essentially every model does — GPT-2 small at d = 768 with 12 heads, LLaMA-2 7B at d = 4096 with 32 heads, both landing at exactly 64. The reasoning is a squeeze from both sides: too few dimensions per head and the head cannot represent a rich enough query, since 8 dimensions is not much of a search space; too many and you are back to one head with one budget. It is empirical rather than derived, and worth noting that head count divides d exactly, so it is constrained by the model width rather than freely chosen.`,isCaseBased:!1},{question:`What do individual heads learn?`,answer:`Some have identifiable jobs — attending to the previous token, tracking syntactic dependencies, matching brackets — and the most important named example is the induction head, which finds an earlier occurrence of the current token and copies what followed it. That mechanism is a substantial part of how in-context learning works, which makes it one of the genuine successes of interpretability. But I would give the caveat honestly: most heads are not interpretable, reading meaning into a head is easy and often wrong, and the pruning results show a large fraction can be removed at inference with negligible loss. So multi-head attention provides capacity for multiple patterns, and training uses some of it.`,isCaseBased:!0},{question:`What is grouped-query attention and why does it exist?`,answer:`Several query heads share a single key/value head. Query capacity is untouched, but the KV cache shrinks by the sharing factor — LLaMA-2 70B uses 64 query heads with 8 KV heads, an eightfold reduction. It exists because at long context the KV cache dominates inference memory: it grows linearly with sequence length and layer count, and for a 7B model at 32K context it runs to tens of gigabytes per sequence, which caps how many requests you can batch. Multi-query attention is the extreme version with a single KV head, faster still but with a measurable quality cost, which is why grouped-query became the standard compromise.`,isCaseBased:!1},{question:`Why is the output projection W_o necessary?`,answer:`Because without it the heads are concatenated and never actually combined. Each head's output would occupy its own fixed slice of the vector passed to the next layer, so head 3 could only ever influence dimensions 192 to 255 and nothing else — the heads would run in parallel channels that never talk. W_o is a full d × d mixing matrix that lets every head's output contribute to every dimension downstream. It also means the heads are not really independent in effect: the model can learn to combine them, which is part of why individual head interpretations are unreliable.`,isCaseBased:!1},{question:`How is multi-head attention implemented efficiently?`,answer:`As one batched operation, never as a loop over heads. The input projections for all heads are a single d × d matrix multiply, then the result is reshaped to separate the head dimension, and the attention itself is a batched matrix multiply across heads. That matters because GPUs are far more efficient on one large matrix multiplication than on many small ones — a loop over 32 heads would be dominated by kernel launch overhead and poor utilisation. It is also why the parameter layout matters: the projections are stored as one matrix precisely so they can be applied as one.`,isCaseBased:!0},{question:`Would you use fewer, larger heads or more, smaller ones?`,answer:`I would stay near d_k = 64 unless there is a specific reason to move, because that is where the field converged and both directions have a clear cost. Fewer, larger heads gives each head a richer query space but fewer independent patterns, and moves back toward the single-budget problem multi-head exists to solve. More, smaller heads gives more patterns but each is representationally thin — at d_k = 8 a head cannot express much of a search. Given that pruning studies show substantial redundancy at the standard setting, I would be more inclined to reduce head count than increase it if I were optimising, and grouped-query attention is the better lever anyway since it targets the actual bottleneck.`,isCaseBased:!1},{question:`What is an induction head and why does it matter?`,answer:`A pair of heads across two layers that implement a copy pattern: the first attends to the previous token, and the second uses that to find an earlier occurrence of the current token and attend to whatever followed it. So having seen "A B" earlier in the context, when the model next sees "A" it can predict "B". It matters because it is a concrete, mechanistically understood circuit for in-context learning — the ability to pick up a pattern from the prompt without any weight update — and because its appearance during training coincides with a sharp improvement in in-context learning ability. That makes it one of the few cases where an emergent capability has a traceable implementation.`,isCaseBased:!1}],flashcards:[{front:`Why heads`,back:`One head's weights sum to 1 — a fixed budget. Two different searches cannot share it. Heads give several patterns at once.`},{front:`The cost`,back:`ZERO. 786,432 parameters at h = 1, 2, 8 and 16, because h·d_k = d always. More heads means smaller heads.`},{front:`W_o`,back:`Mixes the concatenated head outputs. Without it, each head would only ever influence its own slice of the next layer.`},{front:`The two-head demo`,back:`Token 1: head 0 spreads (0.533/0.166/0.048/0.253), head 1 concentrates (0.953). On token 2 the roles reverse.`},{front:`What heads learn`,back:`Some are nameable — previous-token, bracket-matching, INDUCTION heads (find an earlier occurrence, copy what followed).`},{front:`The pruning caveat`,back:`A large fraction of heads can be removed at inference with negligible loss. Substantial redundancy, not 16 distinct specialists.`},{front:`How many heads`,back:`Target d_k ≈ 64. GPT-2 small: 768/12. LLaMA-2 7B: 4096/32. Both exactly 64.`},{front:`Grouped-query attention`,back:`Several query heads share one KV head. LLaMA-2 70B: 64 query, 8 KV — an 8x KV cache cut, with query capacity intact.`}],mindmapMarkdown:`- Multi-head attention
  - Why
    - one head's weights sum to 1: a fixed budget
    - pronoun resolution and syntax are different searches
    - heads run several patterns in parallel
  - The cost is zero
    - slices carved OUT OF d, not added on top
    - 786,432 params at h = 1, 2, 8, 16
    - h * d_k = d always
    - W_o mixes them back, or heads never combine
  - What they do
    - two heads, same token: 0.533-spread vs 0.953-focused
    - some are nameable: previous-token, brackets
    - INDUCTION heads -> in-context learning
    - but most are not interpretable
    - and many can be pruned with no loss
  - How many
    - target d_k ~ 64
    - GPT-2 small 768/12, LLaMA-2 7B 4096/32
    - too few dims: thin query. too many: one budget again
    - implement as ONE batched matmul, never a loop
  - Grouped-query attention
    - query heads share KV heads
    - LLaMA-2 70B: 64 query, 8 KV
    - 8x KV cache cut - the real bottleneck at long context`};export{e as default};