var e={id:`cpp-l0-functions-arrays`,subjectId:`cpp`,level:0,title:`Functions, Pass-by-Value vs Reference & Arrays`,whyItMatters:`Two of the fastest ways to fail a C++ round: pass a vector by value in a hot loop, or trust sizeof on an array parameter. Both traps live here. Learn the three ways a parameter travels and the two ways a C array betrays you, and a whole family of "gotcha" questions turns into free points.`,estMinutes:40,sections:[{type:`intuition`,title:`Declaration vs definition: the menu and the kitchen`,md:`A restaurant menu lists "butter chicken — ₹350". That is a **declaration**: name, what goes in, what comes out. The kitchen recipe is the **definition**: how it is actually made.

- A *declaration* (also called a **prototype**) gives the compiler the function's signature: name, parameter types, return type. No body: \`int add(int a, int b);\`
- A *definition* is the declaration **plus the body**. It is the thing that actually runs.
- The compiler reads a file top to bottom. At a call site it only needs the *menu* — the declaration — to check types.
- Declare as many times as you like. Define **exactly once** in the whole program (this rule has a name: the One Definition Rule).
- This split is not bureaucracy — it is what makes multi-file programs possible. Next note explains how.`},{type:`code`,lang:`cpp`,title:`Prototype up top, body wherever you like`,code:`#include <iostream>

int add(int a, int b);   // declaration: signature + semicolon, no body

int main() {
    std::cout << add(2, 3);   // OK — the compiler has seen the promise
    return 0;
}

int add(int a, int b) {   // definition: the body, exactly one per program
    return a + b;
}`,annotations:{3:`The prototype. Parameter names are optional here — int add(int, int); is the same declaration.`,6:`Without line 3, this call fails: the compiler reads top-down and has not met add yet.`,10:`The linker later connects every call of add to this one body. Two bodies = linker error.`}},{type:`note`,md:'Why headers exist, in one breath: a `.h` file holds **declarations**, a `.cpp` file holds **definitions**. `#include "math_utils.h"` is literal copy-paste — it drops the declarations into your file, so the compiler can type-check your calls without ever seeing the bodies. Each `.cpp` compiles alone; at the end the **linker** (the tool that stitches compiled files into one executable) matches every call to its single definition. That is the entire secret: headers are shared menus, `.cpp` files are private kitchens.'},{type:`intuition`,title:`Pass-by-value: every call is a photocopy`,md:"Hand a clerk a photocopy of your document. Scribble all they want — your original never changes.\n\n- `void f(int n)` — plain type, no `&` — is **pass-by-value**: the argument is **copied** into a brand-new variable `n`.\n- The callee mutates the copy. The caller's variable is untouched. Always.\n- For an `int`, the copy costs nothing. For a `std::string` holding 10 MB or a `std::vector` of a million elements, **every call duplicates the whole thing**.\n- The copy is destroyed the moment the function returns. All that work, thrown away.\n- This is C++'s default. It does exactly what you wrote — no silent \"big objects go by reference\" magic like Java or Python."},{type:`visual`,component:`PointerBoxDiagram`,props:{title:`Value vs reference, frame by frame`,notice:`By value: a second box appears. By reference: a second NAME appears, same box.`,leftLabel:`names in scope`,rightLabel:`boxes in memory`,frames:[{note:`Caller: int score = 80. One name, one box.`,stack:[{name:`score`,to:`b1`}],heap:[{id:`b1`,value:`80`,label:`caller's box`}]},{note:`Call byValue(score). Parameter n gets its OWN box; 80 is copied in.`,stack:[{name:`score`,to:`b1`},{name:`n (by value)`,to:`copy`}],heap:[{id:`b1`,value:`80`,label:`caller's box`},{id:`copy`,value:`80`,label:`copy`}]},{note:`Callee runs n = 100. The write lands in the copy. score still reads 80.`,stack:[{name:`score`,to:`b1`},{name:`n (by value)`,to:`copy`}],heap:[{id:`b1`,value:`80`,label:`caller's box`},{id:`copy`,value:`100`,label:`copy`}]},{note:`byValue returns. The copy dies. The caller never felt a thing.`,stack:[{name:`score`,to:`b1`}],heap:[{id:`b1`,value:`80`,label:`caller's box`}]},{note:`Now call byRef(score). int& n is a second name glued to the SAME box. No copy.`,stack:[{name:`score`,to:`b1`},{name:`n (int&)`,to:`b1`}],heap:[{id:`b1`,value:`80`,label:`caller's box`}]},{note:`Callee runs n = 100. One box, two names — score IS 100 now. The mutation lands.`,stack:[{name:`score`,to:`b1`},{name:`n (int&)`,to:`b1`}],heap:[{id:`b1`,value:`100`,label:`caller's box`}]}]}},{type:`intuition`,title:`References — and why const T& is the default for big things`,md:'A **reference** (`int& n`) is a second name permanently glued to the caller\'s variable. Not a copy, not an address you juggle — an alias.\n\n- `void f(T& n)` — pass-by-reference: zero copy, and writes to `n` **land on the caller\'s object**. Use it when mutating is the point.\n- `void f(const T& n)` — reference, but **read-only**: zero copy AND the compiler rejects any write to `n`. Best of both.\n- The decision rule interviewers want, verbatim: *small types (`int`, `double`, `char`, pointers) → by value. Big types (`std::string`, `std::vector`, your structs) read-only → `const T&`. Need to modify the caller\'s object → `T&`.*\n- `const T&` also accepts temporaries — `f("hello")` works — while plain `T&` refuses them.\n- One warning: `&` **in a parameter type** means reference. The same symbol *on a variable* (`&x`) means "address-of". Same character, two jobs — the pointers module untangles it fully.'},{type:`code`,lang:`cpp`,title:`The three passing styles, side by side`,code:`void byValue(std::string s)  { s += '!'; }
void byRef(std::string& s)   { s += '!'; }
void byConstRef(const std::string& s) {
    // s += '!';   <- refuses to compile: s is read-only here
}

int main() {
    std::string msg = "hi";
    byValue(msg);      // a copy got the '!', then the copy was destroyed
    // msg == "hi"
    byRef(msg);        // no copy — the '!' lands on msg itself
    // msg == "hi!"
    byConstRef(msg);   // no copy AND no risk of mutation
}`,annotations:{1:`A fresh std::string is copy-constructed on EVERY call. For big strings that is real memory and real time.`,2:`& in the parameter type = reference: s is another name for the caller's string.`,3:`const T& — the workhorse. No copy, no writes allowed. Default for any read-only parameter bigger than a pointer.`,9:`Desk-check it: copy made, copy mutated, copy destroyed. msg untouched.`,11:`Same call syntax as by-value — the signature alone decides. Read signatures, not call sites.`}},{type:`code`,lang:`cpp`,title:`Overloading in 60 seconds`,code:`int    maxOf(int a, int b)       { return a > b ? a : b; }
double maxOf(double a, double b) { return a > b ? a : b; }

// maxOf(3, 7)     -> int version    (exact match wins)
// maxOf(2.5, 9.1) -> double version

// int f(int);  double f(int);   -> ERROR: same parameter list.
// Return type is invisible at the call site, so it cannot pick a winner.`,annotations:{2:`Function overloading: same name, DIFFERENT parameter list (count or types). The compiler picks by arguments at the call site.`,7:`The classic quiz: overloads must differ in parameters. Differing only in return type is a compile error.`}},{type:`intuition`,title:`C arrays: fast, fixed, and armed`,md:'A C array is a row of lockers welded to the wall. Fast to reach, impossible to extend, and nobody stops you from reaching past the last one.\n\n- `int scores[5]` — the size is **fixed at compile time**. No growing, no shrinking, ever.\n- There is **no bounds checking**. `scores[7]` compiles, runs, and reads or stomps memory that is not yours.\n- That is **undefined behavior** (UB) — the C++ term for "anything may happen": crash, garbage, or silently *appearing* to work until production.\n- `sizeof(scores)` gives total bytes (5 × 4 = 20 on a typical 4-byte-`int` machine), so `sizeof(scores) / sizeof(scores[0])` = element count — **but only in the scope where the array was defined**.\n- Pass an array to a function and it **decays** — collapses — into a pointer to its first element. The size information is gone. (The full decay story is in the pointers module; today, just know the trap.)'},{type:`code`,lang:`cpp`,title:`The two array traps in six lines`,code:`void takesArray(int a[]) {
    // sizeof(a) here == sizeof(int*) == 8 on 64-bit. The count is GONE.
}

int main() {
    int scores[5] = {90, 85, 77, 60, 88};

    int n = sizeof(scores) / sizeof(scores[0]);   // 20 / 4 = 5. Works HERE only.

    scores[7] = 1;        // compiles. runs. undefined behavior.

    takesArray(scores);   // "scores" decays to a pointer to scores[0]
}`,annotations:{1:`int a[] in a parameter list is cosmetic — the real type is int* (a pointer). The [] is a polite lie.`,8:`The sizeof trick only works where the compiler still sees the whole array — the defining scope.`,10:`No exception, no error message. UB may look fine today and corrupt data on Friday night.`,12:`This silent array-to-pointer collapse is why line 2 reports pointer size, not 20.`}},{type:`intuition`,title:`C-strings vs std::string — and why std::string wins`,md:"A C-string is a `char` array with a secret handshake: a **null terminator** `'\\0'` (a zero byte) marks the end. Every C string function walks forward until it finds it.\n\n- `char raw[] = \"hi\"` is **3** bytes: `'h'`, `'i'`, `'\\0'`. Forget the terminator's byte and everything downstream breaks.\n- Lose the `'\\0'` and `strlen`/`strcmp` walk off the end into memory you do not own — UB again.\n- A C-string inherits every array problem: fixed size, no bounds checks, decays to `char*`.\n- `std::string` fixes all of it: it **knows its own size**, grows automatically, copies with `=`, compares with `==`, and frees its memory itself.\n- Default rule: **use `std::string` everywhere**. Touch C-strings only at boundaries that demand them (old C APIs, `argv`)."},{type:`code`,lang:`cpp`,title:`std::string: the five operations you will use daily`,code:`#include <string>

int main() {
    char raw[] = "hi";                // {'h','i','\\0'} — 3 bytes, size frozen

    std::string s = "faang prep";
    s.size();                         // 10 — stored count, O(1), no walking
    std::string t = s.substr(0, 5);   // "faang" — (start index, LENGTH)
    size_t pos = s.find("prep");      // 6 — index of the first match
    bool missing = s.find("xyz") == std::string::npos;   // npos = "not found"
    s += "!";                         // grows in place: "faang prep!"
    bool ab = std::string("apple") < std::string("banana");   // true
}`,annotations:{4:`The literal "hi" ships a hidden third byte — the null terminator. sizeof(raw) is 3, not 2.`,8:`substr's second argument is a LENGTH, not an end index. Off-by-one favorite.`,9:`size_t: the unsigned integer type the standard library uses for sizes and indexes.`,10:`find returns std::string::npos (a huge unsigned value) when absent. Compare against npos, never against -1.`,12:`Comparison operators do dictionary (lexicographic) order, character by character. "apple" < "banana" because 'a' < 'b'.`}},{type:`note`,md:"The recap — everything in this module:\n- Declaration = signature promise (`;`), definition = the one body. Headers share declarations across `.cpp` files; the linker matches calls to bodies.\n- Parameters: small types by value; big read-only → `const T&`; must mutate → `T&`. By value copies, and the copy dies at return.\n- Overloads differ by parameter list — never by return type alone.\n- C arrays: fixed size, zero bounds checking (out-of-range = UB), `sizeof`-count trick works only in the defining scope, and arrays decay to pointers at function boundaries.\n- C-string = `char` array + `'\\0'`. Prefer `std::string`: knows `size()`, `substr(pos, len)`, `find` → `npos`, `+=`, and `<` is dictionary order."}],quiz:[{question:`void process(std::vector<int> v) is called with a vector of one million ints. What happens?`,options:[{text:`All one million elements are copied on every call`,explanation:`Correct. Plain-type parameter = pass-by-value = full copy. C++ does exactly what the signature says, however expensive.`},{text:`C++ automatically passes large objects by reference`,explanation:`No such magic exists — that is Java/Python behavior. In C++ the signature alone decides, and this one says "copy".`},{text:`It fails to compile — vectors cannot be passed by value`,explanation:`It compiles fine. Legal, just wasteful. The compiler never protects you from a slow design.`},{text:`Only a pointer to the data is copied`,explanation:`That describes reference semantics. C++ value semantics copies the whole object — all million elements.`}],correct:0},{question:`A function only needs to READ a std::string parameter. The idiomatic signature takes…`,options:[{text:`std::string s`,explanation:`Works, but copies the whole string per call. Pay for a copy you never needed.`},{text:`std::string& s`,explanation:`No copy, but it signals intent to modify, and it refuses temporaries like f("hello").`},{text:`const std::string& s`,explanation:`Correct. No copy, compiler-enforced read-only, and it accepts temporaries. The default for big read-only params.`},{text:`std::string* s`,explanation:`Works but clunky: callers must write &arg, and now nullptr is a legal input you have to handle.`}],correct:2},{question:`Inside void f(int arr[]), what is sizeof(arr) on a typical 64-bit machine?`,options:[{text:`The full byte size of the caller's array`,explanation:`That information never arrives — the array decayed to a pointer at the call boundary.`},{text:`8 — the size of a pointer`,explanation:`Correct. int arr[] in a parameter list really means int*, so sizeof measures the pointer, not the array.`},{text:`The element count`,explanation:`sizeof never returns element counts — it returns bytes. And here it is the pointer's bytes.`},{text:`It does not compile`,explanation:`It compiles happily — that is exactly what makes this trap dangerous.`}],correct:1},{question:`int scores[5]; scores[7] = 1; — what does the standard say happens?`,options:[{text:`It throws std::out_of_range`,explanation:`That is vector::at(). C arrays have no bounds checking and throw nothing.`},{text:`Undefined behavior — crash, corruption, or silently "working" are all allowed`,explanation:`Correct. Out-of-bounds access is UB: the program may do literally anything, including looking fine until production.`},{text:`The compiler rejects it`,explanation:`The index is not checked at compile time (and in general cannot be). It compiles cleanly.`},{text:`The write is ignored`,explanation:`Nothing ignores it — the write lands on whatever memory sits 2 slots past the array.`}],correct:1},{question:`Which pair of overloads is ILLEGAL?`,options:[{text:`f(int) and f(double)`,explanation:`Legal — different parameter types. The compiler picks by argument type.`},{text:`f(int) and f(int, int)`,explanation:`Legal — different parameter counts.`},{text:`int f(int) and double f(int)`,explanation:`Correct — illegal. Identical parameter lists, differing only in return type. The call site f(5) gives the compiler no way to choose.`},{text:`f(std::string&) and f(const std::string&)`,explanation:`Surprisingly legal — const-ness of a reference parameter participates in overloading.`}],correct:2},{question:`char s[] = "hi"; — what is sizeof(s)?`,options:[{text:`2`,explanation:`You counted the visible characters but missed the hidden null terminator.`},{text:`3`,explanation:`Correct. The literal "hi" is {'h', 'i', '\\0'} — the terminator is a real byte in the array.`},{text:`8`,explanation:`That would be sizeof a pointer — but s here is a real array in its defining scope, not a decayed pointer.`},{text:`Unknowable at compile time`,explanation:`Array sizes from string literals are fixed at compile time — sizeof knows exactly.`}],correct:1},{question:`s.find("xyz") when "xyz" is not in s returns…`,options:[{text:`-1`,explanation:`find returns an unsigned type — it cannot be -1. Comparing to -1 sometimes "works" by conversion, but the real name is npos.`},{text:`0`,explanation:`0 is a valid MATCH position (found at the start). Returning it for "missing" would be ambiguous.`},{text:`std::string::npos`,explanation:`Correct. npos is a named constant (the largest size_t value) meaning "not found". Always compare against it by name.`},{text:`It throws an exception`,explanation:`find never throws for a missing substring — it reports via npos.`}],correct:2}],interviewQuestions:[{question:`Explain declaration vs definition, and why C++ needs the split at all.`,answer:"A declaration gives the compiler a signature — name, parameter types, return type — so it can type-check calls: `int add(int, int);`. A definition adds the body and may exist exactly once per program (the One Definition Rule). The split exists because C++ compiles each .cpp file independently: file A can call functions defined in file B only because A includes a header carrying B's declarations. The linker then matches every call to its one definition. Without declarations, every file would need every body — no separate compilation, no libraries.",isCaseBased:!1},{question:`Give the parameter-passing decision rule, with the tradeoffs behind it.`,answer:`Small types (int, double, char, pointers) → by value: copying them is as cheap as referencing, and the callee gets safe isolation. Big types read-only (std::string, std::vector, structs) → const T&: zero copy, compiler-enforced immutability, accepts temporaries. Must mutate the caller's object → T&: the mutation is the contract, and the non-const signature advertises it. Tradeoffs to name: by-value costs a copy but gives isolation; T& is free but invites hidden mutation (call sites look identical); const T& is free and safe but adds one level of indirection, which matters only for tiny types.`,isCaseBased:!1},{question:`Case: a service degrades under load. Profiling shows 40% of CPU inside a helper declared void log(std::string line), called millions of times per second with mostly-identical long strings. Diagnose and fix.`,answer:"Diagnosis: pass-by-value — every call copy-constructs the whole string: heap allocation plus a full byte copy, millions of times per second, then destroys it. The profiler is showing you the copy constructor, not the logging. Fix: change the signature to `void log(const std::string& line)` — zero copies, no caller changes, behavior identical since the function only reads. Tradeoffs worth voicing: if log stores the string, it must copy internally anyway (then by-value plus move is a defensible alternative); and the modern step further is std::string_view, which also accepts string literals without constructing a std::string at all. Verify with the profiler after — claims about performance end with measurements.",isCaseBased:!0},{question:`Why does the sizeof(arr)/sizeof(arr[0]) trick break inside a function that takes the array as a parameter?`,answer:`Because arrays decay: a parameter written int arr[] is rewritten by the language to int* arr. The caller's array collapses to a pointer to its first element, and the element count never crosses the function boundary. So sizeof(arr) inside is sizeof(int*) — 8 on 64-bit — and the division gives garbage (8/4 = 2 regardless of the real length). Consequences: the trick is safe only in the defining scope; any function taking a raw array must also take a length parameter. The fixes, in preference order: pass std::vector or std::array (they carry their size), or a pointer plus explicit count.`,isCaseBased:!1},{question:`Case: a report-parsing service crashes intermittently, and occasionally emits records containing fragments of OTHER users' data. The parser copies each input field into char buf[64] with no length check. Walk through diagnosis, severity, and fix.`,answer:`Diagnosis: buffer overflow. Fields longer than 63 chars (plus the null terminator) write past buf into adjacent stack memory — undefined behavior. That explains both symptoms: corrupt a saved register or return address and you crash; corrupt a neighboring buffer and you leak other users' data. Severity: this is not just a bug, it is a security vulnerability class — stack overflows are a classic remote-exploitation vector, so treat it as an incident, not a ticket. Fix: replace the raw buffer with std::string, which sizes itself to the input — this eliminates the class, not the instance. If a C API forces a fixed buffer, use bounded copies (snprintf) and reject oversized input at the trust boundary. Then audit the codebase for the sibling bugs: every fixed-size char buffer fed by external input.`,isCaseBased:!0},{question:`Why is std::string the default over char*? Name concrete reasons, not vibes.`,answer:`Four concrete ones. (1) It knows its own size — size() is stored, O(1), and cannot be fooled by a missing terminator; strlen walks memory and runs off the end if '\\0' is absent. (2) It manages its own memory — grows on +=, frees itself on scope exit; char buffers are fixed and leak-prone the moment you allocate them manually. (3) Value semantics work: = copies, == compares contents; on char* those operators copy and compare POINTERS, a silent logic bug. (4) It carries no decay/bounds traps into function signatures. The honest cost: a std::string may heap-allocate, and C APIs need .c_str(). Neither justifies raw char* in application code.`,isCaseBased:!1},{question:`How does the compiler resolve an overloaded call, and why can overloads never differ by return type alone?`,answer:`Resolution happens at compile time, driven by the argument types: the compiler collects all functions with that name, prefers an exact parameter match, then standard conversions (int → double); if two candidates tie, the call is ambiguous and fails to compile — it never guesses. Return type is excluded because the call site does not have to use the result: given f(5); the expression alone names no return type, so int f(int) vs double f(int) would be undecidable. Rule in one line: overloads are distinguished by what goes IN, never by what comes OUT.`,isCaseBased:!1},{question:`When is pass-by-value actually the RIGHT choice? Give more than "small types".`,answer:`Three honest cases. (1) Small types — int, double, pointers: a copy is one register, cheaper than indirection through a reference. (2) The callee needs a private mutable scratch copy anyway: taking T by value makes the copy once in the signature instead of const T& followed by a manual copy inside — same cost, less code. (3) Sink parameters — the function stores the value (e.g. into a member): by-value lets callers who pass temporaries have them moved instead of copied, which const T& cannot offer. The theme: by-value is wrong only when it creates a copy that nobody needed.`,isCaseBased:!1},{question:`What exactly does the null terminator do, and what breaks without it?`,answer:`A C-string has no length field — it is a bare char array. The convention: a zero byte '\\0' marks the end, and every C string routine (strlen, strcmp, printf %s) walks forward until it hits one. So "hi" occupies 3 bytes, not 2. Without the terminator — a buffer filled to the brim, or a manual char-by-char copy that skips it — those routines keep walking through whatever memory follows: garbage output at best, out-of-bounds reads (UB, crashes, data leakage) at worst. This single fragile convention is a large share of historical C security bugs, and the reason std::string, which stores its size explicitly, is the default.`,isCaseBased:!1},{question:`Your teammate says "headers slow down builds — let's just put function bodies in the header". What is the real tradeoff?`,answer:`Two separate costs to untangle. Correctness: a non-inline function DEFINED in a header gets compiled into every .cpp that includes it — the linker then sees multiple definitions and errors (One Definition Rule). Marking things inline dodges the linker error but the deeper cost remains: every include still recompiles those bodies, and any edit to a body forces recompilation of every file that includes the header — the opposite of the intended speedup. The declarations-in-header, definitions-in-cpp split is what lets an edited body rebuild ONE translation unit. Legitimate exceptions: templates and small intentionally-inline functions, which must live in headers. Default stands: menus in headers, kitchens in .cpp files.`,isCaseBased:!1}],flashcards:[{front:`Declaration vs definition`,back:`Declaration: signature + semicolon — a promise, repeatable. Definition: signature + body — runs, exactly one per program (One Definition Rule).`},{front:`Why do headers exist?`,back:`They carry shared DECLARATIONS so each .cpp compiles alone; the linker later matches calls to the single definition in some .cpp.`},{front:`The parameter decision rule`,back:`Small types → by value. Big + read-only → const T&. Must mutate the caller's object → T&.`},{front:`What does pass-by-value cost?`,back:`A full copy of the argument on EVERY call, destroyed at return. Trivial for int, brutal for a big string or vector.`},{front:`Overloading — the one hard rule`,back:`Overloads must differ in the parameter list (count or types). Return type alone can never distinguish them.`},{front:`C array out-of-bounds access`,back:`No bounds checking — it compiles and runs. Undefined behavior: crash, corruption, or silently "working".`},{front:`sizeof(arr)/sizeof(arr[0])`,back:`Element count — but ONLY in the defining scope. Inside a function, the array has decayed to a pointer and sizeof measures the pointer.`},{front:`Null terminator`,back:`'\\0', the zero byte ending every C-string. strlen/strcmp walk until they find it; without it they walk into unowned memory.`},{front:`std::string essentials`,back:`size() O(1) · substr(pos, LEN) · find → index or std::string::npos · += appends in place · < is dictionary order.`},{front:`string::npos`,back:`The named "not found" constant find returns — the largest size_t. Compare against npos by name, never against -1.`}],mindmapMarkdown:`- Functions, Pass-by-Value vs Reference & Arrays
  - Declaration vs definition
    - prototype = signature promise
    - one body per program (ODR)
    - headers: shared declarations
    - linker matches calls to bodies
  - Parameter passing
    - by value: full copy, dies at return
    - T&: alias — mutation lands
    - const T&: no copy, read-only default
    - rule: small→value, big→const T&
  - Overloading
    - same name, different param list
    - return type can't distinguish
  - C arrays
    - fixed size, no bounds check → UB
    - sizeof trick: defining scope only
    - decays to pointer in params
  - Strings
    - C-string: char[] + '\\0'
    - std::string: knows size, grows, frees
    - substr(pos, LEN) · find → npos
    - < is dictionary order`};export{e as default};