var e={id:`dsa-l0-string-patterns`,subjectId:`dsa`,level:0,title:`String Patterns: Hashing Basics, Palindromes & Anagrams`,whyItMatters:`String questions are the warm-up round at every FAANG screen — and the warm-up is where most rejections happen. Three patterns (count the letters, mirror the pointers, expand the center) solve the entire easy/medium string family. Learn the patterns, not the problems.`,estMinutes:45,sections:[{type:`intuition`,title:`A C++ string is an array wearing a costume`,md:"`std::string` is a mutable char array with text conveniences. Every array trick you know applies directly to it.\n\n- `s[i] = 'x'` writes **in place** — O(1), no new string. In Python, strings are immutable: every \"edit\" secretly builds a whole new string.\n- `s += c` appends with vector-style capacity doubling: amortized O(1).\n- It is a real container: `sort`, `reverse`, `swap(s[i], s[j])` — all legal, all in place.\n- Consequence for interviews: two pointers, in-place reverse, and counting tricks cost nothing extra on C++ strings."},{type:`code`,lang:`cpp`,title:`Mutability in four lines`,code:`string s = "faang";
s[0] = 'F';                      // in-place write: "Faang"
s += '!';                        // amortized O(1) append: "Faang!"
reverse(s.begin(), s.end());     // "!gnaaF" -- edits the chars directly`,annotations:{2:`One line of contrast: in Python this is a TypeError — Python strings are immutable, so every edit allocates a new string.`,4:`string is a container, so any algorithm that works on vector<char> works here. No copies, no conversions.`},py:{code:`s = "faang"
s = "F" + s[1:]                  # strings are IMMUTABLE: this builds a NEW string
s += "!"                         # also a brand new string: "Faang!"
s = s[::-1]                      # "!gnaaF" -- a fresh string again, not an edit`,annotations:{2:`The C++ line s[0] = 'F' is a TypeError in Python. Every "edit" allocates a new string — that is the whole difference between the two panes.`,3:`Because of that, += in a loop is O(n²). Collect pieces in a list and ''.join(parts) once — the standard Python fix, and a real interview answer.`,4:`For character-level work: chars = list(s) gives a mutable buffer, ''.join(chars) turns it back. That is the Python stand-in for in-place string algorithms.`}}},{type:`intuition`,title:`Frequency counting: 26 pigeonholes or a hash table`,md:"Most string questions reduce to one move: **count the letters**. Two containers do it.\n\n- `int cnt[26]` — 26 pigeonholes, one per lowercase letter. Index with `c - 'a'`. Zero hashing, zero allocation, O(1) space. **The default when the problem says \"lowercase English letters\".**\n- `unordered_map<char, int>` — a hash table. Works for ANY characters: uppercase, digits, unicode bytes, unknown alphabets.\n- The array is faster and smaller but hard-coded to one alphabet. Feed it `'é'` and `c - 'a'` indexes outside [0, 26) — undefined behavior, not an error message.\n- Rule: fixed known alphabet → array. Anything general → map. Say the choice out loud in the interview; it is a free signal."},{type:`code`,lang:`cpp`,title:`Valid Anagram — the count-compare pattern`,code:`bool isAnagram(const string& s, const string& t) {
    if (s.size() != t.size()) return false;
    int cnt[26] = {0};
    for (char c : s) cnt[c - 'a']++;
    for (char c : t) cnt[c - 'a']--;
    for (int x : cnt)
        if (x != 0) return false;
    return true;
}`,annotations:{2:`Cheap reject first: different lengths can never be anagrams. Costs nothing, skips all the counting.`,3:`= {0} zero-initializes all 26 slots. A fixed 26-int array is O(1) space — say that, interviewers listen for it.`,5:`One array, two passes: s increments, t decrements. Anagrams cancel to all zeros — no second array, no map compare.`},py:{code:`def isAnagram(s: str, t: str) -> bool:
    if len(s) != len(t):
        return False
    cnt = [0] * 26
    for c in s:
        cnt[ord(c) - ord('a')] += 1
    for c in t:
        cnt[ord(c) - ord('a')] -= 1
    return all(x == 0 for x in cnt)`,annotations:{2:`Cheap reject first: different lengths can never be anagrams. Costs nothing, skips all the counting.`,4:`[0] * 26 is the fixed 26-slot array — O(1) space whatever the input size. Say that, interviewers listen for it.`,5:`One array, two passes: s increments, t decrements. Anagrams cancel to all zeros — no second array, no map compare.`,9:`The one-liner is return Counter(s) == Counter(t). Write it, then say you can do it in O(1) space with the 26-array — that answer order scores best.`}}},{type:`note`,md:`Complexity: O(n) time, O(1) space — the 26-slot array does not grow with input. The lazy alternative (sort both, compare) is O(n log n) and still worth mentioning as the brute force before you show this.`},{type:`intuition`,title:`Group Anagrams: invent a canonical key`,md:`"Group these 10,000 words into anagram families." You cannot compare every pair — that is O(n²) comparisons. Instead, give every family one **canonical key**: a fingerprint every member shares and nobody else has.

- Key option 1 — **sorted string**: "eat", "tea", "ate" all sort to "aet". Cost per word of length k: O(k log k). Total: **O(n · k log k)**.
- Key option 2 — **count array serialized**: count letters into int[26], turn it into a string like "1#0#0#…". Cost per word: O(k + 26). Total: **O(n · k)**.
- Both are correct because both encode the same fact: *anagram = same multiset of letters*. Sorting and counting are just two spellings of that multiset.
- Then one hash map: key → bucket of words. One pass, done.`},{type:`code`,lang:`cpp`,title:`Group Anagrams with the sorted-string key`,code:`vector<vector<string>> groupAnagrams(vector<string>& strs) {
    unordered_map<string, vector<string>> groups;
    for (const string& w : strs) {
        string key = w;
        sort(key.begin(), key.end());   // "eat" "tea" "ate" -> all "aet"
        groups[key].push_back(w);
    }
    vector<vector<string>> out;
    for (auto& [key, bucket] : groups) out.push_back(move(bucket));
    return out;
}`,annotations:{5:`The canonical key. Swap these two lines for a count-array key ("1#0#0#...") to drop the log factor: O(n·k) instead of O(n·k log k). Worth doing when words are long.`,6:`operator[] on a missing key inserts an empty vector first — the counting idiom working for you, not against you.`,9:`Structured bindings + move: hand the buckets over without copying every string again.`},py:{code:`from collections import defaultdict

def groupAnagrams(strs: list[str]) -> list[list[str]]:
    groups = defaultdict(list)
    for w in strs:
        key = ''.join(sorted(w))        # "eat" "tea" "ate" -> all "aet"
        groups[key].append(w)
    return list(groups.values())`,annotations:{6:`The canonical key. sorted() returns a list of chars, so join it back into a string — a list is unhashable and would blow up as a dict key. Swap this for a 26-count tuple to drop the log factor: O(n·k) instead of O(n·k log k).`,7:`defaultdict(list) creates the empty bucket on first touch — the exact behaviour of C++ operator[] on a missing key, working for you rather than against you.`,8:`No copying: .values() hands over the same list objects. This is where C++ needs std::move and Python needs nothing.`}}},{type:`intuition`,title:`Palindromes: the mirror test`,md:`A palindrome reads the same forwards and backwards — it is symmetric around its middle, like a face in a mirror.

- The check writes itself: pointer \`l\` at the start, \`r\` at the end. Compare, step inward, repeat.
- **Invariant**: everything outside [l, r] has already been verified mirror-equal. One mismatch anywhere → not a palindrome, stop instantly.
- Cost: each step kills two characters, so O(n) time, O(1) space. No reversed copy needed — reversing to compare is the O(n)-extra-space brute force.
- This two-pointer skeleton is the base for every palindrome variant that follows.`},{type:`code`,lang:`cpp`,title:`Two-pointer check + Valid Palindrome II (one deletion)`,code:`bool isPal(const string& s, int l, int r) {
    while (l < r) {
        if (s[l] != s[r]) return false;
        l++; r--;
    }
    return true;
}

bool validPalindrome(const string& s) {   // may delete at most ONE char
    int l = 0, r = (int)s.size() - 1;
    while (l < r) {
        if (s[l] != s[r])
            return isPal(s, l + 1, r) || isPal(s, l, r - 1);
        l++; r--;
    }
    return true;
}`,annotations:{2:`Invariant: outside [l, r] is already mirror-verified. Every loop iteration shrinks the unverified window by 2.`,12:`First mismatch = the only place a deletion can help. Everything before it already matched.`,13:`THE branch trick: delete the left char OR the right char, and check the rest both ways. You cannot know which side is the intruder — "abbxa" needs skip-right, "axbba" needs skip-left. Two O(n) checks → still O(n) total.`},py:{code:`def isPal(s: str, l: int, r: int) -> bool:
    while l < r:
        if s[l] != s[r]:
            return False
        l += 1
        r -= 1
    return True

def validPalindrome(s: str) -> bool:   # may delete at most ONE char
    l, r = 0, len(s) - 1
    while l < r:
        if s[l] != s[r]:
            return isPal(s, l + 1, r) or isPal(s, l, r - 1)
        l += 1
        r -= 1
    return True`,annotations:{2:`Invariant: outside [l, r] is already mirror-verified. Every loop iteration shrinks the unverified window by 2.`,9:`For a plain palindrome check Python has s == s[::-1] — but that allocates a reversed copy, and it cannot express "skip one char". This version is O(1) extra space and extends.`,12:`First mismatch = the only place a deletion can help. Everything before it already matched.`,13:`THE branch trick: delete the left char OR the right char, and check the rest both ways. You cannot know which side is the intruder — "abbxa" needs skip-right, "axbba" needs skip-left. Two O(n) checks → still O(n) total.`}}},{type:`intuition`,title:`Longest Palindromic Substring: grow from the center`,md:`Brute force checks all O(n²) substrings at O(n) each — O(n³), dead at n = 1000. Flip the direction: do not ask "is this substring a palindrome?", ask "**how far does a palindrome grow from this center?**"

- Plant l and r at a center, expand outward while \`s[l] == s[r]\`. First mismatch stops the growth — everything inside is already known symmetric.
- The catch: "racecar" has a middle character, "abba" has a middle **crack**. Odd palindromes center on a char, even ones on the gap between two chars.
- So try every char (n centers) AND every crack (n−1 centers): **2n−1 centers** total. Miss the cracks and "abba" returns "a".
- 2n−1 centers × O(n) worst-case expansion = **O(n²) time, O(1) space** — the accepted interview answer. (An O(n) algorithm exists — Manacher's — name it, never code it at this level.)`},{type:`visual`,component:`PointerBoxDiagram`,props:{title:`Expand around center on "racecar"`,notice:`Every palindrome grows from a center: n character centers + (n−1) cracks between characters = 2n−1 centers to try.`,leftLabel:`pointers`,rightLabel:`s = "racecar"`,frames:[{note:`Center at index 1. Odd-length try: l and r both start ON the center — a single char is always a palindrome.`,stack:[{name:`l`,to:`c1`},{name:`r`,to:`c1`},{name:`best`,value:`"a" (len 1)`}],heap:[{id:`c0`,value:`r`,label:`0`},{id:`c1`,value:`a`,label:`1`},{id:`c2`,value:`c`,label:`2`},{id:`c3`,value:`e`,label:`3`},{id:`c4`,value:`c`,label:`4`},{id:`c5`,value:`a`,label:`5`},{id:`c6`,value:`r`,label:`6`}]},{note:`Expand: s[0]=r vs s[2]=c. MISMATCH — expansion stops instantly. This center is exhausted; move on.`,stack:[{name:`l`,to:`c0`,danger:!0},{name:`r`,to:`c2`,danger:!0},{name:`best`,value:`"a" (len 1)`}],heap:[{id:`c0`,value:`r`,label:`0`},{id:`c1`,value:`a`,label:`1`},{id:`c2`,value:`c`,label:`2`},{id:`c3`,value:`e`,label:`3`},{id:`c4`,value:`c`,label:`4`},{id:`c5`,value:`a`,label:`5`},{id:`c6`,value:`r`,label:`6`}]},{note:`Center at index 3 (the "e"). l = r = 3, and the ring test begins again.`,stack:[{name:`l`,to:`c3`},{name:`r`,to:`c3`},{name:`best`,value:`"a" (len 1)`}],heap:[{id:`c0`,value:`r`,label:`0`},{id:`c1`,value:`a`,label:`1`},{id:`c2`,value:`c`,label:`2`},{id:`c3`,value:`e`,label:`3`},{id:`c4`,value:`c`,label:`4`},{id:`c5`,value:`a`,label:`5`},{id:`c6`,value:`r`,label:`6`}]},{note:`s[2]=c vs s[4]=c — match. The ring holds; expand another step.`,stack:[{name:`l`,to:`c2`},{name:`r`,to:`c4`},{name:`best`,value:`"cec" (len 3)`}],heap:[{id:`c0`,value:`r`,label:`0`},{id:`c1`,value:`a`,label:`1`},{id:`c2`,value:`c`,label:`2`},{id:`c3`,value:`e`,label:`3`},{id:`c4`,value:`c`,label:`4`},{id:`c5`,value:`a`,label:`5`},{id:`c6`,value:`r`,label:`6`}]},{note:`s[1]=a vs s[5]=a match, then s[0]=r vs s[6]=r match — the palindrome reaches both edges.`,stack:[{name:`l`,to:`c0`},{name:`r`,to:`c6`},{name:`best`,value:`"racecar" (len 7)`}],heap:[{id:`c0`,value:`r`,label:`0`},{id:`c1`,value:`a`,label:`1`},{id:`c2`,value:`c`,label:`2`},{id:`c3`,value:`e`,label:`3`},{id:`c4`,value:`c`,label:`4`},{id:`c5`,value:`a`,label:`5`},{id:`c6`,value:`r`,label:`6`}]},{note:`l = −1, r = 7: both pointers fell off the ends, loop stops. The palindrome is (l, r) EXCLUSIVE: indices [0, 6], length r − l − 1 = 7.`,stack:[{name:`l`,value:`-1 (off)`,danger:!0},{name:`r`,value:`7 (off)`,danger:!0},{name:`best`,value:`"racecar" (len 7)`}],heap:[{id:`c0`,value:`r`,label:`0`},{id:`c1`,value:`a`,label:`1`},{id:`c2`,value:`c`,label:`2`},{id:`c3`,value:`e`,label:`3`},{id:`c4`,value:`c`,label:`4`},{id:`c5`,value:`a`,label:`5`},{id:`c6`,value:`r`,label:`6`}]}]}},{type:`code`,lang:`cpp`,title:`Longest Palindromic Substring — expand around center`,code:`string longestPalindrome(const string& s) {
    int n = (int)s.size(), bestL = 0, bestLen = 0;
    auto expand = [&](int l, int r) {
        while (l >= 0 && r < n && s[l] == s[r]) { l--; r++; }
        if (r - l - 1 > bestLen) { bestL = l + 1; bestLen = r - l - 1; }
    };
    for (int c = 0; c < n; c++) {
        expand(c, c);        // odd length: center is a character
        expand(c, c + 1);    // even length: center is the crack after c
    }
    return s.substr(bestL, bestLen);
}`,annotations:{4:`The loop always overshoots by one step on each side. So the surviving palindrome is (l, r) EXCLUSIVE — length r − l − 1, starting at l + 1. Off-by-one heaven; memorize the formula.`,8:`Two calls per index = the 2n−1 centers. Drop line 9 and every even-length palindrome ("abba") silently vanishes.`,11:`Total: O(n²) time worst case (think "aaaa…"), O(1) extra space. The accepted answer; name-drop Manacher O(n) as the theoretical ceiling.`},py:{code:`def longestPalindrome(s: str) -> str:
    n = len(s)
    best_l = best_len = 0

    def expand(l: int, r: int) -> None:
        nonlocal best_l, best_len
        while l >= 0 and r < n and s[l] == s[r]:
            l -= 1
            r += 1
        if r - l - 1 > best_len:
            best_l, best_len = l + 1, r - l - 1

    for c in range(n):
        expand(c, c)        # odd length: center is a character
        expand(c, c + 1)    # even length: center is the crack after c
    return s[best_l:best_l + best_len]`,annotations:{6:`nonlocal is the Python spelling of the C++ lambda capture [&]. Without it, assigning best_l inside expand would create a new local and the answer would always come back empty.`,7:`The loop always overshoots by one step on each side. So the surviving palindrome is (l, r) EXCLUSIVE — length r − l − 1, starting at l + 1. Off-by-one heaven; memorize the formula.`,15:`Two calls per index = the 2n−1 centers. Drop this line and every even-length palindrome ("abba") silently vanishes.`,16:`Slice end is exclusive, so s[best_l : best_l + best_len] is the C++ substr(pos, len). Total: O(n²) time worst case, O(1) extra space; name-drop Manacher O(n) as the ceiling.`}}},{type:`intuition`,title:`String hashing: turn a string into a number`,md:`"abc" is secretly a number. Read each letter as a digit (a=1, b=2, c=3) in base 31: \`((1·31 + 2)·31 + 3) = 1026\`. That is a **polynomial hash** — the same positional trick that makes 123 = (1·10 + 2)·10 + 3.

- **Base** B: any number bigger than the alphabet size (31 for lowercase), so "digits" never clash.
- **Mod** M: a big prime (10⁹+7). Real strings would overflow any integer type, so we keep only the remainder — the hash lives in [0, M).
- The payoff: comparing two hashes is **O(1)**, comparing two strings is O(n). A hash is a fingerprint you can compare instantly.
- The price: the mod squashed infinitely many strings into M slots. Different strings CAN share a hash — a **collision**.`},{type:`code`,lang:`cpp`,title:`Polynomial hash in six lines`,code:`long long polyHash(const string& s) {
    const long long B = 31, M = 1000000007;
    long long h = 0;
    for (char c : s)
        h = (h * B + (c - 'a' + 1)) % M;
    return h;
}
// polyHash("abc") = (1*31 + 2)*31 + 3 = 1026`,annotations:{2:`B > alphabet size (31 > 26) so letter-digits stay distinct; M a large prime so remainders spread evenly instead of clumping.`,5:`Horner's rule: multiply-shift, add the next digit, take the remainder. Mod at every step — waiting until the end overflows long long.`,8:`Hand-check: a=1 -> h=1; b: 1*31+2 = 33; c: 33*31+3 = 1026. Map a to 1, not 0, or "a", "aa", "aaa" all hash to 0.`},py:{code:`def polyHash(s: str) -> int:
    B, M = 31, 1_000_000_007
    h = 0
    for c in s:
        h = (h * B + (ord(c) - ord('a') + 1)) % M
    return h

# polyHash("abc") = (1*31 + 2)*31 + 3 = 1026`,annotations:{2:`B > alphabet size (31 > 26) so letter-digits stay distinct; M a large prime so remainders spread evenly instead of clumping. 1_000_000_007 with underscores is just readable spacing.`,5:`Horner's rule: multiply-shift, add the next digit, take the remainder. Python ints cannot overflow, so the mod is here for speed and for matching the C++ answer — not for correctness.`,8:`Hand-check: a=1 -> h=1; b: 1*31+2 = 33; c: 33*31+3 = 1026. Map a to 1, not 0, or "a", "aa", "aaa" all hash to 0.`}}},{type:`note`,md:`What collisions actually mean for you:

- **Equal strings → equal hashes.** Always. That direction is safe.
- **Equal hashes → probably equal strings.** Only probably — pigeonhole: infinitely many strings, only M hash values.
- So a hash can prove "different" instantly, but never prove "equal". On a hash match, either verify char-by-char or accept a ~1/M error chance (and say you are accepting it).
- Where this goes next: slide the hash along a text in O(1) per step and you get **Rabin-Karp** substring search — built in the hard-patterns module.`}],quiz:[{question:`The problem guarantees lowercase English letters only. For frequency counting, prefer…`,options:[{text:`unordered_map<char, int> — it is more general`,explanation:`General, but you are paying hashing and allocation for an alphabet of exactly 26. The array is strictly better here.`},{text:`int cnt[26], indexed by c - 'a'`,explanation:`Correct. Fixed alphabet → direct index: no hashing, no allocation, guaranteed O(1) per touch, O(1) space.`},{text:`map<char, int> — sorted keys help`,explanation:`Sorted keys buy nothing for counting, and every touch costs O(log n). Wrong tool twice over.`}],correct:1},{question:`When does int cnt[26] become the WRONG tool?`,options:[{text:`When strings are longer than 26 characters`,explanation:`String length is irrelevant — the 26 slots count occurrences, not positions.`},{text:`When input may contain uppercase, digits, or unicode`,explanation:`Correct. c - 'a' on anything outside a…z indexes outside [0, 26) — undefined behavior, not an error. General input needs unordered_map (or a bigger array like int[256] for raw bytes).`},{text:`Never — it always works`,explanation:`Feed it 'Z' and c - 'a' is negative: out-of-bounds write, silent corruption.`}],correct:1},{question:`Group Anagrams, n words of length k, using the SORTED-string key. Total complexity?`,options:[{text:`O(n · k)`,explanation:`That is the count-array key. Sorting each word costs k log k, not k.`},{text:`O(n · k log k)`,explanation:`Correct. Each of n words pays an O(k log k) sort; the hash-map insert is O(k) and absorbed.`},{text:`O(n² · k)`,explanation:`n² would mean comparing every pair of words — the canonical key exists precisely to avoid that.`},{text:`O(n log n)`,explanation:`No sorting of the word LIST happens — each word is sorted internally.`}],correct:1},{question:`When does the count-array key ("1#0#0#…") clearly beat the sorted-string key?`,options:[{text:`When words are long — it is O(k) per word instead of O(k log k)`,explanation:`Correct. Counting skips the sort. For long words the log factor is real money; for length-5 words nobody can measure the difference.`},{text:`When there are many words`,explanation:`Both keys pay their cost once per word — the word COUNT does not separate them, word LENGTH does.`},{text:`It is always slower — the key string is 26 slots long`,explanation:`The fixed 26-slot serialization is O(26) = O(1) per word. It wins asymptotically whenever k log k > k + 26.`}],correct:0},{question:`Expand-around-center on a string of length n tries how many centers?`,options:[{text:`n`,explanation:`That covers only odd-length palindromes. "abba" has no middle character — its center is a crack.`},{text:`2n − 1`,explanation:`Correct. n character centers (odd palindromes) + n − 1 cracks between characters (even palindromes).`},{text:`n²`,explanation:`n² is the number of substrings — the whole point of centers is that there are only O(n) of them.`}],correct:1},{question:`Valid Palindrome II (one deletion allowed): at the first mismatch s[l] != s[r], you should…`,options:[{text:`Always delete the left character and continue`,explanation:`"abbxa" breaks this: deleting the left b leaves "bx…" mismatched. Only deleting the RIGHT side (the x) works there.`},{text:`Try deleting the left OR the right char — check both remainders, pass if either is a palindrome`,explanation:`Correct. You cannot know which side is the intruder, so branch both ways: isPal(l+1, r) || isPal(l, r−1). Two O(n) checks — still O(n).`},{text:`Restart the scan from scratch after deleting`,explanation:`Wasted work: everything before the mismatch already matched and stays matched. Branch from the mismatch point.`}],correct:1},{question:`Two strings have EQUAL polynomial hashes. What do you actually know?`,options:[{text:`They are equal`,explanation:`Pigeonhole says no: infinitely many strings, only M hash values. Different strings can collide.`},{text:`They are probably equal — but only a char-by-char check proves it`,explanation:`Correct. Equal strings always hash equal, but the converse fails with ~1/M probability. Verify, or knowingly accept the risk.`},{text:`Nothing at all`,explanation:`You know a lot: unequal hashes would have PROVEN them different. Equal hashes make equality overwhelmingly likely.`}],correct:1},{question:`In C++, s[0] = 'X' on a std::string…`,options:[{text:`Throws — strings are read-only`,explanation:`That is Python (TypeError). C++ strings are mutable char arrays.`},{text:`Writes in place in O(1)`,explanation:`Correct. std::string is a mutable container — direct writes, in-place reverse and sort all work.`},{text:`Creates a modified copy and reassigns s`,explanation:`That is the Python immutability model. C++ edits the existing buffer — no allocation.`}],correct:1}],interviewQuestions:[{question:`Solve Valid Anagram and state the complexity of every approach you mention.`,answer:`Brute force: sort both strings and compare — O(n log n) time. Better: count letters. Reject on length mismatch first, then one int[26]: increment over s, decrement over t; anagram iff all slots end at zero. O(n) time, O(1) space — the 26-slot array is constant regardless of input size. The one-array increment/decrement trick avoids building and comparing two count structures. If the alphabet is not fixed, swap the array for unordered_map<char,int> — still O(n) average, but O(k) space for k distinct chars.`,isCaseBased:!1},{question:`Case: your int[26] anagram checker passes all tests, then crashes intermittently in production on real user input. Diagnose and fix.`,answer:`Real input broke the alphabet assumption: uppercase, digits, accents, or UTF-8 bytes make c - 'a' index outside [0, 26) — an out-of-bounds write, which is undefined behavior: sometimes a crash, sometimes silent corruption, which explains "intermittently". Fixes in order of intent: (1) if the spec truly is lowercase, validate at the boundary and reject bad input loudly; (2) if general text is legal, switch to unordered_map<char,int> — or int[256] indexed by (unsigned char)c for raw bytes; (3) for real unicode, note that char is a byte, not a character — proper handling means iterating code points. The transferable lesson: int[26] encodes an ASSUMPTION; state it out loud in the interview.`,isCaseBased:!0},{question:`Group Anagrams: explain both canonical keys and when you would pick each.`,answer:`Both keys encode the fact that anagram = same multiset of letters. Sorted-string key: sort each word, use it as the map key — O(n · k log k) total, trivially correct, two lines. Count-array key: count into int[26], serialize with separators ("1#0#0#…") — O(n · k) total, drops the log factor. Pick sorted for short words and cleaner code under pressure; pick counts when k is large or the interviewer pushes for optimal. Either way the grouping itself is one unordered_map pass: key → vector of words. Mention the separator in the serialized key — concatenating raw counts without one can make different count vectors collide (e.g. counts 1,12 vs 11,2 both reading "112").`,isCaseBased:!1},{question:`Why is the sorted string a VALID canonical key for anagram grouping? Argue it, do not just assert it.`,answer:`Two directions. (1) If two words are anagrams, they contain the same multiset of letters, and sorting any arrangement of the same multiset produces the identical string — so anagrams always share the key. (2) If two words sort to the same string, that string exhibits both multisets, so the multisets are equal — so non-anagrams never share the key. Same-key ⇔ anagram: exactly the property a canonical form needs. The count-array key passes the same two-direction test, which is why both work.`,isCaseBased:!1},{question:`Walk through the two-pointer palindrome check, then extend it to Valid Palindrome II (one deletion allowed).`,answer:`Base check: l at 0, r at n−1; invariant: everything outside [l, r] is verified mirror-equal. Compare s[l] and s[r]; mismatch → false; match → step inward. O(n) time, O(1) space — no reversed copy. Valid Palindrome II: run the same scan; the FIRST mismatch is the only place a deletion helps (everything earlier already matched). You cannot know whether the left or right char is the intruder — "abbxa" needs skip-right, "axbba" needs skip-left — so branch: return isPal(l+1, r) || isPal(l, r−1). Each branch is a plain O(n) check, so the total stays O(n). The general pattern: budgeted-mismatch problems branch at the first violation.`,isCaseBased:!1},{question:`Case: for Longest Palindromic Substring you checked all substrings and it TLEs at n = 1000. The interviewer asks for better.`,answer:`The brute force is O(n²) substrings × O(n) palindrome check = O(n³) — dead at n = 1000. Reverse the question: instead of testing substrings, GROW palindromes from centers. For each of the 2n−1 centers (n chars for odd lengths, n−1 cracks for even), expand while s[l] == s[r]; the first mismatch caps that center. O(n) centers × O(n) worst expansion = O(n²) time, O(1) space — accepted everywhere. Worth contrasting with the DP table approach: also O(n²) time but O(n²) space, so expansion strictly wins here. Then the name-drop: Manacher's algorithm does O(n), and at this level naming it is worth more than attempting it.`,isCaseBased:!0},{question:`Why 2n−1 centers in expand-around-center — and what exactly breaks if you only try the n character centers?`,answer:`Every palindrome has a unique center: odd-length palindromes center on a character (n candidates), even-length ones center on the crack BETWEEN two characters (n−1 candidates) — total 2n−1. Skip the cracks and every even-length palindrome becomes invisible: on "abba" the four odd centers yield only length-1 palindromes, so the algorithm confidently returns "a" instead of "abba". Silent wrong answer, not a crash — which is why this is a favorite interviewer probe. Implementation form: expand(c, c) and expand(c, c+1) for every index c.`,isCaseBased:!1},{question:`Explain the polynomial rolling hash: what the base and mod are for, and what property makes hashing useful at all.`,answer:`Treat the string as a number in base B: each character is a digit, combined by Horner's rule — h = h·B + digit(c), all mod M. B must exceed the alphabet size (31 for lowercase) so digits never clash; M is a large prime (10⁹+7) because real strings overflow any integer type, and a prime spreads remainders evenly. The property you buy: equal strings ALWAYS produce equal hashes, and a hash comparison is O(1) versus O(n) for a string comparison — a constant-size fingerprint. Two details that show depth: mod at every step, not at the end (overflow); map 'a' to 1, not 0, or "a", "aa", "aaa" all hash to zero.`,isCaseBased:!1},{question:`What is a hash collision, why can it never be fully eliminated, and how do you handle it in an interview solution?`,answer:`A collision is two different strings producing the same hash. Unavoidable by pigeonhole: strings are unbounded, but hashes live in a finite set of M values — infinitely many strings must share slots. Consequences: unequal hashes PROVE strings differ; equal hashes only suggest equality. Handling, in the order interviewers like: (1) verify candidate matches char-by-char — correctness guaranteed, and the verification is amortized cheap when matches are rare; (2) accept the ~1/M false-positive probability explicitly for M ≈ 10⁹; (3) name double hashing — two independent (B, M) pairs pushes collision odds to ~1/M², effectively zero for interview constraints.`,isCaseBased:!1},{question:`Case: the interviewer follows up — "you are comparing substrings with == inside a loop and it is too slow; how does hashing change the game?"`,answer:`Each == on length-m substrings costs O(m); inside a loop over n positions that is O(n·m). Replace every comparison with a hash comparison: O(1) after computing fingerprints. Two upgrades make it practical: precompute prefix hashes so ANY substring hash comes out in O(1), and when sliding a fixed-length window one step, update the hash incrementally — subtract the outgoing char's contribution, multiply by B, add the incoming char — O(1) per slide instead of rehashing m chars. That sliding fingerprint is exactly Rabin-Karp substring search: O(n + m) average. On a hash hit, verify char-by-char to kill false positives. Full build lives in the hard-patterns module.`,isCaseBased:!0}],flashcards:[{front:`C++ string vs Python string`,back:`C++: mutable char array — s[i] = 'x' edits in place, O(1). Python: immutable — every "edit" allocates a new string.`},{front:`int[26] vs unordered_map<char,int> — the rule`,back:`Fixed known alphabet ("lowercase English letters") → int[26], index c − 'a', O(1) space. General/unicode input → unordered_map. Array on bad input = out-of-bounds UB.`},{front:`Valid Anagram recipe`,back:`Length check → one int[26]: ++ over s, −− over t → anagram iff all zeros. O(n) time, O(1) space. (Sort-and-compare brute: O(n log n).)`},{front:`Group Anagrams — two canonical keys`,back:`Sorted string: O(n·k log k). Serialized count array "1#0#…": O(n·k). Both encode "same multiset of letters"; map key → bucket.`},{front:`Two-pointer palindrome invariant`,back:`Everything outside [l, r] is verified mirror-equal. Mismatch → false immediately. O(n) time, O(1) space, no reversed copy.`},{front:`Valid Palindrome II — the branch trick`,back:`At the FIRST mismatch, try both deletions: isPal(l+1, r) || isPal(l, r−1). Cannot know which side intrudes. Two O(n) checks → O(n) total.`},{front:`Expand-around-center — centers and cost`,back:`2n−1 centers: n chars (odd) + n−1 cracks (even — skip these and "abba" is missed). O(n²) time, O(1) space. Palindrome after overshoot: length r−l−1.`},{front:`Polynomial hash formula`,back:`h = (h·B + digit(c)) mod M per char (Horner). B > alphabet size (31), M large prime (1e9+7), 'a' → 1 not 0, mod every step.`},{front:`Hash collision — the two directions`,back:`Equal strings → equal hashes: always. Equal hashes → equal strings: only probably (~1/M error). Hashes prove "different", never "equal" — verify on match.`},{front:`Trigger phrases`,back:`"rearrangement / same letters" → frequency count. "reads same backwards" → two pointers or expand-around-center. "compare many substrings fast" → rolling hash (Rabin-Karp).`}],mindmapMarkdown:`- String Patterns
  - C++ string
    - Mutable char array — s[i] = 'x' in place
    - Python contrast: immutable, edits copy
    - Real container: sort/reverse work
  - Frequency counting
    - int[26]: fixed alphabet, c − 'a', O(1) space
    - unordered_map: unicode / general input
    - Wrong alphabet + array = out-of-bounds UB
  - Anagrams
    - Valid Anagram: one array ++/−−, all zeros, O(n)
    - Group Anagrams: canonical key → hash map
      - Sorted key O(n·k log k)
      - Count key O(n·k)
  - Palindromes
    - Two pointers: outside [l,r] verified, O(n)
    - Valid Palindrome II: branch at first mismatch, || both skips
    - Expand around center
      - 2n−1 centers (chars + cracks)
      - O(n²) time, O(1) space
      - Manacher = O(n) name-drop
  - String hashing
    - String as base-B number mod prime M
    - Horner: h = h·B + digit, mod each step
    - O(1) fingerprint compare
    - Collisions exist — verify on match
    - → Rabin-Karp (hard-patterns module)`};export{e as default};