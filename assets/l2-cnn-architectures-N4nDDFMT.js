var e={id:`dl-l2-cnn-architectures`,subjectId:`dl`,level:2,title:`LeNet to ResNet: The Architectures That Mattered`,whyItMatters:`Four ideas took ImageNet top-5 error from 16.4% to 3.57% in three years. Each was a response to a specific measured failure, and knowing which failure each one answers is what lets you design rather than copy.`,assumes:[`You have read *CNNs*, so you can count convolution parameters`,`You know why gradients vanish with depth`],estMinutes:22,sections:[{type:`intuition`,title:`AlexNet: the same idea, finally affordable`,md:`LeCun's LeNet-5 in 1998 had every structural piece a modern CNN has — convolutions, pooling, a dense head — in about 60,000 parameters. It worked on digits and went no further for fourteen years.

**AlexNet** in 2012 changed nothing fundamental about the architecture and took ImageNet top-5 error from 26% to **16.4%**. What was different was three practical things: **GPUs**, which made a 60-million-parameter network trainable at all; **ReLU** instead of sigmoid, which made depth trainable; and **dropout** plus augmentation, which made 60M parameters survive 1.2M images.

The lesson is worth holding on to: the bottleneck was not the idea. It was compute, an activation function, and regularisation.`},{type:`intuition`,title:`VGG: uniformity, and where the parameters went`,md:`**VGG** (2014, top-5 **7.3%**) made one architectural argument and one accidental discovery.

The argument was small kernels: use only 3x3 convolutions, and stack them. Three stacked 3x3 layers see the same 7x7 region as one 7x7 layer for 45% fewer parameters and two extra non-linearities.

The accident was in the head. VGG16 has 138 million parameters, and the next snippet shows where almost all of them are — a discovery that killed the design pattern it came from.`},{type:`code`,lang:`python`,title:`VGG16, counted layer by layer`,code:`convs = [(3,64,224),(64,64,224),(64,128,112),(128,128,112),
         (128,256,56),(256,256,56),(256,256,56),
         (256,512,28),(512,512,28),(512,512,28),
         (512,512,14),(512,512,14),(512,512,14)]
fcs = [(25088,4096), (4096,4096), (4096,1000)]

conv_p = sum(9*ci*co + co for ci, co, r in convs)
conv_macs = sum(9*ci*co*r*r for ci, co, r in convs)
fc_p = sum(a*b + b for a, b in fcs)
fc_macs = sum(a*b for a, b in fcs)

print('conv  params %-12s  MACs %.2e' % (f'{conv_p:,}', conv_macs))
print('dense params %-12s  MACs %.2e' % (f'{fc_p:,}', fc_macs))
print('total  %s   dense is %.1f%% of params and %.1f%% of compute'
      % (f'{conv_p+fc_p:,}', 100*fc_p/(conv_p+fc_p), 100*fc_macs/(conv_macs+fc_macs)))

# ---- real output ----
# conv  params 14,714,688    MACs 1.53e+10
# dense params 123,642,856   MACs 1.24e+08
# total  138,357,544   dense is 89.4% of params and 0.8% of compute`,annotations:{5:`9·C_in·C_out per 3x3 layer, plus one bias per filter. The r in each tuple is the spatial resolution that layer runs at, which is what determines its compute.`,20:`89.4% of the parameters and 0.8% of the compute, in three dense layers. That single imbalance is why global average pooling replaced the flatten-then-dense head everywhere after this.`,19:`The 13 convolutional layers hold 14.7M parameters and do essentially all 15.3 billion multiply-accumulates. Parameters and compute live in completely different places.`}},{type:`note`,label:`GoogLeNet, and the fact that embarrassed VGG`,md:`In the same 2014 competition, **GoogLeNet** scored **6.7%** — better than VGG's 7.3% — with **6.8 million** parameters against VGG's 138 million. Twenty times smaller and better.

Two ideas did it. The **inception block** runs 1x1, 3x3 and 5x5 convolutions in parallel and concatenates the results, so the network does not have to commit to one kernel size per layer. And **1x1 bottlenecks** project the channel count down before the expensive convolutions and back up afterwards.

It also used global average pooling instead of a dense head, which is where most of the twenty-fold saving came from.

The uncomfortable conclusion is that VGG's 138 million parameters were mostly not doing anything, and the field had been measuring the wrong thing.`},{type:`intuition`,title:`ResNet: the problem was not overfitting`,md:`By 2015 the observation was clear and strange: a **56-layer plain network had higher training error than a 20-layer one**. Higher *training* error — so this was not overfitting, and adding capacity was making the model worse at the thing it was directly optimising.

The diagnosis was optimisation. A deep plain stack cannot easily learn the identity function, so the extra layers cannot even be got out of the way.

**ResNet's** fix is one line: make each block compute **x + F(x)** rather than F(x). Now "do nothing" means driving F to zero, which is the easiest thing a layer can do. And the addition gives the gradient a path that is not multiplied.`},{type:`math`,intro:`The residual block and its derivative. The identity term is the whole point: whatever F does to the gradient, the 1 survives, so the backward product cannot collapse to zero the way a plain stack's can.`,latex:[`y = x + F(x) \\qquad \\frac{\\partial y}{\\partial x} = 1 + \\frac{\\partial F}{\\partial x}`]},{type:`code`,lang:`python`,title:`The gradient product, plain versus residual`,code:`import numpy as np
np.random.seed(0)
T = 1000                                          # median over 1000 networks

for d in [10, 20, 50, 100]:
    plain = np.prod(np.random.normal(0.8, 0.1, (T, d)), axis=1)
    res   = np.prod(1 + np.random.normal(0.0, 0.1, (T, d)), axis=1)
    print('depth %3d:  plain %.3e   residual %.4f' % (d, np.median(plain), np.median(res)))

bottleneck = 256*64 + 9*64*64 + 64*256
print('two 3x3 at 256ch: %s   1x1-3x3-1x1 bottleneck: %s  (%.1fx fewer)'
      % (f'{2*9*256*256:,}', f'{bottleneck:,}', 2*9*256*256/bottleneck))

# ---- real output ----
# depth  10:  plain 9.930e-02   residual 0.9732
# depth  20:  plain 9.780e-03   residual 0.9352
# depth  50:  plain 9.853e-06   residual 0.8398
# depth 100:  plain 8.610e-11   residual 0.6406
# two 3x3 at 256ch: 1,179,648   1x1-3x3-1x1 bottleneck: 69,632  (16.9x fewer)`,annotations:{6:`Factors centred on 0.8 for the plain stack - a mild, realistic per-layer shrinkage, not a pathological one. The median over 1000 draws, because a single network is noisy at depth 100.`,7:`Residual factors are centred on 1 because of the identity term. dF/dx varies around zero, so the whole factor varies around 1 rather than around 0.8.`,17:`Plain falls from 9.9e-02 to 8.6e-11 between depth 10 and 100 - a factor of a billion. The residual product goes 0.97 to 0.64, drifting rather than collapsing.`,18:`The bottleneck design: project 256 channels to 64 with a 1x1, do the expensive 3x3 there, project back up. 69,632 parameters against 1,179,648 - 16.9x fewer, which is what makes 50 and 152-layer networks affordable.`}},{type:`note`,label:`What came after, and what it says`,md:`**DenseNet** connects every layer to every later layer rather than just its neighbour, taking feature reuse further. **EfficientNet** searched for the right way to scale depth, width and resolution together and found they must grow in a fixed ratio — scaling one alone saturates. **ConvNeXt** took a plain ResNet and applied transformer-era training recipes and design choices one at a time, matching vision transformers without any attention at all.

That last result is the interesting one. It suggests a substantial part of the transformer's advantage in vision was the **training recipe** — heavy augmentation, AdamW, long schedules, LayerNorm — rather than attention itself.

Vision transformers still win at very large data scale, where the convolution's locality prior stops being an advantage and starts being a restriction. Below that scale the prior is worth more than the flexibility, which is the same trade the whole module has been about.`}],quiz:[{question:`AlexNet used essentially LeNet's architecture. What actually changed in 2012?`,options:[{text:`A fundamentally new layer type`,explanation:`The structural pieces were all present in LeNet-5 in 1998.`},{text:`GPUs made 60M parameters trainable, ReLU made depth trainable, and dropout plus augmentation made it survive 1.2M images`,explanation:`Correct. The bottleneck was compute, an activation function, and regularisation.`},{text:`A much larger dataset alone`,explanation:`ImageNet mattered, but without GPUs and ReLU the network could not be trained on it.`},{text:`Batch normalisation`,explanation:`BatchNorm arrived in 2015.`}],correct:1},{question:`VGG16's dense head holds 89.4% of the parameters and 0.8% of the compute. What followed from that?`,options:[{text:`Networks got shallower`,explanation:`They got much deeper.`},{text:`Global average pooling replaced the flatten-then-dense head, since almost all the parameters were doing almost no work`,explanation:`Correct — GoogLeNet reached better accuracy with 6.8M parameters against VGG's 138M.`},{text:`Dense layers were made wider`,explanation:`They were removed.`},{text:`Convolutions were replaced by dense layers`,explanation:`The convolutions were doing the work.`}],correct:1},{question:`A 56-layer plain network had higher TRAINING error than a 20-layer one. Why does "training" matter in that sentence?`,options:[{text:`It does not; it is the same as overfitting`,explanation:`Overfitting means low training error and high test error — the opposite.`},{text:`It rules out overfitting and points at optimisation: the deep network could not even fit the data it was directly minimising on`,explanation:`Correct, and it is why the fix is architectural rather than regularisation.`},{text:`It means the learning rate was wrong`,explanation:`The result held across learning rates; it is structural.`},{text:`It means the data was too small`,explanation:`Same data for both depths.`}],correct:1},{question:`How does x + F(x) help?`,options:[{text:`It adds parameters`,explanation:`The skip connection has no parameters when shapes match.`},{text:`The derivative is 1 + dF/dx, so the identity term survives the backward product — and "do nothing" means driving F to zero, which is easy`,explanation:`Correct. Both the optimisation and the gradient-flow argument come from the same 1.`},{text:`It normalises the activations`,explanation:`That is what the BatchNorm inside the block does.`},{text:`It reduces the receptive field`,explanation:`The receptive field is unaffected.`}],correct:1},{question:`At depth 100 the median plain gradient product was 8.6e-11 and the residual one 0.6406. What is the right reading?`,options:[{text:`Residual networks have no gradient decay at all`,explanation:`It drifted from 0.97 to 0.64 - decay exists, it just does not collapse.`},{text:`Plain fell by a factor of a billion between depth 10 and 100 while the residual product merely drifted`,explanation:`Correct. The identity term keeps the factors centred on 1 rather than on 0.8.`},{text:`The residual product is exactly 1 by construction`,explanation:`dF/dx is non-zero, so it varies around 1.`},{text:`The plain factors were pathologically small`,explanation:`0.8 is a mild, realistic per-layer shrinkage.`}],correct:1},{question:`What does the ConvNeXt result suggest about vision transformers?`,options:[{text:`That attention is unnecessary in all cases`,explanation:`ViTs still win at very large data scale.`},{text:`That a substantial part of the ViT advantage was the training recipe — augmentation, AdamW, long schedules — rather than attention itself`,explanation:`Correct: a plain ResNet with those choices matched ViTs without any attention.`},{text:`That convolutions scale better with data`,explanation:`The opposite: the locality prior becomes a restriction at very large scale.`},{text:`That ImageNet is saturated`,explanation:`A separate point, and not what the experiment tested.`}],correct:1}],interviewQuestions:[{question:`Why does ResNet work?`,answer:`Two connected reasons, both from the same identity term. Optimisation: a residual block computes x + F(x), so "do nothing" means driving F to zero, which is the easiest thing a layer can learn — whereas a plain deep stack cannot easily learn the identity and so cannot get its extra layers out of the way. That is what explains the observation that motivated it: a 56-layer plain network had higher training error than a 20-layer one, which rules out overfitting. And gradient flow: the derivative is 1 + dF/dx, so the backward product is over factors centred on 1 rather than below it. Simulated over 100 layers, the median plain product fell to 8.6e-11 while the residual one drifted only to 0.64.`,isCaseBased:!1},{question:`Why is VGG so much larger than GoogLeNet despite being worse?`,answer:`Its dense head. VGG16 has 138 million parameters, and 123.6 million of them — 89.4% — are in three fully-connected layers that account for 0.8% of the compute. The 13 convolutional layers hold 14.7M parameters and do essentially all 15.3 billion multiply-accumulates. GoogLeNet replaced that head with global average pooling and used 1x1 bottlenecks to cut channel counts before expensive convolutions, reaching 6.7% top-5 error with 6.8 million parameters against VGG's 7.3% with 138 million. The uncomfortable reading is that most of VGG's parameters were not doing much, and the field had been measuring the wrong quantity.`,isCaseBased:!0},{question:`Explain the ResNet bottleneck block.`,answer:`Instead of two 3x3 convolutions at full width, it does 1x1 down, 3x3 at the reduced width, 1x1 back up. At 256 channels that is 256×64 plus 9×64×64 plus 64×256 — 69,632 parameters — against 1,179,648 for two 3x3 layers at 256, a factor of 16.9. The 1x1 convolutions are pure channel projections with no spatial extent, so they are cheap, and the expensive spatial operation happens at a quarter of the width. That is what makes 50 and 152-layer networks affordable; the shallower ResNet-18 and ResNet-34 use plain two-3x3 blocks because at their channel counts the saving does not justify the extra layers.`,isCaseBased:!1},{question:`How do you choose an architecture for a new vision problem?`,answer:`Start with a pretrained model rather than an architecture decision — for most problems the right first move is fine-tuning an ImageNet-pretrained ResNet-50 or a ConvNeXt, and that establishes a baseline in an afternoon. The architecture question only becomes real when there is a constraint: latency on-device points at MobileNet or EfficientNet-Lite, very large in-domain data makes a vision transformer competitive, and dense prediction tasks want a feature-pyramid or U-Net structure rather than a classification backbone. I would also weigh the training recipe as heavily as the architecture, since ConvNeXt showed that recipe choices account for a large share of what looked like architectural advantage.`,isCaseBased:!0},{question:`What is an inception block for?`,answer:`It removes the need to commit to one kernel size per layer. The block runs 1x1, 3x3 and 5x5 convolutions plus a pooling path in parallel on the same input and concatenates the outputs, so the network learns how much to weight each scale rather than the architect guessing. The essential detail is the 1x1 bottleneck in front of the 3x3 and 5x5 branches — without it the concatenation makes channel counts explode and the block is unaffordable. Later versions factorised the 5x5 into stacked 3x3, and then the 3x3 into 1x3 and 3x1, chasing the same parameter argument further.`,isCaseBased:!1},{question:`What did EfficientNet actually contribute?`,answer:`A finding about scaling rather than a new block. It showed that depth, width and input resolution have to be scaled together in a fixed ratio — scaling any one alone saturates quickly, because a deeper network with the same resolution runs out of spatial detail to use, and a wider one with the same depth runs out of composition. The compound coefficient makes that ratio explicit and a single parameter controls model size along it. The base architecture came from a neural architecture search, so part of the result is a search outcome rather than an insight, but the compound-scaling observation transfers to architectures it was not searched on, which is the part worth remembering.`,isCaseBased:!1},{question:`Would you use a CNN or a vision transformer today?`,answer:`It depends almost entirely on data scale, and the reason is the locality prior. A convolution assumes nearby pixels are related and the same feature is useful everywhere, which is true and therefore valuable when data is limited — it is free knowledge the model does not have to learn. A transformer assumes nothing and must learn spatial relationships from data, which costs it below roughly ImageNet scale and pays off above it, where the prior becomes a restriction. So: CNN or a hybrid for a typical task with tens of thousands of images, ViT when I have hundreds of millions or a strong pretrained one to fine-tune. ConvNeXt is also a fair answer for either, since it matches ViTs at moderate scale with no attention.`,isCaseBased:!0},{question:`Why do skip connections help beyond gradient flow?`,answer:`Because they change what the network is being asked to learn. Without a skip, each layer must reproduce everything useful from its input as well as add something — so the identity is a function it has to actively construct. With a skip, the layer only has to learn a residual correction, and doing nothing is available for free. There is also loss-landscape evidence: visualisations of ResNets with and without skips show the skip version having a markedly smoother surface, which is a separate mechanism from the gradient product. And they enable feature reuse across depths, which is the observation DenseNet pushed further by connecting every layer to every later one.`,isCaseBased:!1}],flashcards:[{front:`AlexNet (2012, 16.4%)`,back:`Same architecture as LeNet-5. What changed: GPUs, ReLU, dropout + augmentation. The bottleneck was never the idea.`},{front:`VGG (2014, 7.3%)`,back:`Only 3x3 kernels, stacked. Three 3x3 = one 7x7 for 45% fewer params plus two non-linearities.`},{front:`The VGG16 imbalance`,back:`138,357,544 params. Dense head = 123.6M (89.4% of params, 0.8% of compute). The 13 convs = 14.7M and all 15.3e9 MACs.`},{front:`GoogLeNet (2014, 6.7%)`,back:`6.8M parameters against VGG's 138M, and better. Inception blocks, 1x1 bottlenecks, global average pooling.`},{front:`The observation ResNet answered`,back:`A 56-layer plain net had higher TRAINING error than a 20-layer one. Not overfitting — an optimisation failure.`},{front:`The residual block`,back:`y = x + F(x), so dy/dx = 1 + dF/dx. "Do nothing" = drive F to 0, and the 1 survives the backward product.`},{front:`The gradient simulation`,back:`Depth 100 medians: plain 8.610e-11, residual 0.6406. Plain fell a billion-fold from depth 10; residual drifted from 0.97.`},{front:`The bottleneck block`,back:`1x1 down, 3x3 narrow, 1x1 up. 69,632 params against 1,179,648 for two 3x3 at 256 channels — 16.9x fewer.`}],mindmapMarkdown:`- CNN architectures
  - AlexNet 2012 (16.4%)
    - LeNet's architecture, finally affordable
    - GPUs + ReLU + dropout/augmentation
  - VGG 2014 (7.3%)
    - only 3x3, stacked
    - 138,357,544 params
    - dense head: 89.4% of params, 0.8% of compute
    - convs: 14.7M params, all 15.3e9 MACs
  - GoogLeNet 2014 (6.7%)
    - 6.8M params - 20x smaller AND better
    - inception: parallel kernel sizes
    - 1x1 bottlenecks, global average pooling
  - ResNet 2015 (3.57%)
    - the problem: 56 layers had higher TRAINING error than 20
    - not overfitting - an optimisation failure
    - y = x + F(x); dy/dx = 1 + dF/dx
    - depth 100: plain 8.6e-11, residual 0.6406
    - bottleneck 1x1-3x3-1x1: 69,632 vs 1,179,648
  - After
    - DenseNet: connect every layer to every later one
    - EfficientNet: depth/width/resolution scale TOGETHER
    - ConvNeXt: recipe explained much of the ViT gap
    - ViT wins only at very large data scale`};export{e as default};