var e={id:`genai-l1-transformer-block`,subjectId:`genai`,level:1,title:`The Transformer Block`,whyItMatters:`A language model is one block repeated. Counting its parameters — 12d² — tells you why GPT-2 small is 124M and GPT-3 is 175B, and reveals that two thirds of every model is the part nobody talks about.`,assumes:[`You have read *Multi-Head Attention*`,`You have read *BatchNorm vs LayerNorm*`],estMinutes:18,sections:[{type:`intuition`,title:`Two sublayers, each wrapped the same way`,md:`A transformer block has exactly two operations, and each is wrapped identically.

**Attention** lets tokens exchange information — it is the only place in the whole model where positions talk to each other.

**The feed-forward network** processes each position independently: expand to 4d, apply a non-linearity, project back to d. No position sees any other here.

Each is wrapped as \`x = x + sublayer(norm(x))\`. The **residual** gives the gradient an unmultiplied path from the output to the input, and the **norm** keeps the scale controlled. So the whole model is: mix across positions, then think per position, repeated L times.`},{type:`code`,lang:`python`,title:`The parameter count, and where it goes`,code:`def block(d, ff=4):
    return 4*d*d + 2*ff*d*d          # attention (Wq,Wk,Wv,Wo) + FFN (up, down)

for name, d, L, V in [('GPT-2 small', 768, 12, 50257), ('GPT-2 medium', 1024, 24, 50257),
                      ('GPT-3', 12288, 96, 50257), ('LLaMA-2 7B', 4096, 32, 32000)]:
    print('%-13s d=%5d L=%3d  blocks %-16s embeddings %-12s total %s'
          % (name, d, L, f'{L*block(d):,}', f'{V*d:,}', f'{L*block(d) + V*d:,}'))

d = 768
print('\\ninside one block, d=768:')
print('  attention 4*d^2 = %-11s (%.0f%%)' % (f'{4*d*d:,}', 100*4/12))
print('  FFN     2*4*d^2 = %-11s (%.0f%%)' % (f'{8*d*d:,}', 100*8/12))

# ---- real output ----
# GPT-2 small   d=  768 L= 12  blocks 84,934,656       embeddings 38,597,376   total 123,532,032
# GPT-2 medium  d= 1024 L= 24  blocks 301,989,888      embeddings 51,463,168   total 353,453,056
# GPT-3         d=12288 L= 96  blocks 173,946,175,488  embeddings 617,558,016  total 174,563,733,504
# LLaMA-2 7B    d= 4096 L= 32  blocks 6,442,450,944    embeddings 131,072,000  total 6,573,522,944
#
# inside one block, d=768:
#   attention 4*d^2 = 2,359,296   (33%)
#   FFN     2*4*d^2 = 4,718,592   (67%)`,annotations:{2:`4d² for attention — four square matrices, W_q, W_k, W_v and W_o. 8d² for the FFN — one d→4d and one 4d→d. Twelve d² per block, and that is the number worth memorising.`,13:`123,532,032 against the published 124M for GPT-2 small, and 174.6B against GPT-3's 175B. The estimate is close because there is very little in a transformer besides these matrices.`,16:`LLaMA-2 is a little under its true 6.74B because it uses a three-matrix SwiGLU feed-forward rather than two. The shape of the estimate holds; the constant depends on the FFN design.`,20:`Two thirds of every transformer is the feed-forward network — the part with no attention in it at all. Attention gets the name and the papers; the FFN gets the parameters.`}},{type:`note`,label:`What the feed-forward network is for`,md:`Attention moves information between positions but performs almost no computation on it — the output is a weighted average of values, which is a linear operation. The FFN is where the non-linear processing happens.

The **4x expansion** is not arbitrary. Widening before the non-linearity gives the layer room to represent more distinct features than d can hold simultaneously, then the projection back compresses. Ratios from 2x to 8x have been tried; 4x is the empirical landing point.

Interpretability work suggests the FFN acts as a **key-value memory**: the up-projection detects patterns and the down-projection writes associated facts into the residual stream. That framing has held up well — factual recall in language models has been localised to specific FFN layers and edited there.`},{type:`note`,label:`The residual stream, and why pre-norm won`,md:`Because every sublayer is \`x + sublayer(...)\`, the vector flowing through the model is never replaced — only **added to**. That is the **residual stream**: a running total that every block reads from and writes back into.

That framing explains a lot. Layers can specialise without needing to reproduce everything below them, and the gradient reaches layer 1 through a path that is a sum rather than a product.

**Where the norm sits matters as much as which norm it is.** The original transformer used post-norm — \`norm(x + sublayer(x))\` — which normalises the residual stream itself and interrupts the identity path. That made deep models hard to train and required a carefully tuned warmup.

**Pre-norm** — \`x + sublayer(norm(x))\` — puts the norm inside the branch, so the skip path is untouched from input to output. Every modern model uses it, with a final norm before the output head because the stream's magnitude grows with depth.`},{type:`code`,lang:`python`,title:`When attention starts to dominate the cost`,code:`d = 768
print('  n     attention FLOPs      FFN FLOPs     ratio')
for n in [128, 512, 2048, 8192, 32768]:
    attn, ffn = n*n*d*2, n*8*d*d
    print('%6d %14.3e %14.3e %8.2f' % (n, attn, ffn, attn/ffn))

# ---- real output ----
#   n     attention FLOPs      FFN FLOPs     ratio
#    128      2.517e+07      6.040e+08     0.04
#    512      4.027e+08      2.416e+09     0.17
#   2048      6.442e+09      9.664e+09     0.67
#   8192      1.031e+11      3.865e+10     2.67
#  32768      1.649e+12      1.546e+11    10.67`,annotations:{4:`Attention is n²d (quadratic in length), the FFN is 8nd² (linear). Which dominates depends entirely on how n compares to d.`,10:`At n = 128 attention is 4% of the FFN's cost — essentially free, which is why short-sequence transformers spend their time in the feed-forward layers.`,13:`The crossover is near n = 3d. At n = 8192 attention is 2.67x the FFN and at 32,768 it is 10.67x, so the quadratic term has taken over completely.`,14:`That single table explains why nobody worried about attention cost in 2017 with n = 512, and why every long-context paper since 2020 is about attention.`}},{type:`note`,label:`The block, written out`,md:`A modern block in six lines:

- \`h = x + Attention(RMSNorm(x))\` — with RoPE applied to Q and K inside, and a causal mask
- \`x = h + FFN(RMSNorm(h))\` — SwiGLU rather than a plain GELU, in most recent models

And the model is that block L times, with an embedding table at the bottom, a final norm, and an output projection to the vocabulary — usually **weight-tied** to the embedding table, which for GPT-2 small saves 38.6M parameters, nearly a third of the model.

The most useful thing to hold on to is **12d² per block**. It gets you a parameter count from a config in one line, and it makes clear that width costs quadratically while depth costs linearly — which is why models grow wide faster than they grow deep.`}],quiz:[{question:`What are the two operations in a transformer block, and how do they differ?`,options:[{text:`Two attention layers with different masks`,explanation:`Only one is attention.`},{text:`Attention, the only place positions exchange information, and a feed-forward network that processes each position independently`,explanation:`Correct, and each is wrapped as x + sublayer(norm(x)).`},{text:`An encoder and a decoder`,explanation:`That is a full architecture, not a block.`},{text:`Normalisation and residual addition`,explanation:`Those are the wrapper, not the sublayers.`}],correct:1},{question:`The count gave 123,532,032 for GPT-2 small against a published 124M. Why is a rough formula so accurate?`,options:[{text:`Coincidence`,explanation:`It holds for GPT-3 too, at 174.6B against 175B.`},{text:`There is very little in a transformer besides these matrices — 12d² per block plus the embedding table`,explanation:`Correct, which is what makes the formula worth memorising.`},{text:`The published figure was rounded from this formula`,explanation:`It is the actual parameter count.`},{text:`Biases were excluded`,explanation:`Biases are a negligible fraction either way.`}],correct:1},{question:`Two thirds of a transformer block's parameters are in the FFN. What does the FFN do?`,options:[{text:`Mixes information between positions`,explanation:`It processes each position completely independently.`},{text:`The non-linear processing — attention only takes a weighted average, which is linear`,explanation:`Correct, and interpretability suggests it acts as a key-value memory storing facts.`},{text:`Normalises the residual stream`,explanation:`That is the norm layer.`},{text:`Applies positional encoding`,explanation:`That happens in attention or at the embedding.`}],correct:1},{question:`What is the residual stream?`,options:[{text:`The gradient flowing backwards`,explanation:`It is a forward-pass object.`},{text:`A running total that is never replaced, only added to — every block reads from and writes back into it`,explanation:`Correct, and it is why layers can specialise without reproducing everything below them.`},{text:`The attention weights`,explanation:`Those are internal to a sublayer.`},{text:`The output of the final layer`,explanation:`That is one point on the stream.`}],correct:1},{question:`Why did pre-norm replace post-norm?`,options:[{text:`It uses fewer parameters`,explanation:`Identical parameter count.`},{text:`Post-norm normalises the residual stream itself and interrupts the identity path; pre-norm leaves the skip untouched from input to output`,explanation:`Correct, which is why post-norm needed carefully tuned warmup to train deep models at all.`},{text:`RMSNorm requires it`,explanation:`Either norm works in either position.`},{text:`It removes the need for a final norm`,explanation:`Pre-norm models add a final norm because the stream grows with depth.`}],correct:1},{question:`At n = 128 attention is 4% of FFN cost; at n = 32,768 it is 10.67x. What sets the crossover?`,options:[{text:`The number of layers`,explanation:`Both terms scale with L identically.`},{text:`Attention is n²d and the FFN is 8nd², so the crossover is near n = 3d`,explanation:`Correct — it explains why attention cost was ignorable in 2017 at n = 512 and central since 2020.`},{text:`The vocabulary size`,explanation:`That affects the embedding and output layers only.`},{text:`The head count`,explanation:`Total attention cost is independent of how it is split into heads.`}],correct:1}],interviewQuestions:[{question:`Describe a transformer block.`,answer:`Two sublayers, each wrapped identically. Multi-head attention, which is the only place in the model where positions exchange information; and a feed-forward network that processes each position completely independently — expand to 4d, non-linearity, project back. Each is wrapped as x = x + sublayer(norm(x)), so the residual gives the gradient an unmultiplied path and the norm controls the scale. The whole model is that block repeated L times, with an embedding table below and an output projection above. So it alternates: mix across positions, then think per position.`,isCaseBased:!1},{question:`How do you count the parameters in a language model?`,answer:`12d² per block, times the number of layers, plus the embedding table at V×d. The 12 breaks down as 4d² for attention — W_q, W_k, W_v and W_o are all d×d — and 8d² for the feed-forward, one d→4d matrix and one 4d→d. That gives 123.5M for GPT-2 small against a published 124M, and 174.6B for GPT-3 against 175B, which is close because there is very little else in a transformer. Two caveats: models using a three-matrix SwiGLU feed-forward run higher than the formula, and weight tying between the embedding and the output projection saves V×d — for GPT-2 small that is 38.6M, nearly a third of the model.`,isCaseBased:!0},{question:`What does the feed-forward network do, and why is it two thirds of the model?`,answer:`It does the non-linear processing. Attention moves information between positions but computes almost nothing — a weighted average of values is a linear operation — so the FFN is where the actual transformation happens. It is two thirds because it is 8d² against attention's 4d², which follows from the 4x expansion. Interpretability work suggests it behaves as a key-value memory: the up-projection detects patterns and the down-projection writes associated facts into the residual stream, and that framing has held up well enough that factual recall has been localised to specific FFN layers and edited there directly.`,isCaseBased:!1},{question:`Pre-norm or post-norm?`,answer:`Pre-norm, for anything deep. Post-norm — norm(x + sublayer(x)) — normalises the residual stream itself, so the identity path is interrupted at every block; that made deep models hard to train and required a carefully tuned warmup schedule to work at all. Pre-norm puts the norm inside the branch, x + sublayer(norm(x)), so the skip connection carries an unmodified signal all the way from input to output and gradients have a clean path. The cost is that the residual stream's magnitude grows with depth, which is why pre-norm models add a final norm before the output head, and why residual-branch output scaling appears in their initialisation schemes.`,isCaseBased:!1},{question:`When does attention become the bottleneck?`,answer:`Near n = 3d, because attention costs n²d while the FFN costs 8nd². At d = 768 and n = 128, attention is 4% of the FFN's cost — essentially free. At n = 2048 it is 67%. At n = 8192 it is 2.67 times, and at 32,768 it is 10.67 times. That single progression explains the history: nobody worried about attention cost in 2017 with sequences of 512, and every long-context paper since 2020 has been about attention. It also means the right optimisation depends on your sequence length — at short context you should be looking at the feed-forward layers instead.`,isCaseBased:!0},{question:`What is the residual stream and why is it a useful way to think?`,answer:`Because every sublayer is an addition rather than a replacement, the vector flowing through the model is a running total that each block reads from and writes back into. That reframing does real work. It explains why layers can specialise without having to reproduce everything below them — a block only has to contribute its increment. It explains gradient flow, since the path to layer 1 is a sum rather than a product. And it is the foundation of mechanistic interpretability, where circuits are traced as information written into the stream at one layer and read out at another.`,isCaseBased:!1},{question:`Why 4x expansion in the feed-forward layer?`,answer:`Empirical, with a reasonable story. Widening before the non-linearity gives the layer room to represent more distinct features than d can hold simultaneously — under the key-value memory framing, it is the number of patterns the layer can detect — and the projection back compresses the result into the stream. Ratios from 2x to 8x have been tried and 4x is where the accuracy-per-parameter trade lands. Modern models complicate the comparison: SwiGLU uses three matrices instead of two, so implementations reduce the hidden width to roughly 8/3·d to keep the parameter count comparable, which is why LLaMA's FFN dimensions look like odd numbers.`,isCaseBased:!1},{question:`Would you make a model wider or deeper?`,answer:`Wider, usually, and the parameter arithmetic is half the reason: cost is 12d²L, so width is quadratic and depth linear — a given parameter budget buys more capacity per unit of sequential computation by going wide. Depth is also harder to train, needing residuals and careful normalisation to remain stable, and it adds latency that cannot be parallelised away since layer L must wait for layer L−1. Width parallelises across devices cleanly. That said, depth buys compositional capability that width does not, and the empirical scaling work finds an optimal aspect ratio rather than a corner solution, so the honest answer is that both grow and width grows faster.`,isCaseBased:!0}],flashcards:[{front:`The block`,back:`Attention (the ONLY place positions talk) + FFN (each position alone). Each wrapped as x = x + sublayer(norm(x)).`},{front:`The number to memorise`,back:`12d² per block: 4d² attention (Wq,Wk,Wv,Wo) + 8d² FFN (d→4d, 4d→d). Plus V×d for embeddings.`},{front:`It checks out`,back:`GPT-2 small: 123,532,032 vs a published 124M. GPT-3: 174.6B vs 175B. There is very little else in a transformer.`},{front:`Where the parameters are`,back:`TWO THIRDS in the feed-forward network — the part with no attention in it. Attention gets the papers; the FFN gets the parameters.`},{front:`What the FFN is for`,back:`The non-linear processing. Attention only takes a weighted average, which is linear. Likely a key-value memory storing facts.`},{front:`The residual stream`,back:`Never replaced, only added to. Every block reads from and writes into a running total. The basis of mechanistic interpretability.`},{front:`Pre-norm vs post-norm`,back:`Post-norm normalises the STREAM and interrupts the identity path. Pre-norm normalises inside the branch, leaving the skip untouched. Pre-norm needs a final norm.`},{front:`The crossover`,back:`Attention is n²d, FFN is 8nd² — crossover near n = 3d. At n=128 attention is 4% of FFN cost; at 32,768 it is 10.67x.`}],mindmapMarkdown:`- The transformer block
  - Two sublayers
    - attention: the ONLY cross-position operation
    - FFN: each position independently, d -> 4d -> d
    - each wrapped x = x + sublayer(norm(x))
  - Parameter count
    - 12 d^2 per block
      - 4 d^2 attention (Wq, Wk, Wv, Wo)
      - 8 d^2 FFN (up, down)
    - plus V*d embeddings
    - GPT-2 small: 123,532,032 vs 124M published
    - GPT-3: 174.6B vs 175B
    - TWO THIRDS is the FFN
  - The FFN
    - attention is linear; this is where thinking happens
    - 4x expansion is empirical (2x-8x tried)
    - likely a key-value memory: detect, then write facts
  - The residual stream
    - never replaced, only added to
    - layers specialise without reproducing what is below
    - basis of mechanistic interpretability
  - Norm placement
    - post-norm normalises the stream, breaks the identity
    - pre-norm normalises inside the branch
    - pre-norm needs a final norm (stream grows with depth)
  - Cost crossover
    - attention n^2 d, FFN 8 n d^2, crossover near n = 3d
    - n=128: attention is 4% of FFN
    - n=32,768: attention is 10.67x`};export{e as default};