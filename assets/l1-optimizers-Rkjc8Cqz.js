var e={id:`dl-l1-optimizers`,subjectId:`dl`,level:1,title:`Optimizers: SGD, Momentum, RMSProp and Adam`,whyItMatters:`Backprop gives you the gradient. The optimizer decides what to do with it, and on a badly conditioned loss surface that decision is the difference between 54 steps and 223 — or between converging and diverging outright.`,assumes:[`You know that gradient descent steps opposite the gradient`,`You have read *Backpropagation*, so you know where the gradient comes from`],estMinutes:22,sections:[{type:`intuition`,title:`The problem is not the gradient, it is the shape`,md:`Plain gradient descent takes a step proportional to the gradient. On a bowl-shaped loss that works fine.

Real loss surfaces are **ravines**: steep in some directions, nearly flat in others. The gradient points mostly across the ravine rather than along it, so the step is dominated by the steep direction — which is not where you need to go.

Worse, the learning rate is shared. It must be small enough for the steepest direction to remain stable, and that same small value makes progress along the flat direction glacial. Every optimizer below is a different answer to that single conflict.`},{type:`math`,intro:`The four update rules on one gradient g. Momentum accumulates a velocity; RMSProp accumulates squared gradients and divides by their root, giving each coordinate its own effective step size; Adam does both, with a correction for the fact that both averages start at zero.`,latex:[`\\theta \\leftarrow \\theta - \\eta\\, g \\qquad\\text{(SGD)}`,`v \\leftarrow \\beta v + g;\\quad \\theta \\leftarrow \\theta - \\eta\\, v \\qquad\\text{(momentum)}`,`s \\leftarrow \\beta s + (1-\\beta) g^2;\\quad \\theta \\leftarrow \\theta - \\frac{\\eta\\, g}{\\sqrt{s} + \\epsilon} \\qquad\\text{(RMSProp)}`,`\\hat{v} = \\frac{v}{1-\\beta_1^{t}};\\quad \\hat{s} = \\frac{s}{1-\\beta_2^{t}};\\quad \\theta \\leftarrow \\theta - \\frac{\\eta\\, \\hat{v}}{\\sqrt{\\hat{s}} + \\epsilon} \\qquad\\text{(Adam)}`]},{type:`code`,lang:`python`,title:`A ravine, and the stability limit that traps SGD`,code:`def grad(x, y):  return (x, 100.0 * y)
def loss(x, y):  return 0.5 * (x*x + 100.0*y*y)

# curvature is 1 along x and 100 along y: a condition number of 100
for lr in [0.019, 0.021]:
    x, y = 10.0, 1.0
    for t in range(6):
        gx, gy = grad(x, y)
        x -= lr * gx; y -= lr * gy
    print('lr=%.3f after 6 steps: x=%.4f  y=%.4f  loss=%.4f' % (lr, x, y, loss(x, y)))

# ---- real output ----
# lr=0.019 after 6 steps: x=8.9128  y=0.5314  loss=53.8405
# lr=0.021 after 6 steps: x=8.8043  y=1.7716  loss=195.6795`,annotations:{1:`The gradient in y is 100x the gradient in x for the same displacement. This is what an ill-conditioned surface means, and real networks routinely have condition numbers far worse.`,13:`At lr = 0.019 it is stable but crawling. x has moved from 10.0 to only 8.9128 in six steps, and y is still 0.5314 because 0.019 sits just under the limit and the oscillation barely shrinks.`,14:`At lr = 0.021 — a 10% increase — y has grown from 1.0 to 1.7716 and the loss is climbing. The stability limit here is exactly 2/100 = 0.02, and one step past it the whole thing diverges.`}},{type:`note`,label:`Momentum: keep going the way you were going`,md:`Momentum accumulates a **velocity** — a decaying sum of past gradients — and steps along that instead of along the raw gradient.

In the flat direction, successive gradients point the same way, so they add up: with β = 0.9 the velocity approaches roughly **10×** the raw gradient, which is like a ten-times larger learning rate in exactly the direction that needed one.

In the steep direction the gradient flips sign every step, so successive contributions cancel rather than accumulate. The oscillation damps itself. One mechanism, opposite effects in the two directions, which is precisely what the ravine required.`},{type:`code`,lang:`python`,title:`Four optimizers, each at its own best learning rate`,code:`import math

def run(kind, lr, n):
    x, y, st = 10.0, 1.0, {'vx':0, 'vy':0, 'sx':0, 'sy':0}
    for t in range(1, n + 1):
        gx, gy = grad(x, y)
        if kind == 'sgd':
            dx, dy = lr*gx, lr*gy
        elif kind == 'momentum':
            st['vx'] = 0.9*st['vx'] + gx;  st['vy'] = 0.9*st['vy'] + gy
            dx, dy = lr*st['vx'], lr*st['vy']
        elif kind == 'rmsprop':
            st['sx'] = 0.9*st['sx'] + 0.1*gx*gx;  st['sy'] = 0.9*st['sy'] + 0.1*gy*gy
            dx, dy = lr*gx/(math.sqrt(st['sx'])+1e-8), lr*gy/(math.sqrt(st['sy'])+1e-8)
        else:
            st['vx'] = 0.9*st['vx'] + 0.1*gx;      st['vy'] = 0.9*st['vy'] + 0.1*gy
            st['sx'] = 0.999*st['sx'] + 0.001*gx*gx; st['sy'] = 0.999*st['sy'] + 0.001*gy*gy
            mx, my = st['vx']/(1-0.9**t), st['vy']/(1-0.9**t)
            sx, sy = st['sx']/(1-0.999**t), st['sy']/(1-0.999**t)
            dx, dy = lr*mx/(math.sqrt(sx)+1e-8), lr*my/(math.sqrt(sy)+1e-8)
        x -= dx; y -= dy
    return loss(x, y)

for kind, lr, n in [('sgd',0.019,223), ('momentum',0.015,70),
                    ('rmsprop',0.02,524), ('adam',0.5,54)]:
    print('%-9s lr=%-6s reaches loss %.4f in %3d steps' % (kind, lr, run(kind,lr,n), n))

# ---- real output ----
# sgd       lr=0.019  reaches loss 0.0096 in 223 steps
# momentum  lr=0.015  reaches loss 0.0023 in  70 steps
# rmsprop   lr=0.02   reaches loss 0.0092 in 524 steps
# adam      lr=0.5    reaches loss 0.0030 in  54 steps`,annotations:{10:`Momentum: velocity is a decaying sum of gradients, and the step follows the velocity. Note there is no 1/(1−β) scaling here, which is why its usable learning rate is smaller than SGD's.`,13:`RMSProp: divide each coordinate by the root of its own running mean squared gradient. The steep coordinate divides by a large number and the flat one by a small number, so both take steps of similar size.`,27:`Each step count is where that optimizer first drives the loss below 0.01, and each learning rate is the best from a sweep over the same grid — so this compares optimizers rather than tuning luck.`,28:`Momentum reaches the target in 70 steps against SGD's 223 — a 3.2x speedup on a surface where SGD's learning rate is capped by the steep direction.`,29:`RMSProp is the WORST here at 524 steps. Normalising the step sizes fixed the conditioning but threw away the accumulation that carries progress along the flat direction. Adaptive scaling alone is not enough.`,30:`Adam, at 54 steps, is momentum and RMSProp together — and note its learning rate is 0.5, twenty-five times SGD's. Because it divides by the gradient magnitude, its steps are roughly the size of lr regardless of how steep the surface is, so its lr means something completely different.`}},{type:`visual`,component:`Plot`,props:{title:`Loss against step count, each optimizer at its own best learning rate`,notice:`Adam and momentum are effectively done by step 100 while SGD is still at 1.08 and RMSProp at 30.8. The surprise is RMSProp: adaptive per-coordinate scaling on its own is worse than plain SGD here, because it fixes the conditioning but removes the accumulation. Adam is not "the adaptive one" — it is the one that does both, and that is where its advantage comes from.`,kind:`line`,xLabel:`step`,yLabel:`loss`,series:[{name:`SGD`,points:[[1,88.618],[10,40.1471],[25,19.4188],[50,7.3443],[100,1.0784],[200,.0233],[300,5e-4]]},{name:`Momentum`,points:[[1,61.0112],[10,10.6532],[25,8.2068],[50,.3961],[100,1e-4],[200,0],[300,0]]},{name:`RMSProp`,points:[[1,93.245],[10,69.4736],[25,51.9337],[50,39.5098],[100,30.8432],[200,17.4369],[300,7.9044]]},{name:`Adam`,points:[[1,57.625],[10,14.7483],[25,.0308],[50,.2217],[100,.0018],[200,0],[300,0]]}]}},{type:`code`,lang:`python`,title:`Why Adam needs bias correction`,code:`m = 0.0
for t in range(1, 6):
    m = 0.9 * m + 0.1 * 1.0          # a constant gradient of 1.0 every step
    print('t=%d  raw m=%.4f  corrected m/(1-0.9^t)=%.4f' % (t, m, m/(1-0.9**t)))

# ---- real output ----
# t=1  raw m=0.1000  corrected m/(1-0.9^t)=1.0000
# t=2  raw m=0.1900  corrected m/(1-0.9^t)=1.0000
# t=3  raw m=0.2710  corrected m/(1-0.9^t)=1.0000
# t=4  raw m=0.3439  corrected m/(1-0.9^t)=1.0000
# t=5  raw m=0.4095  corrected m/(1-0.9^t)=1.0000`,annotations:{3:`The moving average starts at 0, so at t=1 it holds only 0.1 of a gradient that has been 1.0 the whole time — a tenfold underestimate.`,7:`Dividing by (1 − 0.9^t) recovers 1.0000 exactly at every step. The correction is largest at t=1 and decays to nothing, so it only affects early training.`,8:`Without it the first steps are far too small, and because the same underestimate hits the squared average too, the ratio misbehaves in a way that destabilises early training. This is what warmup schedules also address.`}},{type:`note`,label:`What to actually use`,md:`**AdamW** is the default for transformers and most new work. It is Adam with weight decay applied directly to the weights rather than folded into the gradient — which matters, because inside Adam the L2-as-gradient version gets divided by the adaptive term and stops being weight decay at all.

**SGD with momentum** still wins on convolutional networks for image classification, reaching better final test accuracy than Adam with a good schedule. The gap is real and not fully explained; the working theory is that Adam's adaptivity finds sharper minima that generalise slightly worse.

Two practical notes. Adam's learning rate is not comparable to SGD's — 3e-4 is a common Adam starting point against 0.1 for SGD, because Adam's step size is roughly the learning rate regardless of gradient magnitude. And the **schedule usually matters more than the optimizer**: cosine decay with a short warmup is the standard, and warmup exists because early Adam steps are unreliable while the moving averages have too little history.`}],quiz:[{question:`On the ravine, SGD at lr = 0.019 crawled and at lr = 0.021 diverged. What sets that limit?`,options:[{text:`The starting point (10.0, 1.0)`,explanation:`The limit is the same wherever you start.`},{text:`The curvature in the steepest direction — the limit is 2/100 = 0.02, and the flat direction is stuck with whatever that allows`,explanation:`Correct. That conflict is the problem every optimizer here is answering.`},{text:`Floating-point precision`,explanation:`The divergence is a real dynamical instability, not a numerical one.`},{text:`The number of steps`,explanation:`Divergence at 0.021 begins on step one.`}],correct:1},{question:`How does one momentum mechanism both accelerate the flat direction and damp the steep one?`,options:[{text:`It uses a different β per direction`,explanation:`β is a single shared number.`},{text:`Gradients that keep pointing the same way accumulate; gradients that flip sign cancel`,explanation:`Correct. The flat direction accumulates to roughly 10× at β = 0.9; the oscillating direction cancels itself.`},{text:`It normalises each coordinate by its gradient magnitude`,explanation:`That is RMSProp, a different mechanism.`},{text:`It reduces the learning rate over time`,explanation:`That is a schedule, not momentum.`}],correct:1},{question:`RMSProp took 524 steps — worse than SGD's 223. What does that show?`,options:[{text:`RMSProp was implemented wrongly`,explanation:`The implementation is standard and its learning rate came from the same sweep.`},{text:`Per-coordinate scaling fixes conditioning but removes the accumulation that carries progress along the flat direction`,explanation:`Correct — which is why Adam combines both rather than picking one.`},{text:`RMSProp needs a larger learning rate`,explanation:`Its rate was the best from the same grid the others were tuned on.`},{text:`RMSProp only works on noisy gradients`,explanation:`It helps there, but that does not explain being beaten by SGD on this surface.`}],correct:1},{question:`Adam's best learning rate here was 0.5 against SGD's 0.019. Why are they so different?`,options:[{text:`Adam is more stable so it tolerates any rate`,explanation:`It has its own instabilities; that is not the reason.`},{text:`Adam divides by the gradient magnitude, so its step is roughly the size of lr regardless of the surface — the parameter means something different`,explanation:`Correct, which is why Adam and SGD learning rates are never transferable.`},{text:`Adam uses a different loss`,explanation:`Same loss and same starting point throughout.`},{text:`The sweep grid was different`,explanation:`All four were swept over the identical grid.`}],correct:1},{question:`With a constant gradient of 1.0, Adam's raw moving average was 0.1 at t = 1. Why does that need fixing?`,options:[{text:`It makes the gradient too large`,explanation:`It makes the estimate ten times too small.`},{text:`The average starts at 0, so early estimates are biased toward zero — dividing by (1 − 0.9ᵗ) recovers 1.0000 exactly`,explanation:`Correct, and the correction decays to nothing, so it only affects early training.`},{text:`It causes numerical overflow`,explanation:`The values are small and well behaved.`},{text:`β must be 0.5 instead`,explanation:`β = 0.9 is standard; the bias is corrected rather than avoided.`}],correct:1},{question:`Why is AdamW preferred over Adam with L2 regularisation?`,options:[{text:`It is faster`,explanation:`The cost is essentially identical.`},{text:`L2-as-gradient gets divided by Adam's adaptive term, so it stops functioning as weight decay; AdamW applies decay directly to the weights`,explanation:`Correct — the two are equivalent under plain SGD but not under an adaptive optimizer.`},{text:`AdamW uses less memory`,explanation:`Both keep the same two moment buffers.`},{text:`L2 does not work with momentum`,explanation:`It works fine with momentum; the adaptive division is the issue.`}],correct:1}],interviewQuestions:[{question:`Explain momentum.`,answer:`It keeps a velocity — a decaying sum of past gradients — and steps along that rather than along the current gradient. The reason it helps is that it does two opposite things at once on an ill-conditioned surface. In a direction where gradients consistently point the same way, they accumulate, and at β = 0.9 the velocity approaches about ten times the raw gradient, which is like a ten-times larger learning rate exactly where one was needed. In an oscillating direction the gradient flips sign each step so the contributions cancel and the oscillation damps. On the ravine that was 70 steps against SGD's 223, both at their own best learning rate.`,isCaseBased:!1},{question:`Adam versus SGD with momentum — which do you pick?`,answer:`AdamW for transformers, language models, and anything new where I want to get to a reasonable result without tuning much — it is far more forgiving about the learning rate because it normalises by gradient magnitude. SGD with momentum for convolutional image classification, where it still reaches better final test accuracy with a good schedule; the gap is real and the working explanation is that Adam finds sharper minima that generalise slightly worse. In practice I would start with AdamW to establish that the pipeline works, and only try SGD if I am pushing for the last point of accuracy on a vision task.`,isCaseBased:!1},{question:`Why does Adam need bias correction?`,answer:`Both moving averages are initialised at zero, so early on they underestimate badly. With a constant gradient of 1.0, the first-moment estimate at t = 1 is only 0.1 — a tenfold underestimate — and the second moment, at β₂ = 0.999, is a thousandfold one. Dividing by (1 − βᵗ) recovers the true value exactly: 1.0000 at every step in the worked example. Without it the first steps are wrongly sized and the ratio of two differently-biased estimates misbehaves, which destabilises early training. The correction decays to nothing within a few hundred steps, so it is purely an early-training fix — and it is the same problem warmup schedules address from the other side.`,isCaseBased:!0},{question:`Your training loss is oscillating and not decreasing. What do you check?`,answer:`The learning rate first, because oscillation with no progress is the signature of stepping past the stability limit in the steepest direction — on the worked ravine, 0.019 was stable and 0.021 diverged, a 10% change. So I would cut it by ten and see whether the oscillation resolves, which confirms the diagnosis cheaply. If it does, the fix is not just a smaller rate but momentum or Adam, which handle the conditioning that caused it. If a smaller rate does not help, the problem is elsewhere: batch size too small for the gradient to be meaningful, a bug that mismatches inputs and labels, or exploding gradients that need clipping.`,isCaseBased:!0},{question:`What learning rate schedule would you use and why?`,answer:`Linear warmup then cosine decay, which is the standard for good reasons. Warmup exists because Adam's moment estimates have almost no history in the first few hundred steps, so its updates are unreliable exactly when the weights are most fragile — starting at zero and ramping over a few hundred to a few thousand steps avoids that. Cosine decay then spends most of the budget at a useful rate and anneals smoothly to near zero, which lets the model settle rather than bouncing around a minimum. Step decay works too and is more common in older vision recipes. Honestly the schedule usually matters more than the choice of optimizer.`,isCaseBased:!1},{question:`Why is a well-tuned SGD sometimes better than Adam?`,answer:`Generalisation, specifically on convolutional image models. Adam consistently reaches a lower training loss faster and can end up with worse test accuracy. The leading explanation is that adaptivity steers toward sharper minima — the per-coordinate normalisation lets it take large steps in low-curvature directions and settle in narrow basins — while SGD's gradient noise acts as an implicit regulariser that favours flatter ones. It is also partly historical: vision recipes have been tuned for SGD for a decade, so the comparison is not entirely fair. AdamW closed much of the gap by fixing the weight-decay interaction, which supports the view that some of the original difference was regularisation rather than optimisation.`,isCaseBased:!1},{question:`How do you choose an initial learning rate?`,answer:`A learning-rate range test is the cheapest reliable method: train for a few hundred steps while increasing the rate exponentially, plot loss against rate, and take roughly an order of magnitude below where the loss starts climbing. Failing that, the defaults are informative — 3e-4 for Adam, 0.1 for SGD with momentum on vision — and worth knowing that they are not comparable, because Adam's step is roughly the size of its learning rate regardless of gradient magnitude while SGD's is proportional to the gradient. On the worked ravine the best rates were 0.5 for Adam and 0.019 for SGD, a factor of twenty-five apart on the identical problem. I would also scale the rate with batch size, since a larger batch gives a less noisy gradient that supports a larger step.`,isCaseBased:!0},{question:`What is the memory cost of an optimizer, and when does it matter?`,answer:`SGD stores nothing beyond the gradients. Momentum adds one buffer the size of the parameters. Adam adds two, so optimizer state alone is twice the model size — and in mixed-precision training with a master copy of the weights, the total working set is commonly around four times the parameters in optimizer-related memory. That is decisive at scale: it is why large-model training uses sharded optimizer states across devices, and why memory-efficient variants like Adafactor, which factorises the second moment, or 8-bit optimizers exist at all. For a model that fits comfortably it is irrelevant; at the frontier it is one of the binding constraints.`,isCaseBased:!1}],flashcards:[{front:`The core problem`,back:`Ravines. The learning rate is capped by the STEEPEST direction, which makes the flat direction glacial. Every optimizer answers that one conflict.`},{front:`The stability limit, measured`,back:`Curvature 100 means the limit is 2/100 = 0.02. lr 0.019 crawls; lr 0.021 diverges on step one.`},{front:`Momentum`,back:`Velocity = decaying sum of gradients. Same-direction gradients accumulate (~10× at β=0.9); flipping ones cancel. 70 steps vs SGD's 223.`},{front:`RMSProp`,back:`Divide each coordinate by the root of its own mean squared gradient. On the ravine it took 524 steps — WORSE than SGD.`},{front:`What that tells you about Adam`,back:`Adam is not "the adaptive one" — it is momentum AND RMSProp together. 54 steps. Adaptive scaling alone was worse than plain SGD.`},{front:`Bias correction`,back:`Averages start at 0. Constant gradient 1.0 gives raw m = 0.1 at t=1; dividing by (1 − 0.9ᵗ) gives exactly 1.0000. Decays to nothing.`},{front:`Adam vs SGD learning rates`,back:`Never transferable. Best here: Adam 0.5, SGD 0.019 — 25× apart. Adam's step is ≈ lr regardless of gradient magnitude.`},{front:`AdamW`,back:`Weight decay applied directly to the weights. Inside Adam, L2-as-gradient gets divided by the adaptive term and stops being decay at all.`}],mindmapMarkdown:`- Optimizers
  - The problem
    - ravines: steep one way, flat the other
    - lr capped by the steep direction
    - curvature 100 -> limit 2/100 = 0.02
    - 0.019 crawls, 0.021 diverges
  - SGD
    - step proportional to the gradient
    - 223 steps on the ravine
  - Momentum
    - velocity = decaying sum of gradients
    - same direction accumulates (~10x at beta 0.9)
    - flipping direction cancels
    - 70 steps
  - RMSProp
    - divide by root mean squared gradient per coordinate
    - fixes conditioning, loses accumulation
    - 524 steps - WORSE than SGD
  - Adam
    - momentum + RMSProp
    - 54 steps, at lr 0.5 vs SGD's 0.019
    - bias correction: raw m 0.1 -> 1.0000 at t=1
  - In practice
    - AdamW for transformers (decay applied to weights)
    - SGD+momentum still wins on CNN vision
    - schedule (warmup + cosine) matters more
    - Adam state = 2x model size in memory`};export{e as default};