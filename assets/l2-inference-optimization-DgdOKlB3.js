var e={id:`genai-l2-inference-optimization`,subjectId:`genai`,level:2,title:`Inference Optimization`,whyItMatters:`Generating one token at batch size 1 achieves 1 FLOP per byte of memory read, on hardware that needs about 200 to be compute-bound. Inference is a memory-bandwidth problem wearing a compute problem's clothes, and every technique here follows from that.`,assumes:[`You have read *The Transformer Block*`,`You know what the KV cache is, or are willing to meet it here`],estMinutes:20,sections:[{type:`intuition`,title:`Two phases with opposite problems`,md:`Generation has two distinct phases and they behave nothing alike.

**Prefill** processes the whole prompt at once. Every position is computed in parallel, so the GPU has plenty of work and is **compute-bound** — this is where the quadratic attention cost bites.

**Decode** generates one token at a time, each depending on the last. To produce one token the entire model must be read from memory, and then used for a single token's worth of arithmetic. That is **memory-bandwidth-bound**, and it is where essentially all the time goes.

The distinction matters because the optimisations differ completely. Anything that reduces bytes read helps decode; anything that reduces FLOPs helps prefill.`},{type:`math`,intro:`Arithmetic intensity, and the KV cache. The first says how much work you get per byte read: the batch size B is in the numerator and not the denominator, which is why batching is nearly free. The second is per sequence, and the g is the grouped-query sharing factor.`,latex:[`I = \\frac{2NB}{2N} = B \\quad \\text{FLOPs per byte}`,`M_{KV} = 2 \\cdot L \\cdot n \\cdot \\frac{d}{g} \\cdot b \\text{ bytes}`]},{type:`code`,lang:`python`,title:`The arithmetic intensity that explains everything`,code:`params = 7e9
for B in [1, 8, 32, 128]:
    flops = 2 * params * B              # 2 FLOPs per parameter per token
    read  = params * 2                  # bf16 weights, read once per step
    print('batch %3d: %.2e FLOPs / %.2e bytes = %6.1f FLOPs per byte'
          % (B, flops, read, flops/read))
print('an A100 needs about 200 FLOPs/byte before it is compute-bound')

# ---- real output ----
# batch   1: 1.40e+10 FLOPs / 1.40e+10 bytes =    1.0 FLOPs per byte
# batch   8: 1.12e+11 FLOPs / 1.40e+10 bytes =    8.0 FLOPs per byte
# batch  32: 4.48e+11 FLOPs / 1.40e+10 bytes =   32.0 FLOPs per byte
# batch 128: 1.79e+12 FLOPs / 1.40e+10 bytes =  128.0 FLOPs per byte`,annotations:{4:`The weights are read once per decoding step regardless of batch size — that is the key asymmetry. FLOPs scale with the batch; bytes read do not.`,9:`1.0 FLOP per byte at batch 1 against roughly 200 needed. The GPU is idle about 99.5% of the time, waiting for memory.`,12:`Which is why batching is close to free until you approach the hardware's ratio: serving 32 requests together costs barely more wall-clock time than serving one. It is the single largest throughput lever in inference.`}},{type:`code`,lang:`python`,title:`The KV cache, which is why batching eventually stops being free`,code:`for name, d, L, n, share in [('LLaMA-2 7B, MHA', 4096, 32, 4096, 1),
                             ('LLaMA-2 7B, MHA', 4096, 32, 32768, 1),
                             ('LLaMA-2 70B, GQA 8/64', 8192, 80, 4096, 8)]:
    gb = 2 * L * n * (d // share) * 2 / 1e9        # K and V, 2 bytes each
    print('%-24s n=%6d -> %6.2f GB per sequence' % (name, n, gb))

for name, bits in [('fp32', 32), ('bf16', 16), ('int8', 8), ('int4', 4)]:
    print('7B in %-5s: %5.1f GB' % (name, 7e9 * bits / 8 / 1e9))

# ---- real output ----
# LLaMA-2 7B, MHA          n=  4096 ->   2.15 GB per sequence
# LLaMA-2 7B, MHA          n= 32768 ->  17.18 GB per sequence
# LLaMA-2 70B, GQA 8/64    n=  4096 ->   1.34 GB per sequence
# 7B in fp32 :  28.0 GB
# 7B in bf16 :  14.0 GB
# 7B in int8 :   7.0 GB
# 7B in int4 :   3.5 GB`,annotations:{5:`The cache stores every key and value from every layer for every position, so it grows linearly with sequence length and layer count — and it is per sequence, not shared.`,11:`2.15 GB for one 4K sequence on a 7B model. On an 80 GB card holding 14 GB of weights, that caps you at about 30 concurrent sequences — the KV cache, not the model, is what limits batch size.`,12:`17.18 GB at 32K context. A single long-context request now consumes more memory than the model itself, which is why long context is expensive to serve rather than merely expensive to compute.`,13:`Grouped-query attention with 8 KV heads for 64 query heads: 1.34 GB for a model ten times larger, at the same context length. That is why every recent model uses it.`}},{type:`note`,label:`Quantisation: the cheapest large win`,md:`Since decode is bandwidth-bound, halving the bytes read roughly halves the time. Quantisation does exactly that.

**int8** typically costs almost nothing in quality and halves the memory against bf16. **int4** halves it again — 3.5 GB for a 7B model — with a small but measurable degradation, larger on tasks needing precise reasoning than on conversational ones.

The important detail is that **weights and activations quantise differently**. Weight-only quantisation is easy because the values are fixed and their distribution can be studied offline; activations vary per input and contain outliers that break naive schemes, which is what LLM.int8() and SmoothQuant address.

**GPTQ** and **AWQ** both quantise weights while using a small calibration set to protect the channels that matter most, and both are close to lossless at 4 bits for most models. That is the default deployment path now.`},{type:`code`,lang:`python`,title:`Speculative decoding: two models, fewer sequential steps`,code:`for accept in [0.5, 0.7, 0.9]:
    k = 4
    expected = (1 - accept**(k+1)) / (1 - accept)
    print('accept rate %.1f, k=%d draft tokens -> %.2f tokens per verification'
          % (accept, k, expected))

# ---- real output ----
# accept rate 0.5, k=4 draft tokens -> 1.94 tokens per verification
# accept rate 0.7, k=4 draft tokens -> 2.77 tokens per verification
# accept rate 0.9, k=4 draft tokens -> 4.10 tokens per verification`,annotations:{3:`A small draft model proposes k tokens, and the large model verifies all of them in ONE forward pass — which is nearly free, because that pass was memory-bound and had spare compute.`,9:`At a 70% acceptance rate, 2.77 tokens come out per expensive step instead of 1. That is a 2.8x speedup with no change to the output distribution — the verification is exact, so rejected tokens are simply discarded.`,10:`The gain depends entirely on the acceptance rate, which depends on how well the draft model imitates the target. A draft that is too weak accepts rarely; one that is too strong is not cheap enough to be worth running.`}},{type:`note`,label:`The serving techniques that matter operationally`,md:`**Continuous batching.** Naive batching waits for every sequence in a batch to finish, so one long generation stalls all the others. Continuous batching removes a finished sequence and admits a new one at every step, and typically gives a 2–4x throughput improvement over static batching — usually the largest single serving win.

**PagedAttention.** The KV cache is allocated in fixed-size blocks like operating-system memory pages rather than as one contiguous reservation for the maximum possible length. That removes the fragmentation and over-reservation that wasted the majority of KV memory in earlier systems, and it makes prefix sharing between requests possible. It is the core idea in vLLM.

**Prefix caching.** Requests sharing a system prompt share its KV cache entirely, which for a long shared prefix removes most of the prefill cost.

**Which metric you optimise changes the answer.** Time-to-first-token is a prefill problem; tokens-per-second is a decode problem; throughput is a batching problem. Large batches raise throughput and hurt latency, so you cannot optimise all three, and the honest first question is which one the product actually needs.`}],quiz:[{question:`Why is decoding memory-bandwidth-bound?`,options:[{text:`The model is too large to fit in memory`,explanation:`It fits; the problem is reading it repeatedly.`},{text:`The entire model must be read from memory to produce one token's worth of arithmetic — 1 FLOP per byte against about 200 needed`,explanation:`Correct, so the GPU is idle roughly 99.5% of the time at batch 1.`},{text:`The KV cache is too slow`,explanation:`The weights dominate the bytes read.`},{text:`Attention is quadratic`,explanation:`That bites during prefill, not decode.`}],correct:1},{question:`Why is batching nearly free at small batch sizes?`,options:[{text:`The GPU parallelises the memory reads`,explanation:`Bandwidth is the constraint, not parallelism.`},{text:`The weights are read once per step regardless of batch size, so FLOPs scale with the batch and bytes read do not`,explanation:`Correct — 32 requests cost barely more wall-clock than one, until you approach the hardware ratio.`},{text:`Larger batches use less memory`,explanation:`They use more, via the KV cache.`},{text:`It reduces the number of layers evaluated`,explanation:`Every layer runs regardless.`}],correct:1},{question:`A 7B model in bf16 is 14 GB and one 4K sequence needs 2.15 GB of KV cache. What follows?`,options:[{text:`The model should be quantised first`,explanation:`Useful, but it does not address the binding constraint.`},{text:`On an 80 GB card you fit about 30 concurrent sequences — the KV cache, not the model, caps batch size`,explanation:`Correct, which is why PagedAttention and GQA matter so much.`},{text:`Batch size is unlimited`,explanation:`The cache is per sequence and grows with length.`},{text:`Longer sequences are free`,explanation:`At 32K the cache is 17.18 GB — more than the model.`}],correct:1},{question:`Grouped-query attention gave 1.34 GB for a 70B model against 2.15 GB for a 7B one. How?`,options:[{text:`The 70B model has fewer layers`,explanation:`It has 80 against 32.`},{text:`8 KV heads shared across 64 query heads — an eightfold cache reduction, which more than offsets the larger model`,explanation:`Correct, and it is why every recent model uses GQA.`},{text:`It uses int4 for the cache`,explanation:`Both figures are fp16.`},{text:`A shorter context`,explanation:`Both are at n = 4096.`}],correct:1},{question:`How does speculative decoding give a speedup without changing the output?`,options:[{text:`It approximates the large model's distribution`,explanation:`The verification is exact — rejected tokens are discarded.`},{text:`A draft model proposes k tokens and the large model verifies all of them in one pass, which was memory-bound and had spare compute anyway`,explanation:`Correct — at a 70% accept rate that is 2.77 tokens per expensive step instead of 1.`},{text:`It skips layers`,explanation:`Every layer runs in the verification pass.`},{text:`It caches previous outputs`,explanation:`That is prefix caching, a separate technique.`}],correct:1},{question:`What does continuous batching fix?`,options:[{text:`KV cache fragmentation`,explanation:`That is PagedAttention.`},{text:`Naive batching waits for every sequence to finish, so one long generation stalls the rest — continuous batching admits a new sequence at every step`,explanation:`Correct, typically 2–4x throughput and usually the largest single serving win.`},{text:`Quantisation error`,explanation:`Unrelated.`},{text:`Time to first token`,explanation:`It improves throughput; TTFT is a prefill concern.`}],correct:1}],interviewQuestions:[{question:`Why is LLM inference slow?`,answer:`Because decoding is memory-bandwidth-bound rather than compute-bound, which is the opposite of what people assume. To generate one token the entire model has to be read from memory and then used for one token's worth of arithmetic — at batch size 1 on a 7B model that is 1 FLOP per byte read, on hardware that needs around 200 to saturate its compute. The GPU is idle roughly 99.5% of the time waiting for memory. Everything follows from that: batching is nearly free because the weights are read once per step regardless of batch size, quantisation helps because it halves the bytes read, and adding FLOPs — as speculative decoding does — can be almost costless.`,isCaseBased:!1},{question:`Explain prefill versus decode.`,answer:`Prefill processes the whole prompt in one pass, so every position is computed in parallel and the GPU has plenty of work — it is compute-bound, and it is where the quadratic attention cost bites. Decode generates one token at a time, each depending on the last, so it is inherently sequential and memory-bandwidth-bound. They need opposite optimisations: anything reducing FLOPs helps prefill, anything reducing bytes read helps decode. It also determines which metric you are moving — time-to-first-token is a prefill problem and tokens-per-second is a decode problem, so a long prompt with a short answer and a short prompt with a long answer are different engineering problems.`,isCaseBased:!1},{question:`What is the KV cache and why does it constrain serving?`,answer:`It stores the keys and values from every layer for every position already generated, so each new token attends to them without recomputing. Without it, generating token n would mean reprocessing all n−1 previous tokens every step. The constraint is size: it grows linearly with sequence length and layer count and is per sequence, not shared. For LLaMA-2 7B at 4K context that is 2.15 GB per sequence, so on an 80 GB card holding 14 GB of weights you fit around 30 concurrent requests — the cache, not the model, caps your batch size. At 32K context it is 17.18 GB, more than the model itself, which is why long context is expensive to serve rather than merely expensive to compute.`,isCaseBased:!0},{question:`How would you speed up a deployed model?`,answer:`In order of return. Continuous batching first, since it typically gives 2–4x throughput over static batching and requires no model changes — it removes finished sequences and admits new ones every step rather than waiting for the slowest in a batch. Then quantisation to int8 or int4, which roughly halves or quarters the bytes read, and since decode is bandwidth-bound that translates almost directly into speed. Then PagedAttention to stop the KV cache fragmenting and over-reserving, which raises the achievable batch size. Then speculative decoding if latency rather than throughput is the target. And I would confirm the workload shape first, because a prompt-heavy workload wants prefill optimisation and prefix caching instead.`,isCaseBased:!0},{question:`How does speculative decoding work, and what limits it?`,answer:`A small draft model proposes k tokens autoregressively, then the large model verifies all k in a single forward pass — which is nearly free because that pass was memory-bound and had spare compute. Accepted tokens are kept, and the first rejection discards the rest. The verification is exact, so the output distribution is unchanged, which is what makes it a strictly free speedup rather than a quality trade. The limit is the acceptance rate: at 70% with k = 4 you get 2.77 tokens per expensive step, at 50% only 1.94. And it is a balance — a draft model too weak accepts rarely, one too strong is not cheap enough to be worth running.`,isCaseBased:!1},{question:`What is PagedAttention?`,answer:`Allocating the KV cache in fixed-size blocks, like operating-system memory pages, instead of reserving one contiguous region per sequence sized for the maximum possible length. That reservation was the problem: a request that might generate 2,000 tokens reserved space for 2,000 and typically used a few hundred, and earlier systems wasted the majority of their KV memory to that plus fragmentation. Paging removes both, so the achievable batch size rises substantially. It also enables prefix sharing, because two requests with the same system prompt can point at the same physical blocks. It is the core idea in vLLM and it is a systems insight rather than a modelling one.`,isCaseBased:!1},{question:`Which quantisation would you deploy?`,answer:`int4 weight-only with GPTQ or AWQ as the default, and int8 if the task needs precise reasoning. Both use a small calibration set to identify and protect the channels that matter most, and both are close to lossless at 4 bits for most models — 3.5 GB for a 7B model against 14 in bf16. The detail worth knowing is that weights and activations quantise very differently: weights are fixed so their distribution can be studied offline, while activations vary per input and contain outliers that break naive schemes, which is what LLM.int8() and SmoothQuant exist to handle. So weight-only is the safe default, and I would benchmark on the actual task rather than trusting a perplexity delta.`,isCaseBased:!0},{question:`Latency or throughput — how do you decide?`,answer:`By asking what the product is, because they trade against each other and you cannot have both. An interactive chat interface needs time-to-first-token under a few hundred milliseconds and a token rate above reading speed, so batch sizes stay small and some GPU capacity is deliberately wasted. A batch pipeline scoring a million documents overnight cares only about throughput, so batches get as large as the KV cache allows and latency per request is irrelevant. Most real systems run both paths on separate deployments rather than compromising on one. The mistake I would warn against is optimising average latency when the product is judged on the tail — p99 is usually the number users experience.`,isCaseBased:!0}],flashcards:[{front:`The core fact`,back:`Decode is MEMORY-BANDWIDTH-bound. Batch 1 on a 7B model: 1.0 FLOP per byte read, on hardware needing ~200. The GPU is idle ~99.5% of the time.`},{front:`Prefill vs decode`,back:`Prefill: whole prompt at once, compute-bound, quadratic attention. Decode: one token at a time, bandwidth-bound, where the time goes.`},{front:`Why batching is nearly free`,back:`Weights are read once per step regardless of batch size. FLOPs scale with the batch; bytes read do not.`},{front:`KV cache size`,back:`LLaMA-2 7B: 2.15 GB at 4K, 17.18 GB at 32K — per sequence. On 80 GB it caps you near 30 concurrent requests.`},{front:`GQA on the cache`,back:`LLaMA-2 70B with 8 KV heads for 64 query heads: 1.34 GB at 4K — less than the 7B model with full MHA.`},{front:`Quantisation`,back:`7B: fp32 28 GB, bf16 14, int8 7, int4 3.5. Bandwidth-bound means halving bytes roughly halves time. GPTQ/AWQ are near-lossless at 4 bits.`},{front:`Speculative decoding`,back:`Draft k tokens, verify all in ONE pass. Accept 0.7, k=4 → 2.77 tokens per expensive step. Verification is exact, so output is unchanged.`},{front:`The two serving wins`,back:`Continuous batching (admit a new sequence every step): 2–4x throughput. PagedAttention (block-allocated KV): removes over-reservation and enables prefix sharing.`}],mindmapMarkdown:`- Inference optimization
  - Two phases
    - prefill: whole prompt, COMPUTE-bound, quadratic attention
    - decode: one token, BANDWIDTH-bound, where the time goes
  - The core arithmetic
    - batch 1, 7B: 1.0 FLOP per byte
    - hardware needs ~200 -> idle 99.5% of the time
    - weights read once per step regardless of batch
    - so batching is nearly free
  - The KV cache
    - grows with length x layers, PER SEQUENCE
    - 7B at 4K: 2.15 GB; at 32K: 17.18 GB
    - caps batch size, not the model
    - GQA 8/64 on 70B: 1.34 GB at 4K
  - Quantisation
    - 7B: fp32 28, bf16 14, int8 7, int4 3.5 GB
    - bandwidth-bound -> halving bytes halves time
    - weights easy, ACTIVATIONS have outliers
    - GPTQ / AWQ near-lossless at 4 bits
  - Speculative decoding
    - draft k, verify all in one pass
    - accept 0.7, k=4 -> 2.77 tokens per step
    - exact verification: output unchanged
  - Serving
    - continuous batching: 2-4x, the biggest single win
    - PagedAttention: block KV, no over-reservation
    - prefix caching for shared system prompts
    - TTFT / tokens-per-second / throughput pull apart`};export{e as default};