var e={id:`mlops-l2-data-versioning`,subjectId:`mlops`,level:2,title:`Data & Model Versioning with DVC`,whyItMatters:`"Reproduce last quarter's model" is a one-line request that destroys teams who never versioned their data. Your code is in git, your metrics are in MLflow — and the CSV that produced them was overwritten in March. This module gives you the thing that closes that hole: a content hash in git, the bytes in object storage, and a pipeline that records which hash made which model. It is also the "D" in the grand practical, and the correct answer to the interview question "how do you version data?".`,estMinutes:45,sections:[{type:`intuition`,title:`Git was built for text. Your dataset is not text.`,md:`Git is a photo album that keeps every photo you ever took, forever, and hands the whole album to anyone who wants one picture.

- Git is **content-addressed and immutable**: every version of every file is stored forever, in every clone. That is a feature for source code and a disaster for a 2 GB parquet file.
- Refresh that file monthly for two years and the repository is 48 GB. \`git clone\` becomes a coffee break. CI becomes a coffee break. Every teammate pays for versions they will never open.
- Deleting it does not help. \`git rm\` removes the file from the working tree; the blob stays in **history**, so clones still carry it. Removing it for real means rewriting history and force-pushing over everyone.
- Git also **diffs and merges text line by line**. For a binary file it gives up and prints one useless sentence. There is no "what changed" for a checkpoint.
- And git compresses by delta-ing similar objects. Parquet and \`.pt\` files are already compressed, so the deltas are near-useless — you store roughly the full size every single time.
- The one-line summary: **git is optimised for many small mutable text files; data is few huge immutable binary files.** Opposite shape, opposite tool.`},{type:`code`,lang:`bash`,title:`What committing a dataset actually costs`,code:`git add data/raw.csv && git commit -m 'add training data'
# 2.0 GB is now a permanent object in .git/objects

# thirty monthly refreshes later:
du -sh .git                 # ~61G - every version of every file, forever
git clone git@github.com:acme/churn.git   # a new hire downloads all 61 GB

git rm data/raw.csv && git commit -m 'oops, remove the data'
du -sh .git                 # still ~61G. The commit removed it from the TREE,
                            # not from history. Clones still carry every byte.

git diff HEAD~1 data/raw.csv
# Binary files a/data/raw.csv and b/data/raw.csv differ`,annotations:{2:`Permanent is the operative word. Git never forgets an object that a commit reachable from a branch points at.`,5:`Thirty near-identical 2 GB files. Git cannot delta them usefully because parquet/csv.gz are already compressed.`,6:`Clone downloads the whole history by default. Your onboarding doc now says "this takes 40 minutes".`,9:`The trap everyone falls into. Undoing the commit does not shrink the repo — only a history rewrite (git filter-repo / BFG) plus a coordinated force-push does.`,13:`The entire diff. No line changed, no rows added, no column renamed — just "differ". Code review on data is impossible in git.`}},{type:`note`,md:"The honest word on **git-lfs**: it solves the same problem the same way — a small text pointer in git, the real bytes on a separate LFS server — and if your data is a handful of files and you already pay for GitHub, it is a perfectly reasonable answer. Its weaknesses are practical, not conceptual: LFS storage/bandwidth is billed by your git host rather than sitting in your own cheap S3 bucket, `git clone` still tries to fetch every LFS object by default, and it versions *files* with no idea what a pipeline, a parameter or a metric is. DVC keeps the same pointer trick, puts the bytes in storage you already own, and adds the pipeline layer on top."},{type:`intuition`,title:`The DVC idea: the pointer in git, the bytes in a bucket`,md:'A library catalogue card. The card is three lines and lives in a drawer you can carry. The book is heavy and lives on a shelf. The card tells you *exactly* which book — including the edition.\n\n- **DVC** (Data Version Control) never puts your data in git. It puts a tiny text file in git that names the data by its **content hash**.\n- `data/raw.csv.dvc` is about 180 bytes: a hash, a size, a path. It diffs, reviews, branches and merges like any other text file — because it is one.\n- The bytes go to a **remote**: S3, GCS, Azure Blob, an SSH box, even a shared network drive. Storage you already pay for, at object-storage prices.\n- The payoff sentence: **a git commit now pins an exact dataset version.** Commit `a1b2c3` says "model.pkl came from the CSV whose md5 is 8f14e4…", and `dvc pull` fetches exactly those bytes.\n- Two pushes, always: `git push` sends the pointer, `dvc push` sends the bytes. Forgetting the second one is the single most common DVC mistake — your teammate gets a commit that references data nobody can fetch.\n- `.dvc` files also work on directories and on models: `dvc add models/model.pkl` versions a checkpoint exactly the same way.'},{type:`code`,lang:`yaml`,title:`The pointer file, in full`,code:`# data/raw.csv.dvc  --  about 180 bytes, and THIS is what git stores
outs:
- md5: 8f14e45fceea167a5a36dedd4bea2543
  size: 2147483648
  hash: md5
  path: raw.csv

# data/.gitignore  --  written by 'dvc add' so git never sees the real file
/raw.csv`,annotations:{1:`Committed, reviewed and merged like source code. A pull request that changes a dataset shows up as a one-line hash change.`,3:`The whole mechanism. This hash is both the identity of the data and its address in the cache and on the remote.`,4:`2147483648 bytes = exactly 2 GiB, sitting in S3. Git stores this number, not those bytes.`,6:`Path is relative to the .dvc file. The real file lands here after dvc pull.`,8:`dvc add writes this for you. It is what stops someone accidentally committing the 2 GB by hand.`,9:`Belt and braces: git is explicitly told to ignore the data file, so only the pointer can ever be committed.`}},{type:`code`,lang:`bash`,title:`Two machines, one dataset`,code:`# ---- machine A: the person who has the data ----
dvc init                    # creates .dvc/ and .dvcignore, stages them for git
git commit -am 'dvc init'

dvc remote add -d storage s3://acme-ml/churn   # -d makes it the DEFAULT remote
git commit -am 'add dvc remote'                # .dvc/config is normal tracked text

dvc add data/raw.csv        # 2.0 GB: hashed, moved into .dvc/cache, linked back
# -> wrote data/raw.csv.dvc and data/.gitignore
git add data/raw.csv.dvc data/.gitignore
git commit -m 'raw churn export, 2026-08'

dvc push                    # the BYTES go to S3
git push                    # the POINTER goes to GitHub

# ---- machine B: a teammate, or CI, or you in six months ----
git clone git@github.com:acme/churn.git && cd churn
cat data/raw.csv.dvc        # the pointer is here; the data file is not
dvc pull                    # fetch exactly the bytes this commit pins

git checkout v1-baseline    # an older commit: the .dvc file changes back
dvc checkout                # ...and the working data file follows it`,annotations:{2:`One-time setup per repo. .dvc/config is committed; .dvc/cache is git-ignored local storage.`,5:`Credentials are NOT stored here - DVC uses the same AWS/GCP credential chain as the CLI. Secrets go in .dvc/config.local, which is git-ignored.`,8:`The three things dvc add does: hash the file, move it into the local cache, and leave a link in its place. Your code path never changes.`,11:`This commit is the version. The message describes the data; the hash identifies it exactly.`,13:`dvc push uploads any cache objects the remote is missing. It is deduplicated - unchanged files are skipped.`,14:`Two pushes. If you only git push, your teammate gets a pointer to bytes they cannot fetch: "ERROR: failed to pull data from the cloud".`,19:`The moment that pays for everything. A clone that took two seconds now materialises the exact 2 GB that commit refers to.`,21:`git checkout swaps the POINTER. Your working data file is still the old one until the next line.`,22:`dvc checkout syncs the workspace from the local cache; dvc pull does the same but downloads first if the cache is missing it.`}},{type:`visual`,component:`PointerBoxDiagram`,props:{title:`The pointer indirection, one frame at a time`,notice:`Left is what git stores. Right is where the bytes actually live. Watch the arrow, not the boxes.`,leftLabel:`git repo (tiny)`,rightLabel:`cache + remote storage`,frames:[{note:`After dvc add and two pushes. Git holds a 180-byte text file whose md5 names the data. The 2 GB sits in S3 under that same hash. The commit is the version pin.`,stack:[{name:`raw.csv.dvc @ HEAD`,to:`b1`}],heap:[{id:`b1`,value:`8f14e4... 2.0 GB`,label:`s3 blob`}]},{note:`A teammate clones. Two seconds, because the repo is a few megabytes of code and pointers. The arrow exists; the bytes do not exist locally yet.`,stack:[{name:`git clone (2 s)`,to:`b1`}],heap:[{id:`b1`,value:`8f14e4... 2.0 GB`,label:`remote only`}]},{note:`dvc pull. DVC reads the hash from the pointer, downloads that object into .dvc/cache, and links it into the workspace as data/raw.csv. Same bytes, verified by hash.`,stack:[{name:`data/raw.csv`,to:`c1`},{name:`raw.csv.dvc`,to:`b1`}],heap:[{id:`c1`,value:`.dvc/cache/8f/14e4...`,label:`local`},{id:`b1`,value:`8f14e4... 2.0 GB`,label:`s3 blob`}]},{note:`git checkout v1-baseline. The pointer now names an OLDER hash - but the file on disk is still last month's data. Git moved the card; it cannot move the book. This mismatch is the classic DVC gotcha.`,stack:[{name:`raw.csv.dvc @ v1`,to:`b0`,danger:!0}],heap:[{id:`b0`,value:`a3f9c1... 1.4 GB`,label:`not in cache`,danger:!0},{id:`c1`,value:`workspace: 8f14e4...`,label:`STALE`}]},{note:`dvc pull (or dvc checkout if the cache already has it) brings the matching bytes. Now code, params and data all describe the same moment in history. That is reproducibility, concretely.`,stack:[{name:`data/raw.csv`,to:`c0`},{name:`raw.csv.dvc @ v1`,to:`b0`}],heap:[{id:`c0`,value:`.dvc/cache/a3/f9c1...`,label:`restored`},{id:`b0`,value:`a3f9c1... 1.4 GB`,label:`s3 blob`}]},{note:`The danger frame: git add data/raw.csv. The 2 GB is now a permanent object in .git, in every clone, in CI, forever. git rm later removes it from the tree but not from history - only a filter-repo rewrite and a coordinated force-push can undo this.`,stack:[{name:`git add data/raw.csv`,to:`g1`,danger:!0}],heap:[{id:`g1`,value:`2.0 GB inside .git`,label:`forever`,danger:!0}]}]}},{type:`intuition`,title:`Content-addressed storage: the hash is the filename`,md:"A shipping warehouse where every crate's barcode is generated from what is inside it. Two identical crates get the same barcode — so you only store one.\n\n- DVC hashes the file content (md5 by default) and stores the object at `.dvc/cache/8f/14e45f…` — the first two hex characters are the directory, the rest is the filename.\n- Same content anywhere in the repo, in any branch, in any commit? Same hash, **one copy stored**. Two branches sharing 90% of a dataset directory store that 90% once.\n- This is why `dvc push` after a small change is fast: DVC uploads only objects the remote is missing, not the whole dataset.\n- The hash is also an **integrity check for free**. If a byte flips in transit, the recomputed hash does not match and `dvc pull` tells you instead of silently training on corruption.\n- Same idea git uses for its own objects — DVC just moved the storage somewhere cheap and kept the pointer where the review process lives.\n- Local disk trick: DVC links files from the cache into the workspace rather than copying them, so your 2 GB is not stored twice. Configure `cache.type` (reflink, hardlink, symlink) if disk space matters; the default copies for safety."},{type:`intuition`,title:`dvc.yaml: make, for machine learning`,md:"You changed one hyperparameter. Do you really want to re-run the 40-minute ETL that produced the file the model reads?\n\n- A **stage** is a command plus what it reads and what it writes: `cmd`, `deps`, `params`, `outs`, `metrics`. All declared in `dvc.yaml`, which is committed to git.\n- Stages form a **DAG**: `train`'s dep is `prepare`'s out, so DVC knows the order without you writing it. `dvc dag` draws it.\n- `dvc repro` walks that DAG and re-runs **only the stages whose inputs changed** — where \"changed\" means the hash of a dep, a param value, or the command string itself is different from last time.\n- That is why it feels like `make`: same dependency-graph-plus-timestamps idea, except DVC uses **content hashes**, so touching a file without editing it does not trigger a rebuild.\n- The bookkeeping lands in `dvc.lock` — the hash of every dep and out for the run that actually happened. Commit it. It is the receipt that says *this exact data plus this exact code produced this exact model*.\n- `params:` is the entry that makes this ML-shaped: numbers live in `params.yaml`, and changing `max_depth` invalidates `train` while leaving `prepare` cached."},{type:`code`,lang:`yaml`,title:`A real dvc.yaml (and the params file it reads)`,code:`# dvc.yaml - the pipeline definition, committed to git
stages:
  prepare:
    cmd: python src/prepare.py data/raw.csv data/clean.parquet
    deps:
      - src/prepare.py
      - data/raw.csv
    outs:
      - data/clean.parquet

  train:
    cmd: python src/train.py
    deps:
      - src/train.py
      - data/clean.parquet
    params:
      - train.n_estimators
      - train.max_depth
    outs:
      - models/model.pkl
    metrics:
      - metrics.json:
          cache: false

# params.yaml - every tunable number, in git, in one file
train:
  n_estimators: 300
  max_depth: 8`,annotations:{4:`The stage IS this command. Change the string and DVC treats the stage as changed, because the command is part of the dependency set.`,5:`deps include the CODE, not just the data. Edit prepare.py and this stage re-runs - that is the difference between a pipeline and a cron job.`,7:`data/raw.csv is already dvc-tracked, so DVC compares its hash. Note there is no raw.csv.dvc needed once a stage produces it.`,8:`outs are dvc-tracked automatically: cached, hashed, pushable, and git-ignored. You never write a .dvc file for a pipeline output.`,15:`This out-becomes-dep link is the entire DAG. DVC derives the order from it; you never declare "train runs after prepare".`,16:`params, not deps: DVC watches these specific KEYS inside params.yaml. Editing an unrelated key does not invalidate this stage.`,21:`metrics are just outs that DVC knows how to print and diff. Small JSON/YAML of numbers - accuracy, auc, latency.`,23:`cache: false keeps metrics.json in git itself. It is a few hundred bytes and you want to see it in the diff of a pull request.`,25:`One file, in git, holding every number a human might tune. Hardcoded constants in train.py are invisible to dvc repro.`}},{type:`code`,lang:`bash`,title:`dvc repro, and the thing it refuses to re-run`,code:`dvc repro                      # first run: prepare, then train
# Running stage 'prepare': python src/prepare.py data/raw.csv data/clean.parquet
# Running stage 'train': python src/train.py

dvc repro                      # nothing changed since last time
# Stage 'prepare' didn't change, skipping
# Stage 'train' didn't change, skipping
# Data and pipelines are up to date.

vim params.yaml                # max_depth: 8 -> 12
dvc repro
# Stage 'prepare' didn't change, skipping   <- the 40-minute ETL is not repeated
# Running stage 'train': python src/train.py

git add dvc.lock params.yaml metrics.json && git commit -m 'depth 12'
dvc push                       # the new model.pkl and clean.parquet objects

dvc dag                        # ASCII drawing of the stage graph
dvc metrics show               # the numbers in metrics.json for this commit
dvc metrics diff main          # this branch vs main, metric by metric

dvc exp run -S train.max_depth=16       # a run without a commit per attempt
dvc exp show                            # table: one row per experiment
dvc exp apply exp-a1b2c                 # promote the winner into the workspace`,annotations:{1:`Reproduce, not run. The verb is chosen deliberately: this command is how you rebuild the world from a commit.`,6:`The word to notice is skipping. DVC compared hashes of deps, params and the cmd string, found them identical, and did nothing.`,10:`One number changed, in a file the train stage declared it cares about. Nothing prepare depends on moved.`,12:`The payoff line. prepare is skipped because none of its deps or params changed, even though a downstream stage did.`,15:`dvc.lock is the receipt: the exact hashes of every input and output of the run that actually happened. Commit it or the reproducibility claim is empty.`,20:`Comparing metrics against another branch is a real code-review workflow: the PR shows the model got better or worse, in numbers.`,22:`dvc exp run keeps each attempt out of git history until you want it, so twenty tuning runs do not become twenty commits.`,24:`apply copies that experiment back into the workspace (params, code, outputs) so you can commit it normally.`}},{type:`note`,md:"DVC experiments overlap with MLflow, and the honest split is: **DVC ties a run to exact data and code hashes; MLflow gives you a UI, a tracking server and a model registry.** Many teams run both — `dvc.lock` for reproducibility, MLflow for comparison and the registry — and log the DVC data hash (or the git commit) as an MLflow tag. Whichever you choose, the rule from the tracking module holds: **an experiment record with no dataset version is not reproducible.** Params, metrics and code with an unpinned CSV is a run you can never repeat."},{type:`intuition`,title:`Where DVC stops and Airflow starts`,md:`Both run a DAG of tasks. That is why the question comes up, and why a vague answer sounds junior.

- **DVC pipelines are developer-triggered and single-machine.** You type \`dvc repro\` on your laptop or in CI. There is no scheduler, no worker pool, no retry policy, no backfill, no alerting.
- **Airflow/Prefect are scheduled, distributed and operational.** Cron-like triggers, tasks fanned out across workers, retries with backoff, SLAs, dependencies on external systems, an operator on call.
- What DVC has that they do not: **content-hash caching**. Airflow re-runs a task because the schedule said so; DVC skips it because the inputs are provably identical.
- How real teams split it: Airflow owns the *schedule and the ingestion* (pull yesterday's rows, land them in S3), and one Airflow task shells out to \`dvc repro\` or \`dvc exp run\` for the *train-and-evaluate* part. Airflow decides **when**, DVC decides **what actually needs re-running**.
- The other honest split: DVC for the experimentation loop where a human is iterating, Airflow for the nightly retraining that must run whether or not anyone is watching.
- If a system has to page someone at 3 a.m., it belongs in the orchestrator. If it has to be identical when you re-run it next year, it belongs in DVC. Most pipelines want both.`},{type:`intuition`,title:`What to version, and what never to`,md:'Versioning everything costs storage and slows every pull. Versioning nothing costs you reproducibility. The line is drawn by *can I recompute this?*\n\n- **Raw immutable inputs: yes, always.** The export you cannot regenerate is the one thing that must be pinned. If the source table is mutable or gets deleted, your entire history depends on your copy.\n- **Large intermediate artifacts: it depends.** They are recomputable by definition, so the question is cost. Cheap to rebuild (a 30-second join) → do not push it. Expensive (a 6-hour embedding pass over 10 M documents) → version it, the storage is cheaper than the compute.\n- **Models: yes.** `dvc add models/model.pkl`, or let a pipeline stage own it as an `out`. A model file with no recorded lineage is an artifact nobody can defend in a post-mortem.\n- **Secrets: never.** No credentials in `.dvc/config` — it is committed. Put them in `.dvc/config.local` (git-ignored), or use environment variables and IAM roles.\n- **PII: think first.** Content-addressed storage is append-only by habit; a "delete this user" request is much harder when their row is inside twenty pinned dataset versions. Pseudonymise before the data enters the cache.\n- **The rule that makes it all count:** every experiment record must carry the dataset version alongside the params and metrics. Params without a data hash is a run you cannot repeat — reproducibility becomes a claim rather than a fact.'},{type:`note`,md:"The neighbours, one line each. **LakeFS** gives you git-like branches and commits over an entire S3 bucket — branch your data lake, not just your files. **Delta Lake / Iceberg** add transactional tables with *time travel*: `SELECT * FROM t VERSION AS OF 42`, which is the right answer when your data is a warehouse table rather than a file. **Plain S3 versioning plus a manifest** — turn on bucket versioning, write a JSON manifest listing the object version ids you used, commit the manifest — is unglamorous and completely valid. **Hugging Face Datasets** hosts public/private datasets on git-lfs with a revision hash you can pin. And the simplest thing that works on a small project: **an immutable dated folder** (`data/2026-08-20/`) that nobody ever edits, plus the sha256 of each file written into the run's metadata. That is DVC without DVC, and if it is all you need, ship it."},{type:`intuition`,title:`The interview answer has a shape`,md:`"How do you version data?" is a question with a correct structure. Give the three parts, in order, then the caveats.

1. **Hashes in git, bytes in object storage.** The repo stores a small pointer file containing the content hash; the data lives in S3 or equivalent. A git commit therefore pins an exact dataset version, and \`dvc pull\` materialises it.
2. **A pipeline that records lineage.** Stages declare deps, params and outs; the lock file captures the hash of every input and output of the run that happened. Now "which data made this model" is a lookup, not an archaeology project.
3. **Every experiment carries the data version.** The run record has params, metrics, code commit *and* dataset hash. Without the fourth item the first three are decoration.
4. Then the tradeoffs, unprompted: storage cost and the discipline of two pushes; that recomputable intermediates often should not be versioned; that DVC is not a scheduler, so Airflow or Prefect owns the cron and the retries.
5. And the alternatives, to show you did not just learn one tool: git-lfs for a couple of files, Delta/Iceberg time travel for warehouse tables, LakeFS for a whole bucket, dated immutable folders plus a hash for a small project.
6. The sentence to land on: **reproducibility is code version plus data version plus environment plus params — and data is the one everybody forgets.**`}],quiz:[{question:"You committed a 2 GB CSV to git last month, then ran `git rm` and committed again. How big is a fresh clone today?",options:[{text:`Small — the file was removed, so it is gone`,explanation:`git rm removes it from the working tree and the latest commit only. The blob is still reachable from the older commit, so clones fetch it.`},{text:`Still about 2 GB larger — the blob lives in history and ships with every clone`,explanation:`Correct. Only a history rewrite (git filter-repo or BFG) plus a coordinated force-push actually removes it, and everyone must re-clone.`},{text:`It depends on whether you ran git gc`,explanation:`gc repacks objects; it does not delete objects that reachable commits still point at.`}],correct:1},{question:"What is actually stored in git when you run `dvc add data/raw.csv`?",options:[{text:`A ~180-byte text file containing the content hash, size and path`,explanation:`Correct — plus a line in data/.gitignore so the real file can never be committed by accident. The bytes go to the DVC cache and, on dvc push, to the remote.`},{text:`A compressed copy of the CSV`,explanation:`Nothing about the data content enters git. That is the entire point of the pointer indirection.`},{text:`A symlink to the file in S3`,explanation:`The .dvc file is plain YAML text, not a link, and it names the data by hash rather than by a storage URL.`}],correct:0},{question:'A teammate runs `git pull` and gets a commit you just pushed, but `dvc pull` fails with "failed to pull data from the cloud". Most likely cause?',options:[{text:`Their DVC version is too old`,explanation:`Possible in theory, but it would usually surface as a config or format error, not a missing object.`},{text:`The .dvc file is corrupted`,explanation:`A corrupt pointer fails to parse; this error means the pointer parsed fine and the named object was not found on the remote.`},{text:"You ran `git push` but forgot `dvc push`",explanation:`Correct. The pointer is on GitHub, the bytes never left your machine. Two pushes, always — a pre-push hook or a CI check is the usual fix.`}],correct:2},{question:"Your pipeline has prepare → train. You change only `max_depth` in params.yaml, which only `train` declares. What does `dvc repro` do?",options:[{text:`Re-runs both stages, because the DAG is downstream of params.yaml`,explanation:`DVC tracks the specific param KEYS a stage declares, not the whole file. prepare never mentioned train.max_depth.`},{text:`Skips prepare, re-runs train`,explanation:`Correct. Nothing prepare depends on changed, so its cached output is reused; train declared that param, so it is invalidated. This is the whole reason the file has a params section.`},{text:`Skips both — params are not part of the hash`,explanation:`Params listed in a stage are absolutely part of its state. If they were not, tuning would silently produce stale models.`}],correct:1},{question:`Two branches contain a dataset directory that is 90% identical. What does DVC store on the remote?`,options:[{text:`The shared files once, plus the differing files — storage is content-addressed`,explanation:`Correct. Identical content hashes to the same object, so it is stored and uploaded once regardless of how many branches or commits reference it.`},{text:`Two full copies, one per branch`,explanation:`That would be the behaviour of a naive per-branch backup. Content addressing exists precisely to avoid it.`},{text:`A base copy plus a binary delta`,explanation:`DVC deduplicates whole objects by hash; it does not compute binary deltas between versions the way git tries to.`}],correct:0},{question:`You need nightly retraining that retries on failure, backfills missed days, and pages someone when it breaks. DVC pipelines or Airflow?`,options:[{text:`DVC — dvc repro already handles the DAG`,explanation:`DVC resolves the DAG but has no scheduler, no retry policy, no backfill and no alerting. Something has to invoke it.`},{text:`Neither — write a cron job calling a shell script`,explanation:`Cron gives you a trigger and nothing else: no retries, no backfill, no visibility into which day failed.`},{text:"Airflow for the schedule and retries, with a task that calls `dvc repro` for the train step",explanation:`Correct, and this is how the tools actually split in practice: the orchestrator decides WHEN, DVC decides WHAT genuinely needs re-running.`}],correct:2},{question:`Which of these is the strongest candidate to NOT version?`,options:[{text:`The raw vendor export that gets overwritten monthly at the source`,explanation:`This is the must-version case — if you do not keep it, that version is gone forever and every past experiment becomes unreproducible.`},{text:`A 12 GB join result that rebuilds from versioned inputs in 30 seconds`,explanation:`Correct. It is fully recomputable and cheap to recompute, so pushing it costs storage and pull time for nothing. Version expensive intermediates, skip cheap ones.`},{text:`The trained model file`,explanation:`Models should be versioned (or produced as a pipeline out) — a checkpoint with no lineage is indefensible in a post-mortem.`}],correct:1},{question:"What exactly does `dvc.lock` give you that `dvc.yaml` does not?",options:[{text:`The list of stages and their commands`,explanation:`That is dvc.yaml — the declaration of what the pipeline is.`},{text:`Credentials for the remote`,explanation:`Credentials never go in either file; they belong in .dvc/config.local or the cloud credential chain.`},{text:`The resolved content hashes of every dep and out for the run that actually happened`,explanation:`Correct. dvc.yaml declares intent; dvc.lock is the receipt. Committing it is what turns "we have a pipeline" into "we can reproduce this exact model".`}],correct:2}],interviewQuestions:[{question:`Why not just commit your dataset to git?`,answer:`Three reasons, and I would give all three. Storage: git keeps every version of every file forever in every clone, so a 2 GB file refreshed monthly turns into tens of gigabytes that every developer and every CI job downloads. Deletion does not help — git rm removes it from the tree but the blob stays reachable in history, so undoing the mistake means a filter-repo rewrite and a coordinated force-push. Diffing: git is a line-based text tool; for a parquet file or a checkpoint it prints "Binary files differ", so code review over data is impossible. And compression: git delta-compresses similar objects, but parquet and .pt files are already compressed, so consecutive versions do not delta well and you pay close to full size each time. The general framing: git is built for many small mutable text files, and data is a few huge immutable binary ones.`,isCaseBased:!1},{question:`Explain how DVC works, mechanically, in under a minute.`,answer:`DVC keeps the data out of git and keeps a pointer in it. dvc add hashes the file (md5 by default), moves the bytes into .dvc/cache under a path derived from that hash, links the file back into the workspace, and writes a ~180-byte data/raw.csv.dvc file containing the hash, size and path, plus a .gitignore entry so the real file cannot be committed. You commit the pointer. dvc push uploads any cache objects the configured remote is missing — S3, GCS, Azure, SSH, a shared drive. On another machine, git clone is instant because the repo only holds code and pointers; dvc pull reads the hash from the pointer at the current commit and downloads exactly those bytes, verifying them by hash. The consequence is the sentence that matters: a git commit now pins an exact dataset version, and checking out an old commit plus dvc checkout gives you the data as it was.`,isCaseBased:!1},{question:`What is content-addressed storage and what does it buy you here?`,answer:`The address of an object is derived from its content — the hash is the filename. DVC stores an object at .dvc/cache/8f/14e45f… and the remote mirrors that layout. Three payoffs. Deduplication: identical content has an identical hash, so two branches sharing 90% of a dataset directory store that 90% once, and dvc push only uploads what the remote is missing. Integrity: the hash is a checksum, so a corrupted download fails loudly instead of silently training on garbage. And immutability of reference: a hash cannot go stale the way a path or a mutable S3 key can — s3://bucket/latest.csv means something different every week, whereas a hash always means the same bytes. It is the same idea git uses for its own objects; DVC just moved the storage somewhere cheap and left the pointer where code review lives.`,isCaseBased:!1},{question:`Case: a stakeholder asks you to reproduce the model from Q1 and explain why it scored 0.91 when today's scores 0.86. Walk me through what you do, and what would have made it impossible.`,answer:`What I do if the repo is set up properly: find the commit or tag for that model — ideally the model registry entry points at a git SHA. git checkout that SHA, then dvc pull, which materialises the exact training data, the exact params.yaml and the exact code. dvc repro re-runs the pipeline; if dvc.lock is committed, DVC can verify every input hash matches what the original run recorded, so I know I am reproducing rather than approximating. Then I compare: dvc metrics diff between the two commits shows which metrics moved, and diffing the two .dvc pointers or dvc.lock entries tells me whether the data changed, the params changed, or both. Usually the answer is that the data distribution moved, which turns this into a drift conversation rather than a bug hunt. What would make it impossible: a dataset overwritten in place with no version pinned, hardcoded constants instead of params.yaml, an uncommitted dvc.lock, or an environment with unpinned library versions — reproducibility is code plus data plus params plus environment, and failing any one of the four breaks it. I would also say what the fix is going forward: record the dataset hash on every experiment record, not just the params.`,isCaseBased:!0},{question:`Compare DVC pipelines with Airflow. When do you use which?`,answer:`They overlap on "run a DAG of tasks" and differ everywhere else. DVC is developer-triggered and single-machine: you type dvc repro, it resolves the graph from deps and outs, and it re-runs only the stages whose input hashes, params or command strings changed. It has no scheduler, no worker pool, no retries, no backfill, no alerting. Airflow and Prefect are the operational layer: cron-style schedules, distributed workers, retries with backoff, SLAs, backfills, and a UI an on-call engineer can act on — but a scheduled Airflow task re-runs because the schedule said so, not because anything actually changed. In practice teams use both: Airflow owns ingestion and the schedule, and one task in the DAG shells out to dvc repro or dvc exp run for train-and-evaluate, so the orchestrator decides when and DVC decides what genuinely needs recomputing. My rule of thumb: if it has to page someone at 3 a.m., it belongs in the orchestrator; if it has to be byte-identical when re-run next year, it belongs in DVC.`,isCaseBased:!1},{question:`Case: CI has started failing with "failed to pull data from the cloud" on the main branch, but it works on every developer machine. Debug it.`,answer:`Split it into pointer, bytes and credentials. First, does the object exist on the remote? The classic cause is someone ran git push without dvc push, so the commit references a hash that only lives in their local cache — it works on their laptop and nowhere else. I would check the hash in the .dvc file or dvc.lock against the remote, and confirm by asking that developer to run dvc push. Second, credentials: CI uses a different identity from a laptop, so a missing AWS role, an expired key, or a bucket policy that grants a developer but not the CI runner produces the same message. dvc doctor and a plain aws s3 ls against the remote path separate the two cases quickly. Third, remote configuration: if the URL lives in .dvc/config.local rather than the committed .dvc/config, CI never sees it at all. The durable fixes: a pre-push git hook or a CI job on pull requests that runs dvc status --cloud to fail fast when data was never uploaded, and giving CI its own read role rather than a copied key.`,isCaseBased:!0},{question:`What do you version and what do you deliberately not version?`,answer:`The test is: can I recompute this, and what does it cost? Raw immutable inputs always get versioned — especially anything the source system overwrites, because once it is gone every past experiment is unreproducible. Trained models get versioned, either with dvc add or as a pipeline out, so every artifact has lineage. Intermediates are a judgement call: a join that rebuilds in thirty seconds is not worth the storage and the pull time, whereas a six-hour embedding pass over ten million documents is much cheaper to store than to recompute. Secrets never — .dvc/config is committed, so credentials belong in .dvc/config.local or the cloud credential chain. PII needs thought before it enters a content-addressed cache, because append-only storage makes a deletion request genuinely hard once a user's row sits inside twenty pinned versions; pseudonymise upstream. And the rule that ties it together: every experiment record must carry the dataset version next to its params and metrics, or the reproducibility story is fiction.`,isCaseBased:!1},{question:`What alternatives to DVC would you consider, and when?`,answer:`git-lfs if the data is a handful of files and I already pay for the git host — same pointer trick, but the storage is billed by the host rather than my own bucket, clones try to fetch every object by default, and it knows nothing about pipelines or params. Delta Lake or Iceberg when the data is a warehouse table rather than a file: they give transactional writes and time travel, so pinning a version is a SQL clause like VERSION AS OF 42 and there is no separate tool to teach anyone. LakeFS when I want git-like branches and commits over an entire S3 bucket, which suits a data platform team more than a single model repo. Hugging Face Datasets for public or shared datasets, where you pin a revision hash. And for a small project, an immutable dated folder — data/2026-08-20/ that nobody ever edits — plus the sha256 of each file written into the run's metadata. That last one is DVC's idea without DVC, and if the team is three people it is often the right call.`,isCaseBased:!1},{question:"Case: a colleague ran `git checkout v1-baseline` to reproduce an old model, retrained, and got a different score than the one recorded. Nothing errored. What happened?",answer:`Most likely the pointer moved and the data did not. git checkout swaps the .dvc file to the old hash, but the file sitting in the workspace is still the newer data until dvc checkout or dvc pull runs. Nothing errors, because the training script just reads a path — it has no idea the bytes disagree with the commit. That is the classic DVC gotcha and the reason dvc checkout exists. I would confirm it by comparing the md5 in the checked-out .dvc file against the actual file, which dvc status reports directly. Other candidates to rule out in order: an uncommitted dvc.lock, so the recorded input hashes were never available to verify against; params that were hardcoded in the script rather than declared in params.yaml, so they did not travel with the commit; unpinned library versions changing a default; and genuine nondeterminism from an unseeded random split or GPU nondeterminism, which is why seeds belong in params.yaml too. The preventive fix is a git post-checkout hook running dvc checkout, which DVC can install for you, so the workspace and the commit can never silently disagree.`,isCaseBased:!0},{question:`How does data versioning connect to experiment tracking, and why is either one incomplete alone?`,answer:`Experiment tracking answers "what did we try and how did it score" — params, metrics, artifacts, a comparison UI, a model registry. Data versioning answers "which exact bytes went in". Tracking alone gives you a run with accuracy 0.91 whose training file was overwritten last quarter, so you can read the number and never reproduce it. Versioning alone gives you perfectly pinned data with no record of which run used it. The join is one field: log the dataset hash or the git commit as a tag on every tracked run, so any row in the tracking UI resolves back to exact data. In a DVC-plus-MLflow setup the split is clean — dvc.lock is the reproducibility receipt, MLflow is the comparison surface and registry — and the registry entry points at a git SHA, which through DVC points at the data. The sentence I would end on: reproducibility is code version plus data version plus params plus environment, and data is the term everyone drops.`,isCaseBased:!1}],flashcards:[{front:`Why git alone fails for data`,back:`Every version of every file lives forever in every clone, binary diffs are meaningless, and already-compressed files do not delta — so a monthly 2 GB refresh becomes a repo nobody can clone.`},{front:`The DVC idea in one line`,back:`Content hash in git (a ~180-byte .dvc pointer file), bytes in cheap object storage. A git commit therefore pins an exact dataset version.`},{front:`The two-push rule`,back:`git push sends the pointer, dvc push sends the bytes. Only doing the first is the most common DVC mistake — teammates get a commit referencing data nobody can fetch.`},{front:`git checkout vs dvc checkout`,back:`git checkout swaps the .dvc pointer; the workspace data file is still the old one until dvc checkout (from cache) or dvc pull (downloads first) syncs it.`},{front:`Content-addressed storage payoff`,back:`The hash is the filename, so identical content is stored once (two branches sharing 90% of a dataset store it once), pushes upload only what is missing, and downloads are integrity-checked for free.`},{front:`dvc.yaml stage anatomy`,back:`cmd, deps, params, outs, metrics. deps include the code, params names specific keys in params.yaml, outs are auto dvc-tracked and git-ignored.`},{front:`What dvc repro actually skips`,back:`Any stage whose deps hashes, declared params and cmd string are unchanged. Hash-based, not timestamp-based — touching a file does not trigger a rebuild.`},{front:`dvc.yaml vs dvc.lock`,back:`dvc.yaml declares intent (the stages). dvc.lock is the receipt: resolved hashes of every dep and out for the run that happened. Commit the lock or reproducibility is a claim, not a fact.`},{front:`DVC vs Airflow, the split`,back:`DVC: developer-triggered, single machine, hash-caching, no scheduler or retries. Airflow/Prefect: schedule, distributed workers, retries, backfill, alerting. Airflow decides WHEN, DVC decides WHAT needs re-running.`},{front:`The "how do you version data?" answer shape`,back:`Content hashes in git, bytes in object storage, and a pipeline that records which hash produced which model — plus the dataset version on every experiment record.`}],mindmapMarkdown:`- Data & Model Versioning with DVC
  - Why git alone fails
    - Every version forever, in every clone
    - git rm does not shrink history
    - "Binary files differ" — no useful diff
    - Compressed data does not delta
    - git-lfs: same trick, host-billed, no pipelines
  - The DVC idea
    - Hash in git, bytes in object storage
    - .dvc pointer file ~180 bytes
    - Commit pins an exact dataset version
    - Two pushes: git push + dvc push
    - dvc checkout after git checkout
  - Workflow, two machines
    - dvc init / remote add -d
    - dvc add → cache + pointer + .gitignore
    - clone is instant, dvc pull materialises
  - Content-addressed storage
    - Hash is the filename
    - Dedup across branches and commits
    - Integrity check for free
  - Pipelines (dvc.yaml)
    - Stages: cmd / deps / params / outs / metrics
    - DAG derived from out → dep links
    - dvc repro re-runs only changed stages
    - dvc.lock = the reproducibility receipt
    - dvc exp run, dvc exp show, metrics diff
  - Boundary with orchestration
    - DVC: laptop/CI, hash caching, no scheduler
    - Airflow/Prefect: cron, retries, backfill, alerts
    - Airflow calls dvc repro for train
  - What to version
    - Raw immutable inputs: yes
    - Expensive intermediates: maybe
    - Models: yes, with lineage
    - Secrets: never (.dvc/config.local)
    - Dataset version on every experiment
  - Alternatives
    - LakeFS: branch a whole bucket
    - Delta/Iceberg: table time travel
    - S3 versioning + manifest
    - Hugging Face datasets
    - Dated immutable folder + sha256
  - Interview shape
    - Hashes in git, bytes in storage, pipeline records lineage
    - Reproducibility = code + data + params + environment`};export{e as default};