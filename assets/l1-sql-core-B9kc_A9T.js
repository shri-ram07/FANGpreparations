var e={id:`dbms-l1-sql-core`,subjectId:`dbms`,level:1,title:`SQL Core: SELECT Anatomy, GROUP BY & HAVING`,whyItMatters:`Every data role has a SQL screen, and most of it is one query shape: SELECT-WHERE-GROUP-HAVING-ORDER. The single most-asked question in that screen — "WHERE vs HAVING?" — falls out of one idea: SQL is not executed in the order you write it. Learn the real pipeline once and half of SQL stops being memorization.`,estMinutes:60,sections:[{type:`intuition`,title:`Four dialects, one language`,md:'SQL statements split into four families. Think of a database as an apartment building.\n\n- **DDL** (Data Definition) — the *blueprint*: `CREATE`, `ALTER`, `DROP`. Builds, remodels, or demolishes tables themselves.\n- **DML** (Data Manipulation) — the *residents*: `SELECT`, `INSERT`, `UPDATE`, `DELETE`. Reads and changes rows inside existing tables.\n- **DCL** (Data Control) — the *keys*: `GRANT`, `REVOKE`. Decides who may do what.\n- **TCL** (Transaction Control) — the *undo button*: `COMMIT`, `ROLLBACK`. Groups DML changes into all-or-nothing units.\n- Interviewers use the families as vocabulary: "is TRUNCATE DDL or DML?" is a real question. Knowing which family a statement lives in tells you how it behaves (DDL is usually auto-committed; DML can be rolled back).'},{type:`code`,lang:`sql`,title:`All four families in one runnable tour`,code:`-- DDL: defines the SHAPE of data
CREATE TABLE wishlists (
  id          INTEGER PRIMARY KEY,
  customer_id INTEGER REFERENCES customers(id),
  product_id  INTEGER REFERENCES products(id)
);
ALTER TABLE wishlists ADD COLUMN added_on DATE;

-- DML: works on the ROWS inside that shape
INSERT INTO wishlists VALUES (1, 2, 4, '2025-08-01');
UPDATE wishlists SET product_id = 9 WHERE id = 1;
DELETE FROM wishlists WHERE id = 1;

-- TCL: groups DML into all-or-nothing units
BEGIN;
UPDATE products SET price = price * 0.9;  -- oops: discounted EVERYTHING
ROLLBACK;                                 -- undo all changes since BEGIN

-- DCL: who may do what (SQLite skips this -- single-user file DB)
-- GRANT SELECT ON products TO analyst;
-- REVOKE SELECT ON products FROM analyst;

DROP TABLE wishlists;                     -- DDL again: the table itself dies`,annotations:{2:`DDL touches the schema, not the rows. CREATE brings a table into existence; ALTER remodels it in place.`,12:`DELETE removes rows but the empty table survives. DROP (last line) kills the table AND its data. This contrast is a stock interview question.`,17:`ROLLBACK undoes every change since BEGIN; COMMIT would make them permanent. Without an explicit BEGIN, each statement auto-commits on its own.`,20:`GRANT/REVOKE exist in server databases (Postgres, MySQL) where many users share one server. SQLite is a single file with no user accounts, so the playground has nothing to grant.`}},{type:`intuition`,title:`The kitchen does not read the menu top-to-bottom`,md:"You WRITE a query starting with SELECT. The database EXECUTES it starting with FROM. Like a restaurant order: the customer says the dish name first, but the kitchen starts with ingredients.\n\n- Logical order: **FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT**.\n- `FROM` fetches the raw rows. `WHERE` tests each row alone and discards failures.\n- `GROUP BY` collapses survivors into one group per key. `HAVING` tests whole groups and discards failing groups.\n- Only THEN does `SELECT` compute the output columns — aggregates and aliases are born here.\n- `ORDER BY` sorts the finished rows; `LIMIT` cuts the top of the sorted pile, last of all.\n- This one ordering explains the alias error, WHERE vs HAVING, and why `COUNT(*)` cannot appear in WHERE. It is the skeleton key of SQL."},{type:`note`,md:"The classic trap: `SELECT price * 0.9 AS sale_price FROM products WHERE sale_price < 500` — **error: no such column**. WHERE runs at step 2; the alias `sale_price` is created at step 5 (SELECT). You are referencing something that does not exist yet. Repeat the expression in WHERE, or use a subquery. The one clause where the alias IS safe everywhere: **ORDER BY** — it runs after SELECT. (SQLite is lenient and also accepts aliases in GROUP BY/HAVING; do not rely on that in interviews.) Try it live: [Open the SQL playground](/playground/sql) and run the broken query against the seeded ecommerce DB."},{type:`code`,lang:`sql`,title:`All seven clauses, numbered in execution order`,code:`SELECT customer_id, COUNT(*) AS order_count  -- 5: compute output, alias born
FROM orders                                  -- 1: load the raw rows
WHERE status = 'delivered'                   -- 2: test rows, one by one
GROUP BY customer_id                         -- 3: collapse rows into groups
HAVING COUNT(*) >= 2                         -- 4: test whole groups
ORDER BY order_count DESC, customer_id       -- 6: sort (alias usable NOW)
LIMIT 2;                                     -- 7: cut the sorted result
-- Result: (1, 3), (2, 2)
-- customer 1 has 3 delivered orders, customer 2 has 2`,annotations:{1:`Written first, executed fifth. Everything SELECT needs (surviving rows, formed groups) must already exist.`,3:`The cancelled order (id 104) dies here — before any group forms. WHERE cannot use COUNT(*): no groups exist yet at step 2.`,5:`HAVING sees finished groups and their aggregates. Customers with only 1 delivered order are dropped as whole groups.`,6:`order_count works here because ORDER BY runs after SELECT. The second key, customer_id, breaks ties — without it, LIMIT on tied rows returns an arbitrary winner.`}},{type:`visual`,component:`PointerBoxDiagram`,props:{title:`The logical pipeline, row by row`,notice:`The same query on a SIX-ROW SLICE (not the full table) — so the counts here differ from the result printed above, on purpose: watch the mechanism, not the numbers. Each clause either kills cells or merges them.`,leftLabel:`clause being applied`,rightLabel:`surviving rows / groups`,frames:[{note:`FROM orders — step 1. The pipeline starts with raw rows; nothing is filtered, and SELECT has not even been looked at. Six rows in (order id · customer · status).`,stack:[{name:`FROM orders`,value:`6 rows in`}],heap:[{id:`r101`,value:`101 · cust 1 · delivered`,label:`row`},{id:`r103`,value:`103 · cust 1 · delivered`,label:`row`},{id:`r104`,value:`104 · cust 3 · cancelled`,label:`row`},{id:`r105`,value:`105 · cust 4 · delivered`,label:`row`},{id:`r109`,value:`109 · cust 3 · delivered`,label:`row`},{id:`r112`,value:`112 · cust 4 · delivered`,label:`row`}]},{note:`WHERE status = 'delivered' — step 2. Each row is tested ALONE, no aggregates possible. Row 104 fails and is gone for good: the groups that form next will never see it.`,stack:[{name:`WHERE status = 'delivered'`,value:`per-row test`,to:`r104`,danger:!0}],heap:[{id:`r101`,value:`101 · cust 1 · delivered`,label:`row`},{id:`r103`,value:`103 · cust 1 · delivered`,label:`row`},{id:`r104`,value:`104 · cust 3 · cancelled`,label:`killed by WHERE`,freed:!0},{id:`r105`,value:`105 · cust 4 · delivered`,label:`row`},{id:`r109`,value:`109 · cust 3 · delivered`,label:`row`},{id:`r112`,value:`112 · cust 4 · delivered`,label:`row`}]},{note:`GROUP BY customer_id — step 3. The five survivors collapse into one cell per customer: rows 101+103 merge into the cust-1 group, 105+112 into cust 4, and 109 sits alone in cust 3. From here on the query sees groups, not rows.`,stack:[{name:`cust 1`,value:`2 rows`,to:`g1`},{name:`cust 3`,value:`1 row`,to:`g3`},{name:`cust 4`,value:`2 rows`,to:`g4`}],heap:[{id:`g1`,value:`rows 101, 103`,label:`group: customer 1`},{id:`g3`,value:`row 109`,label:`group: customer 3`},{id:`g4`,value:`rows 105, 112`,label:`group: customer 4`}]},{note:`HAVING COUNT(*) >= 2 — step 4. Now aggregates exist, so groups can be judged. Customer 3's group holds only 1 row — its second order was cancelled and WHERE removed it BEFORE grouping — so the whole group is dropped.`,stack:[{name:`HAVING COUNT(*) >= 2`,value:`per-group test`,to:`g3`,danger:!0}],heap:[{id:`g1`,value:`rows 101, 103 · count 2`,label:`group: customer 1`},{id:`g3`,value:`row 109 · count 1`,label:`dropped by HAVING`,freed:!0},{id:`g4`,value:`rows 105, 112 · count 2`,label:`group: customer 4`}]},{note:`SELECT customer_id, COUNT(*) AS order_count — step 5: output columns are computed and the alias is finally born. ORDER BY sorts these finished rows (step 6) and LIMIT would cut the sorted pile last of all (step 7).`,stack:[{name:`SELECT → ORDER BY`,value:`2 rows out`}],heap:[{id:`o1`,value:`customer_id 1 · order_count 2`,label:`output row 1`},{id:`o2`,value:`customer_id 4 · order_count 2`,label:`output row 2`}]}]}},{type:`intuition`,title:`The WHERE toolbox: five ways to test a row`,md:"WHERE accepts any expression that yields true/false (or NULL — coming next). Five patterns cover 95% of filters.\n\n- **Comparison**: `=`, `<>`, `<`, `>=` … composed with `AND`, `OR`, `NOT`.\n- **IN**: `category_id IN (2, 5)` — shorthand for a chain of ORs. Cleaner and less error-prone.\n- **BETWEEN**: `price BETWEEN 249 AND 499` — **inclusive on both ends**. Interviewers probe exactly this.\n- **LIKE**: pattern match. `%` = any run of characters (including none), `_` = exactly one character. `'M%'` = starts with M.\n- **IS NULL / IS NOT NULL**: the ONLY correct way to test for NULL. `= NULL` silently matches nothing."},{type:`code`,lang:`sql`,title:`Filters against the seeded ecommerce DB`,code:`SELECT name, price FROM products
WHERE category_id IN (2, 5);           -- Books or Sports: 4 rows

SELECT name, price FROM products
WHERE price BETWEEN 249 AND 499;       -- 4 rows: the 249 mug AND both 499 books
                                       -- make it in -- inclusive on BOTH ends

SELECT name, city FROM customers
WHERE city LIKE 'M%';                  -- Aarav, Zoya (both Mumbai)

SELECT name, city FROM customers
WHERE city LIKE '_elhi';               -- Diya, Sana (Delhi)

SELECT name FROM products
WHERE price >= 1000 AND stock < 100;   -- 5 rows: pricey AND scarce`,annotations:{2:`IN (2, 5) reads as category_id = 2 OR category_id = 5. With a long list, IN stays readable where OR chains rot.`,5:`Both endpoints included: price 249 qualifies, price 499 qualifies. "BETWEEN excludes the upper end" is a common wrong belief — it excludes nothing.`,9:`The % can swallow zero characters, so LIKE 'M%' also matches a city named just M. Case sensitivity varies by engine (SQLite: ASCII case-insensitive by default).`,12:`The _ demands exactly one character: _elhi matches Delhi but not New Delhi.`}},{type:`intuition`,title:`NULL is not a value — it is a shrug`,md:'NULL means "unknown / not applicable". SQL logic is **three-valued**: expressions yield TRUE, FALSE, or UNKNOWN — and WHERE only keeps rows where the test is TRUE.\n\n- `NULL = NULL` is **not true** — it is NULL. "Is one unknown equal to another unknown?" — unknown.\n- Every comparison touching NULL yields NULL: `NULL <> 5`, `NULL > 0`, all NULL.\n- So `WHERE city = NULL` matches nothing, ever — even rows whose city IS NULL. Use `IS NULL`.\n- The silent killer: `WHERE city <> \'Delhi\'` does NOT return rows with NULL city. The test yields NULL, not TRUE, and the row vanishes from the result without an error.\n- Aggregates skip NULLs: `COUNT(city)` counts only non-NULL cities; `COUNT(*)` counts rows regardless. `AVG` averages only the non-NULL values.'},{type:`code`,lang:`sql`,title:`The NULL traps, live`,code:`SELECT NULL = NULL;                              -- NULL, not 1
SELECT NULL <> 'Delhi';                          -- NULL again

UPDATE customers SET city = NULL
WHERE name = 'Zoya';                             -- give the DB a real NULL

SELECT name FROM customers WHERE city = NULL;    -- 0 rows, no error
SELECT name FROM customers WHERE city IS NULL;   -- Zoya
SELECT COUNT(*) FROM customers
WHERE city <> 'Delhi';                           -- 5, not 6: Zoya vanished silently

SELECT COUNT(*) AS all_rows, COUNT(city) AS with_city
FROM customers;                                  -- 8 | 7

UPDATE customers SET city = 'Mumbai'
WHERE name = 'Zoya';                             -- restore the seed data`,annotations:{1:`SQLite prints an empty cell — the result is NULL (UNKNOWN), neither 1 nor 0. This one line is the whole three-valued-logic story.`,7:`The comparison yields NULL for every row, WHERE keeps only TRUE, so nothing survives. No warning, no error — the most dangerous kind of bug.`,10:`Six customers live outside Delhi, but Zoya's NULL city fails BOTH city = 'Delhi' and city <> 'Delhi'. Rows can fall through every branch of your logic.`,13:`COUNT(*) counts rows: 8. COUNT(city) counts non-NULL values in that column: 7. Same table, same moment, different answers — say which one you mean.`,16:`The playground DB is scratch space — this puts Zoya back so later examples match the seed. Reloading the page also resets it.`}},{type:`intuition`,title:`Aggregates and GROUP BY: from rows to trays`,md:"An aggregate (`COUNT`, `SUM`, `AVG`, `MIN`, `MAX`) squeezes many rows into one number. Without GROUP BY, the whole table is one group and you get one row back.\n\n- `GROUP BY customer_id` sorts rows into trays, one tray per customer. Aggregates then run **per tray**.\n- The iron rule: every SELECT column must be either **inside an aggregate** or **listed in GROUP BY**. A bare column is ambiguous — a tray holds many rows; which row's value would it even mean?\n- The classic error: `SELECT category_id, name, COUNT(*) … GROUP BY category_id`. `name` is neither grouped nor aggregated.\n- Engines disagree on punishment: Postgres and strict-mode MySQL refuse to run it. **SQLite runs it and silently picks one arbitrary name per group** — a wrong answer that looks right. Worse than an error.\n- `COUNT(*)` counts rows in the tray; `COUNT(col)` counts non-NULL values of that column in the tray. Same distinction as before, now per group."},{type:`code`,lang:`sql`,title:`Aggregates: whole table, then per group`,code:`SELECT COUNT(*) AS n, MIN(price) AS cheapest, MAX(price) AS priciest,
       AVG(price) AS avg_price, SUM(stock) AS units
FROM products;
-- one row: 12 | 249 | 7999 | ~1653.17 | 1465

SELECT category_id, COUNT(*) AS n, AVG(price) AS avg_price
FROM products
GROUP BY category_id
ORDER BY n DESC, category_id;
-- 1|4|3549   2|3|~532.33   3|2|949   4|2|674   5|1|799`,annotations:{3:`No GROUP BY means one implicit group: the whole table collapses to a single row of five numbers.`,8:`One output row per distinct category_id. Every aggregate in SELECT is now computed per group: Electronics averages 3549, Books 532.`,9:`Categories 3 and 4 tie at n = 2 — the second sort key (category_id) makes their order deterministic. Habit worth building now; it pays off at LIMIT time.`}},{type:`code`,lang:`sql`,title:`The classic GROUP BY error — and the SQLite trap`,code:`SELECT category_id, name, COUNT(*) AS n   -- name: not grouped, not aggregated
FROM products
GROUP BY category_id;
-- Postgres / strict MySQL: ERROR -- "name" must appear in GROUP BY
-- SQLite: runs fine, silently returns ONE arbitrary name per category`,annotations:{1:`The group for category 1 holds four products. Which name should this cell show? The question has no answer — that is why strict engines refuse.`,5:`The playground will happily show you Wireless Mouse for Electronics as if it meant something. Interview line: "SQLite's leniency here is a bug factory — I write strict-mode GROUP BY."`}},{type:`intuition`,title:`WHERE vs HAVING — THE interview classic`,md:'A nightclub with a team-dance competition. The **bouncer** (WHERE) checks individuals at the door — underage, no entry, before teams even form. The **judge** (HAVING) scores finished teams — too few dancers, whole team eliminated.\n\n- **WHERE trims rows BEFORE grouping.** It sees one row at a time; aggregates do not exist yet, so `WHERE COUNT(*) > 2` is illegal.\n- **HAVING judges finished groups.** It runs after GROUP BY, so it can use `COUNT`, `SUM`, `AVG` of the group.\n- They are not interchangeable — filtering the same condition early vs late can change the counts themselves, and therefore the result.\n- Performance edge: WHERE also shrinks the data before grouping, so push every per-row condition into WHERE. Keep HAVING for conditions about the group as a whole.\n- One-liner for the interview: *"WHERE filters rows, HAVING filters groups — and WHERE runs first, so it changes what the groups contain."*'},{type:`code`,lang:`sql`,title:`Same threshold, different clause, different customers`,code:`-- Q1: customers with 2+ orders of ANY status
SELECT customer_id, COUNT(*) AS n
FROM orders
GROUP BY customer_id
HAVING COUNT(*) >= 2;
-- 4 rows: customers 1, 2, 3, 4

-- Q2: customers with 2+ DELIVERED orders
SELECT customer_id, COUNT(*) AS n
FROM orders
WHERE status = 'delivered'
GROUP BY customer_id
HAVING COUNT(*) >= 2;
-- 3 rows: customers 1, 2, 4
-- customer 3 (Kabir) dropped: one of his two orders was cancelled,
-- WHERE removed it BEFORE grouping, so his group counted only 1`,annotations:{5:`Kabir has orders 104 (cancelled) and 109 (delivered). With no WHERE, both reach his group: count 2, he passes.`,11:`The cancelled row dies at step 2. HAVING later sees a Kabir-group of size 1 and drops it. Same >= 2 threshold, different world by the time it runs.`,14:`This row-level difference is exactly what interviewers want you to demonstrate — not the definition, the changed result.`}},{type:`note`,md:"**DISTINCT** deduplicates the final output rows: `SELECT DISTINCT city FROM customers` returns 6 rows from 8 customers (Delhi and Mumbai each appear twice in the data, once in the result). Inside an aggregate it dedupes before counting: `COUNT(DISTINCT city)` = 6 while `COUNT(city)` = 8. Note where DISTINCT sits in the pipeline: after SELECT computes output rows, before ORDER BY sorts them."},{type:`code`,lang:`sql`,title:`DISTINCT, multi-key ORDER BY, and deterministic LIMIT`,code:`SELECT DISTINCT city FROM customers;   -- 6 rows (Delhi, Mumbai deduped)

SELECT COUNT(city) AS every_value,
       COUNT(DISTINCT city) AS unique_values
FROM customers;                        -- 8 | 6

SELECT name, price FROM products
ORDER BY price ASC, name ASC           -- key 2 breaks key-1 ties
LIMIT 5;
-- 249 Ceramic Mug | 399 Cotton T-Shirt | 499 Clean Code
-- 499 Grokking Algorithms | 599 The Pragmatic Programmer`,annotations:{1:`DISTINCT applies to the whole output row. SELECT DISTINCT city, name would dedupe (city, name) PAIRS — all 8 rows survive.`,8:`Two books tie at 499. The second key sorts Clean Code before Grokking Algorithms. Without it, their order — and who survives a tight LIMIT — is whatever the engine felt like.`,9:`LIMIT runs dead last, cutting the already-sorted pile. LIMIT without ORDER BY returns an arbitrary, unstable set of rows — never ship that.`}},{type:`note`,md:"Where do NULLs sort? Engines disagree: **SQLite treats NULL as smaller than everything** (first under ASC, last under DESC); **Postgres does the opposite** (NULLs last under ASC). The portable fix is explicit: `ORDER BY city NULLS LAST` (supported by Postgres and SQLite 3.30+). If a sorted report suddenly shows blanks at the top after a database migration — this is why. Now drill it: [Open the SQL playground](/playground/sql) and rebuild every query from this module against the seeded ecommerce DB, then try the checked exercises — writing the pipeline beats rereading it."}],quiz:[{question:`In the logical evaluation order of a SELECT query, which clause runs FIRST?`,options:[{text:`SELECT — it is written first`,explanation:`Written first, executed fifth. The output columns cannot be computed before rows are fetched, filtered, and grouped.`},{text:`FROM`,explanation:`Correct. The pipeline is FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT. Everything starts with raw rows.`},{text:`WHERE`,explanation:`WHERE is early (step 2) but it needs rows to test — FROM must load them first.`},{text:`ORDER BY`,explanation:`ORDER BY is second-to-last: you can only sort rows that already exist in final form.`}],correct:1},{question:`SELECT price * 0.9 AS sale_price FROM products WHERE sale_price < 500 — what happens?`,options:[{text:`Returns discounted products under 500`,explanation:`It would if WHERE ran after SELECT — but WHERE runs first, when the alias does not exist yet.`},{text:`Error: no such column sale_price — WHERE runs before SELECT creates the alias`,explanation:`Correct. Aliases are born at SELECT (step 5); WHERE runs at step 2. Repeat the expression in WHERE, or wrap it in a subquery.`},{text:`Error: arithmetic is not allowed in SELECT`,explanation:`Expressions in SELECT are fine — computing derived columns is what SELECT is for.`},{text:`Runs, but the filter is silently ignored`,explanation:`SQL never silently ignores a clause — an unknown column is a hard error.`}],correct:1},{question:`You want customers with at least 2 DELIVERED orders. Where do the two conditions go?`,options:[{text:`status = 'delivered' in WHERE, COUNT(*) >= 2 in HAVING`,explanation:`Correct. The status test is per-row → WHERE, trimming before groups form. The count test needs the finished group → HAVING.`},{text:`Both in WHERE`,explanation:`WHERE COUNT(*) >= 2 is illegal — WHERE runs before grouping, so no aggregate exists yet to test.`},{text:`Both in HAVING`,explanation:`The count would then include cancelled orders (they were never trimmed), giving wrong customers — and you would group more rows than needed.`},{text:`COUNT(*) >= 2 in WHERE, status in HAVING`,explanation:`Backwards on both: aggregates cannot live in WHERE, and a bare per-row column in HAVING is rejected by strict engines.`}],correct:0},{question:`What does SELECT NULL = NULL return?`,options:[{text:`TRUE / 1`,explanation:`NULL means unknown. "Is one unknown equal to another unknown?" cannot be answered yes.`},{text:`FALSE / 0`,explanation:`It cannot be answered no either — the two unknowns COULD be equal. SQL refuses to commit.`},{text:`NULL`,explanation:`Correct. Three-valued logic: any comparison touching NULL yields NULL (unknown). This is why IS NULL exists.`},{text:`A syntax error`,explanation:`The syntax is fine — the expression is legal and evaluates to NULL.`}],correct:2},{question:`A table has 8 rows; one row has NULL in the city column. What do COUNT(*) and COUNT(city) return?`,options:[{text:`8 and 8`,explanation:`COUNT(city) skips NULLs — it counts values in the column, not rows.`},{text:`8 and 7`,explanation:`Correct. COUNT(*) counts rows regardless of content; COUNT(col) counts only non-NULL values of that column.`},{text:`7 and 7`,explanation:`COUNT(*) never skips anything — a row of all NULLs still counts as a row.`},{text:`An error — you must GROUP BY first`,explanation:`Aggregates without GROUP BY are legal: the whole table acts as one group, returning one row.`}],correct:1},{question:`SELECT category_id, name, COUNT(*) FROM products GROUP BY category_id — what happens?`,options:[{text:`Every engine rejects it`,explanation:`Strict engines do — but SQLite (the playground) runs it, which is the trap.`},{text:`Postgres/strict MySQL error; SQLite runs it, picking one arbitrary name per group`,explanation:`Correct. name is neither grouped nor aggregated — a group holds many names, so the value is ambiguous. SQLite silently guesses; strict engines refuse.`},{text:`It returns all names in the group, comma-joined`,explanation:`That would require an explicit aggregate like GROUP_CONCAT(name) — bare columns never auto-concatenate.`},{text:`It works correctly everywhere — name is determined by category_id`,explanation:`It is not: category 1 alone holds four different product names. Even where a column IS functionally determined, most engines still demand it in GROUP BY.`}],correct:1},{question:`WHERE price BETWEEN 249 AND 499 — does a product priced exactly 499 match?`,options:[{text:`Yes — BETWEEN is inclusive on both ends`,explanation:`Correct. It expands to price >= 249 AND price <= 499. Both endpoints qualify.`},{text:`No — the upper bound is exclusive`,explanation:`A common wrong belief. BETWEEN excludes nothing; if you need a half-open range, write the comparisons yourself.`},{text:`Depends on the database engine`,explanation:`BETWEEN inclusivity is standard SQL — engines agree on this one.`}],correct:0},{question:`GRANT and ROLLBACK belong to which statement families, respectively?`,options:[{text:`DDL and DML`,explanation:`GRANT does not define schema objects, and ROLLBACK does not touch rows directly — it undoes a transaction.`},{text:`DCL and TCL`,explanation:`Correct. GRANT/REVOKE control permissions (Data Control); COMMIT/ROLLBACK control transactions (Transaction Control).`},{text:`TCL and DCL`,explanation:`Swapped — GRANT is about who may do what (control), ROLLBACK is about undoing a unit of work (transaction).`},{text:`Both are DML`,explanation:`DML is SELECT/INSERT/UPDATE/DELETE — statements that read or change rows. Neither GRANT nor ROLLBACK does.`}],correct:1}],interviewQuestions:[{question:`WHERE vs HAVING — explain the difference with an example where they produce different results.`,answer:`WHERE trims rows BEFORE grouping; HAVING judges finished groups after GROUP BY. Because WHERE runs first, it changes what the groups contain. Example on an orders table: "customers with 2+ orders" (HAVING COUNT(*) >= 2, no WHERE) includes a customer whose two orders are one delivered and one cancelled. Add WHERE status = 'delivered' and that customer's cancelled row dies before grouping — the group now counts 1 and HAVING drops it. Same threshold, different result set. Two corollaries to volunteer: aggregates cannot appear in WHERE (no groups exist at step 2), and per-row conditions belong in WHERE for performance — they shrink the data before the group step does its work.`,isCaseBased:!1},{question:`Walk me through the logical evaluation order of a SELECT statement and one bug it explains.`,answer:`FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT. FROM loads rows; WHERE tests each row alone; GROUP BY collapses survivors into groups; HAVING tests whole groups; SELECT finally computes output columns and aliases; ORDER BY sorts; LIMIT cuts last. Bug it explains: WHERE my_alias < 500 fails with "no such column" — the alias is created at SELECT (step 5), and WHERE runs at step 2, referencing a thing that does not exist yet. Same order also explains why COUNT(*) is illegal in WHERE and legal in HAVING, and why ORDER BY is the one clause where the alias is always safe.`,isCaseBased:!1},{question:`Why can a SELECT alias be used in ORDER BY but not in WHERE?`,answer:`Pure pipeline position. The alias is born when SELECT computes the output row (step 5). WHERE runs at step 2 — before the alias exists — so referencing it is an unknown-column error. ORDER BY runs at step 6, after SELECT, on finished output rows where the alias is a real column. Practical workarounds for the WHERE case: repeat the expression (WHERE price * 0.9 < 500), or compute it in a subquery/CTE and filter the outer query. Worth noting: some engines (SQLite, MySQL) are lenient and accept aliases in GROUP BY/HAVING too — convenient, non-portable, avoid in interviews.`,isCaseBased:!1},{question:`COUNT(*) vs COUNT(col) vs COUNT(DISTINCT col) — differences, and when does each mislead?`,answer:`COUNT(*) counts rows, full stop. COUNT(col) counts rows where col is non-NULL — NULLs silently vanish from the tally. COUNT(DISTINCT col) counts unique non-NULL values. Misleads: COUNT(col) understates when the column is sparse (counting phone numbers is not counting customers); COUNT(*) overstates "how many customers ordered" when a customer has several rows — that needs COUNT(DISTINCT customer_id); and AVG has the same NULL behavior, so AVG(col) is sum-of-non-NULLs over count-of-non-NULLs, which may not be "the average per row" the stakeholder meant. The interview-grade habit: for every aggregate, say out loud what happens to NULLs and duplicates.`,isCaseBased:!1},{question:`Case: your "loyal customers" report (2+ orders this quarter) includes a customer whose only two orders were both cancelled. The query GROUPs orders by customer and HAVING COUNT(*) >= 2. Debug it.`,answer:`The count is judging the wrong population: every order reaches the group regardless of status, so two cancelled orders still count 2 and pass HAVING. The status condition is per-row, so it belongs in WHERE — WHERE status = 'delivered' (or NOT IN ('cancelled','returned'), per the business rule) trims those rows before groups form; the customer's group is then empty or size 1 and drops out. The generalized lesson: when a group-level report is wrong, first ask what rows fed the groups — HAVING can only judge what WHERE let through. Also confirm the date filter is per-row too (order date in WHERE), and add a regression case: a customer with exactly two cancelled orders must not appear.`,isCaseBased:!0},{question:`A dashboard counts customers outside Delhi with WHERE city <> 'Delhi'. Customers with unknown city are missing from the count. Why, and what is the fix?`,answer:`Three-valued logic. For a NULL city, city <> 'Delhi' evaluates to NULL (unknown), not TRUE — and WHERE keeps only TRUE rows. So NULL-city rows fail BOTH city = 'Delhi' and city <> 'Delhi'; they fall through every branch without an error. Fix depends on intent: to include unknowns, WHERE city <> 'Delhi' OR city IS NULL (or COALESCE(city, '') <> 'Delhi'). The broader habit: for every nullable column in a filter, decide explicitly where NULLs should land and encode it — the default silently excludes them. This bug class survives code review because the query looks obviously correct.`,isCaseBased:!1},{question:`Case: a "top 5 cheapest products" query (ORDER BY price LIMIT 5) returns a different fifth product on different runs. Prod and staging disagree too. Walk through it.`,answer:`Ties. Several products share the boundary price, and ORDER BY price alone leaves tied rows in unspecified order — the engine returns whichever it reached first, which varies with plan, indexes, and physical row order. LIMIT then cuts through the tie arbitrarily: nondeterministic fifth row, and prod/staging disagree because their storage differs. Fix: make the sort total — add a unique tiebreaker, ORDER BY price ASC, id ASC. Alternative if all tied rows should appear: rank-based approaches (window functions) instead of a raw LIMIT. Interview bonus: the same nondeterminism poisons OFFSET pagination — page 2 can repeat or skip rows unless the ORDER BY is total.`,isCaseBased:!0},{question:`DELETE vs TRUNCATE vs DROP — which family does each belong to, and how do they differ?`,answer:`DELETE is DML: removes rows (optionally filtered by WHERE), fires row triggers, is fully transactional — ROLLBACK restores the rows. TRUNCATE is DDL in most engines: empties the whole table by deallocating storage — much faster than row-by-row DELETE, no WHERE, usually no row triggers, and in some engines it cannot be rolled back or auto-commits (engine-dependent: Postgres CAN roll it back inside a transaction; classic MySQL cannot). DROP is DDL: the table itself ceases to exist — data, structure, indexes, permissions. One-liner: DELETE empties selectively, TRUNCATE empties completely, DROP removes the container. Note for the playground: SQLite has no TRUNCATE; DELETE without WHERE plus its truncate optimization fills the role.`,isCaseBased:!1},{question:`State the GROUP BY rule about non-aggregated columns, and how different engines enforce it.`,answer:`Every column in SELECT must be either inside an aggregate or listed in GROUP BY. Reason: a group holds many rows; a bare column has many candidate values and no defined answer. Enforcement differs: Postgres rejects it outright (allowing only columns functionally dependent on a grouped primary key); MySQL with ONLY_FULL_GROUP_BY (default since 5.7) rejects it, older configs silently pick a value; SQLite always runs it and returns an arbitrary row's value per group — the most dangerous behavior, because the output looks plausible. If you genuinely want "a" value per group, say so explicitly: MIN(name), GROUP_CONCAT(name), or a window-function pattern to pick the top row per group.`,isCaseBased:!1},{question:`Is HAVING legal without GROUP BY? What would it mean?`,answer:`Yes. Without GROUP BY, the whole table forms one implicit group, and HAVING judges that single group. SELECT COUNT(*) FROM orders HAVING COUNT(*) >= 100 returns one row if the table has 100+ orders, zero rows otherwise — a table-level gate. It is rare but a real trivia probe, and it reinforces the model: HAVING filters groups, and one implicit group is still a group. The reverse mistake to flag: HAVING with only per-row conditions (no aggregate) is either rejected or is just a slower, misleading WHERE — per-row logic belongs in WHERE.`,isCaseBased:!1},{question:`SELECT DISTINCT vs GROUP BY for deduplication — are they equivalent, and when do you pick which?`,answer:`For pure dedup they produce the same rows: SELECT DISTINCT city FROM customers equals SELECT city FROM customers GROUP BY city, and modern optimizers often compile them to the same plan. Pick by intent: DISTINCT says "remove duplicate output rows" — use it when you only need unique values. GROUP BY says "form groups I will compute on" — required the moment you want any aggregate per value (COUNT per city). Two details that separate candidates: DISTINCT applies to the entire output row, not just the first column (DISTINCT a, b dedupes pairs); and DISTINCT sits late in the pipeline — after SELECT computes rows, before ORDER BY sorts them.`,isCaseBased:!1}],flashcards:[{front:`The four SQL statement families`,back:`DDL defines shape (CREATE/ALTER/DROP) · DML works on rows (SELECT/INSERT/UPDATE/DELETE) · DCL controls access (GRANT/REVOKE) · TCL controls transactions (COMMIT/ROLLBACK).`},{front:`Logical evaluation order of SELECT`,back:`FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT. Written order ≠ executed order.`},{front:`WHERE vs HAVING, one breath`,back:`WHERE trims rows BEFORE grouping (no aggregates allowed); HAVING judges finished groups (aggregates fine). WHERE runs first, so it changes what groups contain.`},{front:`Why WHERE cannot use a SELECT alias`,back:`Aliases are born at SELECT (step 5); WHERE runs at step 2. Only ORDER BY (step 6) can always use them. Fix: repeat the expression or use a subquery.`},{front:`NULL = NULL evaluates to…`,back:`NULL (unknown), never TRUE. Three-valued logic: TRUE/FALSE/NULL, and WHERE keeps only TRUE. Test NULLs with IS NULL / IS NOT NULL.`},{front:`The <> NULL silent-loss trap`,back:`WHERE city <> 'Delhi' does NOT return NULL-city rows: the test yields NULL, not TRUE. Nullable filter columns need an explicit OR city IS NULL decision.`},{front:`COUNT(*) vs COUNT(col)`,back:`COUNT(*) counts rows. COUNT(col) counts non-NULL values of col. 8 rows with one NULL city → 8 vs 7. AVG/SUM also skip NULLs.`},{front:`The GROUP BY iron rule`,back:`Every SELECT column must be aggregated or listed in GROUP BY. Strict engines error; SQLite silently picks an arbitrary row per group — wrong answers that look right.`},{front:`BETWEEN and LIKE wildcards`,back:`BETWEEN a AND b is inclusive on BOTH ends. LIKE: % = any run of characters (even zero), _ = exactly one character.`},{front:`ORDER BY: ties and NULLs`,back:`Add a second key to break ties or LIMIT picks arbitrary winners. NULL placement varies by engine (SQLite: first ASC; Postgres: last ASC) — force it with NULLS LAST.`}],mindmapMarkdown:`- SQL Core: SELECT Anatomy, GROUP BY & HAVING
  - Four families
    - DDL: CREATE / ALTER / DROP
    - DML: SELECT / INSERT / UPDATE / DELETE
    - DCL: GRANT / REVOKE
    - TCL: COMMIT / ROLLBACK
  - Logical evaluation order
    - FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT
    - Aliases born at SELECT → illegal in WHERE
    - ORDER BY: the safe place for aliases
  - Filtering (WHERE)
    - Comparisons + AND / OR / NOT
    - IN = chained ORs
    - BETWEEN: inclusive both ends
    - LIKE: % any run, _ one char
    - IS NULL, never = NULL
  - NULL three-valued logic
    - NULL = NULL → NULL, not true
    - <> misses NULL rows silently
  - Aggregates
    - COUNT / SUM / AVG / MIN / MAX
    - COUNT(*) rows vs COUNT(col) non-NULL
  - GROUP BY
    - one output row per group
    - non-aggregated column must be grouped
    - SQLite: silent arbitrary pick (trap)
  - HAVING vs WHERE
    - WHERE: rows, before groups
    - HAVING: groups, after — aggregates OK
    - same threshold can differ (cancelled-order demo)
  - Output shaping
    - DISTINCT: dedupe whole output row
    - ORDER BY multi-key: tiebreakers
    - NULL sort order varies → NULLS LAST
    - LIMIT: runs dead last`};export{e as default};