var e={id:`math-l0-matrices-transformations`,subjectId:`math`,level:0,title:`Matrices as Transformations`,whyItMatters:`A matrix is usually taught as a grid of numbers, which explains nothing. Read it as a transformation of space and every rule stops being arbitrary: why order matters, what a determinant is, and why an eigenvector is special.`,assumes:[`You have read *Vectors and the Dot Product*`,`You can multiply and add numbers`],estMinutes:18,sections:[{type:`intuition`,title:`A matrix moves space`,md:`Forget the grid. A matrix is a **function that takes a vector and returns a vector** — it moves every point in space at once, in a way that keeps grid lines straight and evenly spaced and leaves the origin fixed.

The columns tell you exactly what it does. **Column 1 is where the vector [1, 0] lands; column 2 is where [0, 1] lands.** That is not a mnemonic; it is what the multiplication computes, and once you see it you can read a 2x2 matrix at a glance.

So a matrix rotates, stretches, shears or squashes. And **multiplying two matrices is doing one transformation and then the other**, which is why order matters — rotating then stretching is a different result from stretching then rotating.`},{type:`code`,lang:`python`,title:`Two transformations, in both orders`,code:`import numpy as np
R = np.array([[0.0, -1.0], [1.0, 0.0]])   # rotate 90 degrees
S = np.array([[2.0,  0.0], [0.0, 0.5]])   # stretch x by 2, squash y by half
v = np.array([1.0, 0.0])

print('R v =', (R @ v).tolist(), '  S v =', (S @ v).tolist())
print('R @ S =', (R @ S).tolist())
print('S @ R =', (S @ R).tolist())
print('det(R) = %.1f   det(S) = %.1f   det(R@S) = %.1f'
      % (np.linalg.det(R), np.linalg.det(S), np.linalg.det(R @ S)))

# ---- real output ----
# R v = [0.0, 1.0]   S v = [2.0, 0.0]
# R @ S = [[0.0, -0.5], [2.0, 0.0]]
# S @ R = [[0.0, -2.0], [0.5, 0.0]]
# det(R) = 1.0   det(S) = 1.0   det(R@S) = 1.0`,annotations:{2:`Read the columns: [1,0] goes to [0,1] and [0,1] goes to [-1,0]. That is a 90-degree turn, visible without computing anything.`,13:`R@S and S@R are different matrices, not different arrangements of the same one. Stretch-then-rotate sends [1,0] to [0,2]; rotate-then-stretch sends it to [0,0.5].`,16:`det(S) = 1.0 even though it stretches by 2 and squashes by 0.5. The determinant is the AREA scale factor, and 2 × 0.5 = 1 — the shape changes and the area does not.`}},{type:`note`,label:`The determinant, and what zero means`,md:`The determinant is how much the transformation scales **area** (or volume, in higher dimensions). A determinant of 3 triples every area; a determinant of 1 preserves it, as the stretch-and-squash above does.

A **negative** determinant means the space was flipped over — orientation reversed.

A determinant of **exactly zero** is the important case: the transformation has collapsed space into a lower dimension. A 2D plane has been flattened onto a line, and there is no way back, because every point on that line came from a whole line of inputs.

That is what **singular**, **not invertible** and **rank-deficient** all mean, and it is what your linear-algebra library is complaining about when it refuses to solve a system.`},{type:`code`,lang:`python`,title:`A matrix that destroys a dimension`,code:`Sing = np.array([[1.0, 2.0], [2.0, 4.0]])
print('columns:', Sing[:, 0].tolist(), 'and', Sing[:, 1].tolist())
print('det = %.1f   rank = %d' % (np.linalg.det(Sing), np.linalg.matrix_rank(Sing)))

# ---- real output ----
# columns: [1.0, 2.0] and [2.0, 4.0]
# det = 0.0   rank = 1`,annotations:{2:`The second column is exactly twice the first. Both basis vectors land on the same line, so the whole plane is squashed onto it.`,7:`rank 1 means the output is one-dimensional. Rank is the number of dimensions that survive, and a rank below the matrix size is exactly what a zero determinant reports.`,6:`This is not an exotic case. Two nearly identical features in a dataset produce a nearly singular matrix, which is why ordinary least squares blows up on collinear data and why ridge regression adds λI to make it invertible again.`}},{type:`math`,intro:`The eigenvector equation. Read it as: applying the matrix to v does nothing but scale it. Most vectors get rotated by a transformation; an eigenvector is one of the special directions that does not, and λ says by how much it stretches.`,latex:[`A v = \\lambda v`]},{type:`code`,lang:`python`,title:`The directions a matrix leaves alone`,code:`M = np.array([[3.0, 1.0], [1.0, 3.0]])
w, V = np.linalg.eigh(M)
print('eigenvalues :', np.round(w, 4).tolist())
print('eigenvectors:', np.round(V, 4).tolist())
for i in range(2):
    print('  M v%d = %-22s  lambda*v%d = %s'
          % (i, np.round(M @ V[:, i], 4).tolist(), i, np.round(w[i] * V[:, i], 4).tolist()))

# ---- real output ----
# eigenvalues : [2.0, 4.0]
# eigenvectors: [[-0.7071, 0.7071], [0.7071, 0.7071]]
#   M v0 = [-1.4142, 1.4142]       lambda*v0 = [-1.4142, 1.4142]
#   M v1 = [2.8284, 2.8284]        lambda*v1 = [2.8284, 2.8284]`,annotations:{2:`eigh rather than eig, because M is symmetric. Symmetric matrices are guaranteed real eigenvalues and orthogonal eigenvectors, which is why covariance matrices are so pleasant to work with.`,11:`M v0 and 2·v0 are the identical vector. The transformation moved this direction by scaling alone — no rotation at all, which is what makes it an eigenvector.`,9:`The two eigenvectors here are [−0.7071, 0.7071] and [0.7071, 0.7071] — the two diagonals, at right angles to each other. The matrix stretches one diagonal by 4 and the other by 2, and does nothing else.`}},{type:`note`,label:`Where each of these actually shows up`,md:`**Eigenvectors of a covariance matrix are the principal components.** PCA is exactly this computation: the eigenvector with the largest eigenvalue is the direction of greatest variance, and the eigenvalue is that variance. Nothing more is going on.

**The determinant and rank** are why collinear features break linear regression. Two nearly identical columns make XᵀX nearly singular, the inverse explodes, and the fitted weights come out enormous and opposite — which is the concrete mechanism behind the ridge regression example you may have seen.

**Matrix multiplication order** is why a neural network is a composition of transformations, and why you cannot reorder layers.

**SVD** generalises eigendecomposition to any matrix, including non-square ones, and is what underlies low-rank approximation — which is precisely what LoRA does when it fine-tunes a large model by learning a rank-8 update instead of a full one.`}],quiz:[{question:`What do the columns of a matrix tell you?`,options:[{text:`The eigenvalues`,explanation:`Those come from solving Av = λv.`},{text:`Where the basis vectors land — column 1 is the image of [1,0], column 2 of [0,1]`,explanation:`Correct, and it lets you read a 2x2 matrix at a glance.`},{text:`The determinant`,explanation:`That is computed from them, not read off.`},{text:`The rank`,explanation:`Rank is how many columns are independent, which is a further step.`}],correct:1},{question:`Why is R@S different from S@R?`,options:[{text:`A convention of matrix notation`,explanation:`It is a real difference: they send [1,0] to different places.`},{text:`Multiplication composes transformations, and doing one then the other is genuinely a different result`,explanation:`Correct — stretch-then-rotate gives [0,2], rotate-then-stretch gives [0,0.5].`},{text:`Because R is a rotation`,explanation:`Non-commutativity is general, not specific to rotations.`},{text:`They are the same up to rounding`,explanation:`They are different matrices entirely.`}],correct:1},{question:`det(S) = 1.0 for a matrix that stretches x by 2 and squashes y by 0.5. Why?`,options:[{text:`The determinant ignores scaling`,explanation:`It measures exactly that.`},{text:`The determinant is the AREA scale factor, and 2 × 0.5 = 1 — the shape changes, the area does not`,explanation:`Correct, and it is why the determinant alone does not tell you the transformation is harmless.`},{text:`S is a rotation`,explanation:`It is a pure scaling.`},{text:`S is the identity`,explanation:`The identity would leave [1,0] at [1,0]; S sends it to [2,0].`}],correct:1},{question:`A matrix has determinant 0 and rank 1. What has happened?`,options:[{text:`Nothing unusual — it is a valid transformation`,explanation:`It is valid and irreversible, which is the point.`},{text:`Space has been collapsed onto a line: information is destroyed and there is no inverse`,explanation:`Correct — every point on that line came from a whole line of inputs.`},{text:`The matrix is the identity`,explanation:`The identity has determinant 1.`},{text:`The eigenvalues are complex`,explanation:`A zero determinant means one eigenvalue is zero, which is real.`}],correct:1},{question:`What makes a vector an eigenvector?`,options:[{text:`It has length 1`,explanation:`That is a normalisation convention, not the defining property.`},{text:`The transformation only scales it — Mv0 = [−1.4142, 1.4142] is exactly 2·v0, with no rotation`,explanation:`Correct. Most directions get rotated; these do not.`},{text:`It is a column of the matrix`,explanation:`Unrelated.`},{text:`It is orthogonal to the other columns`,explanation:`True for symmetric matrices as a consequence, not the definition.`}],correct:1},{question:`What is PCA, in these terms?`,options:[{text:`A matrix inversion`,explanation:`No inverse is computed.`},{text:`The eigendecomposition of the covariance matrix — the largest-eigenvalue eigenvector is the direction of greatest variance, and the eigenvalue IS that variance`,explanation:`Correct. Nothing more is going on.`},{text:`A determinant calculation`,explanation:`The determinant plays no role.`},{text:`A rank reduction with no eigenvectors involved`,explanation:`It reduces rank by keeping the top eigenvectors.`}],correct:1}],interviewQuestions:[{question:`What is a matrix, conceptually?`,answer:`A linear transformation of space — a function taking a vector to a vector that keeps grid lines straight and evenly spaced and fixes the origin. The most useful concrete handle is that the columns are where the basis vectors land: column one is the image of [1,0], column two of [0,1]. So you can read a 2x2 matrix at a glance rather than computing. And matrix multiplication is composition — do one transformation, then the other — which immediately explains why order matters, why a neural network is a chain of transformations, and why you cannot reorder layers.`,isCaseBased:!1},{question:`What does the determinant mean?`,answer:`The factor by which the transformation scales area, or volume in higher dimensions. Negative means orientation was flipped. The important case is exactly zero: the transformation has collapsed space into a lower dimension, so information is destroyed and there is no inverse — that is what singular, non-invertible and rank-deficient all describe. Worth adding that a determinant of 1 does not mean harmless: the scaling matrix in the example stretches x by 2 and squashes y by 0.5, changing every shape while leaving every area exactly the same.`,isCaseBased:!1},{question:`What is an eigenvector and why should I care?`,answer:`A direction the transformation only scales rather than rotates: Av = λv, with λ saying by how much. Most directions get turned; these do not, which makes them the natural axes of the transformation. The reason to care in machine learning is PCA — it is the eigendecomposition of the covariance matrix, where the largest-eigenvalue eigenvector is the direction of greatest variance and the eigenvalue is that variance. Symmetric matrices, which covariance matrices are, are guaranteed real eigenvalues and mutually orthogonal eigenvectors, which is why the principal components come out at right angles for free.`,isCaseBased:!1},{question:`Why does linear regression break on collinear features?`,answer:`Because collinearity makes XᵀX nearly singular. A matrix whose second column is exactly twice the first has determinant 0 and rank 1 — it has collapsed the plane onto a line — and nearly identical features produce a matrix that is nearly that. The closed-form solution needs the inverse of XᵀX, and inverting a near-singular matrix amplifies noise enormously, which is why the fitted weights come out large and opposite in sign, nearly cancelling. Ridge regression's λI fixes it directly: adding a multiple of the identity moves the eigenvalues away from zero, so the matrix is invertible again and the weights become stable.`,isCaseBased:!0},{question:`What is SVD and where does it matter?`,answer:`It generalises eigendecomposition to any matrix, including rectangular ones: A = UΣVᵀ, where U and V are rotations and Σ is a diagonal of singular values. The interpretation is that every linear transformation is a rotation, then a scaling along axes, then another rotation. Its practical importance is low-rank approximation — keeping only the largest singular values gives the best possible approximation of a given rank, which is the basis of compression, denoising and latent semantic analysis. It is also exactly what LoRA exploits: fine-tuning a large model by learning a rank-8 update instead of a full-rank one, on the empirical finding that the useful update is close to low-rank.`,isCaseBased:!1},{question:`Why can you not reorder the layers of a neural network?`,answer:`Because matrix multiplication does not commute, and each layer is a transformation applied to the output of the previous one. In the worked example, R@S and S@R are different matrices — stretch-then-rotate sends [1,0] to [0,2] while rotate-then-stretch sends it to [0,0.5]. With non-linearities between the layers the composition is even less reorderable, since the activation acts on whatever coordinates it is given. It is worth noting the exception that proves the rule: if you removed the non-linearities, the whole stack WOULD collapse into a single matrix by associativity, and then order would only affect what that one matrix is.`,isCaseBased:!1},{question:`How do you know whether a matrix is invertible, and what do you do when it is not?`,answer:`Determinant non-zero, equivalently full rank, equivalently no zero eigenvalue — these are the same statement. In practice the binary test is not the useful one, because floating-point matrices are rarely exactly singular; the condition number is, since a large one means the inverse amplifies noise even though it technically exists. When a matrix is singular or ill-conditioned the options are regularisation, adding λI as ridge does, which shifts the eigenvalues away from zero; the pseudo-inverse via SVD, which handles the collapsed directions gracefully; or fixing the underlying cause by removing the redundant features. In an iterative setting you would simply not invert at all and use gradient descent.`,isCaseBased:!0},{question:`What does rank mean in practical terms?`,answer:`The number of dimensions that survive the transformation — how much genuinely independent information the matrix carries. A rank-1 matrix maps everything onto a single line, so however many columns it has, it contains one direction's worth of information. That framing is what makes low-rank ideas work: a large weight matrix is often approximately low rank, meaning its useful content is far smaller than its size, and LoRA and model compression both cash that in. It also explains feature redundancy in data: if a design matrix has rank well below its column count, some features are linear combinations of others and are adding parameters without adding information.`,isCaseBased:!1}],flashcards:[{front:`What a matrix IS`,back:`A transformation of space. The COLUMNS are where the basis vectors land — column 1 is the image of [1,0].`},{front:`Multiplication`,back:`Composition: do one transformation, then the other. R@S sends [1,0] to [0,2]; S@R sends it to [0,0.5].`},{front:`Determinant`,back:`The AREA scale factor. Negative = flipped. det(S)=1.0 while stretching x by 2 and squashing y by 0.5 — shape changes, area does not.`},{front:`Determinant zero`,back:`Space collapsed to a lower dimension. No inverse, information destroyed. = singular = rank-deficient.`},{front:`The singular example`,back:`[[1,2],[2,4]] — column 2 is twice column 1. det 0.0, rank 1. Two nearly identical features do this to XᵀX.`},{front:`Eigenvector`,back:`Av = λv — a direction the transformation only SCALES, never rotates. M v0 = [−1.4142, 1.4142] = exactly 2·v0.`},{front:`Why symmetric matrices are nice`,back:`Real eigenvalues and mutually ORTHOGONAL eigenvectors, guaranteed. Covariance matrices are symmetric — hence PCA's right-angled components.`},{front:`PCA in one line`,back:`The eigendecomposition of the covariance matrix. Largest eigenvalue's eigenvector = direction of greatest variance; the eigenvalue IS that variance.`}],mindmapMarkdown:`- Matrices as transformations
  - What it is
    - a function from vectors to vectors
    - grid lines stay straight, origin fixed
    - COLUMNS = where the basis vectors land
  - Multiplication
    - composition: one, then the other
    - R@S sends [1,0] to [0,2]
    - S@R sends [1,0] to [0,0.5]
    - hence you cannot reorder layers
  - Determinant
    - the AREA scale factor
    - negative = orientation flipped
    - det(S) = 1.0 while stretching 2x and squashing 0.5x
    - ZERO = space collapsed, no inverse
  - Rank
    - dimensions that survive
    - [[1,2],[2,4]]: det 0, rank 1
    - collinear features -> near-singular X^T X
    - ridge adds lambda*I to fix it
  - Eigenvectors
    - Av = lambda v: scaled, never rotated
    - M v0 = [-1.4142, 1.4142] = exactly 2 v0
    - symmetric -> real values, orthogonal vectors
    - PCA IS the eigendecomposition of covariance
  - SVD
    - eigendecomposition for any matrix
    - rotation, scale, rotation
    - low-rank approximation -> LoRA`};export{e as default};