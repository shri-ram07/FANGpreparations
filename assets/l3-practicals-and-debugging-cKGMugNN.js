var e={id:`dl-l3-practicals-and-debugging`,subjectId:`dl`,level:3,title:`The Four Practicals: Things You Build Yourself`,whyItMatters:`Reading about a network and building one are different skills, and only the second survives an interview. These are four briefs, sized so each fits an evening — with the parameter budget computed in advance, because a build you cannot size is a build you cannot debug.`,assumes:[`You have read *Backpropagation* and *PyTorch Fundamentals*`,`You have read *The Debugging Playbook*`],estMinutes:18,sections:[{type:`intuition`,title:`Size it before you build it`,md:`Every one of these builds fails the same way if you skip one step: you write it, run it, get a plausible-looking loss, and cannot tell whether it is learning or broken.

So before the first training run, produce **two numbers**. The **parameter count**, because a model with ten times more or fewer parameters than you expected has a shape bug you will not otherwise find. And the **expected initial loss**, which is ln(C) for a classifier — 2.3026 for ten digits — because a model that starts anywhere else is broken before training.

They take a minute and they turn a mysterious failure into a located one. The four builds below are sized in exactly that way.`},{type:`code`,lang:`python`,title:`The parameter budget for all four builds`,code:`mlp = 784*128 + 128 + 128*10 + 10
cnn = 9*1*32 + 32 + 9*32*64 + 64 + (7*7*64)*128 + 128 + 128*10 + 10
V, h = 65, 256
lstm = 4*(h*(V + h) + h) + h*V + V
gen = 100*256 + 256 + 256*784 + 784
disc = 784*256 + 256 + 256*1 + 1

print('P1  MLP 784-128-10          %9s' % f'{mlp:,}')
print('P2  CNN 32-64-dense128-10   %9s' % f'{cnn:,}')
print('P3  char LSTM V=65 h=256    %9s' % f'{lstm:,}')
print('P4  GAN: G %s + D %s = %s' % (f'{gen:,}', f'{disc:,}', f'{gen+disc:,}'))

# ---- real output ----
# P1  MLP 784-128-10            101,770
# P2  CNN 32-64-dense128-10     421,642
# P3  char LSTM V=65 h=256      346,433
# P4  GAN: G 227,344 + D 201,217 = 428,561`,annotations:{2:`The CNN's two conv layers are only 18,816 parameters between them. The 7·7·64 to 128 dense layer is 401,536 — 95% of the model, on the same imbalance VGG showed at a thousand times the scale.`,4:`4x for the LSTM's four gates, plus an output projection back to the 65-character vocabulary. Getting this number wrong by exactly 4 is the classic sign of implementing a plain RNN by mistake.`,13:`P1 at 101,770 parameters against 60,000 training images. It can memorise the training set, which is what makes it a useful first build: you will see overfitting immediately and have to deal with it.`,16:`The GAN's two halves are deliberately close in size — 227,344 and 201,217. A discriminator much stronger than the generator wins early and flattens its gradient, which is the first thing to check when a GAN produces noise.`}},{type:`note`,label:`P1 — an MLP on digits, in NumPy only`,md:`**Build:** 784 → 128 → 10, ReLU hidden, softmax output, cross-entropy loss. Forward and backward written by hand. No framework.

**Why this one first:** it is the only build where you cannot hide behind autograd. You will derive the gradient of softmax-with-cross-entropy and discover it simplifies to \`predicted − target\`, which is the single most useful piece of arithmetic in the subject.

**Pre-flight:** parameters 101,770; initial loss 2.3026.

**Done looks like:** 97%+ test accuracy, and a gradient check against central differences agreeing to about five decimal places on a random sample of weights.`},{type:`note`,label:`P2 — the same dataset, in PyTorch, as a CNN`,md:`**Build:** conv(1→32) → pool → conv(32→64) → pool → dense(3136→128) → dense(128→10).

**Why:** building the same task twice is the point. You will feel how much the framework removes, and you can compare the two directly on the same data, which very few tutorials let you do.

**Pre-flight:** parameters 421,642; initial loss 2.3026 again.

**Done looks like:** 99%+ test accuracy, beating P1 by about two points with four times the parameters — and being able to say why: parameter sharing and locality, not capacity.

**The trap:** getting the flatten dimension wrong. 28 → pool → 14 → pool → 7, so 7·7·64 = 3,136. Print the shape after each layer rather than deriving it, once.`},{type:`note`,label:`P3 — a character-level text generator`,md:`**Build:** an LSTM over characters — embed, one or two LSTM layers at hidden size 256, project back to the vocabulary. Train on any plain text file. Generate by sampling with a temperature.

**Why:** it is the first build where the output is qualitative, so you learn to read samples as a diagnostic. Early output is noise, then character frequencies, then plausible words, then punctuation and quotes that match. That progression is genuinely informative about what the model has learned.

**Pre-flight:** parameters 346,433 at V=65, h=256; initial loss ln(65) = 4.1744.

**The trap:** the targets are the inputs shifted by one. Getting the shift wrong gives a model that trains beautifully and generates nothing — it has learned to copy its input. Check by confirming the initial loss is 4.17 and that a trained model cannot achieve a loss near zero.`},{type:`note`,label:`P4 — a very small GAN`,md:`**Build:** generator 100 → 256 → 784, discriminator 784 → 256 → 1, both plain MLPs, on the same digit dataset.

**Why:** to experience adversarial training being genuinely different. There is no loss curve to read — a falling generator loss can mean a winning generator or a failing discriminator — so you must look at samples, which is the lesson.

**Pre-flight:** 227,344 + 201,217 = 428,561 parameters, deliberately balanced. Discriminator loss should sit near **1.3863** = 2·ln(2) when the two are evenly matched.

**The traps, in the order you will meet them:** the discriminator winning immediately, which flattens the generator's gradient — use the non-saturating loss and consider fewer discriminator steps; and mode collapse, where every sample becomes the same convincing digit, which you can only see by looking at a grid of outputs.`},{type:`note`,label:`How to size a build you have never done`,md:`The four above come with their numbers. The method for the fifth is worth having explicitly.

**Count parameters layer by layer** from the shapes, before writing any code. Dense is in×out + out; conv is k·k·C_in·C_out + C_out; LSTM is 4·(h·(x+h) + h). If you cannot do this from the architecture, you do not yet know what you are building.

**Estimate memory** as parameters × 4 bytes for float32, × 3 for Adam's two moment buffers on top — then add activations, which are batch size × the sum of all layer output sizes and usually dominate.

**Predict the initial loss** from the output layer: ln(C) for classification, the variance of the targets for regression with a zero-initialised head.

**Then check all three on the first run.** A number that is off by a factor of 4 or 10 is a shape bug, and finding it in the first minute is the difference between an evening and a weekend.`}],quiz:[{question:`Why compute the parameter count before the first training run?`,options:[{text:`To choose the batch size`,explanation:`Useful for memory, but not the main reason.`},{text:`A count ten times off from expectation is a shape bug you will not otherwise find`,explanation:`Correct — it converts a mysterious failure into a located one in a minute.`},{text:`To estimate accuracy`,explanation:`Parameter count does not predict accuracy.`},{text:`It is required by the framework`,explanation:`Nothing requires it.`}],correct:1},{question:`In the P2 CNN, the two conv layers hold 18,592 parameters and the dense layer holds 401,536. What does that echo?`,options:[{text:`That convolutions are inefficient`,explanation:`They are doing the work at a fraction of the parameters.`},{text:`VGG's imbalance at a thousand times the scale — parameters concentrate in the dense head, compute in the convolutions`,explanation:`Correct, and it is why global average pooling replaced dense heads.`},{text:`That the model is too small`,explanation:`The distribution, not the total, is the point.`},{text:`A bug in the count`,explanation:`The arithmetic is correct: 7·7·64 × 128 = 401,536.`}],correct:1},{question:`Your char-LSTM parameter count comes out at exactly a quarter of 346,433 minus the projection. What have you done?`,options:[{text:`Used the wrong vocabulary size`,explanation:`That would not give a clean factor of 4.`},{text:`Implemented a plain RNN instead of an LSTM — the 4x is the four gates`,explanation:`Correct, and this is precisely the kind of bug a pre-flight count catches.`},{text:`Forgotten the embedding`,explanation:`That would change it by a different amount.`},{text:`Used a GRU`,explanation:`A GRU would give three quarters, not one.`}],correct:1},{question:`In P3, what does the shift-by-one target bug look like?`,options:[{text:`The loss is nan`,explanation:`It trains cleanly, which is the problem.`},{text:`The model trains beautifully to a near-zero loss and generates nothing — it learned to copy its input`,explanation:`Correct. Confirm the initial loss is ln(65) = 4.1744 and that a trained model cannot reach zero.`},{text:`The loss never moves`,explanation:`It moves very well, downward.`},{text:`The vocabulary is wrong`,explanation:`The vocabulary is fine; the alignment is not.`}],correct:1},{question:`Why are the P4 generator and discriminator sized close together at 227,344 and 201,217?`,options:[{text:`For symmetry in the code`,explanation:`The reason is dynamic, not aesthetic.`},{text:`A discriminator much stronger than the generator wins early and flattens its gradient`,explanation:`Correct, and it is the first thing to check when a GAN produces noise.`},{text:`To keep memory equal`,explanation:`Memory is not the constraint at this size.`},{text:`Because they share weights`,explanation:`They are entirely separate networks.`}],correct:1},{question:`What should a healthy P4 discriminator loss sit near?`,options:[{text:`0, meaning it classifies perfectly`,explanation:`That means the discriminator has won and the generator has no gradient.`},{text:`1.3863 = 2·ln(2) — the discriminator guessing, so real and fake are indistinguishable`,explanation:`Correct, and it is the one number you can read from a GAN loss curve.`},{text:`Whatever the generator loss is`,explanation:`They are not tied to each other.`},{text:`It should fall steadily`,explanation:`A steadily falling discriminator loss means the generator is losing.`}],correct:1}],interviewQuestions:[{question:`Walk me through implementing backprop for an MLP from scratch.`,answer:`Forward: z1 = XW1 + b1, a1 = ReLU(z1), z2 = a1W2 + b2, then softmax and cross-entropy. Backward, and the piece worth knowing by heart: the gradient of softmax-with-cross-entropy with respect to the logits simplifies to predicted minus target — all the exponentials cancel. From there dW2 is a1ᵀ·dz2, db2 is the column sum of dz2, then propagate back through W2ᵀ and multiply elementwise by the ReLU mask, which is just z1 > 0. I would verify it with a gradient check against central differences on a random sample of weights, expecting agreement to about five decimal places.`,isCaseBased:!0},{question:`How do you size a model before building it?`,answer:`Three numbers, all computable from the architecture without writing code. Parameters, layer by layer: dense is in×out + out, conv is k·k·C_in·C_out + C_out, LSTM is 4·(h·(x+h) + h) for the four gates. Memory: parameters times four bytes for float32, times three again for Adam's two moment buffers, plus activations, which are batch size times the sum of layer output sizes and usually dominate. And the expected initial loss, ln(C) for a classifier. Then check all three on the first run — anything off by a factor of 4 or 10 is a shape bug, and finding it in the first minute rather than the third hour is the whole value of doing it.`,isCaseBased:!1},{question:`What does the flatten dimension in a CNN depend on, and why is it a common bug?`,answer:`On every downsampling operation before it, which is why it is easy to get wrong and easy to break later. In the P2 build, 28 goes to 14 after the first pool and 7 after the second, so with 64 channels the dense layer takes 7·7·64 = 3,136 inputs and holds 401,536 parameters. The bug is that changing the input size, adding a pool, or altering a stride silently changes that number, and the error surfaces as a shape mismatch far from the edit. The fix I would use is printing the shape after each layer once during development, and in production using adaptive pooling or global average pooling so the head does not depend on the input size at all.`,isCaseBased:!0},{question:`What is the classic bug in a character-level language model?`,answer:`Target alignment. The target for position t is the character at t+1, and getting that shift wrong gives a model that trains beautifully to a near-zero loss and generates nothing useful, because it has learned the identity — copying its input. It is dangerous precisely because it looks like success. Two checks catch it: the initial loss should be ln(V), 4.1744 for a 65-character vocabulary, and a trained model should not be able to reach a loss near zero, because next-character prediction is genuinely uncertain. If it does, it is reading the answer.`,isCaseBased:!0},{question:`You build a small GAN and it produces noise. What do you check?`,answer:`The discriminator loss first, because it is the one readable number: near 1.3863, which is 2·ln(2), means the two are evenly matched, while near zero means the discriminator has won and the generator has no gradient left. If it has won, use the non-saturating generator loss — maximise log D rather than minimise log(1 − D), which is roughly 19 times more gradient when the generator is losing badly — and consider fewer discriminator steps or a lower discriminator learning rate. Then look at a grid of samples rather than the loss, because mode collapse shows up as every sample being the same convincing digit and is invisible in any curve.`,isCaseBased:!0},{question:`Why build the same task twice, in NumPy and in PyTorch?`,answer:`Because they teach different things and the comparison is the point. The NumPy version is the only place you cannot hide behind autograd — you derive the softmax-with-cross-entropy gradient yourself and find it collapses to predicted minus target, which is the single most useful piece of arithmetic in the subject. The PyTorch version then shows exactly how much the framework removes, and lets you compare directly on the same data: the CNN reaches 99% against the MLP's 97% with four times the parameters, and being able to attribute that to parameter sharing and locality rather than capacity is the actual understanding.`,isCaseBased:!1},{question:`How do you know a generative model is improving without a meaningful loss?`,answer:`By looking, systematically. For a character model the sample progression is genuinely diagnostic — noise, then character frequencies, then word-shaped strings, then real words, then punctuation and quotes that open and close correctly. Each stage tells you what has been learned. For images, a fixed grid of samples from the same latent vectors every N steps, so changes are comparable rather than confounded by resampling. Beyond that, held-out perplexity for a language model, which is a real number, and FID for images. The general principle is to fix whatever you can — the seeds, the prompts, the latents — so that what you are looking at is the model changing and nothing else.`,isCaseBased:!1},{question:`Which of these four teaches the most, and why?`,answer:`The NumPy MLP, without much doubt. Everything afterwards is a variation on machinery you have already seen, and it is the only one that forces you to hold the whole forward-and-backward pass in your head at once — after it, autograd is a convenience rather than magic, and a gradient that comes back None is a hypothesis rather than a mystery. The GAN is second, for a different reason: it is the one that teaches that a loss curve is not always information, which is a habit of mind rather than a technique. The CNN and the character model mostly consolidate.`,isCaseBased:!1}],flashcards:[{front:`The two pre-flight numbers`,back:`Parameter count (off by 10x = a shape bug) and expected initial loss (ln(C) for a classifier). One minute; locates the failure.`},{front:`P1 — NumPy MLP`,back:`784-128-10, 101,770 params, initial loss 2.3026. Target 97%+ and a gradient check to ~5 decimals.`},{front:`The gradient worth memorising`,back:`softmax + cross-entropy with respect to the logits = predicted − target. All the exponentials cancel.`},{front:`P2 — PyTorch CNN`,back:`421,642 params, of which the dense head is 401,536 (95%). 99%+ accuracy. Flatten is 7·7·64 = 3,136.`},{front:`P3 — char LSTM`,back:`V=65, h=256 → 346,433 params, initial loss ln(65) = 4.1744. A count 4x low means you built a plain RNN.`},{front:`The P3 trap`,back:`Targets are inputs shifted by one. Get it wrong and the model trains to near-zero loss and generates nothing — it learned to copy.`},{front:`P4 — tiny GAN`,back:`G 227,344 + D 201,217 = 428,561, deliberately balanced. Healthy discriminator loss ≈ 1.3863 = 2·ln(2).`},{front:`Sizing an unfamiliar build`,back:`Dense in×out+out. Conv k·k·C_in·C_out+C_out. LSTM 4·(h·(x+h)+h). Memory: params×4 bytes ×3 for Adam, plus activations (usually dominant).`}],mindmapMarkdown:`- The four practicals
  - Before every build
    - parameter count (10x off = shape bug)
    - expected initial loss = ln(C)
  - P1 NumPy MLP
    - 784-128-10, 101,770 params, loss 2.3026
    - derive softmax+CE gradient = predicted - target
    - gradient check to ~5 decimals, 97%+
  - P2 PyTorch CNN
    - 421,642 params; dense head is 401,536 (95%)
    - flatten 7*7*64 = 3,136
    - 99%+, and say WHY: sharing and locality
  - P3 char LSTM
    - V=65 h=256 -> 346,433, loss ln(65) = 4.1744
    - 4x = the four gates; 1x means you built an RNN
    - TRAP: targets shifted by one, else it copies
    - read the sample progression as a diagnostic
  - P4 tiny GAN
    - G 227,344 + D 201,217, deliberately balanced
    - healthy D loss ~ 1.3863 = 2 ln 2
    - traps: D wins early; mode collapse (look at a grid)
  - Sizing anything else
    - dense in*out+out, conv k*k*Ci*Co+Co, LSTM 4*(h*(x+h)+h)
    - memory: params x4 bytes, x3 for Adam, plus activations`};export{e as default};