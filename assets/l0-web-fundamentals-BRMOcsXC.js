var e={id:`backend-l0-web-fundamentals`,subjectId:`backend`,level:0,title:`How the Internet Works: One Request, End to End`,whyItMatters:`"What happens when you type a URL and press Enter?" is the most-asked warm-up in the industry — and every later topic (FastAPI, auth, caching, serving a model at 1000 req/s) is just a layer on this one journey. If you cannot narrate DNS, handshake, method, status code without hesitating, nothing above it has anywhere to stand.`,estMinutes:50,sections:[{type:`intuition`,title:`Pressing Enter starts six jobs, not one`,md:`You type \`shop.com/cart\` and hit Enter. It feels like one action. It is six — and five of them finish before the server sees a single byte of your request.

- **Cache check** — does the browser already have this answer? (~0 ms)
- **DNS** — turn the name \`shop.com\` into an address like \`93.184.216.34\`.
- **TCP** — open a reliable pipe to that address. Costs one round trip.
- **TLS** — encrypt the pipe. Costs another round trip.
- **HTTP** — send the actual request; the server works and replies.
- **Render** — the browser parses the response and paints pixels.
- The line to carry into interviews: *most of a "slow request" is usually not the server.*`},{type:`visual`,component:`PointerBoxDiagram`,props:{title:`One request, end to end`,notice:`Step through a cold first request. Watch how much of the clock burns before the server does any work at all.`,leftLabel:`your machine`,rightLabel:`the internet`,frames:[{note:`0 ms. The browser checks its own memory and disk cache first. A fresh hit ends the story here with zero network. We miss, so we need an IP.`,stack:[{name:`url`,value:`shop.com/cart`},{name:`cache`,value:`MISS`}],heap:[{id:`dns`,value:`DNS resolver`,label:`idle`},{id:`srv`,value:`IP unknown`}]},{note:`~20-120 ms cold, ~0 ms cached. The resolver walks root, then .com, then the authoritative server, and caches the answer for its TTL.`,stack:[{name:`lookup`,to:`dns`},{name:`ip`,value:`93.184.216.34`}],heap:[{id:`dns`,value:`root > .com > auth`,label:`TTL 300s`},{id:`srv`,value:`93.184.216.34`,label:`found`}]},{note:`1 RTT (~30-80 ms). SYN, SYN-ACK, ACK. Not one byte of your request has moved yet — this whole round trip is pure setup.`,stack:[{name:`socket`,to:`srv`},{name:`state`,value:`SYN sent`}],heap:[{id:`srv`,value:`93.184.216.34:443`,label:`SYN-ACK`}]},{note:`+1 RTT (~30-80 ms) for TLS 1.3. It buys encryption, integrity, and proof this really is shop.com. That is exactly what the S in HTTPS costs.`,stack:[{name:`socket`,to:`srv`},{name:`tls`,value:`ClientHello`}],heap:[{id:`srv`,value:`cert + key exchange`,label:`TLS 1.3`}]},{note:`Server time, ~1-200 ms. Only now do your bytes fly: request line, headers, optional body. Everything before this was plumbing.`,stack:[{name:`request`,to:`srv`},{name:`line`,value:`GET /cart`}],heap:[{id:`srv`,value:`app > db > JSON`,label:`working`}]},{note:`~30-80 ms back, then parse and paint. Cold total is often 300+ ms — and the server itself was maybe 5 ms of it. That is where optimisation actually lives.`,stack:[{name:`response`,to:`srv`},{name:`status`,value:`200 OK`}],heap:[{id:`srv`,value:`200 OK + JSON body`,label:`5 KB`}]}]}},{type:`intuition`,title:`DNS: the internet runs on numbers, humans run on names`,md:`Machines route by IP address. You remember \`shop.com\`. DNS is the translator between them.

- Your machine asks a **recursive resolver** (your ISP's, or 8.8.8.8): "what is the IP for shop.com?" It does the legwork.
- If it does not know, it walks the tree: a **root** server points to the **.com TLD** server, which points to the **authoritative** server for shop.com.
- The authoritative server owns the truth and answers \`93.184.216.34\`.
- Every answer carries a **TTL** (time to live, in seconds). Everyone along the path caches until it expires.
- So visitor #2 pays ~0 ms instead of ~50 ms. Caching *is* the performance story of DNS.
- The bill for caching: change your IP and old clients keep hitting the dead one until their TTL runs out. Dropping TTL to 60s a day before a migration is a real ops move.`},{type:`intuition`,title:`TCP: shake hands before you talk`,md:`A phone call: you dial, they say "hello?", you say "hi, it's me" — only *then* does anyone say anything useful. TCP is that, in three packets.

- **SYN** — client: "can we talk? my sequence number is X."
- **SYN-ACK** — server: "yes; mine is Y, and I heard your X."
- **ACK** — client: "I heard your Y." The connection is open.
- That is **one full round trip (1 RTT)** spent before a single byte of your request moves.
- RTT is a physics bill: ~5 ms across a city, ~80 ms across an ocean. No framework makes light faster.
- Why pay it? TCP guarantees your bytes arrive **complete, in order, and retransmitted on loss**. HTTP is built on that promise.
- Which is why connection **reuse** matters so much — keep-alive and connection pools pay the handshake once, then send many requests over it.`},{type:`note`,md:`One honest note on **TLS** (the S in HTTPS): it costs one extra round trip in TLS 1.3 (TLS 1.2 cost two), and buys exactly three things — **encryption** so nobody on the path can read your data, **integrity** so nobody can edit it in flight, and **identity** so you know the server really is shop.com, proved by a certificate a trusted authority signed. After the handshake it is the same HTTP either way; TLS only wraps the pipe.`},{type:`intuition`,title:`An HTTP request is three parts. Always.`,md:`Open any HTTP request ever sent, from any language, and you find this exact shape:

1. **Request line** — method, path, version: \`GET /v1/carts/42 HTTP/1.1\`
2. **Headers** — \`Key: Value\` lines: who you are, what format you speak, caching rules.
3. **Blank line**, then the optional **body** — usually the JSON you are sending. GET normally has none.

The response mirrors it exactly: a **status line** (\`HTTP/1.1 200 OK\`), headers, blank line, body. That symmetry is the whole protocol — HTTP is just text framing over a TCP pipe.`},{type:`code`,lang:`bash`,title:`The raw thing, with curl -v`,code:`$ curl -v -X POST https://api.shop.com/v1/carts/42/items \\
  -H 'Content-Type: application/json' \\
  -H 'Authorization: Bearer eyJhbGciOi...' \\
  -d '{"sku": "TSHIRT-M", "qty": 2}'

* Connected to api.shop.com (93.184.216.34) port 443
* TLS 1.3 handshake done: TLS_AES_128_GCM_SHA256

> POST /v1/carts/42/items HTTP/1.1
> Host: api.shop.com
> Authorization: Bearer eyJhbGciOi...
> Content-Type: application/json
> Content-Length: 29
>
> {"sku": "TSHIRT-M", "qty": 2}

< HTTP/1.1 201 Created
< Content-Type: application/json
< Location: /v1/carts/42/items/9f3
< Cache-Control: no-store
< Set-Cookie: session=abc123; HttpOnly; Secure; SameSite=Lax
<
< {"id": "9f3", "sku": "TSHIRT-M", "qty": 2}`,annotations:{1:`-v is verbose: curl prints the raw protocol. Lines starting with > are what you SENT, < is what came BACK.`,6:`DNS already turned the name into this IP before anything connected. Port 443 = HTTPS (80 = plain HTTP).`,7:`One extra round trip, and now the pipe is encrypted and the server has proved its identity.`,9:`The request line: METHOD + PATH + VERSION. Three words — that is the entire first line of every HTTP request.`,10:`Host is mandatory in HTTP/1.1: one IP serves many domains, so the server must be told which site you meant.`,13:`Content-Length is bytes, not characters. Get it wrong and the server hangs waiting for bytes that never come.`,14:`The lone ">" is the blank line. Headers end, body begins. That separator is the whole framing rule.`,17:`201 Created, not 200 OK — a new resource now exists. Which one you pick is exactly what interviewers listen for.`,19:`Location gives the URL of the thing just created, so the client never has to guess it.`,21:`Set-Cookie is the server handing the browser state. HttpOnly blocks JavaScript from reading it; Secure means HTTPS only.`}},{type:`intuition`,title:`The five methods, and the two words that decide retries`,md:`- **GET** — read something. **Safe** and **idempotent**.
- **POST** — create, or "do a thing". Neither safe nor idempotent.
- **PUT** — replace the whole resource at a URL you already know. Idempotent.
- **PATCH** — partially update. Usually *not* idempotent (depends on the patch format — \`increment qty by 1\` clearly is not).
- **DELETE** — remove it. Idempotent: deleting twice leaves the same end state.
- **Safe** means server state does not change at all. **Idempotent** means doing it N times leaves the same state as doing it once.
- These are not trivia words. They decide one thing: *is a retry allowed?*`},{type:`note`,md:"Why idempotency matters: networks lose **responses**, not just requests. Your `POST /payments` succeeded, the reply vanished, the client retried — the user is charged twice. That is why proxies, browsers and HTTP clients happily auto-retry GET/PUT/DELETE but never POST, and why real payment APIs make POST idempotent by hand with an **idempotency key** header: the server stores the key with its result, and a repeat of the same key replays the original response instead of charging again."},{type:`intuition`,title:`Status codes: read the first digit first`,md:"The first digit tells you **whose problem it is**. Learn the four families, then the handful that actually show up.\n\n- **2xx — it worked.** `200 OK` (here is your data) · `201 Created` (new resource, with a `Location` header) · `204 No Content` (worked, nothing to send — the classic DELETE reply).\n- **3xx — go somewhere else.** `301 Moved Permanently` vs `302 Found` (temporary).\n- **4xx — you, the client, broke it.** Retrying the same request unchanged will fail the same way.\n- **5xx — the server broke it.** Retrying may genuinely help.\n- The 301 trap: browsers and search engines cache a 301 **hard**, sometimes forever. Ship one by mistake and users keep getting redirected long after you revert it. Use 302 for anything temporary."},{type:`intuition`,title:`The 4xx codes you will be asked about`,md:`- **400 Bad Request** — malformed: broken JSON, missing required field, wrong type.
- **401 Unauthorized** — badly named: it means *unauthenticated*. We do not know who you are. Send credentials.
- **403 Forbidden** — we know exactly who you are, and you still may not. Better credentials will not help.
- **404 Not Found** — no such resource. Also the polite way to hide the existence of something you are not allowed to see.
- **409 Conflict** — a state clash: duplicate email, editing a version someone else already changed.
- **422 Unprocessable Entity** — syntax fine, meaning wrong. FastAPI/Pydantic returns this for validation failures.
- **429 Too Many Requests** — rate limited. Obey the \`Retry-After\` header instead of hammering.`},{type:`note`,md:`And the 5xx four: **500** Internal Server Error — your code threw. **502** Bad Gateway — the proxy reached your app and got garbage back (or the app is dead). **503** Service Unavailable — the app is up but refusing work: overloaded, still booting, in maintenance. **504** Gateway Timeout — the proxy waited for your app and gave up. Reading a dashboard: a wall of 502/504 almost always means *your app died or got slow*, not that the load balancer broke.`},{type:`intuition`,title:`Four headers to know cold`,md:"- `Content-Type: application/json` — what format the body is in. Wrong value, and the server parses nothing.\n- `Authorization: Bearer eyJhbGciOi...` — who you are. The standard home for JWTs and API keys.\n- `Cache-Control: max-age=60` or `no-store` — how long anyone may reuse this response. The cheapest performance win in web engineering.\n- `Cookie` (client to server) and `Set-Cookie` (server to client) — the browser's automatic little suitcase of state.\n- Cookies vs the `Authorization` header in one line: cookies are sent **automatically** by the browser (great for web apps — and exactly why CSRF exists), headers are sent **deliberately** by your code (great for APIs and mobile). Full deep dive lives in the auth module."},{type:`note`,md:`**JSON** won the wire because it is human-readable, maps straight onto dicts/objects in every language, and needs no schema file to get started. What it costs: it is text (bigger and slower to parse than binary formats), it has no date type (everyone ships ISO-8601 strings and hopes), and no integer-vs-float distinction beyond "number". Fine for 99% of APIs. When you are doing 100k internal calls a second, that is when gRPC/Protobuf earns its extra complexity.`},{type:`intuition`,title:`What an API actually is`,md:`An **API** (Application Programming Interface) is a contract: *"send me this shape, and I will send you back that shape."* A web API is that contract spoken over HTTP.

- The UI is the capability exposed for humans; the API is the same capability exposed for programs.
- The contract *is* the product: URLs, methods, request and response shapes, status codes, error format.
- Break the contract and every client breaks at once — which is the entire reason versioning (\`/v1/\`) exists.`},{type:`intuition`,title:`REST in five ideas`,md:`**REST** is a style, not a library: model your system as **resources**, and use HTTP the way it was designed instead of inventing your own verbs.

- **Resources are nouns.** \`/users/42/orders\` — never \`/getUserOrders\`. Plural nouns, IDs in the path.
- **HTTP methods are the verbs.** GET reads it, POST creates one, PUT/PATCH updates it, DELETE removes it. The URL does not change shape when the action does.
- **Stateless** — every request carries everything needed to serve it (token, IDs, params). The server remembers nothing about you between requests.
- **Uniform interface** — same auth header, same error shape, same status meanings everywhere. A new endpoint should need zero new explanation.
- **HATEOAS** — responses link to what you can do next. The honest version: it is the least-implemented idea in REST, common in textbooks and almost absent from production. Know the word; do not build your answer on it.`},{type:`note`,md:`Statelessness is the one REST principle with a direct systems payoff. If server A remembers nothing about you, request #2 can be served by server B — so you scale by **adding boxes behind a load balancer** (horizontal scaling), and a box dying costs nobody their session. The state still has to live *somewhere*, so you push it out: either the client carries it (a signed token) or a shared store every box can read (Redis, Postgres). This is exactly why JWTs are attractive, and why "sticky sessions" are treated as a smell.`},{type:`note`,md:`One note on versions. **HTTP/1.1** handles one request at a time per connection: request #2 waits for #1 to finish — **head-of-line blocking**. That is why browsers open ~6 connections per domain and why we used to bundle every script into one file. **HTTP/2** fixes it with **multiplexing**: many requests interleaved as frames on a single connection, plus header compression. The catch worth saying out loud: HTTP/2 removes head-of-line blocking at the HTTP layer but not at the TCP layer — one lost packet still stalls every stream on that connection. Solving *that* is why HTTP/3 runs over QUIC/UDP.`}],quiz:[{question:`Your API logs 5 ms server time, but users report ~400 ms on their first click. Where did the time go?`,options:[{text:`DNS lookup + TCP handshake + TLS handshake + network transit`,explanation:`Correct. A cold request pays DNS (~20-120 ms), 1 RTT for TCP, 1 more for TLS, plus transit — all before your handler runs.`},{text:`JSON parsing on the client`,explanation:`Parsing a few KB of JSON is sub-millisecond. It is never the 400 ms.`},{text:`The server timing is wrong`,explanation:`Server timing measures handler time only, honestly. It just does not include anything that happens before the request arrives.`},{text:`The database is slow`,explanation:`A slow DB would show up INSIDE the 5 ms server time. This gap is outside the server entirely.`}],correct:0},{question:`A request times out with no response. Which one is safe for the client to retry automatically?`,options:[{text:`POST /orders`,explanation:`Not idempotent — the first one may have succeeded, so a retry can create a second order.`},{text:`PUT /users/42 with the full user object`,explanation:`Correct. PUT replaces the resource with a fixed value, so doing it twice leaves exactly the same state as once.`},{text:`POST /payments`,explanation:`The worst case of all — a retry can double-charge. It needs an idempotency key to be retry-safe.`},{text:`PATCH /counters/7 with "increment by 1"`,explanation:`That patch is not idempotent: two applications add 2. PATCH is only idempotent if the patch itself is.`}],correct:1},{question:`A user with a perfectly valid token requests someone else’s invoice. Which status?`,options:[{text:`401 Unauthorized`,explanation:`401 means unauthenticated — we do not know who you are. Here we do know, and the token is fine.`},{text:`403 Forbidden (or 404 if you want to hide that it exists)`,explanation:`Correct. Identity is established, permission is not. 404 is the accepted alternative when even revealing existence leaks information.`},{text:`400 Bad Request`,explanation:`Nothing is malformed — the request is well-formed and understood, just not permitted.`},{text:`500 Internal Server Error`,explanation:`Nothing broke on the server. Returning 5xx for a permission denial makes your error dashboards lie.`}],correct:1},{question:`You are moving /old-report to /new-report for one week of maintenance, then moving it back. Which redirect?`,options:[{text:`301 Moved Permanently`,explanation:`Dangerous here: browsers and search engines cache 301 aggressively, so users keep getting redirected long after you revert.`},{text:`302 Found`,explanation:`Correct. 302 says "temporary, do not cache the move" — exactly right for a week-long detour.`},{text:`200 OK with a JavaScript redirect`,explanation:`Works in a browser but is invisible to API clients, crawlers and curl. Use the protocol feature that exists.`},{text:`404 Not Found`,explanation:`The resource still exists, just elsewhere. 404 throws away every inbound link.`}],correct:1},{question:`You repointed your domain to a new server IP, but some users still land on the old box hours later. Why?`,options:[{text:`TCP caches the old IP inside the socket`,explanation:`TCP holds no name-to-IP memory; it only ever sees an IP it was handed.`},{text:`Resolvers and clients cached the old DNS answer until its TTL expires`,explanation:`Correct. Every DNS answer ships with a TTL, and everyone caches until it runs out. Lowering TTL before a migration is the standard fix.`},{text:`The browser cached the HTML page`,explanation:`A cached page would explain stale content, not connections reaching an entirely different server.`},{text:`HTTPS certificates pin the IP address`,explanation:`Certificates bind to domain names, never to IP addresses.`}],correct:1},{question:`Why does REST insist every request be self-contained (stateless)?`,options:[{text:`So any server behind the load balancer can serve any request — which is what makes horizontal scaling work`,explanation:`Correct. No server-side memory of you means request #2 can hit a different box, and a dying box costs nobody their session.`},{text:`Because it makes requests smaller`,explanation:`The opposite: every request must re-send the token and context, so requests get BIGGER. That is the price you pay for the scaling win.`},{text:`Because TCP cannot maintain state`,explanation:`TCP is very much stateful — it holds a connection with sequence numbers. Statelessness here is an application-layer choice.`},{text:`To make responses cacheable`,explanation:`Cacheability is a separate REST constraint driven by Cache-Control, not by statelessness.`}],correct:0},{question:`HTTP/2 multiplexing removes head-of-line blocking…`,options:[{text:`completely, at every layer`,explanation:`Overclaiming — and interviewers probe exactly this. One layer still blocks.`},{text:`at the HTTP layer only — a lost TCP packet still stalls every stream on that connection`,explanation:`Correct. Frames interleave beautifully until TCP has to wait for a retransmit, which is precisely why HTTP/3 moved to QUIC over UDP.`},{text:`by opening more TCP connections per domain`,explanation:`That is the HTTP/1.1 workaround (~6 connections per domain). HTTP/2 goes the other way: one connection, many streams.`},{text:`only when TLS is disabled`,explanation:`Backwards — in practice every browser HTTP/2 implementation requires TLS.`}],correct:1},{question:`DELETE /items/9 succeeds and there is nothing meaningful to send back. Best status code?`,options:[{text:`200 OK with an empty body`,explanation:`Not wrong, but imprecise — 200 promises a representation you are not sending.`},{text:`204 No Content`,explanation:`Correct. 204 means "it worked, and there is deliberately no body" — the textbook DELETE reply.`},{text:`404 Not Found`,explanation:`The delete succeeded. Returning 404 tells the client its request failed.`},{text:`201 Created`,explanation:`201 announces a NEW resource exists, which is the opposite of what just happened.`}],correct:1}],interviewQuestions:[{question:`What happens when I type google.com into a browser and press Enter?`,answer:`Narrate the hops and name the cost of each. (1) Browser checks its caches — memory, disk, and the OS/browser DNS cache; a hit ends it. (2) DNS: the recursive resolver walks root, then the .com TLD, then the authoritative server, returning an IP with a TTL that everyone caches. (3) TCP handshake to that IP on port 443 — SYN, SYN-ACK, ACK, one full RTT before any data. (4) TLS handshake — one more RTT in TLS 1.3, buying encryption, integrity and server identity. (5) The HTTP request goes out: request line, headers, optional body; the server routes it, hits its DB or cache, and returns a status line, headers and body. (6) The browser parses HTML, discovers sub-resources, and repeats for each. The senior move is attaching approximate latencies and finishing with: on a cold request the server itself is often the smallest slice.`,isCaseBased:!1},{question:`Define safe and idempotent. Which methods are which, and why does the distinction earn its keep?`,answer:`Safe = the request does not change server state (GET, HEAD). Idempotent = performing it N times leaves the same state as performing it once (GET, PUT, DELETE — and HEAD). POST is neither; PATCH is idempotent only if the patch itself is (set field X = 5 yes, increment X by 1 no). It matters because networks drop responses, not only requests: if a client cannot tell whether its call landed, idempotency is the only thing making a retry safe. It is why HTTP clients, proxies and load balancers auto-retry GET/PUT/DELETE and refuse to retry POST.`,isCaseBased:!0},{question:`Case: p99 latency of your API tripled after a deploy, but your server-side timing metric is unchanged. Where do you look?`,answer:`Server timing measures only the handler, so the extra time is outside it. Work the path inward: (1) TLS/connection churn — did the deploy break keep-alive, or does a new client library open a fresh connection per request? Every request now pays TCP + TLS round trips. (2) DNS — a new hostname or a very low TTL means resolution on the hot path; or the resolver got slow. (3) Queueing before the handler — fewer workers, a smaller thread/connection pool, or slow request-body reads mean requests wait in the accept queue, invisible to handler timing. (4) Response size — a new field ballooned the payload, so transfer time grew, especially on mobile. (5) The proxy layer itself — check load balancer and gateway latency separately from app latency. Diagnostic tools to name: curl -w timing breakdown (dns, connect, tls, ttfb), comparing p99 at the LB vs the app, and connection-reuse counters.`,isCaseBased:!0},{question:`401 vs 403 vs 404 — when do you send each, and is there a security angle?`,answer:`401 means unauthenticated: no credentials, or expired/invalid ones — the fix is to send valid credentials, and the response should carry a WWW-Authenticate hint. 403 means authenticated but not permitted — better credentials will not help. 404 means no such resource. The security angle: 403 leaks existence. Telling an attacker "this invoice exists, you just cannot see it" is itself information, so for sensitive resources many APIs return 404 for both missing and forbidden. Consistency matters more than the exact choice — pick one policy and apply it everywhere so timing and status codes do not become an enumeration oracle.`,isCaseBased:!1},{question:`Case: design the REST endpoints for playlists that contain songs, including reordering.`,answer:`Resources as nouns, methods as verbs. GET /playlists (list, paginated), POST /playlists (201 + Location), GET /playlists/{id}, PUT /playlists/{id} (full replace) or PATCH for partial, DELETE /playlists/{id} (204). Songs are a sub-resource: GET /playlists/{id}/tracks, POST /playlists/{id}/tracks to add (201 + Location), DELETE /playlists/{id}/tracks/{trackId} (204). Reordering is the interesting part, because "reorder" is a verb and REST has no verb for it — two honest options: PUT /playlists/{id}/tracks with the full ordered list (idempotent, simple, fine up to a few hundred items), or PATCH the position field of a single track. Say the tradeoff out loud: full-list PUT is idempotent and safe to retry but sends everything; single-item PATCH is small but needs conflict handling (409) when two clients reorder concurrently. Round it out with pagination, a versioned prefix /v1, and a consistent error shape.`,isCaseBased:!0},{question:`What does statelessness actually buy you, and where does the state go instead?`,answer:`It buys interchangeable servers: since no box holds your session, any box can serve any request, so you scale horizontally behind a load balancer, deploy by rolling boxes, and lose nothing when one dies. The state does not vanish, it relocates — either onto the client as a signed token (JWT: no server lookup, but revocation is hard and the token is stale until it expires), or into a shared store like Redis or Postgres that every box reads (instant revocation, one extra network hop, and now that store is a dependency). The anti-pattern to name is sticky sessions: pinning a user to one server. It works, but it ruins even load distribution and turns one crash into logged-out users.`,isCaseBased:!1},{question:`Why does HTTPS cost an extra round trip, and is there ever a case for plain HTTP?`,answer:`After TCP is established, TLS negotiates the cipher suite, sends the certificate chain and does the key exchange — one RTT in TLS 1.3 (two in 1.2), before any HTTP byte moves. It buys encryption, tamper-detection, and identity via a certificate signed by a trusted CA. Modern mitigations worth naming: session resumption and 0-RTT skip the handshake for returning clients, and connection reuse amortises it across many requests. The honest answer on skipping it: essentially never on the public internet — browsers penalise it, and any intermediary can read or rewrite the traffic. Inside a trusted network some teams terminate TLS at the load balancer and speak plain HTTP internally for latency, which is a deliberate tradeoff against the zero-trust position that even internal hops should be encrypted.`,isCaseBased:!1},{question:`Case: your mobile client retries any request that times out. Support reports duplicate charges. Fix it.`,answer:`Root cause: POST is not idempotent, and the timeout means the client cannot know whether the first attempt landed — a lost response looks identical to a lost request. Fix: idempotency keys. The client generates a UUID per logical payment attempt and sends it as an Idempotency-Key header; the server stores key plus the resulting response in a table with a unique constraint, and on a repeat of the same key returns the stored response instead of charging again. Details that make the answer senior: the uniqueness must be enforced by the database, not an in-memory check, or concurrent retries race past it; keys need a TTL (24h is common); the key must cover the attempt, not the session, so a genuine second purchase gets a new key; and the client should back off exponentially rather than retry immediately. Also correct the retry policy itself — blanket retries on every method are wrong.`,isCaseBased:!0},{question:`When is it legitimate to use POST for something that only reads data?`,answer:`When the query itself is too big or too sensitive for a URL. URLs have practical length limits (~2k characters through many proxies), they land in browser history, server access logs and Referer headers, and they are cached by intermediaries. So a search with a large structured filter, or a lookup whose parameters are personal data, is commonly sent as POST /searches with a JSON body. The costs to state plainly: you lose caching, you lose bookmarkability, and you lose the retry-safety that GET gives you for free. The compromise many APIs make is POST for the search, returning a result id that is then fetched with a normal cacheable GET.`,isCaseBased:!1},{question:`What is the difference between "an API" and "a REST API"? Must REST mean JSON?`,answer:`An API is any programmatic contract — a library function signature is an API. A web API is that contract over HTTP. REST is one architectural style for web APIs: resources identified by URLs, HTTP methods as the operations, statelessness, a uniform interface, cacheability, and (in theory) HATEOAS. JSON is not part of REST at all — REST is format-agnostic; XML, protobuf or HTML are all valid representations, and content negotiation via the Accept header is the REST-native way to choose. JSON simply won on ergonomics. Also worth naming the alternatives so it sounds like a choice rather than a default: gRPC for high-throughput internal service calls, GraphQL when clients need to shape their own responses and over-fetching is the real problem.`,isCaseBased:!1},{question:`Explain DNS TTL, and plan a migration to a new IP with zero user-visible breakage.`,answer:`TTL is the number of seconds a DNS answer may be cached by resolvers and clients; until it expires, nobody re-asks, so a record change is invisible to them. Plan: (1) days ahead, lower the record TTL to 60 seconds and wait for the OLD TTL to fully expire so everyone has picked up the short one; (2) bring the new server up and keep the old one serving in parallel; (3) change the record; (4) keep the old box alive for well past the TTL — some resolvers and JVM clients ignore TTLs and cache far longer, so plan for stragglers; (5) watch traffic on the old box drain to zero before decommissioning; (6) restore the normal TTL afterwards, since a permanently low TTL means more lookups on the hot path. The honest alternative worth mentioning: keep the DNS name pointing at a load balancer and change what sits behind it — then DNS never has to change at all.`,isCaseBased:!1},{question:`HTTP/1.1 vs HTTP/2 vs HTTP/3 in sixty seconds.`,answer:`HTTP/1.1: text protocol, one request in flight per connection, so request #2 waits for #1 — head-of-line blocking. Workarounds defined an era: ~6 parallel connections per domain, sprite sheets, bundling, domain sharding. HTTP/2: binary framing with multiplexing, so many requests interleave as streams over one connection, plus HPACK header compression and server push (since largely abandoned). Its limit: streams share one TCP connection, so a single lost packet stalls all of them — head-of-line blocking moved down to the transport layer. HTTP/3: runs over QUIC on UDP, where each stream has independent loss recovery, so a lost packet only stalls its own stream; it also folds the TLS handshake into the transport for faster connection setup. Closing line: the semantics — methods, status codes, headers — are identical across all three. Only the wire changed.`,isCaseBased:!1}],flashcards:[{front:`The six hops of one request`,back:`Browser cache -> DNS -> TCP handshake -> TLS -> HTTP request/response -> render.`},{front:`DNS resolution path`,back:`Recursive resolver asks root -> .com TLD -> authoritative server. Answer is cached everywhere until its TTL expires.`},{front:`Why TCP costs 1 RTT before data`,back:`SYN, SYN-ACK, ACK must complete first. Reuse connections (keep-alive, pools) so you pay it once, not per request.`},{front:`What TLS buys, and its price`,back:`Encryption, integrity, server identity — for one extra round trip in TLS 1.3 (two in 1.2).`},{front:`Shape of an HTTP message`,back:`Request line (method, path, version) or status line, then headers, then a blank line, then the optional body.`},{front:`Safe vs idempotent`,back:`Safe = changes nothing (GET). Idempotent = N times equals once (GET, PUT, DELETE). POST is neither; PATCH usually is not.`},{front:`Why idempotency decides retries`,back:`A lost RESPONSE looks like a lost request. Only idempotent calls are safe to retry blindly — POST needs an idempotency key.`},{front:`401 vs 403 vs 404`,back:`401 = we do not know who you are. 403 = we do, and you still may not. 404 = does not exist (or is hidden from you).`},{front:`301 vs 302`,back:`301 permanent — cached hard by browsers and crawlers, painful to undo. 302 temporary — use it for anything you will revert.`},{front:`Statelessness -> horizontal scaling`,back:`No per-server session means any box serves any request, so you scale by adding boxes. State moves to a token or a shared store.`}],mindmapMarkdown:`- How the Internet Works: One Request, End to End
  - The journey
    - Cache -> DNS -> TCP -> TLS -> HTTP -> render
    - Most of the clock is spent before the server
  - DNS
    - Recursive resolver: root -> .com -> authoritative
    - TTL caching; drop TTL before a migration
  - TCP and TLS
    - SYN / SYN-ACK / ACK = 1 RTT of pure setup
    - TLS = +1 RTT: encryption, integrity, identity
    - Reuse connections to pay the handshake once
  - HTTP anatomy
    - Request line: method, path, version
    - Headers, blank line, body
    - Content-Type, Authorization, Cache-Control, Cookie/Set-Cookie
  - Methods
    - GET safe + idempotent; POST neither
    - PUT and DELETE idempotent; PATCH usually not
    - Retry safety and idempotency keys
  - Status codes
    - 2xx: 200 / 201 + Location / 204
    - 3xx: 301 permanent vs 302 temporary
    - 4xx: 400 401 403 404 409 422 429
    - 5xx: 500 502 503 504
  - REST
    - Resources are nouns, methods are the verbs
    - Stateless -> horizontal scaling
    - Uniform interface; HATEOAS rarely built
  - Wire and versions
    - JSON: readable text, no date type
    - HTTP/1.1 head-of-line vs HTTP/2 multiplexing`};export{e as default};