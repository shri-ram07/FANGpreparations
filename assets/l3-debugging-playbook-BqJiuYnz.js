var e={id:`dl-l3-debugging-playbook`,subjectId:`dl`,level:3,title:`The Debugging Playbook: When Training Goes Wrong`,whyItMatters:`A broken training run shows you a symptom, not a cause. This is the symptom-first tree — six failures, what produces each, and the cheapest printed number that tells them apart.`,assumes:[`You have trained a model, or read *PyTorch Fundamentals*`,`You know what cross-entropy is`],estMinutes:20,sections:[{type:`intuition`,title:`Two checks before anything else`,md:`Almost every debugging session that goes badly is one where someone started reading code. Start with two measurements instead.

**Check the initial loss.** Before a single step, a correctly wired classifier predicts uniformly over C classes, so cross-entropy should read **ln(C)**. If it does not, something is wrong before training even begins — and that eliminates most of the search space in one print.

**Overfit one batch.** Take eight examples and train until the loss is near zero. If a model cannot memorise eight examples, the problem is a bug, not the data, the learning rate or the architecture. If it can, those become the suspects.

Both are seconds of work and both partition the problem.`},{type:`code`,lang:`python`,title:`What the loss should say before you train`,code:`import math
for c in [2, 10, 100, 1000, 50257]:
    print('%6d classes -> a fresh model should print %.4f' % (c, math.log(c)))

print()
for p in [0.5, 0.9, 0.95, 0.99]:
    print('majority class %.0f%% -> predicting it always scores %.1f%% accuracy'
          % (100*p, 100*p))

# ---- real output ----
#      2 classes -> a fresh model should print 0.6931
#     10 classes -> a fresh model should print 2.3026
#    100 classes -> a fresh model should print 4.6052
#   1000 classes -> a fresh model should print 6.9078
#  50257 classes -> a fresh model should print 10.8249
#
# majority class 50% -> predicting it always scores 50.0% accuracy
# majority class 90% -> predicting it always scores 90.0% accuracy
# majority class 95% -> predicting it always scores 95.0% accuracy
# majority class 99% -> predicting it always scores 99.0% accuracy`,annotations:{3:`ln(C) is what a uniform prediction costs. Much higher means the output layer is initialised with large weights or the labels are misaligned; much lower means information is leaking, or the classes are imbalanced and the bias was initialised to the prior.`,7:`10.8249 for a GPT-2-sized vocabulary. A language model whose first loss is not near 10.8 has a tokenizer or a target-shifting problem, and no amount of training will reveal which.`,13:`The other half: 90% accuracy is worthless if 90% of the data is one class. Always print the majority-class baseline alongside the metric, because "the model learned nothing" and "the model is excellent" produce the same number.`}},{type:`note`,label:`Symptom 1 — the loss is nan`,md:`Nan on **step one** is the forward pass, not the optimisation — nothing has had time to go wrong dynamically. Suspects: initial weights large enough to overflow, unnormalised inputs, or a log of zero, most often a softmax computed separately from cross-entropy instead of fused.

Nan after **many steps** is dynamics. Suspects, in order: learning rate too high, exploding gradients, or a division whose denominator drifted to zero.

The cheapest discriminator is to print the loss every step and note **when** it breaks, then print activation statistics per layer to find **where**. Gradient clipping is the immediate test for the exploding case — if clipping fixes it, you know what you are dealing with.`},{type:`note`,label:`Symptom 2 — the loss will not move`,md:"Run the overfit-one-batch test first, because it splits the causes cleanly.\n\n**If it cannot overfit eight examples**, it is a bug. Print gradient norms per layer: `None` or exactly zero means the graph is broken — a `.detach()`, a NumPy conversion, a `no_grad` block, or a plain tensor assigned where an `nn.Parameter` was needed. Also check that the optimizer actually received the parameters, and that inputs and labels are aligned rather than shuffled independently.\n\n**If it can overfit but real training is flat**, the bug is elsewhere: learning rate too small, dead ReLUs — count activations that are exactly zero — a saturating activation, or an input scale that makes the useful signal invisible.\n\nAnd check the learning rate by an order of magnitude in each direction before anything more elaborate. It is the single most common cause and the cheapest to rule out."},{type:`note`,label:`Symptoms 3 and 4 — the gap, and the oscillation`,md:`**Training loss falls, validation loss rises.** That is overfitting, and it is the one symptom whose name people reach for reflexively — so confirm it by the gap rather than the level. More data, then augmentation, then early stopping, then weight decay, then dropout, then a smaller model, roughly in that order of value.

But rule out the impostor first: a **validation set that differs from training** — a different preprocessing path, a leak, or a time-based split where the distribution shifted — produces the same curve and none of those fixes help.

**The loss oscillates without improving.** Learning rate too high is the first suspect: cut it by ten and see whether the oscillation resolves, which confirms the diagnosis in one run. If it does not, the batch size may be too small for the gradient to mean anything, or the data may be shuffled such that batches are not representative — a sorted dataset with \`shuffle=False\` produces exactly this.`},{type:`note`,label:`Symptoms 5 and 6 — one class, and the eval cliff`,md:`**The model predicts one class for everything.** Usually correct behaviour on imbalanced data: predicting the majority is genuinely the loss-minimising constant, and the model has found it. Compare against the majority-class baseline before concluding anything is broken. If the imbalance is real, the fixes are class weights, resampling, or focal loss. If the data is balanced and it still happens, look for a collapsed output layer or a learning rate that destroyed the weights early.

**It works in training and fails at evaluation.** The first suspect is \`model.eval()\` — without it, BatchNorm uses batch statistics rather than its frozen running averages and dropout keeps dropping. The second is a preprocessing mismatch: normalisation constants computed on training data but not applied at evaluation, or applied differently. The third is a genuine distribution shift, which is the only one of the three that is a modelling problem rather than a plumbing one.`},{type:`code`,lang:`python`,title:`The one-batch test, and reading what it says`,code:`import numpy as np
np.random.seed(0)
X = np.random.randn(8, 4)
y = (X[:, 0] > 0).astype(float)          # 8 examples, a learnable rule
w, b = np.zeros(4), 0.0

for step in range(1, 301):
    z = X @ w + b
    p = 1 / (1 + np.exp(-z))
    loss = -np.mean(y*np.log(p + 1e-12) + (1-y)*np.log(1-p + 1e-12))
    w -= 0.5 * (X.T @ (p - y)) / 8
    b -= 0.5 * (p - y).mean()
    if step in (1, 10, 50, 150, 300):
        print('step %3d  loss %.4f' % (step, loss))

# ---- real output ----
# step   1  loss 0.6931
# step  10  loss 0.1632
# step  50  loss 0.0588
# step 150  loss 0.0242
# step 300  loss 0.0130`,annotations:{11:`Step 1 prints 0.6931, which is ln(2) — the first check passing. If it printed 3.5 the model would be broken before training and no amount of stepping would reveal why.`,14:`Down to 0.0130 by step 300 and still falling. That is what "can overfit eight examples" looks like, and it means the plumbing is sound: gradients reach the parameters and the optimizer applies them.`,16:`If this line were still near 0.6931, the search would be over before it started — the problem would be a broken graph or a disconnected optimizer, and nothing about the data or the hyperparameters.`}}],quiz:[{question:`A fresh 1,000-class classifier prints an initial loss of 6.9078. What does that tell you?`,options:[{text:`The model has already learned something`,explanation:`It is exactly ln(1000) — a uniform prediction.`},{text:`The output layer is correctly wired and predicting uniformly, which eliminates most of the search space`,explanation:`Correct, and a value far from ln(C) would flag a problem before training begins.`},{text:`The learning rate is correct`,explanation:`The initial loss precedes any step.`},{text:`The data is imbalanced`,explanation:`Imbalance would show as a value below ln(C) if the bias were set to the prior.`}],correct:1},{question:`Your model cannot drive the loss below 0.69 on eight examples. What have you learned?`,options:[{text:`The learning rate is too low`,explanation:`Possible, but the test points somewhere more specific first.`},{text:`It is a bug — a model that cannot memorise eight examples has a broken graph, a disconnected optimizer or misaligned labels`,explanation:`Correct, and that eliminates the data, the architecture and the hyperparameters as suspects.`},{text:`The model is too small`,explanation:`Almost any model can memorise eight examples.`},{text:`You need more data`,explanation:`The opposite — the test deliberately uses almost none.`}],correct:1},{question:`Loss is nan on step one. Where do you look?`,options:[{text:`The learning rate`,explanation:`Nothing has been stepped yet — the forward pass is the suspect.`},{text:`The forward pass: initial weights large enough to overflow, unnormalised inputs, or a log of zero from an unfused softmax`,explanation:`Correct. Nan at step 50 is a different problem — dynamics.`},{text:`Exploding gradients`,explanation:`That is the many-steps case.`},{text:`The optimizer state`,explanation:`It has not been used yet.`}],correct:1},{question:`Training loss falls while validation loss rises. What must you rule out before treating it as overfitting?`,options:[{text:`Nothing — that curve is definitionally overfitting`,explanation:`A validation set that differs from training produces the identical curve.`},{text:`A validation set that differs from training — different preprocessing, a leak, or a time split where the distribution shifted`,explanation:`Correct, and none of the overfitting fixes help in that case.`},{text:`The learning rate`,explanation:`It does not produce a diverging gap.`},{text:`The batch size`,explanation:`Not a cause of this pattern.`}],correct:1},{question:`Your model reports 90% accuracy. When is that meaningless?`,options:[{text:`When the loss is still high`,explanation:`Loss and accuracy can disagree for calibration reasons without either being meaningless.`},{text:`When 90% of the data is one class — predicting it always scores exactly the same`,explanation:`Correct, which is why the majority-class baseline belongs next to every reported metric.`},{text:`When the test set is small`,explanation:`A real concern, but a different one.`},{text:`When there are more than two classes`,explanation:`Accuracy is well defined for any number.`}],correct:1},{question:`Everything works in training and falls apart at evaluation. First suspect?`,options:[{text:`The model overfits`,explanation:`Overfitting shows as a gap that grows during training, not a cliff at evaluation.`},{text:`model.eval() was not called, so BatchNorm uses batch statistics and dropout keeps dropping`,explanation:`Correct — it is a plumbing bug and it is silent.`},{text:`The test set is too hard`,explanation:`Check the plumbing before concluding that.`},{text:`The learning rate`,explanation:`Not applied at evaluation.`}],correct:1}],interviewQuestions:[{question:`Your model is not learning. Walk me through your process.`,answer:`Two measurements before I read any code. First, the initial loss: a correctly wired classifier over C classes should print ln(C) — 2.3026 for ten classes, 10.8249 for a GPT-2 vocabulary — and if it does not, something is wrong before training begins, which eliminates most of the search space in one print. Second, overfit a single batch of eight examples: if the model cannot memorise eight, the problem is a bug, and if it can, the data, the learning rate and the architecture become the suspects instead. Both take seconds and both partition the problem, which is why they come before anything more thoughtful.`,isCaseBased:!0},{question:`Why is the overfit-one-batch test so useful?`,answer:`Because it is a clean partition rather than a hint. Almost any model can memorise eight examples given enough steps, so failing means the plumbing is broken: a detached graph, an optimizer that never received the parameters, labels shuffled independently of inputs, a loss that is not connected to the outputs. Succeeding rules all of that out and points at data, hyperparameters or capacity. It is also fast — seconds, not an epoch — and it fails loudly rather than producing a plausible-looking but wrong number, which is the property that makes a debugging check worth running first.`,isCaseBased:!1},{question:`Loss is nan. How do you narrow it down?`,answer:`By when it happens, because that splits the causes. Nan on step one is the forward pass — nothing has been optimised yet — so the suspects are initial weights large enough to overflow, unnormalised inputs in the thousands, or a log of zero, most often from computing softmax separately instead of using a fused cross-entropy. Nan after many steps is dynamics: learning rate too high, exploding gradients, or a denominator drifting to zero. I would print the loss every step to find when, then activation statistics per layer to find where. Enabling gradient clipping is the cheap discriminator — if it fixes it, the diagnosis is confirmed and I know it is a symptom treatment rather than the cause.`,isCaseBased:!0},{question:`Training loss falls but validation loss rises. Is that always overfitting?`,answer:`No, and treating it as such is a common way to waste a week. A validation set that differs from training produces the identical curve: a different preprocessing path, a leak in one direction, or a time-based split where the underlying distribution shifted — and none of the overfitting remedies help. So I would first confirm the two sets are drawn and processed the same way, and check whether validation examples look like training ones. Once that is ruled out, the remedies in order of value are more data, augmentation, early stopping, weight decay, dropout, and a smaller model last, since a large model with good regularisation usually beats a small one without.`,isCaseBased:!0},{question:`Your model predicts one class for everything. What is happening?`,answer:`Usually it is correct behaviour rather than a bug. On imbalanced data, predicting the majority class genuinely is the loss-minimising constant, and the model has found it — so the first thing I would print is the majority-class baseline, because 90% accuracy on data that is 90% one class is exactly zero learning. If the imbalance is real, the fixes are class weights, resampling, or focal loss, and I would change the metric to something imbalance-aware, since accuracy is the thing that made this look acceptable in the first place. If the data is balanced and it still collapses, I would look for a learning rate that destroyed the weights in the first few steps, or an output layer stuck at a constant.`,isCaseBased:!0},{question:`How do you debug a model that trains fine and fails in production?`,answer:`Almost always a mismatch between the training pipeline and the serving one rather than a modelling failure. The specific things I would check: preprocessing applied identically, including normalisation constants computed on the training set and not recomputed at serving; feature ordering and dtype; and whether model.eval() is set, since BatchNorm and dropout silently behave differently. Then whether the production data distribution matches training — new categories, missing fields, a different time period. The general fix is structural: share the exact preprocessing code between training and serving rather than reimplementing it, and log the model's input distribution in production so drift is visible before it is a failure.`,isCaseBased:!0},{question:`What would you log during training, and why those things?`,answer:`Training and validation loss, obviously, but the diagnostic value is in the rest. Gradient norm per layer, because it distinguishes vanishing, exploding and a broken graph immediately. The learning rate as the schedule moves it, since schedule bugs are common and invisible otherwise. The fraction of activations that are exactly zero, which catches dying ReLUs. The weight-update-to-weight ratio, which should sit around 1e-3 and tells you the learning rate is in the right range without a sweep. And for classification, the prediction distribution across classes, which catches collapse to one class the moment it starts rather than at the end of the run.`,isCaseBased:!1},{question:`How do you know whether your model is actually learning anything?`,answer:`By comparing it to the baseline it has to beat, which is not zero. For classification that is the majority-class rate; for regression, predicting the mean; for a time series, predicting the last value, which is a shockingly strong baseline that many forecasting models fail to beat. Then a simple model — logistic regression or gradient-boosted trees on the same features — because if a neural network cannot beat those, the answer is usually that it should not be a neural network. I would also check a permutation test: shuffle the labels and retrain, and if performance does not collapse, there is a leak and the model is reading the answer from somewhere it should not be.`,isCaseBased:!1}],flashcards:[{front:`Check 1: the initial loss`,back:`A fresh C-class classifier should print ln(C): 0.6931 / 2.3026 / 4.6052 / 6.9078 / 10.8249. Wrong value = broken before training.`},{front:`Check 2: overfit one batch`,back:`Eight examples to near-zero loss. Cannot = a BUG (broken graph, disconnected optimizer, misaligned labels). Can = data/hyperparameters/capacity.`},{front:`The majority baseline`,back:`90% accuracy on 90%-one-class data is zero learning. Print the baseline next to every metric.`},{front:`nan: when, not what`,back:`Step one = the forward pass (init scale, unnormalised inputs, log of zero). Many steps = dynamics (LR, exploding gradients).`},{front:`Loss will not move`,back:`Gradient norms per layer. None or exactly zero = broken graph: detach, NumPy conversion, no_grad, or a plain tensor where nn.Parameter was needed.`},{front:`The overfitting impostor`,back:`A validation set that differs from training gives the identical curve. Rule out preprocessing mismatch, leaks and time-split drift first.`},{front:`Predicts one class`,back:`Usually CORRECT on imbalanced data — the majority is the loss-minimising constant. Compare to the baseline before calling it a bug.`},{front:`Works in training, fails at eval`,back:`model.eval() first (BatchNorm stats, dropout). Then preprocessing mismatch. Then genuine drift — the only one that is a modelling problem.`}],mindmapMarkdown:`- The debugging playbook
  - Before anything
    - initial loss should be ln(C)
      - 10 classes 2.3026, 50257 classes 10.8249
    - overfit 8 examples
      - cannot = a bug
      - can = data / hyperparameters / capacity
    - print the majority-class baseline
  - nan
    - step one -> forward pass: init scale, inputs, log(0)
    - many steps -> LR, exploding gradients
    - clipping is the cheap discriminator
  - Loss will not move
    - gradient norms per layer
    - None or zero = detach / numpy / no_grad / plain tensor
    - optimizer actually got the parameters?
    - inputs and labels aligned?
  - Train falls, val rises
    - rule out the IMPOSTOR first
      - preprocessing mismatch, leak, time-split drift
    - then: data, augmentation, early stop, decay, dropout
  - Oscillates
    - LR too high (cut by 10 to confirm)
    - batch too small, or shuffle=False on sorted data
  - Predicts one class
    - usually correct on imbalanced data
    - class weights, resampling, focal loss
  - Fails at eval only
    - model.eval() -> preprocessing -> real drift`};export{e as default};