var e={id:`backend-l3-serving-ml`,subjectId:`backend`,level:3,title:`Production Serving: Workers, Queues, Streaming & ML APIs`,whyItMatters:`"How would you serve a model at 1000 req/s" is a literal interview question — and it is unanswerable without this module: how many workers, where the queue goes, why batching changes the arithmetic. Everything here is also the API layer of your capstones: the streaming chat endpoint at the end of the mini-GPT and RAG projects is exactly this file.`,estMinutes:60,sections:[{type:`intuition`,title:`WSGI vs ASGI: can a worker put a request on hold?`,md:`A bank with one clerk per customer. The customer says "wait, I am calling my bank" — and the clerk stands frozen for two minutes.

- **WSGI** (Web Server Gateway Interface) is that clerk. Your app is a plain function: server calls it, it returns the whole response. One request owns one worker from start to finish.
- Flask and classic Django speak WSGI. It has served the web for twenty years and is completely fine for CPU-shaped work.
- **ASGI** (Asynchronous Server Gateway Interface) is a clerk who can say "hold on" and serve someone else. Your app is an \`async\` callable that can \`await\` mid-request.
- ASGI is also **message-based**: the response goes out as events (start, body chunk, body chunk…). WSGI has one return value.
- That second difference is the one people miss. Streaming tokens and WebSockets are not "faster WSGI" — WSGI has no vocabulary for them at all.
- FastAPI is built on Starlette, which is ASGI-native. Run it on a WSGI server and it does not boot.`},{type:`code`,lang:`python`,title:`The two contracts, side by side`,code:`# WSGI: sync in, sync out. This is the whole protocol.
def app(environ, start_response):
    start_response('200 OK', [('Content-Type', 'text/plain')])
    return [b'hello']              # worker is busy until this returns

# ASGI: async, message-based. Can await. Can send in pieces.
async def app(scope, receive, send):
    assert scope['type'] in ('http', 'websocket', 'lifespan')
    await send({'type': 'http.response.start', 'status': 200,
                'headers': [(b'content-type', b'text/plain')]})
    await send({'type': 'http.response.body', 'body': b'hello'})`,annotations:{2:`environ is a dict describing the request, start_response a callback for the status line. That is everything WSGI knows.`,4:`One request owns one worker start to finish. 200 slow requests need 200 workers — there is no "pause here" in this shape.`,7:`Three arguments, all async: scope (the connection), receive (incoming events), send (outgoing events).`,8:`'websocket' and 'lifespan' are connection types WSGI cannot express at all. That is the real gap — not speed.`,9:`The response leaves as EVENTS: start, then body chunks. Token streaming exists because of this line.`}},{type:`intuition`,title:`Gunicorn supervises, Uvicorn workers run`,md:`Uvicorn alone is one process with one event loop — one CPU core. Great in dev, wasteful on an 8-core box.

- **Gunicorn** is a process manager: it forks N worker processes, restarts one that crashes, and drains in-flight requests on deploy.
- Gunicorn speaks WSGI natively. The flag \`-k uvicorn.workers.UvicornWorker\` makes each child run a Uvicorn event loop instead.
- So the standard production shape: **Gunicorn manages the processes, Uvicorn workers run your ASGI app.**
- Two layers, two jobs: processes give you multi-core and crash recovery; the event loop inside each gives you thousands of concurrent waits.
- Honest alternative: \`uvicorn --workers 4\` forks by itself now. Fewer moving parts, slightly fewer knobs. Either answer is fine in an interview if you can say what the process manager is for.`},{type:`code`,lang:`bash`,title:`The production command`,code:`# put this in your Dockerfile CMD
gunicorn app.main:app \\
  -k uvicorn.workers.UvicornWorker \\
  -w 4 \\
  -b 0.0.0.0:8000 \\
  --timeout 60 \\
  --graceful-timeout 30 \\
  --max-requests 2000 --max-requests-jitter 200 \\
  --access-logfile -

nproc                       # cores this box reports
cat /sys/fs/cgroup/cpu.max  # the CPU limit the CONTAINER actually gets`,annotations:{2:`module:variable — "the object named app inside app/main.py". Same target string uvicorn takes.`,3:`The flag everyone forgets. Without it Gunicorn spawns WSGI workers and your FastAPI app refuses to start.`,4:`Worker COUNT. Each -w is a separate OS process: own memory, own event loop, own copy of anything you loaded.`,6:`Kill a worker whose request hung longer than this. Too low and big uploads die; too high and one stuck worker eats a slot.`,7:`On deploy, in-flight requests get 30 s to finish before SIGKILL. This flag is what makes a rollout zero-downtime.`,8:`Recycle each worker after ~2000 requests, jittered so they do not all restart at once. Cheap defence against slow leaks.`,12:`In Kubernetes nproc usually reports the NODE cores, not your limit. Sizing workers off it is a classic self-inflicted overload.`}},{type:`note`,md:"The `(2 × cores) + 1` heuristic exists because a worker is not busy 100% of the time — one waits on the database while another computes. It is a **starting point, not a law**: async workers hold hundreds of concurrent requests each (so you want fewer processes), and pure CPU work wants roughly one worker per core. Measure under real load, then tune. And the trap nobody warns you about: **`-w 4` means four processes, so a 2 GB model becomes 8 GB of RAM.** Fork shares pages copy-on-write at first, but Python writes refcounts into every object header and un-shares them within seconds."},{type:`note`,md:`What sits in front, in one breath: **nginx** (or a cloud load balancer) terminates TLS so your app never sees a certificate, serves static files without waking Python, and **buffers** slow clients — it reads the entire request from a phone on 3G and hands your worker a complete one, so a slow uploader cannot hold a worker hostage. It also load-balances across replicas and adds its own timeouts. One caveat that will bite you later in this module: buffering must be **off** for streaming routes, or nginx holds your tokens and delivers them in one lump.`},{type:`intuition`,title:`Task queues: the waiter does not cook`,md:`A waiter takes your order and walks away. He does not stand at the stove for eleven minutes. The ticket goes on a rail; cooks pull tickets.

- Web process: validate the request, push a **job**, return \`202 Accepted\` with a job id. Milliseconds.
- Worker process (Celery): pulls jobs off the queue and does the slow thing. Separate deployment, scaled separately, restartable without dropping traffic.
- The payoff sentence: **a queue decouples your p99 latency from how long the work takes.**
- What belongs on it: emails, PDF/report generation, video transcoding, re-embedding a document set, anything retryable, anything the response does not need.
- FastAPI's \`BackgroundTasks\` looks like the same thing but runs **inside the web process**: a crash or a redeploy loses the work, and there are no retries. Fine for fire-and-forget logging, wrong when losing the job matters.`},{type:`code`,lang:`python`,title:`Celery: enqueue in the route, work in the worker`,code:`from celery import Celery

celery = Celery(
    'jobs',
    broker='redis://redis:6379/0',     # the queue: pending jobs live here
    backend='redis://redis:6379/1',    # results/status, for polling clients
)

@celery.task(bind=True, max_retries=3, acks_late=True)
def send_invoice(self, order_id: str):
    if Invoice.exists(order_id):       # idempotency guard: safe to run twice
        return 'already sent'
    try:
        Invoice.create_and_email(order_id)
    except SMTPError as exc:
        raise self.retry(exc=exc, countdown=2 ** self.request.retries)

# the web side: validate, enqueue, answer
@app.post('/orders', status_code=202)
def create_order(body: OrderIn):
    order = save(body)
    send_invoice.delay(order.id)       # returns before the work starts
    return {'order_id': order.id, 'invoice': 'queued'}`,annotations:{5:`Broker = the queue itself. Redis is simple and fast; RabbitMQ adds real acknowledgements, routing and priorities.`,6:`The result backend is separate and OPTIONAL. Skip it when nobody polls for results — storing every return value is not free.`,9:`acks_late means acknowledge AFTER the task finishes, so a worker killed mid-job returns the job to the queue. It also guarantees you will one day run a task twice.`,11:`The guard that makes at-least-once delivery safe. Check-before-act on a unique key, or make the write itself an UPSERT.`,16:`Exponential backoff: 1 s, 2 s, 4 s. Retrying instantly just hammers a service that is already on fire.`,19:`202 Accepted, not 200 OK — the honest status code for "I took your request, the work is not done yet".`,22:`.delay() serializes the arguments and pushes them to Redis. The request returns without ever touching SMTP.`}},{type:`note`,md:'Vocabulary, once. The **broker** holds pending jobs — it *is* the queue. The **result backend** stores return values and status so a client can ask "is job 7 done?". Different jobs, different stores. When a task fails past `max_retries` it must land in a **dead-letter queue** — a parking lot you can inspect and replay — otherwise failures vanish silently and you find out from a customer. And the rule that makes all of this safe: **every task must be safe to run twice**, because at-least-once delivery means it eventually *will* be.'},{type:`visual`,component:`PointerBoxDiagram`,props:{title:`Request → queue → worker, including the bad day`,notice:`Same system, four situations. Watch where the work actually happens — and what the queue does when traffic outruns the workers.`,leftLabel:`incoming requests`,rightLabel:`queue + workers`,frames:[{note:`The web worker validates the request, drops a job on the queue and answers 202 in about 2 ms. It never does the work itself — it is a waiter, not a cook.`,stack:[{name:`POST /jobs (req 1)`,to:`j1`},{name:`POST /jobs (req 2)`,to:`j2`}],heap:[{id:`j1`,value:`job 1: predict`,label:`queued`},{id:`j2`,value:`job 2: predict`,label:`queued`},{id:`w1`,value:`worker A`,label:`idle`}]},{note:`Worker A pulls job 1 off the head and runs it for 400 ms. Meanwhile the web side keeps accepting: request 3 gets its 202 while job 1 is still cooking. Response latency is now independent of work duration.`,stack:[{name:`POST /jobs (req 3)`,to:`j3`}],heap:[{id:`j2`,value:`job 2: predict`,label:`queue head`},{id:`j3`,value:`job 3: predict`,label:`queued`},{id:`w1`,value:`worker A`,label:`running job 1`}]},{note:`Spike. Traffic hits 900 req/s but two workers only drain ~200/s. The queue grows — that is backpressure, made visible. Queue depth is your number one alert and your autoscaling signal: it says "add workers" long before users see an error. Cap the queue and shed load rather than let it grow forever.`,stack:[{name:`900 req/s inbound`,to:`q`,danger:!0}],heap:[{id:`q`,value:`jobs 4 … 340`,label:`BACKLOG`},{id:`w1`,value:`worker A`,label:`running job 2`},{id:`w2`,value:`worker B`,label:`running job 3`}]},{note:`Batching. The worker waits 10 ms, takes the four requests that arrived in that window, stacks them into one tensor and makes ONE GPU call. Four predictions for barely more than the price of one — each request donated 10 ms of latency to buy roughly 4× the throughput.`,stack:[{name:`reqs 41,42,43,44`,to:`b`}],heap:[{id:`b`,value:`batch of 4`,label:`fused`},{id:`gpu`,value:`one GPU call`,label:`38 ms total`}]}]}},{type:`intuition`,title:`WebSockets vs SSE: a phone call or a radio station`,md:"A phone call: both sides talk, the line stays open, and somebody has to handle it dropping. A radio subscription: the station talks, you listen, and if the signal dies your radio re-tunes by itself.\n\n- **WebSocket** = the phone call. Bidirectional and stateful. Starts as HTTP, then an `Upgrade` handshake switches to its own protocol.\n- Cost of that: it is no longer HTTP. Proxies, load balancers, auth middleware and HTTP caching all need special handling, sticky sessions become an issue, and reconnect logic is yours to write.\n- **SSE** (Server-Sent Events) = the radio. One-way server→client over an ordinary HTTP response that simply never ends. Content type `text/event-stream`, body is `data:` lines.\n- The browser's `EventSource` reconnects automatically and can resume from `Last-Event-ID`. You write no reconnect code.\n- **LLM token streaming is one-way**: you POST a prompt, the server sends tokens. That is SSE's exact shape — which is why the OpenAI-style APIs stream over SSE, not WebSockets.\n- Rule: **SSE when only the server talks. WebSocket when both sides talk continuously** — multiplayer, collaborative cursors, live audio."},{type:`code`,lang:`python`,title:`The WebSocket version, for when the client talks back`,code:`from fastapi import FastAPI, WebSocket, WebSocketDisconnect

app = FastAPI()

@app.websocket('/ws/chat')
async def chat(ws: WebSocket) -> None:
    await ws.accept()                       # completes the HTTP Upgrade
    history: list[str] = []
    try:
        while True:                         # the connection LIVES here
            prompt = await ws.receive_text()
            history.append(prompt)
            async for token in generate(prompt, history):
                await ws.send_text(token)   # server -> client
            await ws.send_json({'done': True, 'turns': len(history)})
    except WebSocketDisconnect:
        pass                                # client closed: normal, not an error`,annotations:{7:`accept() is mandatory — without it the handshake never completes and the client hangs.`,8:`State lives in the function scope for the connection's lifetime. Convenient, and exactly why WebSockets need sticky sessions: this memory exists on ONE worker.`,11:`The infinite loop is the point: one coroutine per live connection, parked at await most of the time. 10k idle connections cost memory, not CPU.`,16:`Disconnects are routine (tab closed, tunnel died). Catch it or your logs fill with tracebacks that mean nothing.`}},{type:`note`,md:"Compare the two snippets side by side: SSE is a normal HTTP response you happen to write slowly — every proxy, log, and auth middleware already understands it. The WebSocket needs `accept()`, its own disconnect handling, per-connection state on one worker, and sticky routing. Choose it only when the client genuinely needs to keep talking."},{type:`code`,lang:`python`,title:`A FastAPI streaming endpoint (SSE)`,code:`import json
from fastapi import FastAPI, Request
from fastapi.responses import StreamingResponse

app = FastAPI()

async def token_stream(prompt: str, request: Request):
    async for token in llm.generate(prompt):     # async generator
        if await request.is_disconnected():
            break                                # user left: stop the GPU
        yield f'data: {json.dumps({"t": token})}\\n\\n'
    yield 'data: [DONE]\\n\\n'

@app.post('/v1/chat/stream')
async def chat(body: ChatIn, request: Request):
    return StreamingResponse(
        token_stream(body.prompt, request),
        media_type='text/event-stream',
        headers={'Cache-Control': 'no-cache', 'X-Accel-Buffering': 'no'},
    )`,annotations:{8:`An async generator. Every yield is flushed to the client the instant it happens — the response starts long before the answer exists.`,9:`Without this check, a user who closed the tab leaves a GPU generating tokens nobody will ever read. Everyone forgets this line.`,11:`The SSE wire format: a line starting with "data: ", then a BLANK line. Two newlines, or the browser buffers your whole answer.`,12:`A sentinel so the client knows the stream finished normally rather than the connection dying.`,14:`POST rather than the EventSource GET, because a prompt wants a request body. Client side you read it with the fetch streaming API.`,19:`Tells nginx not to buffer this response. Skip it and tokens arrive in one lump in production while streaming perfectly on localhost.`}},{type:`note`,md:'The WebSocket version in FastAPI is `@app.websocket("/ws")` on an `async def ws(websocket: WebSocket)` that calls `await websocket.accept()` and then loops on `receive_text()` / `send_text()`. Reach for it only when the client must push mid-session. For a chat UI where the user sends one prompt and then only listens, SSE gives you the same experience with plain HTTP, free reconnects and no sticky-session problem.'},{type:`intuition`,title:`Serving a model: load it once, warm it up`,md:`Loading a model is unpacking an entire kitchen. You do not unpack it per customer.

- \`model = load(...)\` inside a route handler costs 2–30 seconds of disk read plus GPU transfer **on every request**. This is the number one mistake in ML serving.
- Load once at process start into a module-level object. FastAPI's **lifespan** handler is the correct hook: it runs after the worker boots, before it accepts traffic.
- Remember the worker maths: this happens **per process**. Four Gunicorn workers hold four copies of the weights. That is why GPU serving usually means *one* process per GPU with async concurrency inside it.
- **Warm it up.** The first inference is often 5–50× slower than the rest — CUDA context creation, lazy kernel compilation, cuDNN autotuning, JIT tracing. Run two or three dummy predictions at startup so the first real user does not pay for it.
- Gate your health check on it: \`/healthz\` must return 200 only *after* the model is loaded and warm, or the load balancer routes traffic into a cold worker.`},{type:`code`,lang:`python`,title:`Model at startup, validated input, versioned route`,code:`from contextlib import asynccontextmanager
from fastapi import FastAPI
from fastapi.concurrency import run_in_threadpool
from pydantic import BaseModel, Field

ml = {}                                  # ONE per process, not per request

@asynccontextmanager
async def lifespan(app: FastAPI):
    ml['model'] = load_model('sentiment-v3.pt')   # ~4 s, paid once
    for _ in range(3):
        ml['model'].predict(['warmup'])           # first call is the slow one
    ml['ready'] = True
    yield                                         # ---- serving traffic ----
    ml.clear()                                    # shutdown: free the GPU

app = FastAPI(lifespan=lifespan)

class PredictIn(BaseModel):
    text: str = Field(min_length=1, max_length=2000)

@app.get('/healthz')
def healthz():
    return {'ok': bool(ml.get('ready'))}

@app.post('/v1/predict')
async def predict(body: PredictIn):
    scores = await run_in_threadpool(ml['model'].predict, [body.text])
    return {'score': scores[0], 'model_version': 'sentiment-v3'}`,annotations:{6:`Module level, so it survives between requests. And note: every Gunicorn worker gets its own — -w 4 means four copies of the weights in RAM.`,10:`Loaded at boot, before the first request. Loading inside the route handler is the classic blunder: 4 seconds added to every call.`,12:`Warmup. The first inference builds the CUDA context and compiles kernels; it can be 5–50× slower than steady state.`,14:`Everything before yield is startup, everything after is shutdown. That shutdown half is what lets you release GPU memory cleanly on deploy.`,20:`Validation at the trust boundary. Without max_length, one 10 MB "text" is a GPU denial-of-service that costs the attacker nothing.`,22:`The load balancer must only send traffic once this says ok — otherwise the first users hit a worker with no model in it.`,28:`The model call is blocking C code. Running it directly in an async route freezes the event loop for every other request on this worker.`,29:`Always return the model version. When quality drops next month, this field is how you find out which weights did it.`}},{type:`intuition`,title:`Batching: the elevator rule`,md:`Running an elevator for one person per trip is legal and stupid. Wait ten seconds, carry eight.

- A GPU is thousands of cores that want a big matrix. One request is a 1×768 sliver — the hardware sits ~95% idle while paying full kernel-launch and memory-transfer overhead.
- **Dynamic batching**: hold arriving requests for a tiny window (5–20 ms) or until N pile up, run them as one tensor, split the results back to their callers.
- The numbers that make it obvious: batch 1 at 30 ms = 33 req/s. Batch 16 at 60 ms = 266 req/s. You paid 2× latency and bought 8× throughput.
- Say the tradeoff the way interviewers want it: **batching donates a bounded amount of p50 latency to buy a large amount of throughput.** The cap on the wait is what keeps p99 predictable.
- For LLMs the modern version is **continuous batching** (vLLM): new requests join the batch between token steps instead of waiting for the whole batch to finish.
- In production, do not hand-roll it if you can avoid it — Triton Inference Server, TorchServe and vLLM do this properly. But you must be able to explain the mechanism.`},{type:`code`,lang:`python`,title:`A micro-batcher in ~30 lines`,code:`import asyncio

MAX_BATCH, MAX_WAIT = 16, 0.010     # 16 items, or 10 ms — whichever comes first
q: asyncio.Queue = asyncio.Queue(maxsize=1000)

async def batcher():                # start as a task inside lifespan()
    loop = asyncio.get_running_loop()
    while True:
        text, fut = await q.get()   # idle here until the first request lands
        batch, futs = [text], [fut]
        deadline = loop.time() + MAX_WAIT
        while len(batch) < MAX_BATCH:
            left = deadline - loop.time()
            if left <= 0:
                break
            try:
                text, fut = await asyncio.wait_for(q.get(), left)
            except asyncio.TimeoutError:
                break
            batch.append(text)
            futs.append(fut)
        outs = await run_in_threadpool(model.predict, batch)   # ONE pass
        for f, out in zip(futs, outs):
            f.set_result(out)

@app.post('/v1/predict')
async def predict(body: PredictIn):
    fut = asyncio.get_running_loop().create_future()
    try:
        q.put_nowait((body.text, fut))
    except asyncio.QueueFull:
        raise HTTPException(503, 'overloaded')   # shed load, never queue forever
    return {'score': await asyncio.wait_for(fut, timeout=2.0)}`,annotations:{3:`The two knobs. MAX_WAIT is the latency you agree to donate; MAX_BATCH is what the GPU can swallow in one pass.`,6:`One long-lived task per process. Start it in the lifespan handler, right after the model finishes loading.`,9:`Nothing waits when traffic is thin — the batch window only opens once a first request exists. Idle servers stay fast.`,17:`This is the window. Keep collecting, but never past the deadline: a bounded wait is what keeps p99 bounded.`,22:`The whole point of the file. One forward pass for up to 16 inputs instead of 16 separate passes.`,24:`Each caller was parked on its own future; setting the result wakes exactly that one request.`,30:`put_nowait, not await put — when the queue is full the request must fail fast rather than join an infinite line.`,32:`Backpressure made explicit. At overload, 503 immediately. Silent unbounded queueing is how a spike becomes an outage.`}},{type:`note`,md:"Ship models like you ship code. Put the version **in the path** (`/v1/predict`) when the contract breaks — different input schema, different output shape — and put `model_version` in **every response body** always, so each logged prediction is traceable to a specific file. For rolling out new weights behind the same contract, route by header: `X-Model-Variant: canary` sends 1% of traffic to the new model while you compare accuracy and latency, then you flip the default. Two more production musts: a **timeout on every inference call** (a hung forward pass must fail fast, not hold a worker), and an explicit **503 when the queue is full** — shedding load is a feature."},{type:`intuition`,title:`The 1000 req/s question: do the arithmetic out loud`,md:`The interviewer will not accept "add more servers". They want a number and the reasoning that produced it.

1. **Measure one request.** Say sentiment inference is 40 ms on CPU — measured with a load test, not guessed. On GPU it is 8 ms at batch 1, and 4 ms per item at batch 16.
2. **Divide.** One CPU core at 40 ms serves 25 req/s. 1000 ÷ 25 = **40 cores**. That is five 8-core boxes for the model alone, and every worker process holds its own copy of the weights.
3. **Price the GPU alternative.** 4 ms per item at batch 16 = 250 req/s per GPU → four GPUs, or two with a bigger batch. Suddenly cheaper and simpler than 40 cores.
4. **State the batching cost.** Add ~10 ms of queue wait to ~60 ms of batched compute: p99 moves from roughly 70 ms to roughly 120 ms under burst. Ask whether the product can pay that. If the SLA is 50 ms, batching is off the table and you buy hardware instead.`},{type:`math`,intro:`The only formula you need — Little's Law, which turns "1000 req/s" into a worker count.`,latex:[`L = \\lambda \\times W \\quad\\Longrightarrow\\quad \\text{requests in flight} = \\text{req/s} \\times \\text{seconds per request}`,`1000 \\times 0.040 = 40 \\;\\text{in flight} \\;\\Rightarrow\\; 40 \\text{ CPU cores at one request per core}`,`\\text{batch } 16:\\quad \\frac{16}{0.060\\,\\text{s}} \\approx 266 \\text{ req/s per GPU} \\;\\Rightarrow\\; 4 \\text{ GPUs}`]},{type:`intuition`,title:`And then the part that separates seniors`,md:`The arithmetic gets you a number. These five points get you the offer.

- **Plan for the spike, not the average.** 1000 req/s average means 3000 at peak. Autoscale on **queue depth**, not CPU — CPU is already pinned by the time things are actually bad.
- **Bound the queue.** A hard cap plus a 503 turns a burst into slower service; an unbounded queue turns it into an out-of-memory kill and a full outage.
- **Cache.** Identical inputs are common (the same prompt, the same product id). A Redis read-through cache in front of the model can delete a large slice of your traffic before it costs a GPU cycle.
- **Make the model cheaper.** Quantization (int8), distillation, ONNX Runtime or TensorRT compilation routinely buy 2–4× before you buy any hardware.
- **Say the operational bits.** Load balancer with health checks, per-replica timeouts, canary rollout, and a dashboard on p50/p99 latency, queue depth and GPU utilization.
- The shape being graded: **measure → arithmetic → hardware choice → batching tradeoff → autoscaling and backpressure.**`},{type:`note`,md:`Where this lands in the plan: this module is the API layer of both capstones. The mini-GPT and the RAG system each end at a **streaming chat endpoint** — a model loaded once at startup, a queue in front of the GPU, and SSE carrying tokens out. Build the sentiment or image-classifier serving API first; the LLM one is the same skeleton with a longer generator.`}],quiz:[{question:`Why can FastAPI not run on a plain WSGI server?`,options:[{text:`WSGI has no way to await mid-request and no concept of WebSockets or streamed events`,explanation:`Correct. WSGI is one sync call returning one response — async routes, long-lived sockets and chunked event sends have no representation in it.`},{text:`WSGI cannot parse JSON bodies`,explanation:`WSGI hands you the raw body; parsing JSON is the framework's job and works fine under WSGI.`},{text:`WSGI is slower at HTTP parsing`,explanation:`Speed is not the barrier — the protocol shape is. A WSGI app literally cannot express a paused request.`}],correct:0},{question:"You run `gunicorn -w 4` with a service that loads a 2 GB model. Expected memory?",options:[{text:`About 2 GB — the workers share the model`,explanation:`Fork shares pages copy-on-write initially, but CPython writes refcounts into object headers and un-shares them almost immediately.`},{text:`About 8 GB — each worker process loads its own copy`,explanation:`Correct, and this is why GPU serving usually runs one process per GPU with async concurrency inside rather than many workers.`},{text:`About 2 GB — Gunicorn loads it once and passes a handle to children`,explanation:`Gunicorn manages processes; it has no idea what a model is and shares nothing deliberately.`}],correct:1},{question:`Which of these belongs on a Celery queue rather than inside the request?`,options:[{text:`Validating the request body`,explanation:`Validation must happen before you accept the job — it decides whether there is a job at all.`},{text:`Reading one row by primary key`,explanation:`A sub-millisecond read. Queueing it adds latency and moving parts for nothing.`},{text:`Generating a 40-page PDF report the user will download later`,explanation:`Correct: slow, retryable, and the response does not need it. Return 202 with a job id and let a worker build it.`}],correct:2},{question:`Why must every Celery task be safe to run twice?`,options:[{text:`Retries plus at-least-once delivery mean a task eventually WILL run twice`,explanation:`Correct. With acks_late a worker killed mid-job returns the job to the queue — the side effects from the first attempt already happened.`},{text:`Because Celery runs tasks in parallel`,explanation:`Parallelism across different jobs is not the issue; re-delivery of the SAME job is.`},{text:`Because Redis loses data on restart`,explanation:`That is a durability concern (real for Redis), but duplicates come from redelivery and retries, which happen even with a durable broker.`}],correct:0},{question:`Streaming LLM tokens to a browser chat UI. SSE or WebSocket?`,options:[{text:`WebSocket — only it can send many messages on one connection`,explanation:`SSE also sends many messages; it just cannot receive them. The traffic here is one-way, so the extra machinery buys nothing.`},{text:`SSE — the traffic is one-way, it stays plain HTTP, and the browser reconnects for free`,explanation:`Correct, and it is why OpenAI-style streaming APIs use SSE. Proxies, auth middleware and load balancers all keep working unchanged.`},{text:`Neither — poll every 200 ms for new tokens`,explanation:`Polling adds latency and wasted requests, and cannot deliver a token the instant it is generated.`}],correct:1},{question:`Streaming works perfectly on localhost, but in production the whole answer arrives at once. First suspect?`,options:[{text:`A buffering reverse proxy — turn off proxy buffering / send X-Accel-Buffering: no`,explanation:`Correct. nginx buffers responses by default; it happily holds every chunk until the generator finishes. Localhost has no proxy, hence the difference.`},{text:`The model generates too fast`,explanation:`A fast model would make chunks arrive quickly, not all at the end.`},{text:`A missing await in the route`,explanation:`A missing await usually breaks the response entirely rather than delivering a perfectly correct one late.`}],correct:0},{question:`Batch 1 takes 30 ms. Batch 16 takes 60 ms for the whole batch. What did you buy and what did you pay?`,options:[{text:`Both latency and throughput improved`,explanation:`Per-request latency got worse: 30 ms became 60 ms plus the batching wait. There is always a cost.`},{text:`Throughput went from ~33 to ~266 req/s (8×) while per-request latency roughly doubled`,explanation:`Correct. 16 ÷ 0.060 s ≈ 266 req/s. That is the batching trade in one line: bounded latency donated, large throughput bought.`},{text:`Batching always hurts latency for no throughput gain`,explanation:`It hurts latency, but the throughput gain is exactly why GPUs are batched everywhere in production.`}],correct:1},{question:`Where should a model be loaded in a FastAPI service?`,options:[{text:`Inside the route handler, so it is always the freshest file`,explanation:`That pays the multi-second load on every single request. It is the most common ML-serving mistake.`},{text:`In a lifespan/startup handler, once per process, followed by a few warmup predictions`,explanation:`Correct, and gate /healthz on it so the load balancer never routes traffic into a worker whose model is still loading.`},{text:`Lazily, on the first request that needs it`,explanation:`Better than every request, but that unlucky first user waits seconds — and under multiple workers several users pay it. Warm at startup instead.`}],correct:1}],interviewQuestions:[{question:`Explain WSGI vs ASGI, and why FastAPI requires ASGI.`,answer:`WSGI is a synchronous contract: the server calls your app function with an environ dict and a start_response callback, and you return the complete body. One request occupies one worker start to finish. ASGI is the async, message-based equivalent: your app is an async callable taking scope/receive/send, so it can await mid-request and can send the response as multiple events. FastAPI needs ASGI for two reasons, and the second is the one people forget: async route handlers need a loop to await on, and streamed responses plus WebSockets are protocol types WSGI cannot express at all — there is no "send another chunk" in WSGI, and no scope type for a socket that stays open.`,isCaseBased:!1},{question:`What does Gunicorn add over running uvicorn directly, and how many workers do you configure?`,answer:`Gunicorn is a process manager: it forks N workers, restarts a worker that crashes or hangs past --timeout, and drains in-flight requests on deploy via --graceful-timeout, which is what makes rollouts zero-downtime. With -k uvicorn.workers.UvicornWorker each child runs an ASGI event loop, so you get multi-core from the processes and high concurrency from the loop inside each. Worker count: start at (2 × cores) + 1 for ordinary I/O-bound APIs, then measure — that heuristic assumes a worker is idle part of the time. Async workers hold far more concurrency each so you want fewer; CPU-bound work wants about one per core; and anything holding a large model wants very few, because -w N means N copies of the weights in RAM. Also read the container CPU limit, not nproc, which reports node cores in Kubernetes.`,isCaseBased:!1},{question:`How would you serve a model at 1000 requests per second?`,answer:`I answer with arithmetic, not adjectives. (1) Measure one request under load: say 40 ms on CPU, 8 ms on GPU at batch 1, 4 ms per item at batch 16. (2) Divide: one core at 40 ms = 25 req/s, so 1000 req/s = 40 cores — five 8-core boxes, each process holding its own copy of the weights. Little's Law states it generally: in-flight = throughput × latency. (3) Compare hardware: batch 16 at 4 ms/item = ~266 req/s per GPU, so four GPUs, or two with a bigger batch — usually cheaper and simpler than 40 cores. (4) Name the batching cost: ~10 ms queue wait on top of ~60 ms batched compute pushes p99 from ~70 ms to ~120 ms; if the SLA is 50 ms, batching is off and I buy hardware. (5) Architecture: load-balanced replicas, model loaded once per process at startup with warmup, a bounded request queue with dynamic batching in front of the GPU, timeouts on every inference, and 503 when the queue is full. (6) Scaling and cost: autoscale on queue depth rather than CPU, provision for the 3× peak rather than the average, cache identical inputs in Redis, and try quantization/distillation/TensorRT for 2–4× before adding machines. Then I say what I would monitor: p50/p99, queue depth, GPU utilization, and batch size distribution.`,isCaseBased:!0},{question:`When do you use a task queue instead of doing the work in the request or in a background task?`,answer:`Three tests. Does the response need the result? If not, it is a candidate. Does it take longer than a request should live (roughly hundreds of milliseconds)? Then it must move. Does losing it matter? That is what separates a queue from FastAPI BackgroundTasks — BackgroundTasks runs inside the web process, so a crash or a redeploy silently drops the work and there are no retries, while a broker keeps the job until a worker acknowledges it. The queue also buys you separate scaling (workers and web scale independently), retries with backoff, and a visible depth metric you can alert and autoscale on. The cost is real: another broker to run, at-least-once semantics that force every task to be idempotent, and a job-status endpoint the client has to poll.`,isCaseBased:!1},{question:`Case: a customer received the same invoice email three times. The task is in Celery. Diagnose and fix.`,answer:`Cause: at-least-once delivery. With acks_late the job is acknowledged only after it completes, so a worker OOM-killed or redeployed mid-send returns the job to the queue and the whole task reruns — including the send that already happened. Retries on a transient SMTP error do the same. Fix at the task level: make it idempotent. Take a natural unique key (order_id), and either check-before-act inside a transaction or write a dedupe row with a unique constraint and let the insert fail on a duplicate. Better still, split it: mark invoice_sent atomically before the side effect, or use the provider's idempotency key so the SMTP/API side deduplicates for you. Then make failures visible: cap max_retries, route final failures to a dead-letter queue, and alert on it — otherwise you learn about problems from customers. The general rule to say out loud: exactly-once delivery does not exist; you get at-least-once delivery plus idempotent effects.`,isCaseBased:!0},{question:`SSE or WebSockets for streaming an LLM response? Defend the choice.`,answer:`SSE. The traffic is one-way — the client POSTs a prompt, the server sends tokens — and SSE is just an HTTP response that never ends, so every piece of existing infrastructure keeps working: reverse proxies, auth middleware, load balancers, CDNs, HTTP logging. The browser's EventSource reconnects automatically and can resume with Last-Event-ID, so I write no reconnect logic. WebSockets are bidirectional and stateful, which means an Upgrade handshake, sticky sessions, proxy configuration, and my own heartbeat and reconnect code — real cost for a capability I do not use here. I would switch to WebSockets when the client must push continuously mid-session: collaborative editing, live audio, multiplayer. The one production gotcha to mention either way: disable proxy buffering, or nginx delivers the whole "stream" in one lump.`,isCaseBased:!1},{question:`Case: p99 latency tripled right after you deployed a second model into the same FastAPI service. Debug it.`,answer:`First, confirm the shape: is p50 also up, or only p99? Only p99 means contention or queueing, not a uniformly slower path. Hypotheses in order. (1) The new model runs a blocking call inside an async route — it holds the event loop and every unrelated request on that worker waits behind it. Test: check whether the handler is async without run_in_threadpool; fix by offloading to a thread pool or a separate worker process. (2) Memory pressure — the second model pushed the process into swap, or on GPU the two models now contend for VRAM and the driver is serializing or thrashing. Check RSS/VRAM per worker. (3) Worker count is now wrong: same -w value, twice the memory per worker, so fewer effective cores and more context switching. (4) Warmup regression: if workers recycle (--max-requests), each recycled worker pays a cold first inference again, which shows up exactly as a p99 tail. Fix path: separate the two models into their own deployments so they scale and fail independently — that is usually the right answer, not tuning. I would confirm with per-route latency histograms rather than a single service-wide number.`,isCaseBased:!0},{question:`How do you roll out a new model version without downtime and without risking quality?`,answer:`Versioning first: the path carries the contract (/v1/predict → /v2/predict only when the input or output schema changes), and every response carries model_version so any logged prediction is traceable to specific weights. For the same contract with new weights, run both versions and route by header or a percentage — X-Model-Variant: canary sends 1% of traffic to the new model while I compare latency, error rate and, where I have labels or a proxy metric, quality. Because the model is loaded at startup, a rollout is just a normal rolling deploy with a readiness probe gated on load-plus-warmup, so no traffic hits a cold worker, and --graceful-timeout drains in-flight requests. Rollback is a config flip, not a rebuild. Shadow traffic is the safer variant when you cannot risk any user impact: send a copy of real requests to the new model and compare outputs offline without serving them.`,isCaseBased:!1},{question:`What is backpressure, and what do you do when the queue grows faster than workers drain it?`,answer:`Backpressure is the system telling upstream "slow down" instead of silently accepting work it cannot do. A growing queue is the signal — it is the earliest honest indicator of overload, well before CPU or error rate move, which is why queue depth is the metric to alert and autoscale on. Responses, in order: scale workers out if the work is parallel and the downstream (database, GPU) can take it; cap the queue and return 503 or 429 with Retry-After when it is full, so a burst degrades into slower service instead of an out-of-memory kill; shed low-priority traffic first if you have request classes; and set a deadline on every queued item so nothing is executed long after the client gave up. The failure mode to name: an unbounded in-memory queue converts a temporary spike into a permanent outage, because memory grows, the process is killed, and the in-flight work is lost.`,isCaseBased:!1},{question:`Why is the first inference so much slower than the rest, and what do you do about it?`,answer:`The first call pays one-time setup that never appears again: CUDA context creation, kernel compilation and cuDNN autotuning for the specific input shape, lazy weight transfer to device memory, JIT tracing or graph compilation, and cold page cache on the weights file. It is commonly 5–50× the steady-state latency. The fix is warmup: after loading the model in the lifespan handler, run a few dummy predictions at representative input shapes — note "shapes" plural, because shape-specialized kernels mean a new sequence length can trigger a fresh compile. Then gate the readiness probe on warmup finishing so the load balancer only sends traffic to warm workers. Watch it too: if you use --max-requests to recycle workers, every recycle re-pays the cold start, and that shows up as an unexplained p99 tail.`,isCaseBased:!1},{question:`Case: your streaming endpoint ships. Users report the answer appears all at once, and your GPU stays busy long after people close the tab. Find both bugs.`,answer:`Bug one is buffering, not your code: the reverse proxy holds the response until the generator finishes. Fix with proxy_buffering off for that location, or send X-Accel-Buffering: no plus Cache-Control: no-cache from the endpoint. The tell is that it streams correctly on localhost, where there is no proxy. If it still lumps, check for a middleware that reads or rewrites the response body (a compression or logging middleware will happily consume the whole stream). Bug two is the missing cancellation check: an SSE generator keeps generating after the client disconnects unless you test request.is_disconnected() (or catch the cancellation) and break. Every abandoned tab then holds a GPU slot generating tokens nobody reads, which at scale is a large fraction of your capacity. Add both, plus a hard token/time cap per request so a single stream cannot run forever.`,isCaseBased:!0},{question:`Explain dynamic batching precisely, and tell me when you would NOT batch.`,answer:`Dynamic batching holds incoming inference requests in a queue for a bounded window — say 10 ms, or until N have arrived, whichever comes first — stacks them into one tensor, runs a single forward pass, and returns each result to its own waiting caller. It works because a GPU is throughput hardware: a batch of 16 costs far less than 16× a batch of 1, since kernel-launch and memory-transfer overhead is amortized. The tradeoff, exactly: you donate up to the window in added latency to buy a multiple of throughput, and the window cap is what keeps p99 predictable. I would not batch when the latency budget is tight enough that even a 10 ms window breaks the SLA; when traffic is so thin that batches never fill and you are adding wait for nothing (a good batcher avoids this by opening the window only on the first arrival); when the model runs on CPU, where the batch win is much smaller; or when inputs have wildly different shapes so padding wastes more than batching saves. For LLMs specifically I would prefer continuous batching, where new requests join between token steps rather than waiting for the whole batch to finish.`,isCaseBased:!1}],flashcards:[{front:`WSGI vs ASGI in one line`,back:`WSGI: sync, one request owns one worker, one return value. ASGI: async and message-based — can await, sends the response in events, and can express WebSockets.`},{front:`The standard FastAPI production command`,back:`gunicorn app.main:app -k uvicorn.workers.UvicornWorker -w N — Gunicorn manages processes, Uvicorn workers run the ASGI loop.`},{front:`Worker count heuristic`,back:`(2 × cores) + 1 as a STARTING POINT for I/O-bound APIs, then measure. Async workers want fewer; CPU work wants ~1 per core.`},{front:`The -w N trap for ML services`,back:`N workers = N processes = N copies of the model in RAM. Fork's copy-on-write does not save you: refcount writes un-share the pages.`},{front:`Broker vs result backend`,back:`Broker (Redis/RabbitMQ) holds the pending jobs — it is the queue. The result backend stores return values/status for polling, and is optional.`},{front:`Why every queued task must be idempotent`,back:`Delivery is at-least-once: retries and acks_late redelivery mean the task WILL run twice. Guard with a unique key or an UPSERT; exactly-once does not exist.`},{front:`SSE vs WebSocket — the pick`,back:`SSE when only the server talks (LLM token streaming): plain HTTP, auto-reconnect, no sticky sessions. WebSocket when both sides push continuously.`},{front:`Streaming works locally, lumps in prod`,back:`A buffering reverse proxy. Disable proxy_buffering or send X-Accel-Buffering: no. Also check request.is_disconnected() so closed tabs stop the GPU.`},{front:`The model-loading rule`,back:`Load once per process in the lifespan/startup handler, run 2–3 warmup predictions (first inference is 5–50× slower), and gate /healthz on it.`},{front:`Batching, and the 1000 req/s recipe`,back:`Batching donates a bounded wait (10 ms) for a multiple of throughput. Recipe: measure one request → divide (req/s × latency = in-flight) → pick CPU/GPU/batch → autoscale on queue depth with a capped queue.`}],mindmapMarkdown:`- Production Serving
  - Server interface
    - WSGI: sync, one request per worker
    - ASGI: async, events, WebSockets — FastAPI needs it
  - Processes in front
    - Gunicorn manages, Uvicorn workers run
    - (2 × cores) + 1, then MEASURE
    - N workers = N model copies in RAM
    - nginx: TLS, static, buffering (off for SSE)
  - Task queues
    - Web enqueues, worker consumes
    - Broker vs result backend
    - Retries → idempotent tasks, dead-letter queue
    - BackgroundTasks loses work on restart
  - Streaming
    - SSE: one-way, plain HTTP, auto-reconnect
    - WebSocket: bidirectional, stateful
    - StreamingResponse + async generator
  - Serving ML
    - Load at startup (lifespan), never per request
    - Warmup: first inference is 5–50× slower
    - Pydantic caps the input size
    - /v1/predict, model_version, canary header, timeouts
  - Batching
    - Wait 10 ms, fuse into one GPU call
    - Latency donated, throughput multiplied
  - 1000 req/s
    - Measure one request first
    - in-flight = req/s × latency → 40 ms = 40 cores
    - GPU + batch 16 → ~266 req/s per GPU
    - Autoscale on queue depth, cap the queue`};export{e as default};