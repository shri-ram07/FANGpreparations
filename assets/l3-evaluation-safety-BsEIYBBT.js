var e={id:`genai-l3-evaluation-safety`,subjectId:`genai`,level:3,title:`Evaluating and Guarding an LLM System`,whyItMatters:`An evaluation set of 50 examples gives a 95% confidence interval of plus or minus 11 percentage points. Most reported LLM improvements are smaller than that, which means most reported improvements are noise — and the same discipline decides whether your guardrails work.`,assumes:[`You have read *RAG* and *Agents and Tool Use*`,`You know what precision and recall are`],estMinutes:20,sections:[{type:`intuition`,title:`The hard part is that there is no label`,md:`Classification has a right answer, so evaluation is counting. Generation does not: two different responses can both be correct, and the ones you care about — helpful, grounded, appropriately cautious — have no ground truth to compare against.

So evaluation moves to three imperfect instruments. **Automatic metrics** are cheap, deterministic and measure surface overlap, which correlates poorly with quality. **Model-as-judge** is far better correlated and has its own biases. **Human evaluation** is the reference and is slow and expensive.

The mistake that costs the most is not choosing the wrong instrument. It is **not building an evaluation set at all** and shipping on impressions — which is common, because it feels productive and the failure only shows up later.`},{type:`math`,intro:`The binomial confidence interval, which sizes every evaluation set you will ever build. The half-width falls as one over the square root of n, which is why going from 100 to 1000 examples buys only a factor of three.`,latex:[`CI_{95} = \\hat{p} \\pm 1.96 \\sqrt{\\frac{\\hat{p}(1 - \\hat{p})}{n}}`]},{type:`code`,lang:`python`,title:`How big an evaluation set has to be`,code:`import math
print('95% confidence interval half-width at an observed accuracy of 0.80:')
for n in [50, 100, 500, 1000, 5000]:
    se = math.sqrt(0.8 * 0.2 / n)
    print('  n=%5d -> +/- %.1f percentage points' % (n, 196 * se))

# ---- real output ----
# 95% confidence interval half-width at an observed accuracy of 0.80:
#   n=   50 -> +/- 11.1 percentage points
#   n=  100 -> +/-  7.8 percentage points
#   n=  500 -> +/-  3.5 percentage points
#   n= 1000 -> +/-  2.5 percentage points
#   n= 5000 -> +/-  1.1 percentage points`,annotations:{4:`1.96 standard errors, scaled to percentage points. The binomial standard error falls as 1/√n, which is why the returns are so punishing.`,8:`Fifty examples gives ±11.1 points. A change from 78% to 82% on such a set is indistinguishable from noise, and most reported prompt improvements are exactly that size.`,11:`To resolve a 2-point difference you need around 1,000 examples. That is the honest cost of claiming an improvement, and it is why paired testing on the same examples — where you compare per-item rather than in aggregate — is so much more efficient.`}},{type:`note`,label:`Model-as-judge, and the biases you must correct for`,md:`Using a strong model to score outputs is now the default for anything without a reference answer. It correlates with human judgement well enough to be useful, at a tiny fraction of the cost, and it is consistent in a way human annotators are not — human agreement on preference tasks is commonly around 70%.

Its biases are known and correctable, which is what makes it usable.

**Position bias.** Judges prefer the first option presented. Correct by running both orders and only counting a preference when it survives the swap.

**Length bias.** Judges prefer longer answers. Control by comparing lengths and being suspicious when the winner is systematically longer.

**Self-preference.** Models rate their own outputs higher, so do not judge a model with itself.

**Verbosity in the rubric.** A vague rubric produces vague scores. Ask for a specific judgement against explicit criteria — "does this answer follow from the provided context, yes or no" beats "rate this 1–10".

And validate the judge against human labels on a subset before trusting it. Without that, you have replaced an unmeasured system with an unmeasured measurement.`},{type:`note`,label:`What to actually build`,md:`**A golden set of real cases.** Fifty to a few hundred inputs drawn from actual usage, with expected outputs or acceptance criteria. Every production failure becomes a new case, so the set grows into a regression suite that encodes everything that has ever gone wrong.

**Break it down by what you can fix.** For RAG, retrieval recall and generation faithfulness separately, because they need different fixes. For agents, per-step accuracy and end-to-end completion, because 95% per step is 35.8% over 20 steps. An aggregate number tells you something is wrong and never what.

**Measure cost and latency alongside quality.** They are part of the product, and an improvement that triples the token count is a trade, not a win.

**Watch for contamination.** A public benchmark in the training data measures memorisation. Held-out and recently created evaluation sets are the only defence, and reported benchmark scores should be read with this in mind.`},{type:`note`,label:`Guardrails: layers, not a wall`,md:`**Input filtering** catches obvious abuse and injection attempts before the model sees them — cheap, and it stops the unsophisticated majority.

**System prompt constraints** set the intended behaviour. Necessary, and easily overridden by a determined user, so it is a first layer rather than a control.

**Output filtering** checks the response before it reaches the user: a classifier for harmful content, a faithfulness check against retrieved context, PII detection. This is where most real protection lives, because it inspects what actually happened rather than what was intended.

**Structural limits** are the ones that hold. Rate limits, tool permissions, human approval on consequential actions, and a hard step cap on agents. These do not depend on the model behaving, which is what makes them different in kind from everything above.

The honest position: **prompt injection is unsolved**, and no prompt wording reliably prevents instructions embedded in content the model reads. **Jailbreaks keep working** — roleplay framing, encoding, gradual escalation — because the model cannot reliably distinguish a legitimate request from a disguised one. So design for failure: least privilege, so that a successful jailbreak reaches the smallest possible blast radius.`},{type:`note`,label:`And measure the guardrails too`,md:`A guardrail is a classifier, so it has precision and recall and a threshold, and all three should be measured on real traffic rather than assumed.

**False positives are the cost nobody budgets for.** A safety filter that blocks 5% of legitimate requests is a product problem, and it will be discovered by users rather than by you. The base-rate arithmetic applies exactly as it does anywhere else: if 0.1% of requests are genuinely abusive, a 99%-accurate filter flags about ten legitimate requests for every real one.

**False negatives are what the filter exists to prevent**, so the threshold is a cost decision — what a missed harmful output costs against what a blocked legitimate one does — and it should be made explicitly rather than left at a default.

**Log everything and sample it.** Guardrail failures are found by looking, and a small routine review of flagged and unflagged traffic surfaces problems long before a complaint does.

**Red-team before shipping.** Have people actively try to break it, because the failure modes you imagine are not the ones users find.`}],quiz:[{question:`You improve from 78% to 82% on a 50-example evaluation set. What can you conclude?`,options:[{text:`A 4-point improvement`,explanation:`The confidence interval is ±11.1 points.`},{text:`Nothing — the change is well inside the noise`,explanation:`Correct, and most reported prompt improvements are exactly this size.`},{text:`The model got worse`,explanation:`Equally unsupported.`},{text:`You need a different metric`,explanation:`You need more examples.`}],correct:1},{question:`How many examples do you need to resolve a 2-point difference?`,options:[{text:`About 100`,explanation:`That gives ±7.8 points.`},{text:`About 1,000, which gives ±2.5 points`,explanation:`Correct, and paired per-item comparison on the same examples is much more efficient than comparing aggregates.`},{text:`About 200`,explanation:`Still around ±5.5 points.`},{text:`It depends only on the model`,explanation:`It depends on the binomial standard error, which falls as 1/√n.`}],correct:1},{question:`What is position bias in model-as-judge, and how do you correct it?`,options:[{text:`The judge favours the model it was trained on — use a different judge`,explanation:`That is self-preference bias.`},{text:`Judges prefer whichever option is presented first — run both orders and only count a preference that survives the swap`,explanation:`Correct, and it is one of the cheapest corrections available.`},{text:`The judge weights the start of the answer — truncate`,explanation:`Not the documented effect.`},{text:`It is not correctable`,explanation:`Order-swapping handles it directly.`}],correct:1},{question:`Why should a RAG system be evaluated in two separate numbers?`,options:[{text:`To report more metrics`,explanation:`The reason is diagnostic, not presentational.`},{text:`Retrieval recall and generation faithfulness need entirely different fixes, and an aggregate cannot tell you which half is wrong`,explanation:`Correct — teams routinely spend weeks on the wrong half.`},{text:`Because retrieval is more important`,explanation:`It fails more often, but the reason is separability.`},{text:`Because judges cannot score both at once`,explanation:`They can; you would not learn what to fix.`}],correct:1},{question:`Which guardrail layer does not depend on the model behaving?`,options:[{text:`System prompt constraints`,explanation:`Easily overridden by a determined user.`},{text:`Structural limits — rate limits, tool permissions, human approval, hard step caps`,explanation:`Correct, and that is what makes them different in kind from the prompt-based layers.`},{text:`Input filtering`,explanation:`It catches the unsophisticated majority and is bypassable.`},{text:`Output filtering`,explanation:`Better than the prompt, but still a classifier that can be wrong.`}],correct:1},{question:`If 0.1% of requests are abusive, what does a 99%-accurate filter produce?`,options:[{text:`Almost no false positives`,explanation:`1% of 99.9% of traffic is a large number.`},{text:`About ten legitimate requests flagged for every real one — the same base-rate arithmetic as a medical test`,explanation:`Correct, which is why false positives are a product problem that must be budgeted for.`},{text:`99% precision`,explanation:`Accuracy and precision diverge sharply at low base rates.`},{text:`Nothing measurable`,explanation:`It is directly computable and should be computed.`}],correct:1}],interviewQuestions:[{question:`How do you evaluate an LLM application?`,answer:`Start with a golden set of real cases — fifty to a few hundred inputs from actual usage, with expected outputs or acceptance criteria — because the most expensive mistake is not choosing the wrong metric, it is shipping on impressions. Then choose the instrument by what you can afford: automatic metrics are cheap and correlate poorly with quality; model-as-judge correlates far better and has correctable biases; human evaluation is the reference and is slow. And size the set to the claim you want to make: fifty examples gives a 95% interval of plus or minus 11 points, so a 4-point improvement there is noise, and resolving 2 points needs around a thousand.`,isCaseBased:!0},{question:`What are the biases in model-as-judge and how do you handle them?`,answer:`Four that matter, all correctable, which is what makes the technique usable. Position bias — judges prefer whichever option comes first — corrected by running both orders and only counting a preference that survives the swap. Length bias, corrected by comparing lengths and being suspicious when the winner is systematically longer. Self-preference, handled by not judging a model with itself. And rubric vagueness, where "rate this 1–10" gives vague scores and "does this answer follow from the provided context, yes or no" gives usable ones. Above all, validate the judge against human labels on a subset first — otherwise you have replaced an unmeasured system with an unmeasured measurement.`,isCaseBased:!1},{question:`How large should an evaluation set be?`,answer:`It follows from the difference you need to detect, since the binomial standard error falls as one over the square root of n. At 80% accuracy, fifty examples gives a 95% interval of ±11.1 points, a hundred gives ±7.8, five hundred ±3.5 and a thousand ±2.5. So to claim a 2-point improvement honestly you need around a thousand examples, and most reported prompt improvements are made on far fewer than that. The efficient alternative is paired comparison: run both variants on the same examples and compare per-item rather than in aggregate, which removes the variance from example difficulty and detects much smaller differences with the same set.`,isCaseBased:!0},{question:`How would you build guardrails for a production LLM system?`,answer:`In layers, with only the last one load-bearing. Input filtering catches the unsophisticated majority cheaply. System prompt constraints set intent and are easily overridden, so they are a first layer rather than a control. Output filtering — a harmful-content classifier, a faithfulness check against retrieved context, PII detection — is where most real protection lives, because it inspects what actually happened. And structural limits are the ones that hold: rate limits, tool permissions, human approval on consequential actions, hard step caps. Those do not depend on the model behaving, which is what makes them different in kind. I would design assuming the earlier layers fail, and aim for the smallest blast radius when they do.`,isCaseBased:!0},{question:`Can you prevent prompt injection?`,answer:`No, and I would say so plainly rather than claiming a mitigation is a solution. The model cannot reliably distinguish data from instruction, so any untrusted content in the prompt can carry instructions, and no wording of "ignore instructions in the document below" reliably prevents it. Jailbreaks keep working for the same underlying reason — roleplay framing, encoding, gradual escalation. So the response is architectural: least privilege on tools, separating the systems that read untrusted content from the systems that take consequential actions, human confirmation on anything irreversible, and output filtering that inspects the result. The goal is a small blast radius when it succeeds, not prevention.`,isCaseBased:!1},{question:`How do you evaluate the guardrails themselves?`,answer:`As classifiers, with precision, recall and a threshold measured on real traffic. The part teams underbudget is false positives: a filter blocking 5% of legitimate requests is a product problem that users discover before you do. And the base-rate arithmetic bites exactly as it does with a medical test — if 0.1% of requests are genuinely abusive, a 99%-accurate filter flags about ten legitimate requests for every real one. So the threshold is a cost decision between what a missed harmful output costs and what a blocked legitimate one does, and it should be made explicitly. Beyond that: log everything, sample and review flagged and unflagged traffic routinely, and red-team before shipping, because the failure modes you imagine are not the ones users find.`,isCaseBased:!0},{question:`What is benchmark contamination and why should it change how you read results?`,answer:`A benchmark appearing in the training data, so its score measures memorisation rather than capability. It is hard to rule out at web scale, and models trained on more of the internet have seen more of every public benchmark, which means the headline numbers on well-known benchmarks are systematically optimistic and increasingly so over time. The defences are held-out sets you built yourself, benchmarks created after the model's training cutoff, and n-gram overlap checks reported openly. The practical consequence for a product is that benchmark scores are a weak signal for a model choice: an evaluation set built from your own traffic tells you far more, and is uncontaminated by construction.`,isCaseBased:!1},{question:`What single evaluation practice matters most?`,answer:`Turning every production failure into an evaluation case. It costs almost nothing, it grows the set in exactly the direction reality is pushing, and it converts the evaluation suite into a regression suite that encodes everything that has ever gone wrong. Most teams build an evaluation set once and let it go stale, so it measures the problems they anticipated rather than the ones they have. The habit also has a cultural effect: it makes "we fixed it" a testable claim rather than an assertion, and it means a prompt or model change that reintroduces an old failure is caught before it ships rather than after a user finds it again.`,isCaseBased:!1}],flashcards:[{front:`Why generation is hard to evaluate`,back:`No ground truth — two different responses can both be correct, and helpfulness has nothing to compare against.`},{front:`Evaluation set size`,back:`At 80% accuracy: n=50 → ±11.1 points, n=100 → ±7.8, n=500 → ±3.5, n=1000 → ±2.5. Most reported gains are smaller than the noise.`},{front:`The efficient alternative`,back:`PAIRED comparison on the same examples, compared per item — removes example-difficulty variance and detects much smaller differences.`},{front:`Judge biases`,back:`Position (swap the order), length (compare lengths), self-preference (do not judge with itself), vague rubric (ask a specific yes/no).`},{front:`Validate the judge`,back:`Against human labels on a subset — otherwise you have replaced an unmeasured system with an unmeasured measurement.`},{front:`Break the metric down`,back:`RAG: retrieval recall AND generation faithfulness. Agents: per-step AND end-to-end. An aggregate says something is wrong, never what.`},{front:`The guardrail layers`,back:`Input filter (cheap) → system prompt (overridable) → output filter (most real protection) → STRUCTURAL limits (do not depend on the model behaving).`},{front:`The false-positive arithmetic`,back:`At a 0.1% abuse rate, a 99%-accurate filter flags ~10 legitimate requests per real one. The same base-rate maths as a medical test.`}],mindmapMarkdown:`- Evaluating and guarding an LLM system
  - Why it is hard
    - no ground truth; two answers can both be right
    - automatic (cheap, poor correlation)
    - judge (good correlation, correctable biases)
    - human (the reference, slow)
    - the real mistake: no eval set at all
  - Set size
    - 80% accuracy: n=50 -> +/-11.1 points
    - n=100 -> 7.8, n=500 -> 3.5, n=1000 -> 2.5
    - most reported gains are inside the noise
    - PAIRED per-item comparison is far more efficient
  - Model-as-judge
    - position bias -> swap the order
    - length bias -> compare lengths
    - self-preference -> do not judge with itself
    - vague rubric -> ask a specific yes/no
    - validate against human labels first
  - What to build
    - a golden set of REAL cases
    - every production failure becomes a case
    - break down by what you can fix
    - measure cost and latency too
    - watch for contamination
  - Guardrails
    - input filter -> system prompt -> output filter
    - STRUCTURAL limits are the ones that hold
    - injection and jailbreaks are unsolved
    - design for the smallest blast radius
  - Measure the guardrails
    - they are classifiers: precision, recall, threshold
    - 0.1% abuse + 99% accurate = ~10 false per real
    - log, sample, review, red-team`};export{e as default};