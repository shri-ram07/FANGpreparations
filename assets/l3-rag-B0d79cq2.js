var e={id:`genai-l3-rag`,subjectId:`genai`,level:3,title:`Retrieval-Augmented Generation`,whyItMatters:`A model cannot know your documents and cannot be retrained every time one changes. RAG is the standard answer, and almost every RAG system that fails does so in retrieval, not generation — which is why it has to be measured separately.`,assumes:[`You have read *Vector Search at Scale*`,`You know what a context window is`],estMinutes:20,sections:[{type:`intuition`,title:`Two systems, and only one of them usually breaks`,md:`RAG is: embed the question, retrieve the most relevant chunks, put them in the prompt, and ask the model to answer from them.

It is the right answer to a specific problem — the model needs to **know** something it was not trained on, or something that changes. Fine-tuning teaches behaviour and is poor at facts; retrieval supplies facts and changes nothing about behaviour.

The critical framing is that RAG is **two systems**: a retriever and a generator. When the answer is wrong, one of them is at fault, and in practice it is nearly always the retriever — the model cannot answer from a chunk it was never shown. So they must be measured separately, and a single end-to-end score tells you nothing about which half to fix.`},{type:`math`,intro:`The context budget, and the retrieval metric that matters most. The first says chunk size and k are not independent choices - they multiply against the window. The second is recall@k: of the documents that should have been found, what fraction appeared in the top k. Nothing downstream recovers from a document that was never retrieved.`,latex:[`k \\cdot c + p \\le C_{\\max}`,`R@k = \\frac{|\\text{relevant} \\cap \\text{top-}k|}{|\\text{relevant}|}`]},{type:`code`,lang:`python`,title:`The context budget, which decides the chunking`,code:`ctx = 8000
for chunk in [200, 500, 1000, 2000]:
    print('chunk %4d tokens -> %2d chunks fit an %s-token window'
          % (chunk, ctx // chunk, f'{ctx:,}'))

for k, chunk in [(3, 500), (10, 500), (20, 500), (10, 2000)]:
    toks = k * chunk + 500
    print('k=%2d chunks of %4d + 500 prompt = %6s tokens -> $%.5f at $3/M'
          % (k, chunk, f'{toks:,}', toks * 3 / 1e6))

# ---- real output ----
# chunk  200 tokens -> 40 chunks fit an 8,000-token window
# chunk  500 tokens -> 16 chunks fit an 8,000-token window
# chunk 1000 tokens ->  8 chunks fit an 8,000-token window
# chunk 2000 tokens ->  4 chunks fit an 8,000-token window
# k= 3 chunks of  500 + 500 prompt =  2,000 tokens -> $0.00600
# k=10 chunks of  500 + 500 prompt =  5,500 tokens -> $0.01650
# k=20 chunks of  500 + 500 prompt = 10,500 tokens -> $0.03150
# k=10 chunks of 2000 + 500 prompt = 20,500 tokens -> $0.06150`,annotations:{6:`Chunk size and k are not independent choices — they multiply into the context, and the window is what constrains their product.`,11:`Small chunks retrieve precisely but arrive without context; large chunks carry context but dilute the embedding, because one vector must represent 2,000 tokens of possibly unrelated material.`,15:`k=10 at 500 tokens costs 1.65 cents a query. At a million queries a month that is $16,500, so k is a budget decision as much as a quality one.`,16:`10 chunks of 2,000 is four times the cost of 10 of 500, for material the model will largely ignore. Retrieving more is not free and is frequently not better.`}},{type:`note`,label:`Chunking is where most of the quality lives`,md:`More RAG systems are fixed by changing the chunking than by changing the model or the index.

**Fixed-size chunks with overlap** is the default: 500 tokens with 50 of overlap, so a fact split across a boundary appears whole in one of the two. Cheap and unprincipled.

**Structure-aware chunking** splits on real boundaries — sections, paragraphs, function definitions — so a chunk is a coherent unit rather than an arbitrary window. Almost always better where the document has structure.

**Contextual chunking** prepends a short summary of the parent document to each chunk before embedding, so a chunk saying "it costs $40 per month" carries the knowledge of what "it" is. This measurably improves retrieval and is worth the preprocessing cost.

The diagnostic is direct: take a failed query, look at what was retrieved, and ask whether a human given only those chunks could have answered. If not, it is a chunking or retrieval problem and no prompt change will fix it.`},{type:`note`,label:`The pipeline that actually works`,md:`**Hybrid retrieval.** Dense vectors plus BM25 keyword search, fused. Not optional — embeddings blur exactly the exact matches that product codes and error numbers depend on.

**Over-fetch, then rerank.** Retrieve 50 candidates cheaply, then order them with a cross-encoder and keep the top 5. This is usually the single largest quality improvement available, because the bi-encoder's job is recall and the cross-encoder's is precision.

**Query rewriting.** The user's question is often a poor search query — pronouns from earlier turns, missing context, or several questions at once. Rewriting it with the model before retrieval, and decomposing multi-part questions, both help substantially.

**Cite the sources.** Requiring the model to quote the chunk it used makes hallucination visible and gives the user something to check. It is the cheapest safety measure in the whole system.

**Say when nothing was found.** If retrieval returns nothing relevant, the correct output is "I do not have information about that", and the model must be instructed to produce it — otherwise it will answer from parametric memory and sound exactly as confident.`},{type:`note`,label:`Measure the two halves separately`,md:`**Retrieval** is measured with ranking metrics against a set of question-and-correct-source pairs. **Recall@k** — is the needed document in the top k at all — is the one that matters most, because nothing downstream can recover from its absence. **MRR or NDCG** then say whether it is near the top.

**Generation** is measured on faithfulness: does the answer follow from the retrieved context. That is typically a judge model comparing the answer against the chunks, and it is what catches the model answering from memory instead of from the context.

**Answer quality** against a reference is the third, and least diagnostic, number.

The reason to split them is that the fixes are entirely different. Perfect retrieval with hallucinated answers is a prompting or model problem; grounded answers to the wrong documents is a chunking or index problem. A single end-to-end score cannot distinguish them, and teams routinely spend weeks on the wrong half.`},{type:`note`,label:`Long context has not replaced this`,md:`A million-token context window suggests skipping retrieval and pasting everything in. Three reasons it has not happened.

**Cost.** Attention is quadratic, and you pay for every input token on every request. Retrieving 5,000 relevant tokens against sending 500,000 is a hundredfold cost difference on a query the retrieval got right anyway.

**Latency.** Prefill on a very long context takes seconds, before the first output token.

**Quality.** The "lost in the middle" result — retrieval accuracy is high at the start and end of a long context and drops substantially in the middle — means a large window is a ceiling rather than a working budget.

Long context does change the design, though: chunks can be larger, k can be higher, and you can afford to be less precise. The trend is hybrid, with retrieval narrowing a corpus to a generous context rather than to a tight one.`}],quiz:[{question:`When a RAG system gives a wrong answer, which half is usually at fault?`,options:[{text:`The generator — models hallucinate`,explanation:`It happens, but it is the less common cause.`},{text:`The retriever — the model cannot answer from a chunk it was never shown`,explanation:`Correct, which is why the two must be measured separately.`},{text:`Both equally`,explanation:`Retrieval failures dominate in practice.`},{text:`The embedding model specifically`,explanation:`One possible retrieval cause among several, chunking usually being larger.`}],correct:1},{question:`What is the trade-off in chunk size?`,options:[{text:`Larger chunks are always better`,explanation:`One vector representing 2,000 tokens is a diluted vector.`},{text:`Small chunks retrieve precisely but lack context; large chunks carry context but dilute the embedding`,explanation:`Correct, and the two multiply with k into the context window.`},{text:`It only affects cost`,explanation:`It affects retrieval quality more than cost.`},{text:`Chunk size does not affect the embedding`,explanation:`The embedding must represent the whole chunk.`}],correct:1},{question:`k=10 chunks of 500 tokens costs $0.0165 a query. Why does that matter?`,options:[{text:`It does not — inference is cheap`,explanation:`At a million queries a month that is $16,500.`},{text:`k is a budget decision as much as a quality one, and 10 chunks of 2,000 costs four times as much for material the model will largely ignore`,explanation:`Correct. Retrieving more is not free and frequently not better.`},{text:`Because the model charges per chunk`,explanation:`It charges per token.`},{text:`Because larger k improves quality linearly`,explanation:`It does not; returns flatten quickly.`}],correct:1},{question:`What is contextual chunking?`,options:[{text:`Splitting on paragraph boundaries`,explanation:`That is structure-aware chunking.`},{text:`Prepending a short summary of the parent document to each chunk before embedding, so "it costs $40" carries what "it" is`,explanation:`Correct, and it measurably improves retrieval for the preprocessing cost.`},{text:`Using the conversation history as the query`,explanation:`That is query rewriting.`},{text:`Retrieving neighbouring chunks alongside the match`,explanation:`A different technique, sometimes called context expansion.`}],correct:1},{question:`Why is over-fetch-then-rerank usually the largest single quality win?`,options:[{text:`It retrieves more documents`,explanation:`It sends fewer to the model, after ordering more.`},{text:`The bi-encoder's job is recall and the cross-encoder's is precision — 50 candidates cheaply, then order them accurately and keep 5`,explanation:`Correct, and a cross-encoder over the full corpus is impossible.`},{text:`It reduces cost`,explanation:`It adds a reranking pass.`},{text:`It removes the need for hybrid search`,explanation:`The two are complementary.`}],correct:1},{question:`Why has long context not replaced RAG?`,options:[{text:`Context windows are still too small`,explanation:`They reach a million tokens.`},{text:`Cost (quadratic attention, paid per request), latency (seconds of prefill), and lost-in-the-middle quality degradation`,explanation:`Correct — a large window is a ceiling, not a working budget.`},{text:`Models cannot read that much`,explanation:`They can; they attend unevenly.`},{text:`Retrieval is more accurate than attention`,explanation:`Not the argument; the argument is cost, latency and uneven attention.`}],correct:1}],interviewQuestions:[{question:`What is RAG and when should you use it?`,answer:`Embed the question, retrieve the most relevant chunks from a corpus, put them in the prompt, and have the model answer from them. Use it when the model needs to know something it was not trained on or something that changes — current policy, internal documents, anything with a date on it. The complementary decision is fine-tuning, which teaches behaviour and is poor at facts: facts need many repetitions to stick and are still recalled unreliably, and any fact that changes makes the fine-tune stale. The framing I would emphasise is that RAG is two systems, a retriever and a generator, and almost every failure is in the first one.`,isCaseBased:!1},{question:`How do you debug a RAG system that gives wrong answers?`,answer:`Split the two halves first, because the fixes are entirely different and a single end-to-end score cannot tell them apart. Take failing queries and look at what was actually retrieved: if a human given only those chunks could not have answered, it is a retrieval problem and no prompt change will fix it. If the chunks contained the answer and the model got it wrong, it is a generation problem — prompting, or the model answering from parametric memory instead of the context. In my experience it is retrieval the large majority of the time, and within retrieval it is usually chunking rather than the index or the embedding model.`,isCaseBased:!0},{question:`How do you choose chunk size?`,answer:`It is a trade between precision and context, and the two multiply with k into the context window — 500-token chunks let 16 fit an 8,000-token window, 2,000-token chunks only 4. Small chunks retrieve precisely but arrive without the surrounding context; large ones carry context but dilute the embedding, because one vector has to represent 2,000 tokens of possibly unrelated material. My defaults would be 500 tokens with 50 of overlap, structure-aware boundaries wherever the document has them, and contextual chunking — prepending a summary of the parent document — which is worth the preprocessing cost. And I would tune it against a retrieval evaluation set rather than by intuition, because it moves the number more than the index does.`,isCaseBased:!0},{question:`What does a good retrieval pipeline look like?`,answer:`Hybrid retrieval first — dense vectors plus BM25, fused — because embeddings blur exactly the exact matches that product codes and error numbers depend on, so it is a repair rather than an optimisation. Then over-fetch and rerank: get 50 candidates cheaply from the bi-encoder, order them with a cross-encoder, keep 5. That split is usually the largest single quality win, since recall and precision are different jobs. Then query rewriting, because a user's question is often a poor search query — pronouns from earlier turns, or several questions at once. And on the output side, require citations, and instruct the model to say when nothing relevant was found, since otherwise it answers from memory with identical confidence.`,isCaseBased:!1},{question:`How do you evaluate a RAG system?`,answer:`In three separate numbers, and the separation is the point. Retrieval, with ranking metrics against question-and-correct-source pairs: recall@k matters most, because nothing downstream recovers from the right document being absent, and MRR or NDCG then say whether it is near the top. Faithfulness, usually a judge model checking whether the answer follows from the retrieved chunks — this is what catches the model answering from parametric memory. And answer quality against a reference, which is the least diagnostic. The reason to keep them apart is that perfect retrieval with hallucinated answers and grounded answers to the wrong documents need completely different fixes, and teams routinely spend weeks on the wrong half.`,isCaseBased:!0},{question:`Has long context made RAG obsolete?`,answer:`No, for three reasons. Cost: attention is quadratic and you pay for every input token on every request, so retrieving 5,000 relevant tokens against sending 500,000 is a hundredfold difference on a query retrieval would have got right anyway. Latency: prefill on a very long context takes seconds before the first output token. And quality: the lost-in-the-middle result shows retrieval accuracy is high at the start and end of a long context and drops substantially in the middle, so the advertised window is a ceiling rather than a working budget. What long context does change is the design — larger chunks, higher k, less need for precision — so the trend is hybrid rather than replacement.`,isCaseBased:!1},{question:`Your RAG system hallucinates despite good retrieval. What do you do?`,answer:`First confirm the diagnosis by checking the retrieved chunks actually contained the answer, since "good retrieval" is usually assumed rather than measured. If they did, the fixes are on the generation side. Prompt explicitly to answer only from the provided context and to say so when it is not there — models default to being helpful, and helpful means answering. Require citations, quoting the chunk used, which both makes hallucination visible and empirically reduces it. Add a faithfulness check with a judge model against the retrieved context, and surface or block low-scoring answers. And consider the model: smaller models ground less reliably, and this is one of the places where the capability difference shows up clearly.`,isCaseBased:!0},{question:`What is query rewriting and why does it help?`,answer:`Using the model to turn the user's message into a better search query before retrieval. It helps because the two are different things: a user in a conversation says "what about the enterprise one?", which contains almost no retrievable content, and the rewriter turns it into "enterprise plan pricing and features" using the earlier turns. It also handles decomposition — a question with two parts retrieves badly as one query and well as two — and can expand jargon or generate several query variants to widen recall. The cost is an extra model call before retrieval, which adds latency, so it is worth measuring whether it earns its place rather than adding it reflexively.`,isCaseBased:!1}],flashcards:[{front:`RAG is TWO systems`,back:`A retriever and a generator. Almost every failure is retrieval — the model cannot answer from a chunk it never saw. Measure them separately.`},{front:`Retrieve vs fine-tune`,back:`Retrieve for KNOWLEDGE (changes, dated, private). Fine-tune for BEHAVIOUR (format, tone, structure).`},{front:`The context budget`,back:`8,000-token window: 16 chunks of 500, or 4 of 2,000. Chunk size and k multiply.`},{front:`The cost of k`,back:`k=10 at 500 tokens = $0.0165/query. 10 of 2,000 = $0.0615. At a million queries a month that is $16,500 vs $61,500.`},{front:`Chunking trade`,back:`Small: precise retrieval, no context. Large: context, but one vector must represent 2,000 tokens — a diluted embedding.`},{front:`Contextual chunking`,back:`Prepend a summary of the parent document before embedding, so "it costs $40" carries what "it" is. Measurably better.`},{front:`The pipeline`,back:`Hybrid (dense + BM25) → over-fetch 50 → cross-encoder rerank to 5 → cite sources → say when nothing was found.`},{front:`Three separate metrics`,back:`Recall@k (retrieval), faithfulness (does the answer follow from the chunks), answer quality. Different fixes, so never one score.`}],mindmapMarkdown:`- RAG
  - What it is
    - embed question, retrieve chunks, answer from them
    - RETRIEVE for knowledge; FINE-TUNE for behaviour
    - two systems: retriever and generator
    - nearly every failure is the retriever
  - The budget
    - 8,000 window: 16 chunks of 500, or 4 of 2,000
    - k=10 x 500 = $0.0165/query
    - 10 x 2,000 = $0.0615 for material largely ignored
  - Chunking (where the quality is)
    - fixed size + overlap: 500/50, the default
    - structure-aware: sections, paragraphs, functions
    - contextual: prepend a parent summary
    - diagnostic: could a human answer from what was retrieved?
  - The pipeline
    - hybrid dense + BM25 (a repair, not an option)
    - over-fetch 50, cross-encoder rerank to 5
    - query rewriting (pronouns, decomposition)
    - cite sources; say when nothing was found
  - Measure separately
    - recall@k -> nothing recovers from absence
    - faithfulness -> answered from memory?
    - answer quality -> least diagnostic
    - one score cannot tell you which half to fix
  - Long context
    - has NOT replaced it
    - quadratic cost, seconds of prefill
    - lost in the middle
    - it changes the design, not the need`};export{e as default};