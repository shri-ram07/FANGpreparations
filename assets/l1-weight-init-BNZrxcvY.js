var e={id:`dl-l1-weight-init`,subjectId:`dl`,level:1,title:`Weight Initialisation`,whyItMatters:`Initialisation is a single line of code you write once and never think about again — and it decides whether a ten-layer network trains at all. Get it wrong by a factor of three and the signal reaching layer 10 is 3e-10 of what left layer 1.`,assumes:[`You have read *Backpropagation*, so you know gradients are a product of per-layer terms`,`You know what a standard deviation is`],estMinutes:18,sections:[{type:`intuition`,title:`Why zero is not an option`,md:`The obvious initialisation is all zeros, and it destroys the network before training starts.

With identical weights, every unit in a layer computes the **identical output**, so in the backward pass every unit receives the **identical gradient**, so they update identically and stay identical forever. A 256-unit layer behaves as one unit, permanently.

This is the **symmetry-breaking** problem, and it is why weights are random. Biases can safely be zero — the weights already break the symmetry.

Random, then. The question that remains is: random at what scale?`},{type:`intuition`,title:`Scale is a compounding decision`,md:`A forward pass multiplies by a weight matrix at every layer, so the size of the activations is a **product** across layers — exactly like the gradient in backprop.

If each layer shrinks the signal a little, ten layers shrink it a lot. If each layer grows it a little, ten layers overflow.

What is needed is for each layer to leave the activation's spread roughly **unchanged**, so the product stays near 1 however deep the network is. The next snippet measures four choices against that standard.`},{type:`code`,lang:`python`,title:`Activation spread through ten ReLU layers`,code:`import numpy as np, math
np.random.seed(0)
n = 256

def sweep(std, name):
    a = np.random.randn(1000, n)
    out = []
    for _ in range(10):
        W = np.random.randn(n, n) * std
        a = np.maximum(0, a @ W)
        out.append(a.std())
    print('%-24s' % name, ' '.join('%8.4f' % v for v in out[:5]), ' ... ', '%10.3e' % out[-1])

sweep(0.01,               'std 0.01 (too small)')
sweep(math.sqrt(1.0 / n), 'Xavier  sqrt(1/n)')
sweep(math.sqrt(2.0 / n), 'He      sqrt(2/n)')
sweep(0.1,                'std 0.1 (too large)')

# ---- real output ----
# std 0.01 (too small)       0.0931   0.0108   0.0013   0.0002   0.0000  ...   3.094e-10
# Xavier  sqrt(1/n)          0.5828   0.4116   0.2991   0.2028   0.1527  ...   2.303e-02
# He      sqrt(2/n)          0.8284   0.8394   0.8250   0.8156   0.7855  ...   6.848e-01
# std 0.1 (too large)        0.9304   1.0562   1.1205   1.3228   1.4725  ...   3.354e+00`,annotations:{11:`The standard deviation of the activations after each layer. This is the quantity that must stay roughly constant with depth.`,19:`Too small: 0.0931 after one layer and 3.1e-10 after ten. Every activation is effectively zero, so every gradient is too and nothing learns.`,21:`He initialisation holds near 0.83 the whole way down — 0.8284 at layer 1 and 0.6848 at layer 10. That is what "preserves the scale" looks like.`,22:`Too large: growing steadily to 3.354. On a deeper network or a slightly larger std this becomes inf, and the loss is nan on the first forward pass.`,20:`Xavier decays from 0.5828 to 0.023 — a factor of 25. It is not catastrophic, but it is the wrong constant for ReLU, and the reason is the next section.`}},{type:`math`,intro:`The two standard rules. Both keep the variance of the output equal to the variance of the input; they differ by a factor of 2 because of what the activation does to the signal. Here n is fan-in, the number of inputs to a unit.`,latex:[`\\mathrm{Var}(W) = \\frac{1}{n_{\\text{in}}} \\quad \\text{(Xavier / Glorot — tanh, sigmoid)}`,`\\mathrm{Var}(W) = \\frac{2}{n_{\\text{in}}} \\quad \\text{(He / Kaiming — ReLU)}`]},{type:`note`,label:`Where the factor of 2 comes from`,md:`Xavier assumes the activation is roughly linear and symmetric near zero, which is true of tanh. Under that assumption, variance 1/n_in keeps the spread constant.

ReLU zeroes every negative input. On a symmetric distribution that is **half the values**, which halves the variance at every layer. Doubling the weight variance to 2/n_in exactly compensates.

So the rule is mechanical: **He for ReLU-family activations, Xavier for tanh and sigmoid**. Using Xavier with ReLU is not a disaster — the sweep above shows a factor-of-25 decay over ten layers rather than a collapse — but it is measurably the wrong constant, and it gets worse with depth.`},{type:`note`,label:`Fan-in, fan-out, and what changed`,md:`Some formulations use \`2/(fan_in + fan_out)\` instead of \`1/fan_in\`. The distinction is which pass you are protecting: fan-in keeps the **forward** activations stable, fan-out keeps the **backward** gradients stable, and the average compromises between them. PyTorch defaults to fan-in, which is the usual choice.

Two things have made this less critical than it was. **Normalisation layers** re-standardise activations at every layer, so a bad initialisation is corrected after one layer rather than compounding. **Residual connections** give the signal an unmultiplied path, so the product argument does not apply to it.

That is why modern networks are far more forgiving here. It still matters — the first few steps happen before any of that has settled — and it matters absolutely for a network without normalisation, which is exactly the case you meet when writing one from scratch.`}],quiz:[{question:`Why can weights not be initialised to zero?`,options:[{text:`The gradients would be zero`,explanation:`Gradients are generally non-zero; the problem is that they are identical.`},{text:`Every unit in a layer computes the same output, so every unit receives the same gradient and they stay identical forever`,explanation:`Correct. A 256-unit layer permanently behaves as one unit — the symmetry-breaking problem.`},{text:`Zero causes division by zero`,explanation:`Nothing divides by the weights.`},{text:`The network would overfit`,explanation:`It would underfit drastically — it has effectively one unit per layer.`}],correct:1},{question:`Can biases be initialised to zero?`,options:[{text:`No, for the same symmetry reason`,explanation:`The weights already break the symmetry, so the biases need not.`},{text:`Yes — random weights already break the symmetry`,explanation:`Correct, and zero biases are the standard default.`},{text:`Only in the output layer`,explanation:`It applies throughout.`},{text:`Only with ReLU`,explanation:`It is independent of the activation.`}],correct:1},{question:`std 0.01 gave an activation spread of 3.1e-10 after ten layers. Why is that fatal?`,options:[{text:`The forward pass overflows`,explanation:`It underflows toward zero, not overflows.`},{text:`Every activation is effectively zero, so every gradient is too, and nothing learns`,explanation:`Correct. The scale compounds multiplicatively across layers.`},{text:`It causes dead ReLU units`,explanation:`The units are not stuck negative; the whole signal has vanished.`},{text:`It only affects the last layer`,explanation:`The decay is visible at every layer, worsening with depth.`}],correct:1},{question:`Why does He initialisation use 2/n while Xavier uses 1/n?`,options:[{text:`He is for deeper networks`,explanation:`Depth is not what distinguishes them.`},{text:`ReLU zeroes half the values on a symmetric distribution, halving the variance — doubling the weight variance compensates exactly`,explanation:`Correct. Xavier assumes a roughly linear symmetric activation like tanh.`},{text:`He uses fan-out instead of fan-in`,explanation:`Both are usually stated with fan-in.`},{text:`He was found empirically with no derivation`,explanation:`The factor of 2 follows directly from ReLU's effect on variance.`}],correct:1},{question:`Xavier with ReLU decayed from 0.5828 to 0.023 across ten layers. What is the right reading?`,options:[{text:`It is catastrophic and the network cannot train`,explanation:`A factor of 25 is bad but not a collapse — compare 3.1e-10 for std 0.01.`},{text:`It is the wrong constant rather than a disaster, and it gets worse with depth`,explanation:`Correct — He held near 0.83 the whole way down.`},{text:`It proves Xavier is obsolete`,explanation:`Xavier remains correct for tanh and sigmoid.`},{text:`The seed was unlucky`,explanation:`The decay is systematic, not random.`}],correct:1},{question:`Why is initialisation less critical in modern networks?`,options:[{text:`Optimizers correct for it`,explanation:`Adam adapts step sizes but does not restore vanished activations.`},{text:`Normalisation layers re-standardise at every layer and residual connections give an unmultiplied path, so bad scale does not compound`,explanation:`Correct — though it still matters for the first steps and absolutely for a network without normalisation.`},{text:`Modern networks are shallower`,explanation:`They are far deeper.`},{text:`Float32 has more range now`,explanation:`The numeric format has not changed.`}],correct:1}],interviewQuestions:[{question:`Why not initialise all weights to zero?`,answer:`Because it destroys the network before training starts. With identical weights every unit in a layer computes an identical output, so in the backward pass every unit receives an identical gradient, so they update identically and remain identical forever — a 256-unit layer permanently behaves as one unit. Randomness is what breaks that symmetry. Biases are fine at zero, since the weights have already broken it. The follow-up worth volunteering is that random alone is not enough: the scale of that randomness is what decides whether a deep network trains.`,isCaseBased:!1},{question:`Explain Xavier and He initialisation.`,answer:`Both choose the weight variance so that the spread of the activations is preserved from layer to layer, because the forward pass is a product across layers and anything other than 1 compounds. Xavier sets Var(W) = 1/fan_in, derived assuming the activation is roughly linear and symmetric near zero, which fits tanh and sigmoid. He sets 2/fan_in, and the factor of 2 is because ReLU zeroes half the values on a symmetric distribution and therefore halves the variance at each layer. In a ten-layer sweep, He held the activation std near 0.83 throughout while Xavier with ReLU decayed from 0.58 to 0.023.`,isCaseBased:!1},{question:`What happens if the initial weights are too large?`,answer:`Activations grow layer by layer — in a ten-layer sweep, std 0.1 climbed from 0.93 to 3.35, and a slightly larger constant or a deeper network takes it to inf, at which point the loss is nan on the very first forward pass. Even short of that, large activations push saturating activations into their flat region where the derivative is near zero, so the network is dead on arrival despite the signal being large. The gradients explode by the same product argument in reverse. The symptom is unmistakable: nan or a huge loss in the first few steps, before any learning could plausibly have gone wrong.`,isCaseBased:!0},{question:`Fan-in or fan-out?`,answer:`It depends which pass you are protecting. Fan-in scaling keeps the forward activations stable; fan-out keeps the backward gradients stable; the Glorot average, 2/(fan_in + fan_out), compromises between them. PyTorch defaults to fan-in and that is the usual choice, largely because forward stability is what determines whether the network produces a meaningful loss at all. In practice the difference is small for layers where fan-in and fan-out are comparable, and only becomes noticeable in strongly asymmetric layers such as a wide-to-narrow projection.`,isCaseBased:!1},{question:`Does initialisation still matter now that we have BatchNorm and residual connections?`,answer:`Less than it did, and not zero. Normalisation re-standardises activations at every layer, so a bad initial scale is corrected after one layer instead of compounding across ten; residual connections give the signal an unmultiplied path, so the product argument does not apply along the skip. But the first optimisation steps happen before any of that has settled, and a badly scaled start can still put the model somewhere it recovers from slowly. And for a network without normalisation — which is exactly what you write in an interview or from scratch — it is still decisive. There is also active work on initialisation schemes specifically for very deep residual networks, which suggests it has not been fully absorbed.`,isCaseBased:!1},{question:`How would you initialise the final layer of a classifier?`,answer:`Small weights and a bias set to the log-odds of the class prior rather than zero. Initialising the bias to the prior means the network starts by predicting the base rate, which is the best constant prediction, so the first steps are spent learning the features rather than climbing out of a badly wrong starting confidence. On a heavily imbalanced problem this matters a lot — a zero bias starts the model at 50/50 on a 1%-positive task, and the early gradient is dominated by correcting that. It also makes the initial loss interpretable: it should equal the entropy of the label distribution, which is a useful sanity check that the pipeline is wired correctly.`,isCaseBased:!0},{question:`Your loss is nan from the very first step. Walk through it.`,answer:`Nan on step one means the forward pass, not the optimisation — nothing has had time to go wrong dynamically. So I would check initialisation scale first, since weights too large make the activations overflow directly, and print the activation statistics layer by layer to find where they blow up. Then the input data, for nans or unnormalised features in the thousands. Then the loss itself, for a log of zero — an unclamped log, or a softmax computed separately from cross-entropy rather than fused. If step one is clean and nan appears at step fifty, it is a different problem: learning rate or exploding gradients, and clipping is the immediate test.`,isCaseBased:!0},{question:`What initialisation would you use for a transformer?`,answer:`Normal with a small fixed standard deviation, typically 0.02, rather than a fan-scaled rule — that is what GPT and BERT use. The important extra piece is scaling the residual-branch output projections by 1/sqrt(2N) for N layers, because without it the residual stream's variance grows with depth as each block adds to it. Layer normalisation handles a lot of the rest, and pre-norm placement — normalising inside the residual branch rather than after it — is what makes deep transformers trainable without a warmup-dependent tightrope. So the answer is less about the weight distribution and more about controlling what accumulates in the residual stream.`,isCaseBased:!1}],flashcards:[{front:`Why not zeros`,back:`Identical weights → identical outputs → identical gradients → identical forever. A 256-unit layer becomes one unit. Biases CAN be zero.`},{front:`Why scale matters`,back:`The forward pass is a PRODUCT across layers, like the gradient. Anything but ≈1 per layer compounds.`},{front:`std 0.01 over ten layers`,back:`0.0931 → 3.094e-10. Every activation effectively zero, so every gradient is too.`},{front:`std 0.1 over ten layers`,back:`0.9304 → 3.354, still climbing. A little deeper and it is inf, with nan loss on the first forward pass.`},{front:`Xavier`,back:`Var(W) = 1/fan_in. Assumes a roughly linear symmetric activation — tanh, sigmoid.`},{front:`He, and the factor of 2`,back:`Var(W) = 2/fan_in. ReLU zeroes half the values, halving the variance; doubling the weight variance compensates exactly. Held std ≈0.83 across ten layers.`},{front:`Xavier with ReLU`,back:`Decayed 0.5828 → 0.023 over ten layers. The wrong constant rather than a disaster — and worse with depth.`},{front:`Why it matters less now`,back:`Normalisation re-standardises each layer; residuals give an unmultiplied path. Still decisive for the first steps and for a network without normalisation.`}],mindmapMarkdown:`- Weight initialisation
  - Not zeros
    - identical weights -> identical gradients forever
    - a 256-unit layer becomes one unit
    - biases CAN be zero
  - Scale compounds
    - forward pass is a product across layers
    - need each layer to leave the spread ~unchanged
  - Measured over ten ReLU layers
    - std 0.01: 0.0931 -> 3.094e-10
    - Xavier: 0.5828 -> 0.023
    - He: 0.8284 -> 0.6848
    - std 0.1: 0.9304 -> 3.354 and climbing
  - The rules
    - Xavier Var = 1/fan_in, for tanh/sigmoid
    - He Var = 2/fan_in, for ReLU
    - factor 2 = ReLU zeroes half the values
    - fan_in protects forward, fan_out protects backward
  - Why it matters less now
    - normalisation corrects after one layer
    - residuals give an unmultiplied path
    - still decisive without normalisation`};export{e as default};