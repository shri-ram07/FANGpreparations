var e={id:`genai-l0-tokenization`,subjectId:`genai`,level:0,title:`Tokenization: How Text Becomes Numbers`,whyItMatters:`Tokenization decides what the model can see. It is why a model cannot count the letters in "strawberry", why the same 10,000 characters cost 2,500 tokens in English and 9,090 in Hindi, and why perplexity numbers from two papers cannot be compared.`,assumes:[`You know that a model takes numbers, not text`,`You have seen a Python dictionary and a loop`],estMinutes:20,sections:[{type:`intuition`,title:`Neither words nor characters work`,md:`A model consumes numbers, so text must be cut into pieces and each piece given an id. The two obvious cuts both fail.

**Words**: the vocabulary is unbounded — new names, typos, compounds — so anything unseen becomes a single \`UNK\` token and its information is destroyed. And "run", "runs" and "running" are three unrelated ids.

**Characters**: the vocabulary is tiny and nothing is ever unknown, but sequences become enormously long, and since attention costs grow with the square of length, that is expensive. Each token also carries almost no meaning on its own.

**Subwords** take the middle. Frequent words stay whole, rare ones split into pieces, and nothing is ever unknown because the pieces bottom out at single characters.`},{type:`math`,intro:`The merge criterion, and the one line that separates BPE from WordPiece. BPE takes the most frequent adjacent pair. WordPiece divides by how often each piece occurs alone, so a pair only wins if the two halves appear together more than their individual frequencies would predict - which is why it favours meaningful units over merely common ones.`,latex:[`(a, b)^{*}_{BPE} = \\arg\\max_{(a,b)} \\; c(a, b)`,`(a, b)^{*}_{WP} = \\arg\\max_{(a,b)} \\; \\frac{c(a, b)}{c(a)\\, c(b)}`]},{type:`code`,lang:`python`,title:`Byte-pair encoding, six merges on four words`,code:`import collections
corpus = {'low': 5, 'lower': 2, 'newest': 6, 'widest': 3}
words = {w: list(w) + ['</w>'] for w in corpus}

def pair_counts(words):
    c = collections.Counter()
    for w, sym in words.items():
        for i in range(len(sym) - 1):
            c[(sym[i], sym[i+1])] += corpus[w]
    return c

for step in range(6):
    best, cnt = pair_counts(words).most_common(1)[0]
    print('merge %d: %-10s (seen %2d)' % (step+1, ''.join(best), cnt))
    for w, sym in words.items():
        out, i = [], 0
        while i < len(sym):
            if i < len(sym)-1 and (sym[i], sym[i+1]) == best:
                out.append(sym[i] + sym[i+1]); i += 2
            else:
                out.append(sym[i]); i += 1
        words[w] = out
for w, sym in words.items():
    print('  %-8s -> %s' % (w, sym))

# ---- real output ----
# merge 1: es         (seen  9)
# merge 2: est        (seen  9)
# merge 3: est</w>    (seen  9)
# merge 4: lo         (seen  7)
# merge 5: low        (seen  7)
# merge 6: ne         (seen  6)
#   low      -> ['low', '</w>']
#   lower    -> ['low', 'e', 'r', '</w>']
#   newest   -> ['ne', 'w', 'est</w>']
#   widest   -> ['w', 'i', 'd', 'est</w>']`,annotations:{3:`Start from individual characters, with </w> marking the end of a word so the tokenizer can tell "est" inside a word from "est" ending one.`,14:`The algorithm is exactly this: count adjacent pairs, merge the most frequent, repeat. There is nothing statistical or learned about it beyond counting.`,28:`"est" was merged before "lo" even though "low" appears in two words, because "est" appears 9 times across newest and widest against 7 for "lo". Frequency is the only criterion.`,30:`"low" ends up as a single token because it was frequent; "lower" becomes low + e + r, reusing the piece. That reuse is the whole point — the model sees the shared stem.`}},{type:`note`,label:`What the model can and cannot see`,md:'The model never sees letters. "strawberry" arrives as roughly three chunks — something like `str`, `aw`, `berry` — so asking how many r\'s it contains is asking it to introspect about a representation it does not have.\n\nThat single fact explains a family of failures: counting letters, reversing strings, rhyming, and arithmetic on numbers whose digits get grouped inconsistently. A model that can write a compiler struggling to count letters is not a reasoning failure; it is being asked about something below its input resolution.\n\nThe same applies to **whitespace**, which is part of the token. ` world` with a leading space and `world` without are different tokens with different ids, which is why trailing whitespace in a prompt measurably degrades output.'},{type:`code`,lang:`python`,title:`The same 10,000 characters, in four languages and formats`,code:`for kind, cpt in [('English prose', 4.0), ('Python code', 2.9),
                  ('JSON', 2.3), ('Hindi (Devanagari)', 1.1)]:
    print('%-20s %.1f chars/token -> %s chars = %s tokens'
          % (kind, cpt, f'{10000:,}', f'{int(10000/cpt):,}'))

# ---- real output ----
# English prose        4.0 chars/token -> 10,000 chars = 2,500 tokens
# Python code          2.9 chars/token -> 10,000 chars = 3,448 tokens
# JSON                 2.3 chars/token -> 10,000 chars = 4,347 tokens
# Hindi (Devanagari)   1.1 chars/token -> 10,000 chars = 9,090 tokens`,annotations:{1:`Approximate ratios for a GPT-style tokenizer, whose merges were learned on a corpus that is overwhelmingly English.`,9:`Code costs 38% more tokens than prose for the same characters — indentation, punctuation and identifiers split badly, and JSON is worse again at 74% more.`,11:`Hindi costs 3.6 times as many tokens as English for the same text. That is a direct API cost multiplier, a direct reduction in usable context, and a quality penalty, all from the tokenizer's training data — and it applies to most languages not written in Latin script.`}},{type:`note`,label:`The choices that follow from vocabulary size`,md:`A **larger vocabulary** means shorter sequences, which is cheaper to run since attention grows with the square of length — but a larger embedding table and a larger output softmax, and rarer tokens get less training signal each.

A **smaller vocabulary** means the reverse. The usual landing zone is 30,000–100,000: GPT-2 at 50,257, LLaMA-2 at 32,000, and recent multilingual models pushing past 128,000 specifically to fix the non-English penalty.

Two consequences worth carrying:

**Perplexity is not comparable across tokenizers.** A model predicting more, easier tokens reports a lower number regardless of quality, so cross-paper perplexity comparisons are close to meaningless.

**Token counts are what you pay for**, not words or characters — which is why the cost of the same document varies by a factor of nearly four depending on what language it is written in.`},{type:`note`,label:`BPE, WordPiece, SentencePiece`,md:`**BPE** merges the most *frequent* pair, as above. Used by GPT, and by RoBERTa at the byte level, which guarantees nothing is ever unknown because every byte is in the vocabulary.

**WordPiece** merges the pair that most increases the *likelihood* of the corpus, rather than the most frequent — a slightly different criterion producing similar results. Used by BERT, with its distinctive \`##\` continuation marker.

**SentencePiece** treats the input as a raw byte stream with no pre-tokenization, so it needs no language-specific word splitting — essential for languages that do not use spaces, such as Chinese and Japanese. Used by T5 and LLaMA.

The practical differences are small. What matters far more is **what corpus the merges were learned on**, because that is what decides which languages and formats are cheap.`}],quiz:[{question:`Why not tokenize by words?`,options:[{text:`Words are too long`,explanation:`Length is not the issue.`},{text:`The vocabulary is unbounded, so anything unseen becomes UNK and its information is destroyed`,explanation:`Correct, and "run"/"runs"/"running" would be three unrelated ids.`},{text:`Words cannot be given numeric ids`,explanation:`They can; the problem is coverage.`},{text:`It makes sequences too long`,explanation:`Word tokenization gives the shortest sequences.`}],correct:1},{question:`In the BPE trace, "es" was merged before "lo". Why?`,options:[{text:`Alphabetical order`,explanation:`The algorithm has no notion of alphabetical order.`},{text:`It appeared 9 times against 7 — frequency is the only criterion`,explanation:`Correct. Count adjacent pairs, merge the most frequent, repeat.`},{text:`"es" is more meaningful`,explanation:`BPE has no notion of meaning.`},{text:`It appeared in more distinct words`,explanation:`The count is weighted by word frequency, not distinct words.`}],correct:1},{question:`Why can a model that writes working code fail to count the r's in "strawberry"?`,options:[{text:`It is a reasoning failure`,explanation:`The information is not in its input at all.`},{text:`It never sees letters — the word arrives as a few chunks, so it is being asked about something below its input resolution`,explanation:`Correct, and the same explains reversing strings, rhyming and digit-grouped arithmetic.`},{text:`The training data had no spelling examples`,explanation:`More data would not supply a representation the input lacks.`},{text:`It is a context length problem`,explanation:`The word fits in any context.`}],correct:1},{question:`The same 10,000 characters cost 2,500 tokens in English and 9,090 in Hindi. What causes this?`,options:[{text:`Hindi is more information-dense`,explanation:`The cause is the tokenizer, not the language.`},{text:`The merges were learned on an overwhelmingly English corpus, so non-Latin scripts split into many small pieces`,explanation:`Correct — a 3.6x cost multiplier, less usable context, and a quality penalty, all from training data.`},{text:`Unicode requires more bytes`,explanation:`Byte count is not the same as token count, and byte-level BPE would still merge common Hindi sequences if it had seen them.`},{text:`Hindi has a larger alphabet`,explanation:`Alphabet size does not determine merge quality.`}],correct:1},{question:`What is the trade-off in vocabulary size?`,options:[{text:`Larger is always better`,explanation:`It grows the embedding table and the output softmax, and gives rare tokens less signal.`},{text:`Larger means shorter sequences (cheaper attention) but a bigger embedding table and softmax, and less signal per rare token`,explanation:`Correct — the usual landing zone is 30,000–100,000.`},{text:`It only affects memory`,explanation:`It affects sequence length, which affects compute quadratically.`},{text:`It has no effect on the model`,explanation:`It changes almost everything downstream.`}],correct:1},{question:`Why can you not compare perplexity across two papers?`,options:[{text:`They use different random seeds`,explanation:`Perplexity is deterministic given a model and test set.`},{text:`A model predicting more, easier tokens reports lower perplexity regardless of quality`,explanation:`Correct — perplexity is per token, and tokenizers define what a token is.`},{text:`Perplexity is unbounded`,explanation:`True, but not what prevents comparison.`},{text:`They use different log bases`,explanation:`That is a fixed rescaling and could be corrected for.`}],correct:1}],interviewQuestions:[{question:`Explain byte-pair encoding.`,answer:`Start with every word split into characters, count all adjacent pairs weighted by word frequency, merge the most frequent pair into a single symbol, and repeat until you reach the target vocabulary size. On a toy corpus that gives merges like "es" then "est" then "est</w>", and words like "low" end up as one token while "lower" becomes low + e + r, reusing the stem. The value is that frequent words stay whole, rare ones decompose into meaningful pieces, and nothing is ever out of vocabulary because the pieces bottom out at single characters. The only criterion is frequency — there is nothing semantic in it.`,isCaseBased:!1},{question:`Why do LLMs fail at counting letters?`,answer:`Because they never see letters. "strawberry" arrives as roughly three chunks, so asking how many r's it contains asks the model to introspect about a representation it does not have — the information is below its input resolution. The same explains reversing strings, rhyming, and arithmetic where a number's digits get grouped inconsistently by the tokenizer. It is worth stating clearly in an interview that this is not a reasoning failure, because the instinct is to treat it as evidence about intelligence when it is evidence about input format. The fix, where it matters, is to give the model a tool rather than more training.`,isCaseBased:!0},{question:`What are the practical consequences of tokenizer choice?`,answer:`Cost, context and quality, all at once. The same 10,000 characters is 2,500 tokens in English, 3,448 in Python, 4,347 in JSON and about 9,090 in Hindi — so a Hindi document costs 3.6 times as much through an API, fits 3.6 times less into the same context window, and is harder for the model because it is spread across more, less meaningful pieces. That is entirely a consequence of which corpus the merges were learned on. It also means perplexity cannot be compared across tokenizers, and that structured formats like JSON are quietly expensive in a prompt.`,isCaseBased:!0},{question:`BPE, WordPiece or SentencePiece?`,answer:`The differences are smaller than the names suggest. BPE merges the most frequent pair; WordPiece merges the pair that most increases corpus likelihood, which is a different criterion producing similar vocabularies; SentencePiece treats the input as a raw byte stream with no pre-tokenization, which makes it essential for languages that do not use spaces, such as Chinese and Japanese. Byte-level BPE, as in GPT-2 and RoBERTa, guarantees nothing is ever unknown because every byte is in the vocabulary. What actually matters far more than the algorithm is the corpus the merges were learned on, since that determines which languages and formats are cheap.`,isCaseBased:!1},{question:`How would you choose a vocabulary size?`,answer:`From the trade rather than a default. Larger vocabulary means shorter sequences, which is cheaper because attention grows with the square of length, but a bigger embedding table and output softmax, and each rare token gets less training signal. Smaller means the reverse. The usual landing zone is 30,000 to 100,000 — GPT-2 at 50,257, LLaMA-2 at 32,000 — and recent multilingual models push past 128,000 specifically to reduce the non-English penalty. If I were building for a specific domain I would train the tokenizer on that domain's text, since a general tokenizer splits domain terms badly and that cost is paid on every single request.`,isCaseBased:!0},{question:`Why does trailing whitespace in a prompt hurt output quality?`,answer:`Because whitespace is part of the token, not a separator between tokens. " world" with a leading space and "world" without are different ids with different embeddings. So a prompt ending in a space puts the model in a state where the next token must start mid-word, which is a distribution it has seen far less often during training and handles worse. It is a small, real and easily avoided effect, and it is a good illustration of the general principle that tokenization details leak into behaviour in ways that look mysterious until you know where the boundaries are.`,isCaseBased:!1},{question:`Should you train your own tokenizer?`,answer:`Only for a genuinely specialised domain, and knowing the cost. If your text is full of chemical formulae, genomic sequences, or a language the base tokenizer handles badly, a domain tokenizer can cut sequence length substantially, and that saving is paid back on every request forever. The cost is that you can no longer use a pretrained model directly — the embedding table is tied to the vocabulary — so you are committing to pretraining or at least to a careful embedding-surgery step. For most projects the answer is no: use the model's tokenizer, and if the language penalty is severe, pick a model with a multilingual vocabulary instead.`,isCaseBased:!1},{question:`How do you count tokens for cost estimation?`,answer:`With the actual tokenizer, never with a heuristic — tiktoken for OpenAI models, the Hugging Face tokenizer for open models. The rules of thumb are useful for a first estimate and dangerous for a budget: roughly 4 characters per token for English prose, but 2.9 for Python, 2.3 for JSON and about 1.1 for Devanagari script, so the same document can vary by nearly a factor of four. I would also count the full request rather than the visible prompt: system prompts, few-shot examples, retrieved context and conversation history all count, and in a RAG system the retrieved chunks usually dominate the total by a wide margin.`,isCaseBased:!0}],flashcards:[{front:`Why not words or characters`,back:`Words: unbounded vocabulary, UNK destroys information. Characters: sequences too long, and attention is quadratic in length.`},{front:`BPE, in one line`,back:`Count adjacent pairs weighted by word frequency, merge the most frequent, repeat. Frequency is the ONLY criterion.`},{front:`The BPE trace`,back:`"es" (9) merged before "lo" (7). "low" ends as one token; "lower" becomes low + e + r, reusing the stem.`},{front:`Why letter counting fails`,back:`The model never sees letters — "strawberry" arrives as ~3 chunks. Below its input resolution, not a reasoning failure.`},{front:`The language tax`,back:`10,000 characters: English 2,500 tokens, Python 3,448, JSON 4,347, Hindi 9,090. A 3.6x cost and context multiplier.`},{front:`Vocabulary size trade`,back:`Larger: shorter sequences (attention is quadratic) but bigger embedding table and softmax, less signal per rare token. Usual range 30k–100k.`},{front:`Perplexity is not comparable`,back:`Across tokenizers. A model predicting more, easier tokens reports lower perplexity regardless of quality.`},{front:`Whitespace is part of the token`,back:`" world" and "world" are different ids. A prompt ending in a space forces a mid-word continuation the model has seen less often.`}],mindmapMarkdown:`- Tokenization
  - Why subwords
    - words: unbounded vocab, UNK destroys information
    - characters: sequences too long, attention is quadratic
    - subwords: frequent whole, rare split, never unknown
  - BPE
    - count adjacent pairs, merge the most frequent, repeat
    - frequency is the ONLY criterion
    - "es" (9) before "lo" (7)
    - low -> one token; lower -> low + e + r
  - What the model cannot see
    - letters: "strawberry" is ~3 chunks
    - hence counting, reversing, rhyming, digit arithmetic
    - whitespace is PART of the token
  - The language tax
    - 10,000 chars: English 2,500, Python 3,448
    - JSON 4,347, Hindi 9,090
    - cost, context and quality, all at once
    - it comes from the merge corpus
  - Vocabulary size
    - larger: shorter sequences, bigger table and softmax
    - 30k-100k typical; 128k+ for multilingual
    - perplexity NOT comparable across tokenizers
  - The variants
    - BPE (GPT), WordPiece (BERT), SentencePiece (LLaMA)
    - byte-level guarantees nothing is unknown
    - the corpus matters more than the algorithm`};export{e as default};