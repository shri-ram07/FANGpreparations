var e={id:`py-l0-variables-and-flow`,subjectId:`python`,level:0,title:`Variables, Types & Control Flow`,whyItMatters:'Python screening rounds start here, and so do the traps: `/` vs `//`, input() handing you a string, a loop that never ends. Interviewers use these basics to filter fast. This module also installs the one mental model — names bound to objects — that explains half of Python\'s "weird" behavior later.',estMinutes:45,sections:[{type:`intuition`,title:`Variables are sticky notes, not boxes`,md:"In C, a variable is a box: `x = 5` puts 5 inside box x. Python has no boxes.\n\n- `x = 5` creates an **object** (the int 5) somewhere in memory, then sticks a note labeled `x` on it.\n- Assignment never copies data. It only moves or adds sticky notes.\n- `y = x` means: put a second note on the **same** object. Two names, one thing.\n- Reassigning `x = 'hello'` peels x's note off the int and sticks it on a new string. The int is untouched.\n- This is the **names-vs-objects** model. Memorize the sentence: *names are bound to objects*. It explains mutation surprises, `is` vs `==`, and default-argument bugs later."},{type:`code`,lang:`python`,title:`Two names, one object`,code:`x = [1, 2, 3]    # a list: an ordered bag of values (full story next module)
y = x            # NO copy — a second sticky note on the SAME list
y.append(4)
print(x)         # [1, 2, 3, 4] — x "changed" because it IS y's object

x = 'hello'      # x's note moves to a new str object
print(y)         # [1, 2, 3, 4] — y still points at the list`,annotations:{2:`Assignment copies the reference, never the data. This one fact prevents a whole class of bugs.`,4:`Mutating through either name is visible through both — same object underneath.`,6:`Rebinding x does not touch the list. Only the sticky note moved.`}},{type:`intuition`,title:`The five types you meet on day one`,md:'- **int** — whole numbers. Python ints have **no size limit**: `2 ** 1000` just works. No overflow, ever.\n- **float** — decimals, stored in binary. Consequence: `0.1 + 0.2` gives `0.30000000000000004`. Never compare floats with `==`; never use floats for money.\n- **str** — text in quotes, single or double. Immutable (cannot be changed in place).\n- **bool** — `True` / `False`. Secretly a subclass of int: `True + True` is `2`. Handy for counting.\n- **None** — the "nothing here" object. Not zero, not empty string — its own type. Test it with `x is None`.'},{type:`note`,md:"Python is **dynamically typed**: the *object* has a type, the *name* does not. The same name can point at an int now and a string later. Ask any object its type with `type(x)` — you will use this constantly while debugging."},{type:`intuition`,title:`Division has four spellings — interviews test all four`,md:"- `/` — true division. **Always returns a float**, even for `8 / 2` → `4.0`. Number one Python-vs-C++ gotcha.\n- `//` — floor division. Chops toward **negative infinity**, not toward zero: `7 // 2` is `3`, but `-7 // 2` is `-4` (not -3!).\n- `%` — remainder. The workhorse: `n % 2 == 0` means even, `n % 10` grabs the last digit. Result takes the **sign of the divisor**.\n- `**` — power. `2 ** 10` is 1024. Not `^` — that is XOR, a classic silent bug.\n- The rest are what you expect: `+ - *`, and shorthand like `x += 1` (there is no `x++` in Python)."},{type:`visual`,component:`PythonPlayground`,props:{code:`a = 7
b = 2
print(type(a).__name__, type(2.5).__name__, type('hi').__name__)
print(type(True).__name__, type(None).__name__)
print('7 / 2  =', a / b)
print('7 // 2 =', a // b)
print('7 % 2  =', a % b)
print('7 ** 2 =', a ** b)
print('-7 // 2 =', -7 // 2)
print('True + True =', True + True)`,precomputedOutput:`int float str
bool NoneType
7 / 2  = 3.5
7 // 2 = 3
7 % 2  = 1
7 ** 2 = 49
-7 // 2 = -4
True + True = 2`,caption:`The five core types and all four division-family operators. Change the numbers and predict before you run — especially the negative floor division.`}},{type:`intuition`,title:`Comparisons, and the lazy and/or`,md:"Comparisons (`== != < <= > >=`) return a bool. Two Python twists:\n\n- **Chaining**: `18 <= age < 65` is one expression doing two checks. Reads like math.\n- **Truthiness**: in a condition, `0`, `0.0`, `''` (empty string), `None`, and `False` count as false. Everything else counts as true.\n- `and` / `or` are **lazy** (short-circuit): they stop as soon as the answer is known. `False and crash()` never calls crash().\n- They return an **operand**, not a bool: `'' or 'default'` returns `'default'`. This powers two everyday idioms — the fallback (`or`) and the guard (`and`).\n- `not` flips: `not None` is `True`."},{type:`code`,lang:`python`,title:`Short-circuit idioms you will read in every codebase`,code:`age = 20
print(18 <= age < 65)          # chaining: one expression, two checks

name = ''
print(name or 'Anonymous')     # fallback: first truthy operand wins

user = None
print(user and user.upper())   # guard: and stops at the falsy value — no crash`,annotations:{2:`Equivalent to (18 <= age) and (age < 65) — but you write it like the math you learned in school.`,5:`Gotcha: this treats 0 and '' as "missing". If 0 is a legal value, use an explicit \`if x is None\` check instead.`,8:"user is None (falsy), so `and` returns None WITHOUT evaluating user.upper(). Remove `user and` and this line crashes."}},{type:`intuition`,title:`Talking to the world: print, f-strings, input`,md:"- `print(a, b)` prints values separated by spaces, then a newline. `print(x, end=' ')` swaps the newline for a space.\n- **f-strings** are the modern way to format: `f'{name} scored {marks}'` — put `f` before the quote, drop variables inside `{}`. Bonus: `f'{price:.2f}'` rounds to 2 decimals.\n- `input('prompt')` shows the prompt, waits, and returns what the user typed — **always as a str**. Type `42` and you get `'42'`.\n- So every number from input() needs `int(...)` or `float(...)` before math or comparison. Forgetting this is a rite of passage; the code section below does it right."},{type:`intuition`,title:`if / elif / else: the first branch that wins, wins`,md:"Python has no braces. **Indentation IS the syntax** — the indented block under a colon is the body.\n\n- `if` checks a condition. `elif` (else-if) checks only if everything above failed. `else` catches the rest.\n- Checks run top to bottom and **stop at the first True** — so order by priority, most specific first.\n- `=` assigns, `==` compares. Python refuses `if x = 5` with a SyntaxError — one C bug it deletes for you.\n- Mixed tabs and spaces cause IndentationError. Pick spaces (4 per level) and never think about it again."},{type:`code`,lang:`python`,title:`input → convert → branch → f-string`,code:`raw = input('Marks: ')     # input() ALWAYS returns a str
marks = int(raw)           # convert first, or every comparison is a bug

if marks >= 90:
    grade = 'A'
elif marks >= 75:          # only checked if the first test failed
    grade = 'B'
else:
    grade = 'C'

print(f'{marks} marks -> grade {grade}')`,annotations:{1:`Type 85 at the prompt and raw is '85', a string. '85' >= 90 would crash with TypeError in Python 3.`,6:`Because elif runs only after the first test failed, marks is guaranteed < 90 here — no need to write 75 <= marks < 90.`,11:`f-string: the f prefix makes {marks} and {grade} substitute their values into the text.`}},{type:`intuition`,title:`Two loops: while for "until", for for "each"`,md:"- `while condition:` repeats as long as the condition is True. Use when you don't know how many rounds: retry until success, read until empty.\n- `for x in range(n):` runs exactly n times. Use when you can count the work up front.\n- `range(5)` yields 0,1,2,3,4 — **starts at 0, stop value excluded**. `range(1, 6)` yields 1..5. `range(10, 0, -2)` counts down 10,8,6,4,2.\n- range is lazy: `range(10**9)` costs almost no memory — it produces numbers one at a time, never a billion-item list.\n- The classic while bug: forget to change the condition variable inside the body → infinite loop. Every dev ships one eventually."},{type:`code`,lang:`python`,title:`while — and the bug that never terminates`,code:`n = 1
while n <= 5:        # keeps looping while this is True
    print(n, end=' ')
    n += 1           # delete this line -> infinite loop
print()              # output: 1 2 3 4 5`,annotations:{2:`The condition is checked BEFORE each round. If n starts at 6, the body never runs at all.`,4:`Something in the body must push the condition toward False. This line is the loop's exit ramp.`}},{type:`intuition`,title:`break, continue — and the else nobody expects`,md:'- `break` — leave the loop immediately. "Found it, stop looking."\n- `continue` — skip the rest of THIS round, jump to the next. "This item doesn\'t apply."\n- Loops can have an `else`: it runs only if the loop finished **without hitting break**. Read it as *"no break happened"*.\n- Real use: search loops — break when found; the else block is your clean "not found" branch, no flag variable needed.\n- It is rarely used and surprises readers — know it for interviews, use it sparingly in teams.'},{type:`visual`,component:`PythonPlayground`,props:{code:`for n in range(1, 16):
    if n % 15 == 0:
        print('FizzBuzz', end=' ')
    elif n % 3 == 0:
        print('Fizz', end=' ')
    elif n % 5 == 0:
        print('Buzz', end=' ')
    else:
        print(n, end=' ')
print()

for n in range(2, 8):
    if 91 % n == 0:
        print(f'91 is divisible by {n}')
        break
else:
    print('no divisor found')`,precomputedOutput:`1 2 Fizz 4 Buzz Fizz 7 8 Fizz Buzz 11 Fizz 13 14 FizzBuzz 
91 is divisible by 7`,caption:`FizzBuzz (the % 15 check MUST come first — see what happens if you move it last), then break + for-else as a flag-free search. Change 91 to 97 (a prime) to see the else branch fire.`}},{type:`note`,md:"Gotcha recap — the five that bite in interviews and in prod:\n\n- `input()` returns str. Convert before math.\n- `/` always gives float; `//` floors toward negative infinity (`-7 // 2 == -4`).\n- `0.1 + 0.2 != 0.3` — floats are binary approximations. Compare with a tolerance; use cents (ints) or `decimal` for money.\n- `x or default` swallows 0 and `''` — use `is None` when 0 is legal.\n- `**` is power; `^` is XOR and won't error, just silently gives wrong numbers."}],quiz:[{question:`What does 8 / 2 evaluate to in Python 3?`,options:[{text:`4 (an int)`,explanation:`That is C++ thinking. Python's / is TRUE division and always returns a float — even when it divides evenly.`},{text:`4.0 (a float)`,explanation:`Correct. / always returns float. Want an int result? Use // (floor division).`},{text:`It depends on the operands`,explanation:`No — / is float unconditionally. That predictability is the point of splitting / and //.`}],correct:1},{question:`-7 // 2 equals…`,options:[{text:`-3`,explanation:`That is truncation toward zero (C++ behavior). Python floors toward NEGATIVE INFINITY instead.`},{text:`-4`,explanation:`Correct. -3.5 floored (pushed down the number line) is -4. Interviewers love this one.`},{text:`-3.5`,explanation:`// always produces a whole result — the fractional part is floored away.`}],correct:1},{question:`The user types 42 at an input() prompt. What does input() return?`,options:[{text:`The str '42'`,explanation:`Correct. input() ALWAYS returns a string, whatever was typed. int(raw) converts it.`},{text:`The int 42`,explanation:`Never — input() does no conversion. '42' + 1 would crash with TypeError.`},{text:`int if it looks numeric, else str`,explanation:`Python does not guess types from content. It is always str — you convert explicitly.`}],correct:0},{question:`x = [1, 2] then y = x then y.append(3). What does print(x) show?`,options:[{text:`[1, 2]`,explanation:`This assumes y = x made a copy. It did not — assignment only adds a sticky note to the SAME object.`},{text:`[1, 2, 3]`,explanation:`Correct. x and y are two names bound to one list. Mutating through either name is visible through both.`},{text:`An error`,explanation:`Nothing illegal happened — this runs fine, which is exactly why the shared-object surprise is dangerous.`}],correct:1},{question:`What does 0 or 'default' evaluate to?`,options:[{text:`True`,explanation:`or does not return a bool — it returns one of its OPERANDS.`},{text:`'default'`,explanation:`Correct. 0 is falsy, so or moves on and returns the first truthy operand. Gotcha: this also fires when 0 is a legitimate value.`},{text:`0`,explanation:`0 is falsy, so or keeps looking. It would return 0 only if BOTH operands were falsy (or returns the last one).`}],correct:1},{question:`range(1, 5) produces which numbers?`,options:[{text:`1, 2, 3, 4, 5`,explanation:`The stop value is EXCLUDED — half-open ranges are Python-wide convention (slicing works the same way).`},{text:`1, 2, 3, 4`,explanation:`Correct. Start included, stop excluded. Bonus: len(range(a, b)) is simply b - a.`},{text:`0, 1, 2, 3, 4`,explanation:`That is range(5) — with one argument the start defaults to 0. Here the start is explicitly 1.`}],correct:1},{question:`When does the else block on a for loop run?`,options:[{text:`When the loop body never executed`,explanation:`No — an empty loop still finishes without break, so else DOES run then. The trigger is about break, not emptiness.`},{text:`When the loop finished without hitting break`,explanation:`Correct. Read loop-else as "no break happened" — which is why it pairs with search loops as the not-found branch.`},{text:`After every loop, always`,explanation:`If break fires, else is skipped — that conditional skip is its entire reason to exist.`}],correct:1}],interviewQuestions:[{question:`Explain "names are bound to objects". How is a Python variable different from a C++ variable?`,answer:'A C++ variable is a fixed box of memory; assignment copies a value into the box. A Python "variable" is just a name (sticky note) bound to an object living elsewhere; assignment binds the name to an object and **never copies data**. Consequences worth naming: `y = x` gives two names for one object (mutations visible through both), rebinding a name does not affect the old object, and types belong to objects, not names — the same name can point at an int and then a str. This model is the foundation for `is` vs `==`, mutability traps, and the mutable-default-argument bug later.',isCaseBased:!1},{question:`Walk me through / vs // vs % — including what happens with negative numbers.`,answer:"`/` is true division: always float, even 8/2 → 4.0. `//` is floor division: rounds toward **negative infinity**, so -7 // 2 = -4 (C++ truncates toward zero and gives -3). `%` returns the remainder with the **sign of the divisor**, and Python guarantees the invariant `a == (a // b) * b + a % b` — floor division and % are designed as a matched pair. Practical payoff: `x % n` is always in [0, n) for positive n, which makes wrap-around indexing (circular buffers, clock arithmetic) work without special-casing negatives — a real advantage over C++.",isCaseBased:!1},{question:"Case: a prod CLI reads a threshold with input() and filters records with `if score > threshold`. QA reports that threshold 100 lets through scores of 20. Walk me through the bug.",answer:"input() returned the **str** '100', and score was probably also parsed as a str. String comparison is lexicographic (character by character): '20' > '100' is True because '2' > '1'. So every score starting with a big digit passes. If score had been an int, Python 3 would at least have crashed with TypeError (int > str is illegal) — the silent version needs both sides to be strings. Fix: convert at the boundary — `threshold = int(input(...))` — and validate there too (wrap in try/except ValueError for junk input). Rule to state: **parse inputs into real types at the edge of the program; never let strings masquerade as numbers deeper in.**",isCaseBased:!0},{question:`Why does 0.1 + 0.2 == 0.3 evaluate to False, and what do you do about it?`,answer:"Floats are binary fractions; 0.1 and 0.2 have no exact binary representation, so each is stored slightly off, and the sum lands at 0.30000000000000004. This is IEEE-754, not a Python bug — same in C++ and Java. Handling, in order of preference: (1) compare with tolerance — `math.isclose(a, b)`; (2) for money, avoid floats entirely: store integer cents, or use the `decimal` module when you need decimal arithmetic with control over rounding. Tradeoff to name: `decimal` is exact for decimal values but much slower — fine for invoices, wrong for a million-element ML loop.",isCaseBased:!1},{question:"What do `and` and `or` actually return, and how does short-circuiting get used in real code?",answer:"They return an **operand**, not a bool, and stop as soon as the answer is known. Two idioms: the guard — `if d != 0 and total / d > 5` never divides by zero because the right side is skipped when the left is False; the fallback — `name or \"Anonymous\"` returns the first truthy operand. Tradeoff of the fallback: it treats ALL falsy values (0, '', empty list) as missing. If 0 is a legal value, `x if x is not None else default` is the correct spelling. Mentioning that distinction unprompted is what makes the answer senior.",isCaseBased:!1},{question:`Case: an on-call page — a data-cleanup script has been running for 6 hours; normally it takes 2 minutes. It is stuck in a while loop. What are the likely causes and how do you confirm?`,answer:'A while loop only ends when its condition goes False or break fires, so the suspects are: (1) the body stopped moving the condition variable — an increment behind an `if` or `continue` that skips it on some input; (2) the exit condition is unreachable for this data — e.g. `while total != target` where float accumulation makes total step OVER target without ever equalling it (use >= for float progress, never !=); (3) an upstream change means the "done" state never arrives (empty batch → nothing consumed). Confirm by getting eyes inside the loop: attach with py-spy (or add a print of the loop variables) and watch whether they change. Prevention: every while loop gets a progress guarantee or a max-iterations safety valve.',isCaseBased:!0},{question:"What is None, and why is `x is None` preferred over `x == None`?",answer:"None is a singleton object meaning \"no value here\" — its own type (NoneType), distinct from 0, '', and False. There is exactly one None in the whole interpreter, so identity (`is` — \"same object?\") is the precise test and cannot be fooled. `==` calls the object's equality method, which a class can override — a custom object could answer True to `== None` and lie to you, and it is also marginally slower. Same reason `if x:` is not a None-check: 0 and '' are falsy too. State the ladder: `if x:` tests truthiness, `== ` tests equality, `is None` tests identity — pick the narrowest one that matches your intent.",isCaseBased:!1},{question:`What does else on a loop do, and when would you actually use it?`,answer:'The else block runs only if the loop completed **without break** — mnemonic: "no break happened". Canonical use: search loops. `for candidate in options: if fits(candidate): break` and the else block handles not-found — replacing the `found = False` flag variable and its bookkeeping. Tradeoffs to name honestly: it reads like "else = loop didn\'t run", which is wrong, so many style guides discourage it; and any loop-else can be rewritten with a flag or by extracting the search into a function that returns early (usually the cleanest). Know it cold for reading others\' code; reach for it rarely.',isCaseBased:!1},{question:`Is range(10**9) a memory problem? What does range actually create?`,answer:"No. range is a **lazy** sequence object: it stores only start, stop, and step, and manufactures each number on demand — `range(10**9)` costs a few dozen bytes, not a billion ints. It is also smarter than a generator: it supports len(), indexing, and `x in range(...)` membership in O(1) (arithmetic, not scanning). Contrast for the interviewer: `list(range(10**9))` DOES materialize everything and will eat tens of GB — laziness is lost the moment you wrap it in list(). This is the first instance of a theme Python repeats everywhere: prefer lazy sequences until you truly need the whole thing at once.",isCaseBased:!1},{question:`True is secretly an int — explain, and give one practical use.`,answer:"bool is a subclass of int: True behaves as 1, False as 0, so `True + True == 2` and `True == 1` is True. Practical idiom: count matches with `sum(x > 10 for x in data)` — each comparison yields a bool, sum treats them as 0/1. It also explains oddities like `True in [1, 2]` being True (equality with 1). Tradeoff: the counting idiom is concise and Pythonic, but readers unfamiliar with bool-as-int may prefer an explicit loop counter — know your audience. Historical note if asked: bool arrived in Python 2.3, and making it an int subclass kept all the old 0/1-based code working.",isCaseBased:!1}],flashcards:[{front:`What does assignment (=) do in Python?`,back:"Binds a name to an object — a sticky note, never a copy. `y = x` gives two names for ONE object."},{front:`/ vs //`,back:`/ is true division, ALWAYS float. // is floor division — rounds toward negative infinity: -7 // 2 == -4.`},{front:`input() returns…`,back:`Always a str, whatever was typed. Convert with int()/float() before math or comparison.`},{front:`The falsy values`,back:`0, 0.0, '' (empty string), None, False — plus empty containers. Everything else is truthy.`},{front:`What do and / or return?`,back:"An OPERAND, not a bool — and they short-circuit (stop as soon as the answer is known). `'' or 'x'` → 'x'."},{front:`range(a, b) yields…`,back:`a, a+1, …, b-1 — start included, stop EXCLUDED. Lazy: numbers made on demand, O(1) memory.`},{front:`Loop else runs when…`,back:`The loop finished WITHOUT break. Mnemonic: "no break happened". Use: not-found branch of a search loop.`},{front:`Checking for None`,back:"`x is None` — identity against the singleton. Not == (overridable), not `if x:` (0 and '' are falsy too)."},{front:`0.1 + 0.2 == 0.3?`,back:`False — floats are binary approximations. Compare with math.isclose(); use integer cents or decimal for money.`}],mindmapMarkdown:`- Variables, Types & Control Flow
  - Names & objects
    - assignment = sticky note, no copy
    - y = x → one object, two names
    - types live on objects, not names
  - Core types
    - int (no overflow)
    - float (binary → 0.1+0.2 trap)
    - str, bool (int subclass)
    - None → test with is None
  - Operators
    - / float · // floor to −∞
    - % sign of divisor · ** power
    - chaining: 18 <= age < 65
    - and/or: lazy, return operand
  - I/O
    - print(end=), f'{x:.2f}'
    - input() → always str
  - Branching
    - if / elif / else, first True wins
    - indentation IS syntax
  - Loops
    - while = until, for + range = each
    - range: lazy, stop excluded
    - break / continue
    - loop-else = "no break"`};export{e as default};