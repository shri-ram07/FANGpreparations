var e={id:`dbms-l1-subqueries-ctes`,subjectId:`dbms`,level:1,title:`Subqueries, CTEs & Views`,whyItMatters:`The moment a SQL question says "above average", "never ordered", "per department", or "second highest", a plain SELECT stops being enough — the filter itself must be computed from the data. Subqueries and CTEs are how. This module also carries two guaranteed interview probes: the NOT IN + NULL trap that silently empties a report, and correlated vs non-correlated — the distinction every SQL round checks.`,estMinutes:45,sections:[{type:`intuition`,title:`A query inside a query: compute the filter first`,md:`"Who earns above average?" You cannot write \`WHERE salary > average\` — SQL has no idea what the average is until something computes it.

- So compute it in place: a **subquery** — a full SELECT in parentheses, sitting where a value would sit.
- A **scalar subquery** returns exactly one row with one column: a single value, as if you had typed the number yourself.
- The engine runs the inner query first, gets 1,853,333, then the outer query behaves as if that were hard-coded.
- This is the most-asked SQL pattern after joins. Own it before anything fancier.`},{type:`code`,lang:`sql`,title:`The canonical scalar subquery: above-average salary`,code:`-- Who earns above the company average?
SELECT name, salary
FROM employees
WHERE salary > (SELECT AVG(salary) FROM employees)
ORDER BY salary DESC;
-- 6 rows: Ananya, Harsh, Farhan, Dev, Bharat, Gauri (avg = 1,853,333)
-- Chitra and Esha sit at 1,850,000 -- out by 3,333. Averages are cruel.`,annotations:{4:`The subquery runs once, produces one value, and the comparison proceeds as if the number were literal. No reference to the outer row — that detail matters enormously in a minute.`,6:`Desk-check against the seeded employees DB: total 27,800,000 over 15 people = 1,853,333.33. Six salaries clear it.`}},{type:`note`,md:"The scalar contract: **one row, one column**. If the subquery could return two rows, `>` is meaningless — Postgres and MySQL throw an error, SQLite silently uses the *first* row, which is worse than an error. Guarantee the shape: aggregates like `AVG` and `MAX` always return exactly one row."},{type:`intuition`,title:`IN: membership in a list you did not type`,md:'`WHERE id IN (1, 6, 10)` checks membership in a hand-typed list. A subquery builds the list on demand: `WHERE id IN (SELECT manager_id FROM employees)` — "is this id anyone\'s manager?"\n\n- `IN` tolerates NULLs in the list: it only needs one TRUE match, and a real match gives it one.\n- `NOT IN` does not. One NULL in the list and the whole result is **empty**. Not slightly wrong — EMPTY.\n- Why: `id NOT IN (1, 6, NULL)` unrolls to `id <> 1 AND id <> 6 AND id <> NULL`. Comparing anything to NULL (NULL means "no value here") yields **UNKNOWN**, and `WHERE` keeps only TRUE.\n- The rule: `NOT IN` only against columns that can never be NULL. Everything else → `NOT EXISTS`.'},{type:`code`,lang:`sql`,title:`IN works, NOT IN detonates — watch it happen`,code:`-- Managers: every id that appears as someone's manager_id
SELECT name FROM employees
WHERE id IN (SELECT manager_id FROM employees);
-- 6 rows: Ananya, Bharat, Farhan, Gauri, Jai, Nikhil

-- Non-managers. The trap:
SELECT name FROM employees
WHERE id NOT IN (SELECT manager_id FROM employees);
-- 0 rows. Not nine. ZERO.

-- Fix 1: kick the NULLs out of the list
SELECT name FROM employees
WHERE id NOT IN (SELECT manager_id FROM employees
                 WHERE manager_id IS NOT NULL);   -- 9 rows

-- Fix 2, the pro habit: NOT EXISTS shrugs at NULLs
SELECT e.name FROM employees e
WHERE NOT EXISTS (SELECT 1 FROM employees m
                  WHERE m.manager_id = e.id);     -- same 9 rows`,annotations:{3:`The list contains NULL (the four department heads report to nobody) — IN does not care. A match is a match; the NULL entries just never match anyone.`,8:`The landmine. The list is (1, 2, 6, 7, 10, 14, NULL). For a non-manager like Chitra, every <> test passes except "id <> NULL" — UNKNOWN. TRUE AND UNKNOWN = UNKNOWN, WHERE drops the row. Every row. Silently.`,18:`NOT EXISTS asks "does a matching row exist?" — it reasons about rows, never compares a value to NULL. This is why it is the default anti-join in production code.`}},{type:`intuition`,title:`Correlated vs non-correlated: THE distinction`,md:`Non-correlated: the inner query is self-contained. The engine runs it **once**, keeps the value, moves on. The \`AVG(salary)\` above never mentions the outer row — one run, fifteen comparisons.

- **Correlated**: the inner query references a column of the outer row (like \`e.dept_id\`). It cannot run alone — it is a question asked *about each row*.
- Mental model: a loop. For each outer row, plug its values into the subquery, re-run it, test the result.
- 15 rows → 15 executions. A million outer rows over a million-row inner scan → a trillion operations, if nothing intervenes.
- Spotting trick: cover the outer query with your hand. If the subquery still runs by itself, it is non-correlated.`},{type:`code`,lang:`sql`,title:`Correlated: above YOUR OWN department average`,code:`-- Who beats their own department's average?
SELECT e.name, e.salary
FROM employees e
WHERE e.salary > (SELECT AVG(x.salary)
                  FROM employees x
                  WHERE x.dept_id = e.dept_id);
-- 7 rows: Ananya, Dev | Farhan, Harsh | Jai, Mahi | Nikhil
-- Dept avgs: Eng 2,280,000  Data 2,225,000  Sales 1,362,500  HR 1,025,000`,annotations:{6:`e.dept_id belongs to the OUTER query. That single reference is what makes this correlated: conceptually, the inner AVG re-runs for each of the 15 employees with a different dept_id plugged in.`,7:`Compare with the company-wide version: Jai (1,400,000) is far below the company average but above Sales's 1,362,500 — correlation changes the question, not just the plan.`}},{type:`visual`,component:`PointerBoxDiagram`,props:{title:`Correlated subquery execution: the re-run, made visible`,notice:`Four of the 15 outer rows shown. The arrow is the subquery being re-pointed — and re-run — at each row in turn.`,leftLabel:`the subquery (per run)`,rightLabel:`outer rows (employees e)`,frames:[{note:`Run #1 — outer row Dev. Plug in e.dept_id = 1: AVG(Engineering) = 2,280,000. Dev earns 2,400,000 > that → Dev survives the WHERE.`,stack:[{name:`AVG WHERE dept_id = 1`,value:`2,280,000`,to:`r1`}],heap:[{id:`r1`,value:`Dev · 2,400,000`,label:`Engineering`},{id:`r2`,value:`Gauri · 1,950,000`,label:`Data`},{id:`r3`,value:`Mahi · 1,550,000`,label:`Sales`},{id:`r4`,value:`Ojas · 950,000`,label:`HR`}]},{note:`Run #2 — Gauri. Same subquery text, new parameter: AVG(Data) = 2,225,000. Gauri earns 1,950,000 < that → filtered out. The subquery genuinely executed AGAIN.`,stack:[{name:`AVG WHERE dept_id = 2`,value:`2,225,000`,to:`r2`,danger:!0}],heap:[{id:`r1`,value:`Dev · 2,400,000`,label:`kept`},{id:`r2`,value:`Gauri · 1,950,000`,label:`Data`,freed:!0},{id:`r3`,value:`Mahi · 1,550,000`,label:`Sales`},{id:`r4`,value:`Ojas · 950,000`,label:`HR`}]},{note:`Run #3 — Mahi. AVG(Sales) = 1,362,500. Mahi earns 1,550,000 > that → kept. Third execution of the same inner query, third different answer.`,stack:[{name:`AVG WHERE dept_id = 3`,value:`1,362,500`,to:`r3`}],heap:[{id:`r1`,value:`Dev · 2,400,000`,label:`kept`},{id:`r2`,value:`Gauri · 1,950,000`,label:`filtered out`,freed:!0},{id:`r3`,value:`Mahi · 1,550,000`,label:`Sales`},{id:`r4`,value:`Ojas · 950,000`,label:`HR`}]},{note:`Run #4 — Ojas. AVG(HR) = 1,025,000. Ojas earns 950,000 < that → out. Full table: 15 rows = 15 runs. Cost model: outer rows × cost of one subquery run.`,stack:[{name:`AVG WHERE dept_id = 4`,value:`1,025,000`,to:`r4`,danger:!0}],heap:[{id:`r1`,value:`Dev · 2,400,000`,label:`kept`},{id:`r2`,value:`Gauri · 1,950,000`,label:`filtered out`,freed:!0},{id:`r3`,value:`Mahi · 1,550,000`,label:`kept`},{id:`r4`,value:`Ojas · 950,000`,label:`HR`,freed:!0}]},{note:`Contrast — the NON-correlated version. No e.* inside means nothing to re-point: the subquery runs ONCE, the value is cached, and the outer scan just compares every row against it. One execution vs one-per-row: that is the whole performance story.`,stack:[{name:`AVG(salary) — runs ONCE`,value:`1,853,333`,to:`c1`}],heap:[{id:`c1`,value:`1,853,333`,label:`computed once, reused 15×`},{id:`r1`,value:`Dev · 2,400,000`,label:`compared vs cache`},{id:`r2`,value:`Gauri · 1,950,000`,label:`compared vs cache`},{id:`r3`,value:`Mahi · 1,550,000`,label:`compared vs cache`}]}]}},{type:`note`,md:'The per-row loop is the **mental model**, not always the physical plan. Postgres and MySQL 8 often *decorrelate* — silently rewrite the correlated subquery into a join or a one-pass grouped aggregate — so the measured cost can be far below rows × subquery-cost. SQLite is more literal. The interview move: explain the loop, add "the optimizer may rescue this", and finish with "I would check the plan with `EXPLAIN` rather than guess."'},{type:`intuition`,title:`EXISTS: semi-join thinking`,md:'`EXISTS (subquery)` asks one thing: does at least one row come back? TRUE at the first hit, FALSE if the scan ends empty — it never fetches the actual data.\n\n- This is a **semi-join**: probe the other table, keep or drop the outer row, produce no columns from the inner one, match at most once.\n- That "at most once" is the superpower: a join used as a filter duplicates the outer row once per match; `EXISTS` cannot duplicate anything.\n- `NOT EXISTS` is the safe anti-join — it reasons about row existence, not value equality, so NULLs cannot poison it the way they poison `NOT IN`.\n- Convention: write `SELECT 1` inside. The value is discarded, so give the engine nothing worth computing.'},{type:`code`,lang:`sql`,title:`EXISTS and NOT EXISTS on the departments table`,code:`-- Departments that actually have people
SELECT d.name FROM departments d
WHERE EXISTS (SELECT 1 FROM employees e
              WHERE e.dept_id = d.id);
-- Engineering, Data, Sales, HR. Flip to NOT EXISTS -> just Research.`,annotations:{3:`SELECT 1 because EXISTS only checks whether ANY row comes back — the selected value is ignored and the engine stops at the first match. This is correlated (d.id), but each run can exit immediately.`,5:`Research is the seeded empty department — the row EXISTS filters out and NOT EXISTS would keep.`}},{type:`intuition`,title:`Subqueries in FROM: derived tables`,md:`A subquery can also sit in \`FROM\`, where a table would. Its result set becomes a **derived table** — a temporary, unnamed table that lives for this one query.

- Classic use: aggregate first, join second. Build the per-department averages ONCE, then join every employee to their department's row.
- Same 7-row answer as the correlated version — but the shape is explicitly "one aggregation pass + one join". No per-row re-run to worry about.
- Rule engines enforce: a derived table **must have an alias** (\`AS da\`) — its columns need a name to be referenced.`},{type:`code`,lang:`sql`,title:`Derived table: aggregate once, join once`,code:`SELECT e.name, e.salary, da.avg_sal
FROM employees e
JOIN (SELECT dept_id, AVG(salary) AS avg_sal
      FROM employees
      GROUP BY dept_id) AS da
  ON da.dept_id = e.dept_id
WHERE e.salary > da.avg_sal;
-- Same 7 rows as the correlated version -- computed once, joined once.`,annotations:{3:`The parenthesized SELECT is the derived table: 4 rows of (dept_id, avg_sal), built exactly once, then treated like any other table in the join.`,8:`This is what "decorrelating by hand" looks like — the rewrite you offer when a correlated subquery is too slow and the optimizer did not save you.`}},{type:`intuition`,title:`CTEs: name your steps`,md:'Nest three derived tables and the query reads inside-out, like unwrapping parcels. A **CTE** (common table expression) flattens it: `WITH name AS (…)` defines a named step up front.\n\n- Read top to bottom: `dept_avg`, then `above` (which *uses* dept_avg), then the final SELECT. A pipeline — variables for queries.\n- Reuse: several later steps can reference one CTE; a derived table would have to be copy-pasted into each spot.\n- Debugging: run `SELECT * FROM above` to inspect any middle step alone. Try doing that with a four-deep nested subquery.\n- Honesty clause: CTEs are a readability tool first. Engines may inline or materialize them, so never promise "the CTE caches it" — that is engine-specific.'},{type:`code`,lang:`sql`,title:`The multi-step pipeline: WITH a AS (…), b AS (…) SELECT …`,code:`WITH dept_avg AS (
  SELECT dept_id, AVG(salary) AS avg_sal
  FROM employees
  GROUP BY dept_id
),
above AS (
  SELECT e.dept_id
  FROM employees e
  JOIN dept_avg d ON d.dept_id = e.dept_id
  WHERE e.salary > d.avg_sal
)
SELECT dp.name, COUNT(*) AS beat_the_avg
FROM above a
JOIN departments dp ON dp.id = a.dept_id
GROUP BY dp.name;
-- Data 2 | Engineering 2 | HR 1 | Sales 2`,annotations:{1:`WITH opens the pipeline. Each step gets a name; commas separate steps; the final bare SELECT consumes them.`,9:`A later CTE freely references an earlier one — that chaining is what turns a monster query into readable stages.`,12:`The final SELECT reads like a sentence because every hard part already has a name. This style is the difference between SQL people review and SQL people fear.`}},{type:`note`,md:'**Recursive CTE**, one honest note: `WITH RECURSIVE team AS (seed-row SELECT UNION ALL step that joins back to team)` re-applies the step until no new rows appear — the only way plain SQL walks a hierarchy of unknown depth. The org-chart walk: seed = Farhan; step = "everyone whose manager is already in team" → pulls Gauri and Harsh, then Ishita (who reports to Gauri), then stops. SQLite supports it, so the walk runs in the playground. For now, know the shape and the org-chart story — deeper recursion mechanics are a level-3 concern.'},{type:`intuition`,title:`Views: a saved query wearing a table costume`,md:`\`CREATE VIEW v AS SELECT …\` stores the QUERY under a name — zero rows stored. Querying the view splices the saved SELECT into your query's plan: a **virtual table**.

- Use 1 — **security slice**: analysts get a view exposing salaries only aggregated by department, never per person. Grant them the view, not the table.
- Use 2 — **canned complexity**: the gnarly four-join "enriched orders" query, written correctly once by one person, queried simply forever by everyone.
- Always fresh: the underlying query re-runs on every use. Also never free: the join cost is paid on every use.
- Views take no indexes of their own, and complex ones reject writes in most engines — they are read-time conveniences, not storage.`},{type:`code`,lang:`sql`,title:`Create a view, query it like a table`,code:`CREATE VIEW dept_salaries AS
SELECT d.name AS dept, e.name AS employee, e.salary
FROM employees e
JOIN departments d ON d.id = e.dept_id;

-- Anyone can now query it with no idea a join is involved:
SELECT dept, COUNT(*) AS headcount
FROM dept_salaries
GROUP BY dept ORDER BY dept;
-- Data 4 | Engineering 5 | HR 2 | Sales 4`,annotations:{1:`This stores the query text in the catalog — no data is copied, no storage is used beyond the definition itself.`,8:`Each query against the view re-runs the join underneath. Fresh results guaranteed; free results not.`}},{type:`note`,md:"**Materialized view** = the opposite trade: store the RESULT rows like a real table, serve them instantly, accept that they go stale until refreshed (Postgres: `REFRESH MATERIALIZED VIEW`). SQLite has no materialized views — emulate with `CREATE TABLE AS SELECT` plus your own refresh job. Interview one-liner: view = always fresh, pay per query; materialized view = instant reads, pay in staleness and refresh cost."},{type:`intuition`,title:`Subquery or join? The interview tradeoff`,md:`Both can express most filters. The choice has actual rules — recite them, do not vibe them:

- Need columns *from the other table* in your output → **join**. A subquery in WHERE can only filter; it cannot project the inner table's columns.
- Pure existence filter ("has at least one…", "never…") → **EXISTS / NOT EXISTS**. A join here duplicates outer rows on one-to-many matches and forces a DISTINCT to clean up.
- Aggregate compared per group → **derived table or CTE + join** on big data; a correlated subquery risks the per-row loop if the optimizer does not decorrelate.
- Readability tiebreak: a scalar subquery often reads exactly like the English question — fine for one-offs and interviews on small data.
- Whichever you pick, name the other option and why not. That sentence *is* the interview answer.`},{type:`note`,md:`Every query in this module runs verbatim on the seeded employees database — [Open the SQL playground](/playground/sql) and retype them from memory: the above-average filter, the NOT IN trap (watch it return nothing), its NOT EXISTS fix, and the CTE pipeline. Ten minutes of typing beats an hour of reading.`}],quiz:[{question:`WHERE salary > (SELECT AVG(salary) FROM employees) — what must that subquery return for the comparison to make sense?`,options:[{text:`Exactly one row with one column — a scalar`,explanation:`Correct. It sits where a number would sit, so it must BE one value. Aggregates guarantee this shape.`},{text:`One column, any number of rows`,explanation:`That is the IN-list shape. A > comparison against many rows is meaningless — Postgres/MySQL error, and SQLite silently takes the first row, which is worse.`},{text:`Anything — SQL flattens the result automatically`,explanation:`SQL never flattens. Shape mismatches are errors (or silent first-row picks in SQLite), not conveniences.`}],correct:0},{question:`id NOT IN (1, 6, 10, NULL) — what does this evaluate to for an id NOT in the list, like 3?`,options:[{text:`TRUE — 3 is not in the list`,explanation:`Intuitive and wrong. The expansion includes "3 <> NULL", which is UNKNOWN, and TRUE AND UNKNOWN = UNKNOWN.`},{text:`UNKNOWN — so the WHERE drops the row, and every other row too`,explanation:`Correct. One NULL in the list makes NOT IN unable to ever produce TRUE. Result: an empty report with no error message.`},{text:`A runtime error`,explanation:`No error is raised — the silence is exactly what makes this landmine dangerous in production.`}],correct:1},{question:`What makes a subquery correlated?`,options:[{text:`It references a column from the outer query`,explanation:`Correct. That reference means it cannot run standalone — conceptually it re-runs once per outer row.`},{text:`It appears inside the WHERE clause`,explanation:`Location is irrelevant — both correlated and non-correlated subqueries live in WHERE, FROM, and SELECT.`},{text:`It returns more than one row`,explanation:`That is about result shape (scalar vs list), a completely separate axis from correlation.`},{text:`It uses an aggregate function`,explanation:`Both kinds use aggregates freely — the company-wide AVG was non-correlated, the per-department AVG was correlated.`}],correct:0},{question:`Why do people write SELECT 1 inside an EXISTS subquery?`,options:[{text:`EXISTS only checks whether ANY row comes back — the value is ignored, so 1 signals "nothing to compute here"`,explanation:`Correct. The engine can stop at the first matching row without materializing anything. SELECT * would work identically; 1 states the intent.`},{text:`It returns the count of matching rows`,explanation:`That would be COUNT(*). EXISTS is a boolean — one hit is enough, counting would be wasted work.`},{text:`It selects the first column of the table`,explanation:`It is the literal number 1, not a column position. SQL only treats numbers as positions in ORDER BY.`}],correct:0},{question:`A CTE defined once is referenced by two later steps. Is it guaranteed to be computed only once?`,options:[{text:`Yes — CTEs are always materialized once`,explanation:`Engine-specific, not guaranteed. Postgres may inline CTEs since v12; MySQL makes its own choices. Never promise caching.`},{text:`No — engines may inline or materialize it; the readability win is guaranteed, the caching win is not`,explanation:`Correct. Treat CTEs as named steps for humans first. If single-computation matters, verify with the query plan.`},{text:`CTEs cannot be referenced twice`,explanation:`They can — multiple references is precisely the reuse advantage over copy-pasting a derived table.`}],correct:1},{question:`What does CREATE VIEW actually store?`,options:[{text:`The query text — rows are computed fresh each time the view is queried`,explanation:`Correct. A view is a virtual table: zero stored rows, always-current results, underlying cost paid per query.`},{text:`A snapshot of the rows at creation time`,explanation:`That describes a MATERIALIZED view — stored rows, instant reads, staleness until refreshed.`},{text:`Both the query and a cached copy of its rows`,explanation:`No engine does both by default — you choose: virtual (query only) or materialized (rows, needing refresh).`}],correct:0},{question:`You filter employees to those with "at least one sale" by joining to a sales table. What goes wrong that EXISTS avoids?`,options:[{text:`Duplicate output rows — one per matching sale — forcing a DISTINCT; EXISTS is a semi-join and keeps each outer row at most once`,explanation:`Correct. A join produces one output row per MATCH; an employee with 50 sales appears 50 times. EXISTS answers yes/no per outer row.`},{text:`The join silently drops employees with sales`,explanation:`An inner join keeps every matched row — the problem is too many copies, not too few.`},{text:`EXISTS is always slower, so the join is still right`,explanation:`Backwards — engines implement EXISTS as a semi-join that can stop at the first match, often faster than join + DISTINCT.`}],correct:0}],interviewQuestions:[{question:`Correlated vs non-correlated subquery — define both, give an example of each, and state the performance implication.`,answer:`Non-correlated: the inner query is self-contained — WHERE salary > (SELECT AVG(salary) FROM employees). It runs once, the value is cached, done. Correlated: the inner query references an outer column — WHERE e.salary > (SELECT AVG(salary) FROM employees x WHERE x.dept_id = e.dept_id). Conceptually it re-runs once per outer row, so the naive cost is outer-rows × inner-cost — a million × a million is a disaster. Two qualifiers that mark a strong answer: modern optimizers often decorrelate into a join or grouped aggregate, so the mental model and the physical plan can differ; and the deterministic check is "cover the outer query — does the subquery still run alone?"`,isCaseBased:!1},{question:`Case: your "customers who never ordered" report worked in dev but returns ZERO rows in prod. The query is WHERE id NOT IN (SELECT customer_id FROM orders). Debug it.`,answer:`Prime suspect: NULLs in orders.customer_id in prod (guest checkouts, data imports) that dev did not have. NOT IN unrolls to id <> v1 AND id <> v2 AND … — one NULL in the list makes one comparison UNKNOWN, and TRUE AND UNKNOWN = UNKNOWN, so no row can ever pass. No error, just an empty report. Verify: SELECT COUNT(*) FROM orders WHERE customer_id IS NULL. Fix: NOT EXISTS (SELECT 1 FROM orders o WHERE o.customer_id = c.id) — it reasons about row existence, immune to NULLs — or add WHERE customer_id IS NOT NULL inside the NOT IN list. Prevention worth naming: a NOT NULL constraint on the FK column if business rules allow, and a team norm of NOT EXISTS for anti-joins.`,isCaseBased:!0},{question:`IN vs EXISTS vs JOIN for an existence filter — when does each win?`,answer:`IN: cleanest to read when the list is small and the column is NOT NULL; the engine typically hashes the list. EXISTS: the semantic fit — semi-join, stops at first match, never duplicates outer rows, NULL-safe in its NOT form; the default for "has at least one". JOIN: mandatory the moment you need the inner table's columns in the output — but as a pure filter it multiplies rows on one-to-many matches and needs DISTINCT. Close with honesty: modern optimizers frequently compile all three to the same plan, so the choice is usually about correctness edges (NOT IN + NULL) and reader clarity, not raw speed — and EXPLAIN settles disputes.`,isCaseBased:!1},{question:`Second-highest salary using only subqueries. Then poke holes in your own solution.`,answer:`SELECT MAX(salary) FROM employees WHERE salary < (SELECT MAX(salary) FROM employees) — the inner scalar finds the top (3,200,000 in the seeded DB), the outer finds the max below it (2,750,000, Harsh). Holes to volunteer: ties at the top are handled correctly because MAX works on distinct-value logic — five people at the top salary still leave "the max below top" as the true second-highest; if no second value exists (all salaries equal, or one row), it returns NULL — which LeetCode 176 actually wants, but a report might not; and generalizing to Nth-highest this way needs N nested subqueries — that is the cue to mention DISTINCT + ORDER BY + LIMIT/OFFSET, and DENSE_RANK once window functions are on the table.`,isCaseBased:!1},{question:`Give me your decision rule: subquery or join?`,answer:`Three questions in order. (1) Do I need the other table's columns in my output? Yes → join; a WHERE-subquery can only filter. (2) Is it a pure existence test? Yes → EXISTS/NOT EXISTS — semi-join semantics, no duplicate blowup, NULL-safe negation. (3) Is it an aggregate compared per group on big data? → compute the aggregate once in a derived table or CTE and join it, rather than a correlated subquery that risks the per-row loop. Everything else is readability: a scalar subquery that mirrors the English question is fine. And always end with the tradeoff sentence — naming the road not taken is what separates a memorized answer from an engineering one.`,isCaseBased:!1},{question:`Case: you inherit a 200-line dashboard query — five levels of nested derived tables. The team is scared to touch it. What do you do, and what does it buy?`,answer:`Refactor to a CTE pipeline: WITH step1 AS (…), step2 AS (…), … — each nested level becomes a named stage read top-to-bottom. Guaranteed wins: reviewability (each stage states intent), debuggability (SELECT * FROM step2 in isolation to find where numbers go wrong), reuse (two stages can share one upstream CTE instead of duplicated nesting), and safe incremental refactoring — verify each stage matches the old subquery before moving on. NOT guaranteed: performance. Engines may inline or materialize CTEs, so the plan can be identical — or occasionally worse if a materialization blocks predicate pushdown. So I benchmark before/after with EXPLAIN and say the refactor is for humans first; any speedup is a bonus.`,isCaseBased:!0},{question:`How would you list everyone under a given manager, any depth? Sketch the recursive CTE.`,answer:`WITH RECURSIVE team AS (SELECT id, name FROM employees WHERE id = :boss UNION ALL SELECT e.id, e.name FROM employees e JOIN team t ON e.manager_id = t.id) SELECT * FROM team. Two parts: the seed (the boss row) and the step (everyone whose manager is already in team), re-applied until no new rows appear. On the seeded DB with Farhan as boss: seed Farhan → step 1 adds Gauri and Harsh → step 2 adds Ishita (reports to Gauri) → step 3 adds nothing, stop. Points worth adding: this is the only way plain SQL traverses unknown-depth hierarchies; a cycle in the data (a manages b manages a) loops forever unless you track depth or the engine caps recursion; and an adjacency-list schema (manager_id) is exactly what makes this necessary.`,isCaseBased:!1},{question:`Case: a correlated subquery over the orders table was fine at launch; two years and 50M rows later, the query takes minutes. Walk through your response.`,answer:`Diagnose first: EXPLAIN. If the plan shows the subquery executing per outer row (a dependent/correlated subquery node), the cost is outer-rows × inner-scan — that is the regression, data growth just exposed it. Fixes in order: (1) index the correlation column (e.g. orders.customer_id) so each per-row probe is an index seek, not a scan — often a 100× win for one line of DDL; (2) decorrelate by hand — compute the aggregate once in a derived table or CTE (GROUP BY customer_id) and JOIN it, turning N executions into one pass plus a hash join; (3) if the engine supports it, check whether a newer version decorrelates automatically. Name the tradeoff: the join version computes aggregates for ALL groups even if the outer query filters to a few — for highly selective outer filters, an indexed correlated probe can actually win. That nuance is the senior answer.`,isCaseBased:!0},{question:`When do you reach for a view, and what does it NOT solve?`,answer:`Reach for it in two situations. Security slice: expose a subset of columns/rows — analysts query dept_salary_summary and physically cannot see individual salaries; permissions attach to the view, not the table. Canned complexity: a hairy multi-join written correctly once, consumed simply by everyone — an API for your schema. What it does NOT solve: performance. A view stores only the query; every use re-runs it, so putting a slow query in a view produces a slow view. It also takes no indexes of its own, and complex views reject writes in most engines. If reads must be instant, that is the materialized-view conversation — stored rows, refresh job, staleness tradeoff.`,isCaseBased:!1},{question:`The anti-join three ways — NOT IN, NOT EXISTS, LEFT JOIN … IS NULL. Compare them.`,answer:`All express "rows in A with no match in B". NOT IN: reads most naturally but detonates on NULLs in the list — one NULL and the result is empty; only safe against provably NOT NULL columns. NOT EXISTS: the production default — semi-join semantics, NULL-safe, optimizes well everywhere. LEFT JOIN … WHERE b.key IS NULL: keeps every A row, unmatched ones carry NULLs in B's columns, then filters to exactly those; works on every engine, exposes the join mechanics explicitly, and handily produces B's columns if you need to show "what would have matched". Modern optimizers usually compile NOT EXISTS and the LEFT JOIN form to the same anti-join plan — so the real differences are the NOT IN NULL hazard and reader taste. Interviewers ask this to hear the word "NULL" — say it early.`,isCaseBased:!1}],flashcards:[{front:`Scalar subquery`,back:`A subquery returning exactly one row, one column — usable anywhere a value goes. Canonical: WHERE salary > (SELECT AVG(salary) FROM employees).`},{front:`NOT IN + NULL landmine`,back:`One NULL in the list → id <> NULL is UNKNOWN → NOT IN can never be TRUE → EMPTY result, no error. Fix: filter IS NOT NULL, or use NOT EXISTS.`},{front:`Correlated vs non-correlated, one breath`,back:`Non-correlated: self-contained, runs ONCE. Correlated: references an outer column, conceptually re-runs per outer row. Test: cover the outer query — does the inner still run alone?`},{front:`EXISTS semantics`,back:`TRUE at the first matching row, value ignored (hence SELECT 1) — a semi-join: filter the outer row, never duplicate it, never project inner columns.`},{front:`Why NOT EXISTS beats NOT IN for anti-joins`,back:`It reasons about row existence, not value comparisons — NULLs cannot poison it. The production-default anti-join.`},{front:`Derived table`,back:`A subquery in FROM, treated as a temporary table; alias required. Pattern: aggregate once (GROUP BY), then join — the manual decorrelation of a slow correlated subquery.`},{front:`CTE (WITH)`,back:`Named query steps read top-to-bottom: WITH a AS (…), b AS (…) SELECT …. Later steps use earlier ones. Readability guaranteed; caching engine-specific.`},{front:`Recursive CTE shape`,back:`WITH RECURSIVE t AS (seed SELECT UNION ALL step joining back to t) — re-applies the step until no new rows. The org-chart walk of unknown depth.`},{front:`View vs materialized view`,back:`View: stores the QUERY — always fresh, cost paid per use, no own indexes. Materialized: stores the ROWS — instant reads, goes stale, needs refresh. SQLite has only plain views.`},{front:`Subquery vs join decision`,back:`Need inner columns in output → join. Pure existence filter → EXISTS (no duplicate blowup). Per-group aggregate on big data → derived table/CTE + join.`}],mindmapMarkdown:`- Subqueries, CTEs & Views
  - Scalar subqueries
    - one row, one column
    - salary > (SELECT AVG…) — canonical
  - IN / NOT IN
    - NOT IN + NULL → EMPTY result
    - fix: IS NOT NULL, or NOT EXISTS
  - Correlated vs non-correlated
    - non-correlated once; correlated per outer row
    - optimizer may decorrelate — check EXPLAIN
  - EXISTS / NOT EXISTS
    - semi-join: stop at first hit (SELECT 1)
    - NOT EXISTS = NULL-safe anti-join
  - Derived tables (FROM subqueries)
    - alias required
    - aggregate once, join once
  - CTEs (WITH)
    - WITH a AS (…), b AS (…) — named pipeline
    - recursive: seed UNION ALL step (org chart)
  - Views
    - stored query = virtual table; security slice
    - materialized view: rows stored, goes stale
  - Subquery vs join
    - inner columns needed → join
    - existence filter → EXISTS (no duplicates)`};export{e as default};