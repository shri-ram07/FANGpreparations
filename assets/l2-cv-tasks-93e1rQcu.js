var e={id:`dl-l2-cv-tasks`,subjectId:`dl`,level:2,title:`The CV Task Map: Detection and Segmentation`,whyItMatters:`Classification, detection and segmentation share a backbone and differ entirely in what comes after it. Each head brings a specific structural problem — and detection's is that 99.95% of its predictions are background.`,assumes:[`You have read *CNNs*, so you know what a feature map and a stride are`,`You know what IoU is`],estMinutes:20,sections:[{type:`intuition`,title:`Same backbone, different question`,md:`All three tasks run the same convolutional stack. What differs is what you ask of the final feature map.

**Classification** asks *what is in this image* — one label for the whole thing. **Detection** asks *what, and where* — a set of boxes with labels. **Segmentation** asks *what is this pixel* — a label for every one.

The output shapes tell you what each will cost. For a 640x640 image and 80 classes: classification produces **80** numbers, detection with 100 boxes produces **8,500**, and semantic segmentation produces **32,768,000**. That three-order-of-magnitude spread is why the architectures diverge so sharply after the backbone.`},{type:`intuition`,title:`Detection's structural problem is a set`,md:`Classification has a fixed output shape. Detection does not: an image contains zero objects or forty, and there is no natural ordering for them, so the head must produce a **set** of variable size.

The standard answer is to make the output fixed again. Cover the image with a dense grid of pre-defined boxes — **anchors** — at several scales and aspect ratios, and ask a fixed question at each: is there an object here, of what class, and how should this box be adjusted?

Now the output shape is fixed and the problem becomes classification plus regression. The cost is arithmetic, and it is severe.`},{type:`code`,lang:`python`,title:`How many anchors, and how few of them are objects`,code:`total = 0
for stride in [8, 16, 32, 64, 128]:
    n = (640 // stride) ** 2
    total += n * 9                          # 3 scales x 3 aspect ratios
    print('stride %3d: %2dx%-2d = %6s locations x 9 = %s anchors'
          % (stride, 640//stride, 640//stride, f'{n:,}', f'{n*9:,}'))

print('total anchors: %s' % f'{total:,}')
for objs in [3, 10, 40]:
    print('%2d objects -> ~%d positive anchors = %.4f%% of all anchors'
          % (objs, objs*4, 100*objs*4/total))

# ---- real output ----
# stride   8: 80x80 =  6,400 locations x 9 = 57,600 anchors
# stride  16: 40x40 =  1,600 locations x 9 = 14,400 anchors
# stride  32: 20x20 =    400 locations x 9 = 3,600 anchors
# stride  64: 10x10 =    100 locations x 9 = 900 anchors
# stride 128:  5x5  =     25 locations x 9 = 225 anchors
# total anchors: 76,725
#  3 objects -> ~12 positive anchors = 0.0156% of all anchors
# 10 objects -> ~40 positive anchors = 0.0521% of all anchors
# 40 objects -> ~160 positive anchors = 0.2085% of all anchors`,annotations:{2:`Five feature-map levels from a feature pyramid, each at a different stride. Small objects are found at stride 8 where resolution is high; large ones at stride 128 where the receptive field is.`,4:`Nine anchors per location — three scales times three aspect ratios — so one grid cell can propose a tall box, a wide box and a square box at three sizes.`,18:`76,725 predictions from one image. Every one is classified and regressed on every forward pass, which is why detection costs far more than classification on the same backbone.`,20:`A typical image with 10 objects makes 0.052% of the anchors positive. Roughly 1,900 background anchors for every foreground one — and that ratio, not the boxes, is detection's hard problem.`}},{type:`note`,label:`What that imbalance breaks, and the two fixes`,md:`With 1,900 background anchors per object, an ordinary cross-entropy loss is dominated by easy negatives. Each contributes almost nothing individually and there are so many that in aggregate they drown out the objects. A model that predicts "background" everywhere achieves 99.95% accuracy and is useless.

**Two-stage detectors** (R-CNN through Faster R-CNN) avoid it by filtering first: a region proposal network produces a few hundred candidate regions, and the classifier only ever sees a sampled, roughly balanced subset of those.

**One-stage detectors** classify every anchor and fix the loss instead. **Focal loss** multiplies cross-entropy by (1 − p)^γ, so an easy negative predicted at p = 0.99 contributes a ten-thousandth of its usual weight at γ = 2, while a hard example is barely touched. That single change is what made one-stage detectors match two-stage accuracy at one-stage speed.

The current generation removes anchors altogether — **DETR** predicts a fixed set of 100 boxes and matches them to the truth with the Hungarian algorithm, and **YOLO** since v8 predicts box distances directly from each point.`},{type:`code`,lang:`python`,title:`Why segmentation needs skip connections`,code:`r = 640
for L in range(6):
    print('after %d downsamples: %dx%d' % (L, r, r))
    r //= 2
print('a 5-pixel-wide object at 640 is %.2f pixels at 1/32' % (5 / 32))

# ---- real output ----
# after 0 downsamples: 640x640
# after 1 downsamples: 320x320
# after 2 downsamples: 160x160
# after 3 downsamples: 80x80
# after 4 downsamples: 40x40
# after 5 downsamples: 20x20
# a 5-pixel-wide object at 640 is 0.16 pixels at 1/32`,annotations:{4:`The backbone downsamples to build a large receptive field — it must see a lot of context to know what an object is. That is exactly what a classifier needs.`,13:`And exactly what segmentation cannot afford. At 20x20 the network knows WHAT is there and has thrown away WHERE with any precision.`,14:`A thin structure five pixels wide does not survive to 1/32 resolution at all. Upsampling from the bottleneck alone recovers a blurred blob, which is why encoder-decoder networks pass the high-resolution encoder features across to the decoder — the U-Net skip connections.`}},{type:`note`,label:`Three kinds of segmentation, which people conflate`,md:`**Semantic** labels every pixel with a class. Two adjacent people are one region of "person" — it cannot count.

**Instance** separates objects: person 1 and person 2 are distinct masks. Mask R-CNN is the standard approach — detect, then predict a mask inside each box. It typically ignores background classes like "sky" and "road" entirely.

**Panoptic** does both: every pixel gets a class, and every countable object gets an instance id. "Things" (people, cars) get instances; "stuff" (sky, road) gets a class only.

Choose by the question. "How much of this field is diseased?" is semantic. "How many cells are in this image?" is instance. Getting this wrong is a common and expensive scoping error, because a semantic model cannot be made to count no matter how well it is trained.`},{type:`note`,label:`What all three heads share`,md:`**Transfer learning is the default.** A backbone pretrained on ImageNet or by self-supervision transfers to all three tasks; only the head is new. Training a detection backbone from scratch needs far more data than most projects have.

**Resolution is the dominant cost knob.** Compute is quadratic in the input side, so halving resolution quarters the cost — and it is usually the first thing to try when a model is too slow, and the first thing to raise when small objects are being missed.

**The metric follows the task.** Accuracy for classification, mAP for detection, mean IoU or Dice for segmentation. Dice is preferred in medical imaging because it handles the extreme foreground/background imbalance of a small lesion in a large scan better than pixel accuracy, which a model can score 99% on by predicting "healthy" everywhere — the same imbalance problem as detection, in a different head.`}],quiz:[{question:`For a 640x640 image and 80 classes, the three tasks output 80, 8,500 and 32,768,000 numbers. What does that spread explain?`,options:[{text:`That segmentation is more accurate`,explanation:`Output size says nothing about accuracy.`},{text:`Why the architectures diverge sharply after a shared backbone — each head faces a different scale of problem`,explanation:`Correct. The backbone is common; everything after it is not.`},{text:`That detection is the hardest task`,explanation:`Its difficulty comes from the set-output and imbalance problems, not the output size.`},{text:`That classification is obsolete`,explanation:`It is the cheapest and most widely used of the three.`}],correct:1},{question:`Why do detectors use anchors at all?`,options:[{text:`To improve IoU`,explanation:`IoU is how the result is scored, not why anchors exist.`},{text:`An image has a variable number of objects with no natural order — anchors turn a set-output problem into a fixed-shape classification-plus-regression one`,explanation:`Correct, and the cost of that convenience is the anchor count.`},{text:`To reduce the number of predictions`,explanation:`They produce 76,725 predictions from one image.`},{text:`Because CNNs cannot regress coordinates`,explanation:`They can, and anchor-free detectors do exactly that.`}],correct:1},{question:`With 10 objects, 0.052% of 76,725 anchors are positive. Why does that break ordinary cross-entropy?`,options:[{text:`The loss becomes numerically unstable`,explanation:`It is perfectly stable and perfectly useless.`},{text:`Easy negatives each contribute little but there are ~1,900 per object, so in aggregate they drown out the objects`,explanation:`Correct — predicting "background" everywhere scores 99.95% accuracy.`},{text:`The gradients explode`,explanation:`They are dominated by background, not large.`},{text:`Anchors overlap each other`,explanation:`Overlap is handled by NMS and is a separate issue.`}],correct:1},{question:`How does focal loss address that?`,options:[{text:`It removes background anchors before the loss`,explanation:`That is the two-stage approach; focal loss keeps them all.`},{text:`It multiplies cross-entropy by (1 − p)^γ, so an easy negative at p = 0.99 contributes a ten-thousandth of its usual weight at γ = 2`,explanation:`Correct, while a hard example is barely affected.`},{text:`It reweights by class frequency`,explanation:`That is class balancing, which does not distinguish easy from hard.`},{text:`It caps the loss at a maximum`,explanation:`It scales down easy examples rather than capping.`}],correct:1},{question:`Why do segmentation networks need skip connections from encoder to decoder?`,options:[{text:`To speed up training`,explanation:`That is a residual-connection argument, and a different one.`},{text:`The bottleneck at 20x20 knows what is there but has discarded where — a 5-pixel structure is 0.16 pixels at 1/32`,explanation:`Correct. Upsampling from the bottleneck alone recovers a blurred blob.`},{text:`To reduce parameters`,explanation:`They add connections, not remove parameters.`},{text:`To prevent overfitting`,explanation:`They restore spatial detail; regularisation is incidental.`}],correct:1},{question:`You need to count cells in a microscopy image. Which task?`,options:[{text:`Semantic segmentation`,explanation:`It labels pixels by class, so touching cells merge into one region and cannot be counted.`},{text:`Instance segmentation — it separates touching objects into distinct masks`,explanation:`Correct, and this scoping error is common and expensive because no amount of training makes a semantic model count.`},{text:`Classification`,explanation:`It gives one label for the whole image.`},{text:`Panoptic segmentation`,explanation:`It would work, but it also labels background "stuff" you do not need here.`}],correct:1}],interviewQuestions:[{question:`Explain the difference between the three CV tasks.`,answer:`They share a backbone and differ in the head. Classification asks what is in the image and produces one label. Detection asks what and where, producing a set of boxes with labels. Segmentation asks what each pixel is. The output sizes make the practical difference concrete: for a 640×640 image with 80 classes, classification is 80 numbers, detection with 100 boxes is 8,500, and semantic segmentation is 32.8 million. Segmentation further splits into semantic, which labels pixels by class and therefore cannot count touching objects, instance, which separates them, and panoptic, which does both — and choosing the wrong one is a scoping error no amount of training fixes.`,isCaseBased:!1},{question:`What is an anchor box and why do detectors use them?`,answer:`A pre-defined box of a given scale and aspect ratio, tiled densely across the feature map. They exist because detection has a variable-size, unordered output — zero objects or forty, with no natural ordering — which a fixed-shape network cannot produce directly. Anchors convert it into a fixed question at each position: is there an object here, of what class, and how should this box be adjusted. On a 640×640 image with a five-level pyramid and nine anchors per location that is 76,725 predictions per image. Modern detectors are moving away from them: DETR predicts a fixed set of 100 boxes and matches them to the truth with the Hungarian algorithm, and YOLO since v8 regresses distances from each point.`,isCaseBased:!1},{question:`Why is class imbalance so much worse in detection than in classification?`,answer:`Because the ratio is set by the anchor grid rather than by the data. With 76,725 anchors and 10 objects, roughly 0.052% are positive — about 1,900 background anchors per object. Each easy negative contributes almost nothing individually, but in aggregate they dominate the loss completely, and a model predicting background everywhere reaches 99.95% accuracy. Two-stage detectors avoid it by filtering first, so the classifier only sees a sampled, roughly balanced subset of proposals. One-stage detectors keep all the anchors and fix the loss instead, with focal loss down-weighting easy examples by (1 − p)^γ — that change is what let one-stage detectors match two-stage accuracy at one-stage speed.`,isCaseBased:!0},{question:`Explain focal loss.`,answer:`It is cross-entropy multiplied by (1 − p)^γ, where p is the probability assigned to the correct class. The effect is to scale down examples the model already gets right: at γ = 2, an easy negative predicted with p = 0.99 contributes a ten-thousandth of its usual weight, while a hard example at p = 0.5 loses only a factor of four. So the loss automatically concentrates on what the model is still failing at, without needing to sample or threshold anything. γ = 2 with an α of 0.25 is the standard setting. It was introduced for detection precisely because the anchor grid creates an imbalance no sampling scheme handled cleanly, and it transfers to any extreme-imbalance problem.`,isCaseBased:!1},{question:`One-stage or two-stage detector?`,answer:`One-stage for anything real-time — YOLO and the RetinaNet family run at video rates and, since focal loss, are close enough in accuracy that the gap rarely justifies the cost. Two-stage when accuracy matters more than latency and especially for small objects, where the region proposal step plus ROI alignment still helps: the classifier gets a properly cropped, aligned feature region rather than a fixed anchor. The trend is toward one-stage and anchor-free, and DETR-style set prediction removed both the anchors and NMS from the pipeline, which is architecturally cleaner even where it is not yet faster.`,isCaseBased:!1},{question:`Why does a U-Net have skip connections?`,answer:`Because segmentation needs two things that fight each other. Knowing what an object is requires a large receptive field, which requires downsampling — at 640×640 with five downsamples the bottleneck is 20×20. But knowing where the boundary is requires resolution, and a five-pixel structure is 0.16 pixels at 1/32 scale: it has not survived. Upsampling from the bottleneck alone recovers a blurred blob with the right class and the wrong edges. The skip connections pass high-resolution encoder features directly across to the matching decoder level, so the decoder gets semantics from below and spatial detail from the side. That is why the architecture works so well on small datasets in medical imaging.`,isCaseBased:!0},{question:`Your detector misses small objects. What do you do?`,answer:`Resolution first, since a small object may simply not survive to the feature map it is being detected on — raising the input size is the most direct fix and its cost is quadratic, which is worth knowing before promising it. Then check that the feature pyramid has a high-resolution level and that anchors at that level match the actual object sizes in the data; anchor scales inherited from COCO are frequently wrong for a specific domain, and clustering the ground-truth box sizes tells you directly. Then tiling: run inference on overlapping crops at full resolution and merge, which is standard in aerial and document imagery. And check the evaluation — COCO reports AP_S separately precisely because small-object AP is often less than half the overall number and the aggregate hides it.`,isCaseBased:!0},{question:`When would you use Dice loss instead of cross-entropy for segmentation?`,answer:`When the foreground is a small fraction of the image, which in medical imaging it usually is — a lesion occupying 1% of a scan. Pixel-wise cross-entropy lets a model score 99% by predicting "healthy" everywhere, the same failure detection has with background anchors. Dice is computed from the overlap between predicted and true masks, so it is normalised by the size of the foreground region and an empty prediction scores zero rather than well. The practical answer is usually a combination — Dice plus cross-entropy — because Dice alone has awkward gradients when the prediction is nearly empty, and the cross-entropy term stabilises early training.`,isCaseBased:!1}],flashcards:[{front:`The three tasks, by output size`,back:`640×640, 80 classes: classification 80 numbers, detection (100 boxes) 8,500, semantic segmentation 32,768,000.`},{front:`Why anchors exist`,back:`Detection has a variable-size unordered output. Anchors turn it into a fixed question per location: object? class? box adjustment?`},{front:`The anchor count`,back:`640×640, five pyramid levels, 9 anchors per location = 76,725 predictions per image.`},{front:`Detection's real problem`,back:`10 objects = 0.052% positive anchors — about 1,900 background per object. Predicting background everywhere scores 99.95%.`},{front:`The two fixes`,back:`Two-stage: filter first, classify a balanced subset. One-stage: keep everything, use focal loss.`},{front:`Focal loss`,back:`CE × (1 − p)^γ. At γ=2, an easy negative at p=0.99 contributes a ten-thousandth of its weight; a hard one at p=0.5 loses only 4x.`},{front:`Why U-Net skips`,back:`The 20×20 bottleneck knows WHAT and has lost WHERE — a 5-pixel structure is 0.16 pixels at 1/32. Skips supply the resolution.`},{front:`Semantic / instance / panoptic`,back:`Semantic: class per pixel, cannot count. Instance: separates objects, ignores stuff. Panoptic: both — things get instances, stuff gets a class.`}],mindmapMarkdown:`- The CV task map
  - Same backbone, different head
    - classification: 80 numbers
    - detection (100 boxes): 8,500
    - semantic segmentation: 32,768,000
  - Detection
    - output is a SET of variable size
    - anchors make it fixed-shape again
    - 640x640, 5 levels, 9 per location = 76,725
    - 10 objects -> 0.052% positive (~1,900:1)
    - background-everywhere scores 99.95%
    - two-stage: filter first (Faster R-CNN)
    - one-stage: focal loss CE x (1-p)^gamma
    - anchor-free now: DETR (Hungarian), YOLOv8
  - Segmentation
    - bottleneck at 20x20 knows what, lost where
    - 5-pixel object = 0.16 px at 1/32
    - U-Net skips supply the resolution
    - semantic: class per pixel, CANNOT count
    - instance: separate masks, ignores stuff
    - panoptic: things get ids, stuff gets classes
  - Shared
    - pretrained backbone is the default
    - resolution is quadratic - the main cost knob
    - metric follows task: accuracy / mAP / IoU or Dice`};export{e as default};