var e={id:`dbms-l2-transactions`,subjectId:`dbms`,level:2,title:`Transactions, ACID & Isolation Levels`,whyItMatters:`"Explain ACID with a real example" and "which isolation level stops this?" are two of the most reliably asked DBMS questions in any backend or ML-infra loop. They are also the difference between code that moves money correctly and code that loses it under load. This module gives you the four promises, the three concurrency phenomena as timelines you can redraw on a whiteboard, and the deadlock story every engineer eventually lives through at 9am.`,estMinutes:55,sections:[{type:`intuition`,title:`One unit of work, or none of it`,md:`Move ₹100 from asha to vikram. That is TWO updates — debit one row, credit another — but ONE real-world action.

- If the app crashes between the two updates, ₹100 has vanished from the bank. Unacceptable.
- A **transaction** is the fix: a group of statements the database promises to apply **all-or-nothing**.
- \`BEGIN\` opens the unit. \`COMMIT\` makes everything inside it permanent, together. \`ROLLBACK\` throws everything away, together.
- Between BEGIN and COMMIT your changes are **provisional** — who else can see them, and when, is the entire second half of this module.
- One sentence to keep: *a transaction turns many statements into one indivisible step.*`},{type:`code`,lang:`sql`,title:`BEGIN / COMMIT / ROLLBACK — the whole API`,code:`-- Demo schema (SQLite-ready)
CREATE TABLE accounts (id INTEGER PRIMARY KEY, name TEXT, balance INTEGER);
INSERT INTO accounts VALUES (1, 'asha', 500), (2, 'vikram', 300);

BEGIN;
UPDATE accounts SET balance = balance - 100 WHERE id = 1;
UPDATE accounts SET balance = balance + 100 WHERE id = 2;
COMMIT;
-- asha 400, vikram 400: both changes landed together

BEGIN;
UPDATE accounts SET balance = balance - 100 WHERE id = 1;
ROLLBACK;
-- asha STILL 400: the lone debit officially never happened`,annotations:{5:`BEGIN opens the all-or-nothing unit. Without it, most databases run in autocommit: every statement is its own tiny transaction — the debit could commit alone.`,8:`COMMIT is the moment of truth: both updates become permanent in one atomic step. Nobody can ever observe the debit without the credit.`,13:`ROLLBACK undoes everything since BEGIN. The debit was executed, then unhappened. This is what your app should do when any step of a multi-step change fails.`}},{type:`note`,md:"Run it yourself: [Open the SQL playground](/playground/sql) and paste the block above. Then delete the `BEGIN`/`COMMIT` lines and re-run the transfer one statement at a time — that is exactly the autocommit failure mode the transaction exists to prevent."},{type:`intuition`,title:`ACID: four promises, four real examples`,md:`- **Atomicity** — all-or-nothing. *The transfer:* debit AND credit, or neither. A crash halfway rolls the debit back.
- **Consistency** — every declared rule (PRIMARY KEY, FOREIGN KEY, CHECK, NOT NULL) holds before AND after. *Example:* with \`CHECK (balance >= 0)\`, a transaction that would overdraw is rejected whole — the database never moves from one valid state to an invalid one.
- **Isolation** — concurrent transactions do not see each other's half-done work. *Example:* while the transfer is mid-flight, a balance report never sees the debit without the credit.
- **Durability** — once COMMIT returns, the change survives a power loss. *Example:* the ATM says "done", the data center loses power a millisecond later — the money still moved.
- Durability's mechanism in one line: changes are written to a **write-ahead log (WAL)** on disk *before* COMMIT returns — full story in the internals module (L3).`},{type:`intuition`,title:`Phenomenon 1: dirty read — trusting the uncommitted`,md:`Two transactions, one account row. Read the timeline top to bottom.

- t1 — T1: \`BEGIN\`; debits asha: balance 500 → 400. **Not committed.**
- t2 — T2: reads asha's balance → sees **400**.
- t3 — T1: hits an error → \`ROLLBACK\`. Balance is 500 again.
- T2 built its answer on a value that officially never existed. That is a **dirty read**: reading another transaction's **uncommitted** write.
- It is the worst of the three phenomena — you can observe states the database never agreed to.`},{type:`visual`,component:`PointerBoxDiagram`,props:{title:`Dirty read, frame by frame`,notice:`One account row on the right. Watch T2 read a value that T1 later takes back.`,leftLabel:`transactions`,rightLabel:`the account row`,frames:[{note:`Starting point: asha's balance is 500, committed. Two transactions are about to touch the same row.`,stack:[{name:`T1 (transfer)`,value:`idle`},{name:`T2 (report)`,value:`idle`}],heap:[{id:`bal`,value:`balance = 500`,label:`committed`}]},{note:`T1: BEGIN; UPDATE … balance = 400. The write happened — but T1 has not committed. This 400 is provisional.`,stack:[{name:`T1 wrote 400`,to:`bal`},{name:`T2 (report)`,value:`idle`}],heap:[{id:`bal`,value:`balance = 400`,label:`UNCOMMITTED`}]},{note:`T2 reads the row at READ UNCOMMITTED and sees 400 — data its owner might still take back. This read is the dirty read.`,stack:[{name:`T1 wrote 400`,to:`bal`},{name:`T2 read`,value:`400`,to:`bal`,danger:!0}],heap:[{id:`bal`,value:`balance = 400`,label:`UNCOMMITTED`}]},{note:`T1 fails and ROLLBACKs: the row is 500 again. T2's report is built on a 400 that never existed. At READ COMMITTED, T2 would have seen 500 and this frame would be impossible.`,stack:[{name:`T1`,value:`ROLLED BACK`},{name:`T2 read`,value:`400`,to:`ghost`,danger:!0}],heap:[{id:`bal`,value:`balance = 500`,label:`committed again`},{id:`ghost`,value:`400 — the ghost value`,freed:!0}]}]}},{type:`intuition`,title:`Phenomenon 2: non-repeatable read — same row, two answers`,md:`- t1 — T1: \`BEGIN\`; reads asha's balance → **500**.
- t2 — T2: updates asha's balance to 400 and **COMMITs**.
- t3 — T1: reads the SAME row again → **400**. One transaction, one row, two different answers.
- That is a **non-repeatable read**: a row you already read is changed (or deleted) by another *committed* transaction before you re-read it.
- Key contrast with a dirty read: T2's data was committed — perfectly real — just inconsistent *within T1's timeline*. So READ COMMITTED cannot stop it.`},{type:`intuition`,title:`Phenomenon 3: phantom read — new rows appear`,md:`- t1 — T1: \`BEGIN\`; runs \`SELECT COUNT(*) FROM orders WHERE amount > 10000\` → **12**.
- t2 — T2: INSERTs a new 15,000 order and COMMITs.
- t3 — T1: re-runs the exact same query → **13**. A row appeared out of nowhere.
- That is a **phantom read**: repeating a **predicate** (a WHERE condition) returns rows that did not exist the first time.
- The distinction interviews test: non-repeatable = an *existing row changed*; phantom = *new rows match*. Locking the rows you read stops the first but not the second — you cannot lock rows that do not exist yet. Stopping phantoms needs predicate/range locking or full serializability.`},{type:`intuition`,title:`Isolation levels: how much weirdness you tolerate for speed`,md:`The SQL standard defines four levels, each by which phenomena it still permits.

- **READ UNCOMMITTED** — permits all three: dirty, non-repeatable, phantom.
- **READ COMMITTED** — stops dirty reads; still permits non-repeatable + phantom.
- **REPEATABLE READ** — also stops non-repeatable reads; still permits phantoms (says the standard).
- **SERIALIZABLE** — stops all three. The outcome is *as if* transactions ran one at a time.
- The ladder trades **concurrency for safety**: each step up forbids one more phenomenon and allows fewer interleavings — which is why nobody defaults to SERIALIZABLE.`},{type:`note`,md:`Real-world defaults: **Postgres defaults to READ COMMITTED**, **MySQL (InnoDB) defaults to REPEATABLE READ**. Both use **MVCC** (multi-version concurrency control): instead of blocking readers, the engine keeps old row versions and serves each transaction a consistent **snapshot** — so **readers don't block writers and writers don't block readers**. Honest footnotes: Postgres has no true READ UNCOMMITTED (asking for it gets you READ COMMITTED), and InnoDB's REPEATABLE READ blocks most phantoms too via snapshots and next-key locks — real engines are usually *safer* than the standard's minimum for their level.`},{type:`intuition`,title:`Locks: how isolation is enforced`,md:`- **Shared lock (S)** — "I am reading this." Many transactions can hold S on the same item at once.
- **Exclusive lock (X)** — "I am writing this." Exactly one holder; blocks every other S and X request.
- Compatibility in one breath: S+S fine, S+X blocked, X+X blocked — readers coexist, a writer is alone.
- **Granularity**: lock a row (maximum concurrency, more bookkeeping) ↔ lock a page or whole table (cheap to manage, but everyone queues). Engines default to row locks and may escalate when one transaction locks too many.
- Under MVCC, plain reads usually need no lock at all — but writes still take X locks, which is why the next two ideas exist.`},{type:`intuition`,title:`Two-phase locking: grow, then shrink`,md:`- **2PL rule**: every transaction has a **growing phase** (may acquire locks, never release) then a **shrinking phase** (may release, never acquire). After releasing its first lock, it may not take another — ever.
- Why it works: at its **lock point** (the moment of the last acquisition) a transaction holds locks on everything it touches, so no rival can slip a conflicting change between its reads and writes.
- Ordering transactions by their lock points produces an equivalent one-at-a-time schedule — that is why 2PL guarantees **serializability**.
- Practice: engines use **strict 2PL** — hold write locks until COMMIT — which additionally prevents dirty reads and cascading rollbacks.
- The price of holding locks longer: more waiting, and the possibility of a cycle of waiters. Enter the deadlock.`},{type:`intuition`,title:`Deadlock: the circular wait`,md:`- T1 locks row A, then wants row B. T2 locks row B, then wants row A. Each waits for the other — forever. That cycle is a **deadlock**.
- Neither transaction is buggy alone; the *interleaving plus opposite lock order* is the bug.
- **Detection** (what databases do): maintain a waits-for graph, find the cycle, pick a **victim**, roll it back with a "deadlock detected" error. Your app must catch that error and **retry** the victim.
- **Prevention** (what YOU do): make every transaction lock resources in the **same global order** — e.g. always update the lower account id first. Same order → a cycle is impossible.
- Also helps: keep transactions short, and never hold locks across user think-time or network calls.`},{type:`visual`,component:`PointerBoxDiagram`,props:{title:`Deadlock: two transactions, two rows, one cycle`,notice:`Solid arrow = lock held. Red dashed arrow = waiting for a lock someone else holds.`,leftLabel:`transactions`,rightLabel:`rows (locks)`,frames:[{note:`T1: UPDATE row A → takes an exclusive lock on A. Perfectly normal so far.`,stack:[{name:`T1 holds A`,to:`ra`},{name:`T2`,value:`idle`}],heap:[{id:`ra`,value:`row A`,label:`X-lock: T1`},{id:`rb`,value:`row B`,label:`unlocked`}]},{note:`T2: UPDATE row B → exclusive lock on B. Two transactions, two locks, zero conflict.`,stack:[{name:`T1 holds A`,to:`ra`},{name:`T2 holds B`,to:`rb`}],heap:[{id:`ra`,value:`row A`,label:`X-lock: T1`},{id:`rb`,value:`row B`,label:`X-lock: T2`}]},{note:`T1 now wants row B — but T2 holds it. T1 blocks and waits. Still fine: T2 could finish and release.`,stack:[{name:`T1 holds A`,to:`ra`},{name:`T1 wants B`,to:`rb`,danger:!0},{name:`T2 holds B`,to:`rb`}],heap:[{id:`ra`,value:`row A`,label:`X-lock: T1`},{id:`rb`,value:`row B`,label:`X-lock: T2`}]},{note:`T2 wants row A — held by T1. Now T1 waits for T2 AND T2 waits for T1: the arrows cross. A cycle in the waits-for graph = deadlock. Neither can ever proceed on its own.`,stack:[{name:`T1 holds A`,to:`ra`},{name:`T1 wants B`,to:`rb`,danger:!0},{name:`T2 holds B`,to:`rb`},{name:`T2 wants A`,to:`ra`,danger:!0}],heap:[{id:`ra`,value:`row A`,label:`X-lock: T1`},{id:`rb`,value:`row B`,label:`X-lock: T2`}]},{note:`The deadlock detector finds the cycle and picks a victim: T2 is rolled back, its lock on B releases, T1 acquires B and commits. The app retries T2. Prevention beats cure: had both locked A before B, no cycle could form.`,stack:[{name:`T1 holds A`,to:`ra`},{name:`T1 got B`,to:`rb`},{name:`T2`,value:`VICTIM — rolled back`}],heap:[{id:`ra`,value:`row A`,label:`X-lock: T1`},{id:`rb`,value:`row B`,label:`X-lock: T1`}]}]}},{type:`intuition`,title:`The interview skill: name the phenomenon, name the level`,md:`Given any interleaving, run this checklist: (1) was the read data **committed**? No → dirty read. (2) Did an **existing row change** between two reads? → non-repeatable read. (3) Did **new rows** match a repeated WHERE? → phantom. Then quote the *minimum* standard level that stops it.

- **Scenario 1** — T1 computes interest: reads balance 1000 … T2 withdraws 500 and COMMITs … T1 re-reads the same row to apply interest and sees 500. Committed data, same row, changed between reads → **non-repeatable read**. Minimum fix: **REPEATABLE READ**. (READ COMMITTED does NOT help — T2's change was committed, which is exactly what READ COMMITTED permits you to see.)
- **Scenario 2** — T1 audits a day's payments: runs \`SELECT SUM(amount) FROM payments WHERE day = '2026-08-19'\` at the start and again at the end to cross-check … between them T2 INSERTs a late payment for that day and COMMITs → the two sums differ. New rows matched a repeated predicate → **phantom read**. Minimum fix by the standard: **SERIALIZABLE**.
- Always attach the tradeoff: the stronger level buys correctness with throughput — SERIALIZABLE can block more or abort transactions that must be retried.`}],quiz:[{question:`T2 reads a balance that T1 has updated but not committed. T1 then rolls back. What did T2 experience?`,options:[{text:`A dirty read`,explanation:`Correct. T2 read uncommitted data that was later revoked — a value that officially never existed.`},{text:`A non-repeatable read`,explanation:`Non-repeatable needs COMMITTED data changing between two reads by the same transaction. Here the data was never committed at all.`},{text:`A phantom read`,explanation:`Phantoms are about NEW ROWS matching a repeated predicate — not about one row's uncommitted value.`},{text:`A deadlock`,explanation:`Nobody is waiting on anybody — this is a visibility problem, not a locking cycle.`}],correct:0},{question:`What is the MINIMUM standard isolation level that prevents dirty reads?`,options:[{text:`READ UNCOMMITTED`,explanation:`This is the one level that permits dirty reads — its name says so.`},{text:`READ COMMITTED`,explanation:`Correct. You only ever see committed data. Higher levels also prevent dirty reads, but the question asks for the minimum.`},{text:`REPEATABLE READ`,explanation:`Prevents them too, but overshoots — READ COMMITTED is already enough, and "minimum" is what interviews ask.`},{text:`SERIALIZABLE`,explanation:`Prevents everything — maximum, not minimum. Quoting it here signals you don't know the ladder.`}],correct:1},{question:`Non-repeatable read vs phantom read — the precise difference?`,options:[{text:`They are two names for the same thing`,explanation:`The standard separates them precisely because different locking is needed for each.`},{text:`Non-repeatable: an existing row you read changed. Phantom: NEW rows match your repeated WHERE condition`,explanation:`Correct. Changed row vs appearing rows. Row locks on what you read stop the first but cannot stop the second — you can't lock rows that don't exist yet.`},{text:`Non-repeatable involves uncommitted data; phantoms involve committed data`,explanation:`Both involve COMMITTED changes from another transaction — uncommitted data is the dirty read's territory.`}],correct:1},{question:`COMMIT returns, then the server loses power. The change survives. Which ACID property, via what mechanism?`,options:[{text:`Durability — the change was in the write-ahead log on disk before COMMIT returned`,explanation:`Correct. WAL first, COMMIT acknowledgment second. On restart, the log replays anything not yet in the data files.`},{text:`Atomicity — all-or-nothing includes power loss`,explanation:`Atomicity says no HALF-transactions; it doesn't by itself guarantee a committed change persists through a crash.`},{text:`Isolation — other transactions could not interfere`,explanation:`Isolation is about concurrent visibility, not surviving crashes.`},{text:`Consistency — constraints keep the data valid`,explanation:`Constraints hold, but that says nothing about surviving a power cut.`}],correct:0},{question:`Why does two-phase locking (2PL) guarantee serializability?`,options:[{text:`Because it makes transactions run one at a time`,explanation:`Transactions still overlap heavily under 2PL — that is the point. Only the ORDER of conflicting accesses is constrained.`},{text:`Because ordering transactions by their lock points yields an equivalent serial schedule`,explanation:`Correct. At its lock point a transaction holds everything it touches, so no conflicting change can interleave — the schedule is equivalent to running them serially in lock-point order.`},{text:`Because 2PL prevents deadlocks`,explanation:`It does NOT — 2PL transactions deadlock happily. Deadlock handling is a separate mechanism.`}],correct:1},{question:`T1 locks A then wants B; T2 locks B then wants A — daily deadlock. The cleanest permanent fix?`,options:[{text:`Raise the isolation level to SERIALIZABLE`,explanation:`More isolation means more locking — this makes deadlocks MORE likely, not less.`},{text:`Make every transaction acquire locks in the same global order (e.g. lower id first)`,explanation:`Correct. With one agreed order, a cycle in the waits-for graph is impossible — deadlock cannot form.`},{text:`Increase the lock wait timeout`,explanation:`The cycle never resolves by waiting — a timeout just converts a fast detection into a slow one.`},{text:`Add an index on the locked columns`,explanation:`Indexes can reduce HOW MANY rows get locked (sometimes helping), but do not break an ordering cycle on the same two rows.`}],correct:1},{question:`Default isolation levels: Postgres and MySQL (InnoDB)?`,options:[{text:`Both default to SERIALIZABLE`,explanation:`No mainstream engine defaults to SERIALIZABLE — too much concurrency cost.`},{text:`Postgres: READ COMMITTED; InnoDB: REPEATABLE READ`,explanation:`Correct. A favorite trivia check — and worth adding that both engines are MVCC, so their levels behave better than the standard's minimum.`},{text:`Postgres: REPEATABLE READ; InnoDB: READ COMMITTED`,explanation:`Exactly backwards — the classic trap.`}],correct:1},{question:`Under MVCC, why does a long-running SELECT not block a concurrent UPDATE on the same rows?`,options:[{text:`The SELECT reads an older committed version — a snapshot — while the UPDATE writes a new version`,explanation:`Correct. Multiple versions of each row coexist; readers see their snapshot, writers create new versions. Readers don't block writers, writers don't block readers.`},{text:`SELECTs take exclusive locks but release them per row`,explanation:`Plain MVCC reads take no row locks at all — that's the whole trick.`},{text:`The UPDATE silently waits until the SELECT finishes`,explanation:`That is exactly the blocking MVCC was invented to avoid.`}],correct:0}],interviewQuestions:[{question:`Explain ACID with a concrete example for each letter.`,answer:`Atomicity: a money transfer is debit + credit; a crash between them rolls the debit back — all-or-nothing. Consistency: declared rules (PKs, FKs, CHECK balance >= 0) hold before and after every transaction — an overdrawing transaction is rejected whole, so the DB moves only between valid states. Isolation: while the transfer is mid-flight, a concurrent balance report never sees the debit without the credit — no half-done work leaks. Durability: once COMMIT returns, the change survives power loss, because it was written to the write-ahead log on disk before the COMMIT was acknowledged. The pattern interviewers reward: one crisp real example per letter, not four definitions.`,isCaseBased:!1},{question:`Walk me through a dirty read on a timeline, and name the minimum isolation level that prevents it.`,answer:`T1: BEGIN, update balance 500 → 400, no commit yet. T2: reads the row, sees 400. T1: error, ROLLBACK — balance is 500 again. T2 acted on a value that never officially existed. That is a dirty read: consuming another transaction's uncommitted write. Minimum fix: READ COMMITTED — you only ever see committed data. Worth adding: in Postgres you cannot even opt into dirty reads (READ UNCOMMITTED silently behaves as READ COMMITTED), and MVCC engines fix this without blocking — T2 just reads the last committed version.`,isCaseBased:!1},{question:`Non-repeatable read vs phantom read — precise difference, and why row locks stop one but not the other.`,answer:`Non-repeatable: T1 reads a row, T2 updates or deletes THAT row and commits, T1 re-reads and gets a different answer — an existing row changed. Phantom: T1 runs a predicate query (WHERE amount > 10000), T2 inserts a NEW matching row and commits, T1 re-runs the query and extra rows appear. The locking asymmetry: to stop non-repeatable reads you can lock the rows you read — they exist, so they are lockable. Phantoms come from rows that did not exist at first read, and you cannot lock a row that does not exist — stopping them requires predicate/range locking (InnoDB next-key locks) or full serializability. Minimum standard levels: REPEATABLE READ stops the first, SERIALIZABLE stops the second.`,isCaseBased:!1},{question:`Case: production deadlocks every day at 9am sharp. Walk me through how you debug and fix it.`,answer:`The 9am regularity is the biggest clue: a scheduled job (batch settlement, report, cron import) starts then and collides with normal traffic. Step 1 — get evidence, not guesses: the engine logs every detected deadlock with the two statements and locks involved (SHOW ENGINE INNODB STATUS latest-deadlock section in MySQL; Postgres logs the "deadlock detected" DETAIL). Step 2 — read the cycle: typically the batch job updates rows in one order (say, account id DESC or in file order) while the app updates in another (id ASC), giving the classic T1 holds A wants B / T2 holds B wants A. Step 3 — fix by prevention: make both paths lock in the same global order (sort ids ascending before updating); shrink the batch transaction (commit per chunk instead of one giant transaction holding thousands of locks); check for a missing index — a table-scan UPDATE locks far more rows than intended, widening the collision surface. Step 4 — accept that detection is the backstop: keep the retry-on-deadlock logic, because the DB resolves cycles by rolling back a victim, and the app must replay it. Bonus point: move the 9am job to a quieter window — valid, but say it as mitigation, not the fix.`,isCaseBased:!0},{question:`Case: a payment service occasionally debits the sender but never credits the receiver. Logs show the app crashed between the two UPDATE calls. Diagnose and fix.`,answer:`Diagnosis: the two updates ran as separate autocommit statements — each was its own transaction, so the debit committed alone and the crash prevented the credit. Atomicity was never requested, so the DB never promised it. Fix: wrap both updates in one transaction — BEGIN, debit, credit, COMMIT — so a crash before COMMIT rolls the debit back automatically. Hardening beyond the fix: make the operation idempotent (a transfer id with a uniqueness check) so retries after failure cannot double-apply; verify the ORM or driver is not in autocommit mode silently (a very common footgun); add a CHECK (balance >= 0) so a concurrent overdraw fails loudly. Root cause in one line: multi-statement business operations must be one transaction — the database can only be atomic about what you put inside BEGIN…COMMIT.`,isCaseBased:!0},{question:`Case: a nightly reconciliation job reads a day's payment total at the start of its run and again at the end; the two totals differ and the job flags a false alarm. Name the phenomenon and fix it.`,answer:`New payments for that day are being inserted and committed between the two reads — repeated predicate, new matching rows: a phantom read (if instead existing rows were updated, it would be a non-repeatable read; say which by checking whether row count or row content changed). Fixes, in order of preference: (1) run the whole job at an isolation level with a transaction-lifetime snapshot — Postgres REPEATABLE READ gives one consistent snapshot for the whole transaction and in practice removes both phenomena; SERIALIZABLE is the standard's guaranteed answer; (2) restructure to read once: compute everything from a single query so there is no second read to disagree; (3) reconcile a closed window (yesterday up to midnight) so late inserts fall outside the predicate. Tradeoff to name: stronger isolation can abort the job under conflict, so pair it with a retry.`,isCaseBased:!0},{question:`Why not just run everything at SERIALIZABLE and never think about phenomena again?`,answer:`Because you pay in throughput and in failures. SERIALIZABLE must forbid every interleaving that could differ from some serial order — via range/predicate locking (more blocking, more deadlocks) or serializability checking as in Postgres SSI (transactions abort with serialization errors and must be retried). Under load that means lower concurrency, higher tail latency, and mandatory retry loops in application code. Most workloads are fine at READ COMMITTED / REPEATABLE READ, escalating only the few transactions that need more (or using SELECT … FOR UPDATE to lock exactly what matters). The engineering answer: choose the weakest level whose permitted anomalies your logic can tolerate, and be able to say which anomalies those are.`,isCaseBased:!1},{question:`Shared vs exclusive locks, and the granularity tradeoff.`,answer:`Shared (S) = read lock: many holders simultaneously — readers coexist. Exclusive (X) = write lock: one holder, blocks all other S and X — a writer is alone. Compatibility: S+S yes, S+X no, X+X no. Granularity: row-level locking maximizes concurrency but costs memory and management per lock; page or table locks are cheap but serialize everyone touching that object. Engines default to row locks and may escalate to coarser locks when one transaction accumulates too many. Worth volunteering: in MVCC engines plain SELECTs usually take no locks at all — the S/X story mostly governs writes and explicit locking reads like SELECT … FOR UPDATE / FOR SHARE.`,isCaseBased:!1},{question:`State the two-phase locking protocol and argue why it yields serializability. What does strict 2PL add?`,answer:`Protocol: each transaction has a growing phase in which it may only acquire locks, then a shrinking phase in which it may only release — after the first release, no more acquisitions ever. Argument: consider each transaction's lock point, the instant it holds its maximal lock set. Any conflicting operation by another transaction must fall entirely before or entirely after that point, so ordering transactions by lock points gives a serial schedule with the same conflict order — the concurrent execution is equivalent to that serial one. What 2PL does NOT give: freedom from deadlock (cycles still form) or from cascading aborts (another transaction may have read data from one that later aborts). Strict 2PL fixes the second: hold write locks until COMMIT/ROLLBACK, so nobody reads uncommitted writes — no dirty reads, no cascading rollback. That is what real engines implement.`,isCaseBased:!1},{question:`How does MVCC let readers avoid blocking writers, and what actually differs between READ COMMITTED and REPEATABLE READ in an MVCC engine like Postgres?`,answer:`MVCC keeps multiple versions of each row: an UPDATE writes a new version instead of overwriting in place, and every transaction reads through a snapshot that decides which committed version is visible to it. Readers never wait on writers (they read an older version) and writers never wait on readers. The level difference in Postgres is snapshot lifetime: READ COMMITTED takes a fresh snapshot per STATEMENT — so two SELECTs in one transaction can see different committed data (non-repeatable reads possible); REPEATABLE READ takes one snapshot per TRANSACTION — every read sees the same frozen state (and in Postgres this in practice prevents phantoms too, stronger than the standard requires). Caveat that earns points: MVCC does not eliminate write-write conflicts — two transactions updating the same row still serialize on the row lock, and under REPEATABLE READ the later one may abort with a serialization error.`,isCaseBased:!1},{question:`Interviewer asserts: "Postgres defaults to REPEATABLE READ, right?" Respond.`,answer:`Politely correct it: Postgres defaults to READ COMMITTED; it is MySQL InnoDB that defaults to REPEATABLE READ. Then add the two honest footnotes that show depth: Postgres has no real READ UNCOMMITTED — requesting it gives READ COMMITTED behavior, because MVCC never exposes uncommitted versions anyway; and InnoDB's REPEATABLE READ is stronger than the standard's, blocking most phantoms via consistent snapshots and next-key locks. Close with the practical rule: never assume the default matches the standard's table — check the engine's documentation, because the standard defines maximum permitted anomalies, and engines are free to be stricter.`,isCaseBased:!1}],flashcards:[{front:`Transaction, one sentence`,back:`An all-or-nothing unit of statements: BEGIN opens it, COMMIT makes everything permanent together, ROLLBACK discards everything together.`},{front:`ACID, one line per letter`,back:`Atomicity: all-or-nothing (debit+credit or neither). Consistency: constraints hold before and after. Isolation: no seeing half-done work. Durability: committed survives power loss (WAL).`},{front:`Dirty read`,back:`Reading another transaction's UNCOMMITTED write — which may be rolled back, so you saw a value that never existed. Stopped from READ COMMITTED up.`},{front:`Non-repeatable read`,back:`Same transaction reads the same ROW twice; another transaction changed it and COMMITTED in between. Stopped from REPEATABLE READ up.`},{front:`Phantom read`,back:`Repeating a PREDICATE query returns NEW rows inserted (and committed) in between. Row locks can't stop it — rows didn't exist yet. Standard guarantee: SERIALIZABLE.`},{front:`Isolation ladder (what each level stops)`,back:`READ UNCOMMITTED: nothing. READ COMMITTED: dirty. REPEATABLE READ: dirty + non-repeatable. SERIALIZABLE: all three.`},{front:`Real DB defaults + MVCC`,back:`Postgres: READ COMMITTED. MySQL InnoDB: REPEATABLE READ. Both MVCC: snapshots of old row versions → readers don't block writers, writers don't block readers.`},{front:`Shared vs exclusive lock`,back:`S = read lock, many holders. X = write lock, one holder, blocks all. S+S ok; S+X and X+X conflict. Granularity: row = concurrency, table = cheap.`},{front:`Two-phase locking (2PL)`,back:`Growing phase (only acquire) then shrinking phase (only release). Ordering by lock points gives an equivalent serial schedule → serializability. Strict 2PL: hold write locks till COMMIT.`},{front:`Deadlock: cause + both cures`,back:`Circular wait (T1 holds A wants B, T2 holds B wants A). Detection: waits-for graph cycle → roll back a victim → app retries. Prevention: everyone locks in one global order.`}],mindmapMarkdown:`- Transactions, ACID & Isolation Levels
  - Transaction
    - all-or-nothing unit
    - BEGIN / COMMIT / ROLLBACK
    - autocommit: each statement alone
  - ACID
    - Atomicity: debit+credit or neither
    - Consistency: constraints hold
    - Isolation: no half-done peeking
    - Durability: WAL, survives power loss
  - Phenomena (timelines)
    - dirty read: uncommitted value
    - non-repeatable: same row changed
    - phantom: new rows match predicate
  - Isolation levels
    - READ UNCOMMITTED: allows all
    - READ COMMITTED: stops dirty
    - REPEATABLE READ: stops non-repeatable
    - SERIALIZABLE: stops phantoms too
    - defaults: Postgres RC, InnoDB RR
    - MVCC: snapshots, readers ≠ blockers
  - Locks
    - shared (read) vs exclusive (write)
    - granularity: row ↔ page ↔ table
    - 2PL: grow then shrink → serializable
    - strict 2PL: hold X till COMMIT
  - Deadlock
    - circular wait, crossed arrows
    - detection: victim rollback + retry
    - prevention: global lock order
    - keep transactions short`};export{e as default};