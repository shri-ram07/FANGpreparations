var e={id:`cpp-l3-modern-misc`,subjectId:`cpp`,level:3,title:`Modern C++ Toolkit & the Sharp Edges`,whyItMatters:`"What does this print?" — and the snippet hides a dangling pointer, a signed overflow, or a data race. Modern C++ interviews test two things: do you reach for the C++17 conveniences seniors use daily, and do you know exactly where the language stops protecting you. This module is both: the toolkit, then the sharp edges.`,estMinutes:45,sections:[{type:`intuition`,title:`constexpr: do the work before you ship`,md:"A restaurant preps its sauces in the morning, not while you wait. **constexpr** is prep: computation the *compiler* does, results baked into the binary.\n\n- `constexpr` on a function is *permission*, not command: called with constants → computed at compile time; called with runtime values → normal function call.\n- Where compile-time values are REQUIRED: array sizes, template arguments, `static_assert` — anything needed before the program runs.\n- Where it pays: lookup tables and validation moved to build time. A failed `static_assert` cannot ship; a runtime crash can.\n- Cost honesty: heavy constexpr work slows COMPILES. You trade build time for runtime."},{type:`code`,lang:`cpp`,title:`One keyword, two execution times`,code:`constexpr int fib(int n) {
    return n < 2 ? n : fib(n - 1) + fib(n - 2);
}

static_assert(fib(10) == 55, "computed by the COMPILER");
int table[fib(10)];        // legal: array sizes need compile-time constants

constexpr int square(int x) { return x * x; }

int main() {
    int a = square(6);     // folded to 36 in the binary: zero runtime cost
    int n;
    std::cin >> n;
    int b = square(n);     // same function, now a normal runtime call
}`,annotations:{5:`static_assert fires during compilation. A failing one is a build error — the cheapest kind of bug: it cannot reach production.`,6:`This is why constexpr matters: some contexts REQUIRE values the compiler already knows.`,11:`Constant argument → the call disappears; 36 is written straight into the machine code.`,14:`Runtime argument → runs like any function. Permission, not command.`}},{type:`note`,md:"`auto` in three rules: (1) plain `auto` copies — it deduces like pass-by-value, dropping references and top-level `const` (a `const int` becomes a mutable `int` copy); (2) `auto&` binds a reference and keeps `const`; (3) `const auto&` binds to anything without copying. Use `auto` where the type is unnameable or obvious — iterators, lambdas, `make_unique` results. Spell the type out where it carries meaning: integer widths, ownership types. `auto` hides exactly what it saves you from writing."},{type:`code`,lang:`cpp`,title:`Range-based for and the accidental-copy trap`,code:`std::vector<std::string> names = {"ana", "bo", "cy"};

for (auto s : names)         // s is a COPY of each element
    s += "!";                // mutates the copy - names unchanged

for (auto& s : names)        // s is a reference - writes go through
    s += "!";

for (const auto& s : names)  // read without copying: the default
    std::cout << s << '\\n';

const int c = 7;
auto x = c;                  // int (mutable!) - auto DROPS top-level const
auto& y = c;                 // const int& - auto& preserves it
auto z = 3.14f;              // float, not double - deduction is exact`,annotations:{3:`The trap: compiles fine, silently allocates a fresh string every iteration, and your += edits a temporary. Interviewers plant this constantly.`,9:`The habit to build: const auto& to read, auto& to mutate, plain auto only when you WANT a copy.`,13:`Rule 1 in action: auto behaves like pass-by-value, so const and & fall off.`,15:`auto is exact, not "close enough" — 3.14f is a float, and auto will not promote it.`}},{type:`intuition`,title:`Structured bindings: name the pieces`,md:"A function returns two things. `.first` and `.second` tell the reader nothing.\n\n- **Structured bindings** (C++17): `auto [a, b] = expr` unpacks a pair, tuple, array, or plain struct into named variables in one line.\n- Map loops become readable: `for (const auto& [word, count] : freq)`.\n- API results too: `auto [it, inserted] = m.insert(...)` — the bool finally has a name."},{type:`code`,lang:`cpp`,title:`Unpack, don't dig`,code:`std::map<std::string, int> scores{{"ana", 90}, {"bo", 85}};

for (const auto& [name, score] : scores)    // C++17
    std::cout << name << ": " << score << '\\n';

auto [it, inserted] = scores.insert({"cy", 70});
if (!inserted)
    std::cout << "key already existed\\n";

auto [lo, hi] = minmax(v);   // any pair-returning function: names, not .first`,annotations:{3:`Each map element is a pair<const Key, T>. [name, score] unpacks it — goodbye it->first and it->second.`,6:`insert returns pair<iterator, bool>. Bindings turn an API trivia question into readable code.`,10:`Works on anything decomposable: pair, tuple, C arrays, plain structs with public fields.`}},{type:`note`,md:"Memory layout, the honest version: every type has an **alignment** — an address rule (`int` wants an address divisible by 4). The compiler inserts invisible **padding** bytes to satisfy it.\n\n- `struct { char a; int b; char c; }` → **12 bytes**: a(1) + pad(3) + b(4) + c(1) + pad(3).\n- Reordered `struct { int b; char a; char c; }` → **8 bytes**. Same data, a third smaller.\n- The tail padding exists so element [1] of an array of these stays aligned too.\n- Rule of thumb: order fields **largest-first**. At 100 million structs, 4 wasted bytes each is 400 MB of pure padding.\n- Interview line: `sizeof` a struct is NOT the sum of its parts."},{type:`intuition`,title:`Undefined behavior: the compiler owes you nothing`,md:`Your code is a contract with the compiler. **Undefined behavior (UB)** is breaking a clause — and the fine print says: break one clause, ALL guarantees are void.

- UB means the standard places **no requirements** on what happens. Not "crash". Not "random value". *Anything.*
- The interview set: **signed integer overflow**, **out-of-bounds access**, **use-after-free**, **uninitialized reads**.
- Why the language allows it: speed. No bounds checks, no overflow checks — you pay for nothing you did not write.
- The twist: optimizers ASSUME UB never happens and transform code under that assumption — your own overflow check can be deleted as "impossible".
- So UB code can pass every test today and corrupt data after a compiler upgrade. "It ran fine" proves nothing.`},{type:`code`,lang:`cpp`,title:`The classics, and what the optimizer does to them`,code:`bool always(int x) {
    return x + 1 > x;      // the optimizer folds this to: return true;
}

int oob() {
    int a[4] = {1, 2, 3, 4};
    return a[4];           // one PAST the end - no bounds check exists
}

int uninit() {
    int x;                 // no value at all - not zero, not "garbage"
    return x * 2;          // reading it is UB
}`,annotations:{2:`Signed overflow is UB, so the compiler assumes it cannot happen — x + 1 > x becomes true unconditionally, and overflow checks written this way vanish in release builds.`,7:`Often "works" (reads whatever sits after the array), sometimes corrupts the stack, never correct. vector::at() is the bounds-checked alternative, at the cost of a check per access.`,11:`An uninitialized read is not "a random number" — it is UB, and the optimizer may delete whole branches that depend on it.`}},{type:`visual`,component:`PointerBoxDiagram`,props:{title:`Use-after-free, frame by frame`,notice:`Step through. The bug is not the delete — it is the arrow that survives it.`,frames:[{note:`int* p = new int(42) — p lives on the stack and stores an ADDRESS; the int lives on the heap.`,stack:[{name:`p`,to:`h1`}],heap:[{id:`h1`,value:`42`,label:`int`}]},{note:`delete p — the memory returns to the allocator. p is UNCHANGED: it still stores the old address. The arrow is now a lie.`,stack:[{name:`p`,to:`h1`}],heap:[{id:`h1`,value:`42`,freed:!0}]},{note:`std::cout << *p — may STILL print 42 today: the allocator just has not reused the slot yet. "It works" is luck, not correctness. This read is UB.`,stack:[{name:`p`,to:`h1`,danger:!0}],heap:[{id:`h1`,value:`42`,freed:!0}]},{note:`Later: int* q = new int(9000) lands in the SAME slot. Now *p reads q's data — and *p = 0 would silently corrupt it. UB means the compiler owes you NOTHING.`,stack:[{name:`p`,to:`h1`,danger:!0},{name:`q`,to:`h1`}],heap:[{id:`h1`,value:`9000`,label:`q's int`}]}]}},{type:`intuition`,title:`std::thread: real parallelism, real danger`,md:"Python's GIL forces threads to take turns on one interpreter lock. **C++ has no GIL: threads are REAL parallelism** — two threads on two cores genuinely compute at the same time.\n\n- `std::thread t(f)` starts running `f` immediately, on its own OS thread.\n- Before `t` is destroyed you MUST `t.join()` (wait for it) or `t.detach()` (let it run free) — otherwise `std::terminate`.\n- The price: a **data race** — two threads touch the same memory, at least one writes, no synchronization. A data race is UB, not just a stale value.\n- The fix is a lock: `std::mutex` held via `std::lock_guard` — the RAII idea from the smart-pointer module, applied to locking.\n- With great power: C++ gives you the speedup Python threads cannot — and hands you every race the GIL was hiding."},{type:`code`,lang:`cpp`,title:`Two threads, one counter, done right`,code:`#include <thread>
#include <mutex>

int counter = 0;
std::mutex m;

void add() {
    for (int i = 0; i < 100000; ++i) {
        std::lock_guard<std::mutex> lock(m);  // RAII: constructor locks
        ++counter;
    }                                         // destructor unlocks, every path
}

int main() {
    std::thread t1(add);
    std::thread t2(add);   // both run NOW, on two real cores
    t1.join();             // wait for it to finish
    t2.join();             // counter == 200000, every run
}`,annotations:{9:`The RAII module, applied to locks: a mutex is a resource, and the destructor releases it even if the loop body throws. Manual lock()/unlock() leaks the lock on the first exception.`,10:`Without the lock this is a data race: two cores interleave the read-modify-write and updates vanish. In C++ a data race is UB — not merely a wrong count.`,16:`The Python contrast in one line: Python threads queue for the GIL; C++ threads are real parallelism — no GIL, both cores execute simultaneously.`,17:`A std::thread destroyed while still joinable calls std::terminate. Always join (wait) or detach (release); join is almost always what you want.`}},{type:`note`,md:"`std::atomic` in one line: `std::atomic<int> counter` makes single operations (`++counter`, loads, stores) race-free without a mutex — the right tool for a lone shared counter, while a mutex protects multi-step invariants. And `detach()` honestly: the thread runs unsupervised — fine for fire-and-forget background work, dangerous if it touches anything that might be destroyed before it finishes."}],quiz:[{question:'names is a vector<string>. After `for (auto s : names) s += "!";` the vector is…',options:[{text:`Every element ends with "!"`,explanation:`Plain auto COPIES each element — the += lands on the copy, not the vector.`},{text:`Unchanged — each iteration mutated a copy`,explanation:`Correct. auto deduces by value. You need auto& for writes to go through.`},{text:`A compile error`,explanation:`It compiles cleanly — that is exactly why the trap is dangerous.`}],correct:1},{question:`A constexpr function is called with a value read from the user at runtime. What happens?`,options:[{text:`Compile error — constexpr requires constants`,explanation:`No. constexpr is permission to run at compile time, not a requirement.`},{text:`It runs at runtime like a normal function`,explanation:`Correct. Constant args → compile time; runtime args → ordinary call. Same function, two execution times.`},{text:`The result was already cached at compile time`,explanation:`There is nothing to cache — the compiler never saw the input value.`}],correct:1},{question:`On a typical 64-bit build, sizeof(struct { char a; int b; char c; }) is…`,options:[{text:`6 — the sum of the fields`,explanation:`Ignores alignment. The int needs an address divisible by 4, so the compiler pads.`},{text:`12 — padding after each char`,explanation:`Correct: a(1) + pad(3) + b(4) + c(1) + tailpad(3). Reordering largest-first shrinks it to 8.`},{text:`8`,explanation:`8 is what you get AFTER reordering to {int, char, char}. Field order changes sizeof.`}],correct:1},{question:"The compiler turns `return x + 1 > x;` (signed int x) into `return true;`. Why is that legal?",options:[{text:`It is an optimizer bug`,explanation:`It is licensed behavior — every major compiler does it at -O2.`},{text:`Signed overflow is UB, so the compiler may assume it never happens`,explanation:`Correct. If overflow "cannot happen", x + 1 is always greater — and your overflow check is dead code.`},{text:`Signed ints wrap to INT_MIN, so it is true anyway`,explanation:`Wrapping is what many CPUs do, but the LANGUAGE says UB — and if it wrapped, the comparison would be false, not true.`}],correct:1},{question:`A use-after-free read printed the expected value in every test run. Conclusion?`,options:[{text:`The code is correct — the tests prove it`,explanation:`Tests cannot bless UB. It "works" only until the allocator reuses that memory.`},{text:`Still UB — any compiler, allocator, or timing change can turn it into corruption`,explanation:`Correct. UB places no requirements: working today is one of the legal outcomes, and so is corrupting data tomorrow.`},{text:`It is only UB in debug builds`,explanation:`UB is a property of the code, not the build mode — optimization just changes the symptoms.`}],correct:1},{question:`A std::thread object is destroyed while still joinable (no join, no detach). Result?`,options:[{text:`It joins automatically`,explanation:`That is C++20 std::jthread. Plain std::thread refuses to guess.`},{text:`It detaches automatically`,explanation:`No — silently detaching could leave a thread using destroyed objects.`},{text:`std::terminate — the program dies immediately`,explanation:`Correct. The language forces you to state your intent: join or detach, always.`}],correct:2},{question:`Why prefer std::lock_guard over calling m.lock() and m.unlock() yourself?`,options:[{text:`It is faster`,explanation:`Same cost — it compiles down to the same lock/unlock pair.`},{text:`RAII: the destructor unlocks on EVERY exit path, including exceptions and early returns`,explanation:`Correct. Manual unlock is skipped the first time something throws — and the mutex stays locked forever.`},{text:`It prevents deadlocks`,explanation:`It cannot — two lock_guards taken in opposite orders by two threads still deadlock.`}],correct:1},{question:`Two C++ threads run pure CPU number-crunching on a 2-core machine. Compared to one thread…`,options:[{text:`Same speed — a global lock serializes them`,explanation:`That is Python and its GIL. C++ has no interpreter lock.`},{text:`Roughly 2× faster — both cores genuinely compute in parallel`,explanation:`Correct. C++ threads are real OS threads with no GIL — CPU-bound work actually scales.`},{text:`Faster only for I/O-bound work`,explanation:`That is the Python-threads rule. In C++, CPU-bound work parallelizes too.`}],correct:1}],interviewQuestions:[{question:`Explain undefined behavior to a junior: what it is, why C++ has it, and what it means for testing.`,answer:`What: code the standard places NO requirements on — signed overflow, out-of-bounds, use-after-free, uninitialized reads. Not "a crash": any outcome is legal, including appearing to work. Why: performance — the compiler skips bounds and overflow checks and optimizes assuming UB never happens, so you pay only for what you write. Testing consequence: passing tests prove nothing about UB code; the behavior can change with a compiler flag or upgrade. The tradeoff sentence: C++ chose speed over safety nets, so the safety nets are tools you must run — ASan, UBSan — not guarantees the language gives you.`,isCaseBased:!1},{question:`Case: a function returns correct results in debug builds but wrong ones at -O2. No crash. Walk through your diagnosis.`,answer:`Optimization changing RESULTS (not just speed) screams UB — the optimizer transformed code under the assumption UB cannot happen. Plan: (1) rebuild with -fsanitize=undefined,address and run the failing input — UBSan catches signed overflow and bad shifts, ASan catches OOB and use-after-free; (2) prime suspects: uninitialized variable (debug zeroes stack memory more often), signed overflow in index math, OOB read that debug padding hid; (3) bisect with -O1/-O2 per file only if sanitizers come up empty. Name the tradeoff: sanitizers cost 2-10× runtime and are compiled-in, so they run in CI and fuzzing, not production. The wrong move is "fixing" it by shipping -O0 — that hides the bug, it does not remove it.`,isCaseBased:!0},{question:`In a range-based for, when do you write auto, auto&, and const auto&? What does each cost?`,answer:`const auto& to read: binds without copying — the default. auto& to mutate elements in place. Plain auto copies every element — for a vector<string> that is an allocation per iteration, silent and legal. Choose plain auto only when you WANT an independent copy to modify. Bonus nuance that impresses: for tiny trivially-copyable types (int, char) plain auto is fine and can even be faster than the indirection of a reference; the copy trap is about expensive-to-copy element types.`,isCaseBased:!1},{question:`Why does reordering struct fields change sizeof? When does anyone actually care?`,answer:`Alignment: each type wants addresses that are multiples of its alignment (int → 4), so the compiler inserts padding between fields and at the tail (tail padding keeps arrays aligned). {char, int, char} = 12 bytes; {int, char, char} = 8. When it matters: structs stored by the millions (arenas, game entities, network buffers) — padding multiplies into hundreds of MB and wasted cache lines. Tradeoffs: largest-first ordering is free but can hurt logical grouping; #pragma pack removes padding but produces unaligned accesses — slower on x86, potentially crashing on ARM. Order fields first; pack only for wire formats.`,isCaseBased:!1},{question:`A Pythonista asks: "Threads did nothing for my CPU-bound Python code. Will C++ threads be different?" Answer precisely.`,answer:`Yes, fundamentally. Python threads queue for the GIL — one thread executes bytecode at a time, so CPU-bound work gains nothing. C++ has no GIL: std::thread maps to a real OS thread, and 8 threads on 8 cores genuinely compute at 8×-ish. The tradeoff they must hear: the GIL was also protection. In C++ every unsynchronized shared write is a data race — UB, not just a stale read. So C++ buys real parallelism at the price of doing synchronization yourself: mutexes, atomics, or the best option — sharing nothing and merging results at the end.`,isCaseBased:!1},{question:`Case: two threads each increment a shared counter 100,000 times; the total lands below 200,000 and varies per run. Diagnose and give the fix ladder.`,answer:`Diagnosis: data race. ++counter is read-modify-write; two cores interleave (both read 41, both write 42) and increments vanish. And formally it is UB, so "usually close to 200k" is not a guarantee of anything. Fix ladder with tradeoffs: (1) std::atomic<int> — cheapest for a single counter, lock-free on every mainstream platform; (2) std::mutex + lock_guard — needed the moment the invariant spans multiple steps or variables, costs contention; (3) the senior answer: give each thread a LOCAL counter and add them after join — zero contention, fastest by far. Naming option 3 unprompted is what separates candidates: the best synchronization is not sharing.`,isCaseBased:!0},{question:`join() vs detach() — semantics, and when is detach ever defensible?`,answer:`join(): the calling thread blocks until the target finishes — results are ready, its resources are reclaimed, and anything it referenced provably outlived it. detach(): the thread runs unsupervised; you can no longer wait for it or know when it dies. Defensible detach: a true fire-and-forget daemon that owns everything it touches (copies its inputs, no references out). Danger: a detached thread holding a reference to a stack variable that dies first — use-after-free with no stack trace pointing at the cause. Default to join; treat every detach in review as guilty until proven self-contained. And destroying a std::thread while joinable is neither — it is std::terminate.`,isCaseBased:!1},{question:`What does constexpr buy a real codebase beyond "it is faster"?`,answer:`Three concrete wins: (1) required contexts — array sizes, template arguments, anything needing a value at compile time; (2) baked-in lookup tables (CRC tables, parsers, config) — zero startup cost and no runtime init-order issues; (3) moved failure: static_assert on constexpr results turns wrong constants into build errors, which cannot ship. Tradeoffs to name: heavy constexpr evaluation inflates compile times, and it only works when inputs are compile-time constants — a runtime input silently gets a normal call, so measure before assuming the fold happened.`,isCaseBased:!1},{question:`Case: a struct {char flag; int id; bool active; char tag;} is stored 100 million times in RAM. Estimate the memory and cut it.`,answer:`As declared: flag(1)+pad(3)+id(4)+active(1)+tag(1)+tailpad(2) = 12 bytes → 1.2 GB, of which 5 bytes per object — 500 MB — is pure padding. Reorder largest-first {int id; char flag; bool active; char tag;} = 4+1+1+1+pad(1) = 8 bytes → 800 MB: 400 MB saved by moving two lines. Further steps, each with a cost: pack flag/active/tag into one byte of bitfields (cheaper memory, slightly slower access, no address of a bitfield); structure-of-arrays if access patterns scan one field (huge cache win, code complexity up). Say the principle: sizeof is not the sum of the parts, and at scale, field order is a performance decision.`,isCaseBased:!0},{question:`Is a data race just "you might read a slightly stale value"? Why or why not?`,answer:`No — in C++ a data race is UB, full stop. Stale reads are the friendly symptom. Legal outcomes include torn reads (half-updated 64-bit value), invented values, and the optimizer hoisting the read out of your loop entirely — while(!done) becomes while(true) because without synchronization the compiler may assume no other thread changes done. Fixes and their costs: std::atomic establishes ordering for single variables cheaply; a mutex protects multi-variable invariants at contention cost. The interview phrase: unsynchronized concurrent access is not "risky", it is outside the language — the compiler optimizes as if it cannot happen.`,isCaseBased:!1}],flashcards:[{front:`constexpr in one sentence`,back:`Permission to compute at compile time: constant args → done by the compiler, baked into the binary; runtime args → normal call.`},{front:`auto deduction — the three rules`,back:`auto copies (drops & and top-level const) · auto& binds and keeps const · const auto& binds anything, never copies.`},{front:`Range-for habit`,back:`const auto& to read, auto& to mutate, plain auto only when you WANT a copy — plain auto on strings/vectors is the silent-copy trap.`},{front:`Structured bindings`,back:`C++17: auto [k, v] = expr unpacks pair/tuple/array/struct into named variables. Shines on map loops and insert results.`},{front:`Why sizeof(struct) ≠ sum of fields`,back:`Alignment padding — each type wants addresses that are multiples of its alignment. Reorder fields largest-first to shrink.`},{front:`Undefined behavior in one sentence`,back:`The standard places NO requirements — not "crash", ANYTHING. Optimizers assume UB never happens and transform code accordingly.`},{front:`The UB interview set`,back:`Signed integer overflow · out-of-bounds access · use-after-free · uninitialized reads.`},{front:`Data race — definition and severity`,back:`Two threads, same memory, at least one writes, no synchronization. It is UB — not just a stale value.`},{front:`std::lock_guard`,back:`RAII for mutexes: constructor locks, destructor unlocks on every exit path — exceptions included.`},{front:`C++ threads vs Python threads`,back:`No GIL in C++: threads are REAL parallelism, CPU work scales across cores. The cost: data races are yours to prevent. (And: destroy a joinable std::thread → std::terminate.)`}],mindmapMarkdown:`- Modern C++ Toolkit & the Sharp Edges
  - constexpr
    - Compile-time evaluation, permission not command
    - static_assert / array sizes / baked tables
  - auto & range-for
    - auto copies; auto& / const auto& don't
    - Accidental-copy trap in loops
  - Structured bindings (C++17)
    - auto [k, v] over maps
    - insert → [it, inserted]
  - Memory layout
    - Alignment → invisible padding
    - Field ORDER changes sizeof
    - Largest-first rule
  - Undefined behavior
    - No requirements = anything may happen
    - Signed overflow · OOB · use-after-free · uninit read
    - Optimizer assumes UB never happens
    - "It works" proves nothing
  - Threads
    - No GIL — REAL parallelism
    - join or detach, else std::terminate
    - Data race = UB
    - mutex + lock_guard (RAII) · atomic for counters`};export{e as default};