var e={id:`dsa-l0-array-patterns`,subjectId:`dsa`,level:0,title:`Array Patterns: Two Pointers, Sliding Window, Prefix Sums & Kadane`,whyItMatters:`A third of all easy/medium interview questions are an array plus one of four moves: two pointers, a window, a prefix table, or Kadane. Learn the four moves and the trigger phrases that pick between them, and "brute force is O(n²)… but" becomes a sentence you finish in five seconds.`,estMinutes:60,sections:[{type:`intuition`,title:`Four moves kill most nested loops`,md:`The brute force for almost every array question is the same: try all pairs or all subarrays — a nested loop, O(n²). The whole game of this module is deleting that inner loop.

- **Two pointers** — walk in from both ends; every step discards an index forever.
- **Sliding window** — reuse the previous subarray's work instead of recomputing.
- **Prefix sums** — precompute once, answer any range sum by subtraction.
- **Kadane** — carry one running best instead of re-scanning.
- Each is a pattern, not a problem. One pattern → twenty LeetCode questions.`},{type:`intuition`,title:`Converging two pointers: squeeze from both ends`,md:`Two people search a sorted bookshelf for two prices that add to ₹1600. One starts at the cheapest end, one at the priciest. Sum too big? The expensive person steps left. Too small? The cheap person steps right.

- Works because the data is **sorted** — each comparison tells you which pointer is *wrong*.
- The named problem: **Two Sum II** (sorted array, find a pair summing to target).
- Key claim: when the sum is too small, \`a[l]\` is **dead** — its best possible partner was \`a[r]\`, and even that failed. Discard it forever.
- Every step retires one index. n indices → at most n steps → **O(n)**, O(1) space.
- Brute force compared every pair: O(n²). Sorting bought you an oracle.`},{type:`code`,lang:`cpp`,title:`Two Sum II — the converging template`,code:`vector<int> twoSumSorted(const vector<int>& a, int target) {
    int l = 0, r = (int)a.size() - 1;
    while (l < r) {
        int sum = a[l] + a[r];
        if (sum == target) return {l, r};
        if (sum < target) l++;     // a[l] is dead: even its best partner was too small
        else r--;                  // a[r] is dead: even its smallest partner was too big
    }
    return {-1, -1};               // pointers met: no pair exists
}`,annotations:{3:`Invariant: if an answer pair exists, both its indices are still inside [l, r]. Every move must preserve this.`,6:`The discard argument, said out loud: a[r] is the LARGEST partner a[l] can still get. a[l] + a[r] < target means a[l] plus anything remaining is smaller still. a[l] can never be in the answer.`,7:`Mirror image: a[l] is the smallest partner left for a[r]. Still too big → a[r] is unpairable. This certainty is what sortedness buys.`},py:{code:`def twoSumSorted(a: list[int], target: int) -> list[int]:
    l, r = 0, len(a) - 1
    while l < r:
        total = a[l] + a[r]        # \`sum\` is a builtin — name the variable total
        if total == target:
            return [l, r]
        if total < target:
            l += 1                 # a[l] is dead: even its best partner was too small
        else:
            r -= 1                 # a[r] is dead: even its smallest partner was too big
    return [-1, -1]                # pointers met: no pair exists`,annotations:{3:`Invariant: if an answer pair exists, both its indices are still inside [l, r]. Every move must preserve this.`,8:`The discard argument, said out loud: a[r] is the LARGEST partner a[l] can still get. a[l] + a[r] < target means a[l] plus anything remaining is smaller still. a[l] can never be in the answer.`,10:`Mirror image: a[l] is the smallest partner left for a[r]. Still too big → a[r] is unpairable. This certainty is what sortedness buys.`}}},{type:`visual`,component:`PointerBoxDiagram`,props:{title:`Two Sum II: l and r converge on target 16`,notice:`Every move retires one index forever (shown grayed out) — that is the whole O(n) argument.`,leftLabel:`pointers`,rightLabel:`sorted array a`,frames:[{note:`Start at the ends. a[l]=2, a[r]=15, sum 17 > 16 — too big. Only r-- can lower the sum.`,stack:[{name:`l`,to:`i0`},{name:`r`,to:`i5`},{name:`sum`,value:`17 > 16`}],heap:[{id:`i0`,value:`2`,label:`idx 0`},{id:`i1`,value:`3`,label:`idx 1`},{id:`i2`,value:`5`,label:`idx 2`},{id:`i3`,value:`8`,label:`idx 3`},{id:`i4`,value:`11`,label:`idx 4`},{id:`i5`,value:`15`,label:`idx 5`}]},{note:`r moved in: 2 + 11 = 13 < 16 — too small. Now a[l]=2 is dead: 11 is the biggest partner it can still get. l++.`,stack:[{name:`l`,to:`i0`},{name:`r`,to:`i4`},{name:`sum`,value:`13 < 16`}],heap:[{id:`i0`,value:`2`,label:`idx 0`},{id:`i1`,value:`3`,label:`idx 1`},{id:`i2`,value:`5`,label:`idx 2`},{id:`i3`,value:`8`,label:`idx 3`},{id:`i4`,value:`11`,label:`idx 4`},{id:`i5`,value:`15`,label:`discarded`,freed:!0}]},{note:`3 + 11 = 14 < 16 — still too small. a[l]=3 is dead by the same argument. l++ again.`,stack:[{name:`l`,to:`i1`},{name:`r`,to:`i4`},{name:`sum`,value:`14 < 16`}],heap:[{id:`i0`,value:`2`,label:`discarded`,freed:!0},{id:`i1`,value:`3`,label:`idx 1`},{id:`i2`,value:`5`,label:`idx 2`},{id:`i3`,value:`8`,label:`idx 3`},{id:`i4`,value:`11`,label:`idx 4`},{id:`i5`,value:`15`,label:`discarded`,freed:!0}]},{note:`5 + 11 = 16 — found, indices 2 and 4. Three moves, three indices retired. Worst case: n moves total, so O(n).`,stack:[{name:`l`,to:`i2`},{name:`r`,to:`i4`},{name:`sum`,value:`16 = 16`}],heap:[{id:`i0`,value:`2`,label:`discarded`,freed:!0},{id:`i1`,value:`3`,label:`discarded`,freed:!0},{id:`i2`,value:`5`,label:`answer`},{id:`i3`,value:`8`,label:`idx 3`},{id:`i4`,value:`11`,label:`answer`},{id:`i5`,value:`15`,label:`discarded`,freed:!0}]}]}},{type:`intuition`,title:`Container With Most Water: move the shorter wall`,md:`Two walls hold water between them. Held water = **shorter wall's height × distance apart**. Find the pair of walls holding the most.

- Start at both ends (widest container). Each step, one pointer must move in — but which?
- **Always the shorter one.** Here is why it is safe to never look back:
- The area is capped by the shorter wall. Moving the *taller* wall inward shrinks the width and the height cap stays at (or below) the same shorter wall — the area **cannot improve**.
- So every pair involving the current shorter wall is already beaten or tied by what we just measured. That wall is exhausted. Retire it.
- Same skeleton as Two Sum II: one index dies per step → O(n).`},{type:`code`,lang:`cpp`,title:`Container With Most Water`,code:`int maxArea(const vector<int>& h) {
    int l = 0, r = (int)h.size() - 1, best = 0;
    while (l < r) {
        int area = min(h[l], h[r]) * (r - l);   // height capped by the SHORTER wall
        best = max(best, area);
        if (h[l] < h[r]) l++;                   // shorter wall exhausted -- retire it
        else r--;
    }
    return best;
}`,annotations:{4:`Width starts maximal and only shrinks. So a later pair can only win through a taller minimum — which is exactly why the shorter wall must go.`,6:`The proof in one breath: keeping the shorter wall while width shrinks can never beat the area we just recorded. Interviewers ask for exactly this sentence.`},py:{code:`def maxArea(h: list[int]) -> int:
    l, r, best = 0, len(h) - 1, 0
    while l < r:
        area = min(h[l], h[r]) * (r - l)    # height capped by the SHORTER wall
        best = max(best, area)
        if h[l] < h[r]:
            l += 1                          # shorter wall exhausted -- retire it
        else:
            r -= 1
    return best`,annotations:{4:`Width starts maximal and only shrinks. So a later pair can only win through a taller minimum — which is exactly why the shorter wall must go.`,6:`The proof in one breath: keeping the shorter wall while width shrinks can never beat the area we just recorded. Interviewers ask for exactly this sentence.`}}},{type:`intuition`,title:`3Sum: the same move, one loop deeper`,md:`**3Sum**: find all unique triplets summing to 0. The sketch every interviewer expects:

- Sort the array: O(n log n).
- Fix the first element \`a[i]\` with an outer loop. The rest of the problem is now Two Sum II: find a pair in \`a[i+1..]\` summing to \`-a[i]\`.
- Run the converging-pointer scan for each i: O(n) per fix, n fixes → **O(n²)** total. The brute force was O(n³).
- Duplicate handling: skip repeated values at all three positions (advance i, l, r past equal neighbors) so triplets come out unique.
- The lesson: sorted + pair-hunting is two pointers, even when it hides inside a bigger problem.`},{type:`note`,md:`The *other* two-pointer family — **fast/slow pointers** moving in the same direction at different speeds — is how you detect cycles and find midpoints in linked lists (Floyd's algorithm). It gets full treatment in the linked-list module.`},{type:`intuition`,title:`Sliding window, fixed size: stop recomputing`,md:`"Best average temperature over any 7 consecutive days." Brute force sums every 7-day block from scratch: O(n·k). But two neighboring blocks share 6 days — you are re-adding almost everything.

- A window sliding one step right changes by exactly two elements: **one enters, one leaves**.
- So: \`sum += a[i] - a[i-k]\` — an O(1) update instead of an O(k) rescan.
- The named problem: **Maximum Sum Subarray of Size K**.
- Build the first window in O(k), then n−k slides at O(1) each → **O(n)** total, O(1) space.
- Fixed-size windows are the warm-up. The interview weapon is the variable-size window, next.`},{type:`code`,lang:`cpp`,title:`Max sum of any k consecutive elements`,code:`long long maxSumK(const vector<int>& a, int k) {
    long long sum = 0;
    for (int i = 0; i < k; i++) sum += a[i];   // build the first window: O(k)
    long long best = sum;
    for (int i = k; i < (int)a.size(); i++) {
        sum += a[i] - a[i - k];                // one enters, one leaves: O(1) per slide
        best = max(best, sum);
    }
    return best;                               // total O(n), space O(1)
}`,annotations:{3:`Only the first window is ever summed from scratch. Every later window is the previous one, patched.`,6:`The signature line of every fixed-window solution. If your loop re-sums k elements, you are writing the brute force with extra steps.`},py:{code:`def maxSumK(a: list[int], k: int) -> int:
    total = sum(a[:k])                     # build the first window: O(k)
    best = total
    for i in range(k, len(a)):
        total += a[i] - a[i - k]           # one enters, one leaves: O(1) per slide
        best = max(best, total)
    return best                            # total O(n), space O(1)`,annotations:{2:`Only the first window is ever summed from scratch. Every later window is the previous one, patched. (No long long to worry about — Python ints never overflow.)`,5:`The signature line of every fixed-window solution. If your loop re-sums k elements, you are writing the brute force with extra steps.`}}},{type:`intuition`,title:`Variable window: grow greedily, shrink minimally`,md:`**Longest Substring Without Repeating Characters**: longest stretch of a string with no repeated letter. The window is now elastic, ruled by one invariant: *the window never contains a repeat*.

- **Grow**: every step, \`r\` moves right and takes one new character in.
- **Shrink**: if that character breaks the invariant (it is already inside), \`l\` moves right, evicting characters, until the older copy is gone.
- The window is valid again after every step — so \`r - l + 1\` is always a legal answer candidate.
- The O(n) argument, exam-ready: **each index enters the window once (via r) and leaves at most once (via l)**. Total pointer moves ≤ 2n, whatever the inner while looks like.
- This grow/shrink skeleton solves the whole "longest/shortest subarray satisfying a constraint" family.`},{type:`code`,lang:`cpp`,title:`Longest substring without repeats`,code:`int lengthOfLongestSubstring(const string& s) {
    unordered_set<char> in;               // chars currently inside the window
    int best = 0, l = 0;
    for (int r = 0; r < (int)s.size(); r++) {
        while (in.count(s[r]))            // s[r] already inside? invariant broken
            in.erase(s[l++]);             // shrink from the left until it is gone
        in.insert(s[r]);                  // now s[r] can enter
        best = max(best, r - l + 1);      // window is valid here, always
    }
    return best;                          // "abcabcbb" -> 3  ("abc")
}`,annotations:{5:`A while inside a for LOOKS like O(n²). It is not: the while body only ever evicts, and each index can be evicted once in the entire run.`,6:`l only moves right, forever. l and r each travel at most n steps total — amortized O(n). Say "amortized" and the interviewer relaxes.`,8:`Record AFTER restoring the invariant, never before — otherwise you count a window containing a repeat.`},py:{code:`def lengthOfLongestSubstring(s: str) -> int:
    inside = set()                    # chars currently inside the window
    best, l = 0, 0
    for r, c in enumerate(s):
        while c in inside:            # s[r] already inside? invariant broken
            inside.remove(s[l])       # shrink from the left until it is gone
            l += 1
        inside.add(c)                 # now s[r] can enter
        best = max(best, r - l + 1)   # window is valid here, always
    return best                       # "abcabcbb" -> 3  ("abc")`,annotations:{5:`A while inside a for LOOKS like O(n²). It is not: the while body only ever evicts, and each index can be evicted once in the entire run.`,6:`Python has no l++, so the C++ one-liner erase(s[l++]) splits into remove-then-advance. Same two steps, spelled out.`,7:`l only moves right, forever. l and r each travel at most n steps total — amortized O(n). Say "amortized" and the interviewer relaxes.`,9:`Record AFTER restoring the invariant, never before — otherwise you count a window containing a repeat.`}}},{type:`visual`,component:`PointerBoxDiagram`,props:{title:`Variable window on "abcbd" — grow, break, shrink, grow`,notice:`Grow with r every step; shrink with l only when the no-repeat invariant breaks. Grayed cells have left the window.`,leftLabel:`pointers`,rightLabel:`s = "abcbd"`,frames:[{note:`Start: both pointers at index 0. Window "a", length 1. No repeat — nothing to shrink.`,stack:[{name:`l`,to:`i0`},{name:`r`,to:`i0`},{name:`window`,value:`"a"`},{name:`best`,value:`1`}],heap:[{id:`i0`,value:`a`,label:`idx 0`},{id:`i1`,value:`b`,label:`idx 1`},{id:`i2`,value:`c`,label:`idx 2`},{id:`i3`,value:`b`,label:`idx 3`},{id:`i4`,value:`d`,label:`idx 4`}]},{note:`r grows twice more: "abc". Still no repeats, so l never moved. best = 3.`,stack:[{name:`l`,to:`i0`},{name:`r`,to:`i2`},{name:`window`,value:`"abc"`},{name:`best`,value:`3`}],heap:[{id:`i0`,value:`a`,label:`idx 0`},{id:`i1`,value:`b`,label:`idx 1`},{id:`i2`,value:`c`,label:`idx 2`},{id:`i3`,value:`b`,label:`idx 3`},{id:`i4`,value:`d`,label:`idx 4`}]},{note:`r reaches index 3: a second "b". The invariant is broken — the window must shrink before "b" can enter.`,stack:[{name:`l`,to:`i0`},{name:`r`,to:`i3`,danger:!0},{name:`window`,value:`"abc" + b?`},{name:`best`,value:`3`}],heap:[{id:`i0`,value:`a`,label:`idx 0`},{id:`i1`,value:`b`,label:`duplicate!`},{id:`i2`,value:`c`,label:`idx 2`},{id:`i3`,value:`b`,label:`idx 3`},{id:`i4`,value:`d`,label:`idx 4`}]},{note:`Shrink: l evicts s[0]="a", then s[1]="b". The moment the old "b" leaves, the window is valid: "cb".`,stack:[{name:`l`,to:`i2`},{name:`r`,to:`i3`},{name:`window`,value:`"cb"`},{name:`best`,value:`3`}],heap:[{id:`i0`,value:`a`,label:`evicted`,freed:!0},{id:`i1`,value:`b`,label:`evicted`,freed:!0},{id:`i2`,value:`c`,label:`idx 2`},{id:`i3`,value:`b`,label:`idx 3`},{id:`i4`,value:`d`,label:`idx 4`}]},{note:`Grow resumes: window "cbd", length 3. Every index entered once and left at most once — 2n pointer moves max. That IS the O(n) proof.`,stack:[{name:`l`,to:`i2`},{name:`r`,to:`i4`},{name:`window`,value:`"cbd"`},{name:`best`,value:`3`}],heap:[{id:`i0`,value:`a`,label:`evicted`,freed:!0},{id:`i1`,value:`b`,label:`evicted`,freed:!0},{id:`i2`,value:`c`,label:`idx 2`},{id:`i3`,value:`b`,label:`idx 3`},{id:`i4`,value:`d`,label:`idx 4`}]}]}},{type:`intuition`,title:`Prefix sums: odometer readings`,md:"A car odometer shows total km since forever. Distance driven between two cities? Subtract two readings — you never re-drive the road.\n\n- `pre[i]` = sum of the **first i** elements, with `pre[0] = 0`. One O(n) pass builds it.\n- Any range sum, forever after: `sum(a[l..r]) = pre[r+1] - pre[l]` — **O(1)** per query.\n- The trade: O(n) space and one precompute pass buys unlimited O(1) queries. With q queries, O(n + q) beats O(n·q) rescanning.\n- The off-by-one is the whole trap: `pre` has n+1 entries, and the formula is r+1, not r. Draw it once, keep it forever.\n- Works for immutable arrays. If elements update between queries, you need a Fenwick/segment tree — Level 3."},{type:`code`,lang:`cpp`,title:`Build once, answer any range in O(1)`,code:`vector<int> a = {3, 1, 4, 1, 5, 9, 2, 6};
vector<long long> pre(a.size() + 1, 0);   // pre[i] = sum of the FIRST i elements
for (size_t i = 0; i < a.size(); i++)
    pre[i + 1] = pre[i] + a[i];           // pre: 0 3 4 8 9 14 23 25 31

// sum of a[l..r] inclusive = pre[r + 1] - pre[l]
long long mid = pre[6] - pre[2];          // a[2..5] = 4+1+5+9 = 19
long long all = pre[8] - pre[0];          // whole array = 31`,annotations:{2:`n+1 slots and pre[0] = 0 — the empty prefix. This sentinel is what makes ranges starting at index 0 need no special case.`,4:`Each entry is the previous entry plus one element: a single O(n) pass. long long because prefix totals overflow int long before individual elements do.`,7:`Everything before r+1, minus everything before l — what remains is exactly a[l..r]. The +1 is where wrong answers come from; desk-check it once per interview.`},py:{code:`a = [3, 1, 4, 1, 5, 9, 2, 6]
pre = [0] * (len(a) + 1)          # pre[i] = sum of the FIRST i elements
for i, x in enumerate(a):
    pre[i + 1] = pre[i] + x       # pre: 0 3 4 8 9 14 23 25 31

# sum of a[l..r] inclusive = pre[r + 1] - pre[l]
mid = pre[6] - pre[2]             # a[2..5] = 4+1+5+9 = 19
whole = pre[8] - pre[0]           # whole array = 31`,annotations:{2:`n+1 slots and pre[0] = 0 — the empty prefix. This sentinel is what makes ranges starting at index 0 need no special case.`,4:`Each entry is the previous entry plus one element: a single O(n) pass. The stdlib one-liner is list(accumulate(a, initial=0)) from itertools — write the loop in an interview, then say you know the one-liner.`,7:`Everything before r+1, minus everything before l — what remains is exactly a[l..r]. The +1 is where wrong answers come from; desk-check it once per interview.`}}},{type:`intuition`,title:`Subarray Sum Equals K: prefix sums meet a hashmap`,md:'"Count subarrays summing to exactly k" — with negatives allowed, so windows break (growing might *decrease* the sum). Prefix sums turn it into algebra:\n\n- A subarray `(l..r]` sums to k **iff** `pre[r] - pre[l] = k`, i.e. `pre[l] = pre[r] - k`.\n- So walking left to right with running sum `run`: count how many *earlier* prefixes equaled `run - k`. Each one is a valid left edge ending here.\n- Store prefix counts in an `unordered_map` as you go: lookup O(1) average → **O(n)** total.\n- Seed `seen[0] = 1` — the empty prefix — or every subarray starting at index 0 goes uncounted.\n- Bridge line: this "have I seen the complement before?" hashmap is the exact move the upcoming **hashing module** generalizes — Two Sum (unsorted), longest subarray with sum k, and friends.'},{type:`code`,lang:`cpp`,title:`Subarray Sum Equals K`,code:`int subarraySum(const vector<int>& a, int k) {
    unordered_map<long long, int> seen;   // prefix value -> times seen so far
    seen[0] = 1;                          // the empty prefix, before index 0
    long long run = 0;
    int count = 0;
    for (int x : a) {
        run += x;                         // run = pre[here]
        auto it = seen.find(run - k);     // earlier prefix p with run - p = k?
        if (it != seen.end()) count += it->second;
        seen[run]++;
    }
    return count;                         // [1, 2, 3], k=3 -> 2  ([1,2] and [3])
}`,annotations:{3:`Forget this line and [1,2] in the example silently vanishes: run=3 needs an earlier prefix 0 to exist.`,8:`find, not operator[] — operator[] would INSERT a zero for every miss and bloat the map (the STL module trap, live).`,10:`Count BEFORE inserting run: a subarray of sum k=0 must not match its own prefix. Order matters here.`},py:{code:`from collections import defaultdict

def subarraySum(a: list[int], k: int) -> int:
    seen = defaultdict(int)           # prefix value -> times seen so far
    seen[0] = 1                       # the empty prefix, before index 0
    run = 0
    count = 0
    for x in a:
        run += x                      # run = pre[here]
        count += seen.get(run - k, 0)  # earlier prefix p with run - p = k?
        seen[run] += 1
    return count                      # [1, 2, 3], k=3 -> 2  ([1,2] and [3])`,annotations:{5:`Forget this line and [1,2] in the example silently vanishes: run=3 needs an earlier prefix 0 to exist.`,10:`.get(x, 0), not seen[x] — indexing a defaultdict INSERTS a zero for every miss and bloats the map. Exactly the C++ operator[] trap, in Python spelling.`,11:`Count BEFORE inserting run: a subarray of sum k=0 must not match its own prefix. Order matters here.`}}},{type:`intuition`,title:`Kadane: a negative running sum is dead weight`,md:"**Maximum Subarray**: the contiguous run with the largest sum. You are hiking and collecting coins and debts left to right. If everything you carry nets *negative*, dragging it further only poisons what comes next — drop the bag, restart here.\n\n- One value per position: `cur` = best sum of a subarray **ending exactly here**.\n- Recurrence: `cur = max(a[i], cur + a[i])` — extend the previous run, or restart at i.\n- Why optimal: the best subarray ending at i either includes i−1's run or it does not — those are the only two shapes, and we keep the better one at every step. The global answer ends *somewhere*, so tracking `max(cur)` over all i catches it.\n- Restart triggers exactly when `cur < 0`: a negative prefix can only lower any sum it is glued to.\n- O(n) time, O(1) space. This is your first dynamic program — the DP ladder in Level 3 starts here."},{type:`code`,lang:`cpp`,title:`Kadane in six lines`,code:`int maxSubArray(const vector<int>& a) {
    int cur = a[0], best = a[0];        // best subarray ending at 0 is just a[0]
    for (size_t i = 1; i < a.size(); i++) {
        cur = max(a[i] + cur, a[i]);    // extend the run, or restart at i
        best = max(best, cur);
    }
    return best;   // [-2,1,-3,4,-1,2,1,-5,4] -> 6  (the run 4,-1,2,1)
}`,annotations:{2:`Initialize from a[0], NOT from 0. best = 0 returns 0 for an all-negative array — where the true answer is the largest (least negative) element. Classic silent bug.`,4:`a[i] + cur < a[i] exactly when cur < 0 — "the bag is dead weight, drop it". The max() encodes the restart with no if.`},py:{code:`def maxSubArray(a: list[int]) -> int:
    cur = best = a[0]              # best subarray ending at 0 is just a[0]
    for x in a[1:]:
        cur = max(x + cur, x)      # extend the run, or restart at x
        best = max(best, cur)
    return best   # [-2,1,-3,4,-1,2,1,-5,4] -> 6  (the run 4,-1,2,1)`,annotations:{2:`Initialize from a[0], NOT from 0. best = 0 returns 0 for an all-negative array — where the true answer is the largest (least negative) element. Classic silent bug.`,4:`x + cur < x exactly when cur < 0 — "the bag is dead weight, drop it". The max() encodes the restart with no if.`}}},{type:`intuition`,title:`Trigger phrases: hear the question, name the pattern`,md:`Interviews are pattern-recognition under time pressure. Wire these reflexes:

- **"sorted array" + "find a pair/triplet"** → converging two pointers (Two Sum II, 3Sum).
- **"contiguous subarray/substring" + a constraint** ("no repeats", "at most k distinct", "sum ≤ S, all positive") → sliding window, variable size.
- **"every subarray of size exactly k"** → fixed window, enter/leave update.
- **"range sum", "many queries", "between index l and r"** → prefix sums.
- **"subarray summing to k" with negatives possible** → prefix sums + hashmap (windows break on negatives).
- **"maximum subarray" / "best contiguous run"** → Kadane.
- **"cycle" or "middle" in a linked structure** → fast/slow pointers (next module family).`}],quiz:[{question:`Two Sum II on a sorted array: a[l] + a[r] is SMALLER than the target. What is the correct move, and why?`,options:[{text:`r-- , to try a smaller value`,explanation:`Backwards — the sum is already too small; shrinking the larger side only lowers it further.`},{text:`l++ , because a[l] just failed with the largest partner it could ever get`,explanation:`Correct. a[r] is the biggest remaining candidate. If even a[l] + a[r] falls short, a[l] is unpairable — discard it forever.`},{text:`Move both pointers inward to converge faster`,explanation:`Moving both can jump over the answer pair. Only one index per step is provably dead.`}],correct:1},{question:`Container With Most Water: why is it always safe to move the SHORTER wall inward?`,options:[{text:`The taller wall might pair better with a wall further in, so we keep it`,explanation:`That is a description of hope, not the proof. The proof is about why the shorter wall is exhausted.`},{text:`Keeping the shorter wall while width shrinks can never beat the area just measured — height stays capped by that wall, width only drops`,explanation:`Correct. Every future pair containing the current shorter wall is ≤ the area we already recorded. It is exhausted; retire it.`},{text:`It is a heuristic that works on most inputs`,explanation:`It is an exact algorithm with a proof, not a heuristic. Interviewers specifically probe whether you know the difference.`}],correct:1},{question:`The no-repeat-substring solution has a while loop inside a for loop. Why is it O(n) and not O(n²)?`,options:[{text:`The while loop rarely executes in practice`,explanation:`"Rarely" is not an argument. The bound holds on EVERY input, including adversarial ones.`},{text:`Each index enters the window once (via r) and leaves at most once (via l) — at most 2n pointer moves in total`,explanation:`Correct. The amortized argument: charge each while-iteration to the index it evicts. No index is evicted twice.`},{text:`The alphabet has only 26 letters, bounding the loop`,explanation:`The alphabet bounds the WINDOW SIZE, and the algorithm stays O(n) even for arbitrary alphabets. The real argument is enter-once/leave-once.`},{text:`It is O(n²) worst case, but fast on average`,explanation:`No — the 2n bound on total pointer movement is a worst-case guarantee.`}],correct:1},{question:`Sliding a fixed size-k window one step to the right costs how much?`,options:[{text:`O(k) — re-sum the new window`,explanation:`That is the brute force in disguise: n windows × O(k) = O(n·k). The window pattern exists to kill exactly this.`},{text:`O(1) — add the entering element, subtract the leaving one`,explanation:`Correct. Neighboring windows share k−1 elements; only the two edges change: sum += a[i] − a[i−k].`},{text:`O(log k) — using a heap`,explanation:`For max SUM no heap is needed. (Sliding-window MAXIMUM does need a monotonic deque — a different, harder problem.)`}],correct:1},{question:`With pre[i] = sum of the first i elements (pre[0] = 0), the sum of a[l..r] inclusive is:`,options:[{text:`pre[r] - pre[l]`,explanation:`Off by one: this sums a[l..r-1], dropping a[r]. The most common prefix-sum bug in interviews.`},{text:`pre[r + 1] - pre[l]`,explanation:`Correct. Everything before index r+1, minus everything before index l, leaves exactly a[l..r].`},{text:`pre[r + 1] - pre[l + 1]`,explanation:`This drops a[l] — it sums a[l+1..r].`}],correct:1},{question:`Why can't a sliding window solve "count subarrays with sum = k" when the array contains negative numbers?`,options:[{text:`It can — the window just needs to shrink more carefully`,explanation:`No amount of care fixes it: the window cannot know whether growing or shrinking moves the sum toward k.`},{text:`With negatives, growing the window can DECREASE the sum — the monotonicity that tells the window when to grow vs shrink is gone`,explanation:`Correct. Windows need "grow ⇒ measure rises, shrink ⇒ measure falls". Negatives break that, which is why prefix sums + hashmap take over.`},{text:`Negative k values overflow the sum variable`,explanation:`Overflow is a separate (fixable) concern. The structural problem is lost monotonicity.`}],correct:1},{question:`In Subarray Sum Equals K, why seed the map with seen[0] = 1?`,options:[{text:`To avoid a division by zero in the hash function`,explanation:`No division happens anywhere. The seed is about counting, not hashing.`},{text:`It represents the empty prefix, so subarrays STARTING at index 0 get counted`,explanation:`Correct. When run itself equals k, the matching earlier prefix is pre[0] = 0 — which exists only if you put it there.`},{text:`It handles the case of an empty input array`,explanation:`An empty array returns 0 with or without the seed — the loop never runs.`}],correct:1},{question:`Kadane on [-5, -2, -8] — a correctly written implementation returns:`,options:[{text:`0 — take the empty subarray`,explanation:`Returning 0 is THE classic bug (initializing best to 0). The standard problem requires a non-empty subarray.`},{text:`-2 — the largest single element`,explanation:`Correct. Initializing cur and best from a[0] (not 0) makes all-negative arrays fall out naturally: the best run is one element.`},{text:`-15 — the whole array`,explanation:`Extending a negative run only digs deeper. cur = max(a[i], cur + a[i]) restarts instead.`}],correct:1}],interviewQuestions:[{question:`Prove that Container With Most Water's "move the shorter wall" rule never skips the optimal pair.`,answer:`Exchange argument. Suppose h[l] ≤ h[r] and we move l. The pairs we skip are (l, x) for every x between l and r. For each: width is smaller than (r − l), and height is min(h[l], h[x]) ≤ h[l] — so area(l, x) ≤ h[l] × (r − l) = area(l, r), which we already recorded. Every skipped pair is dominated by a measured one, so the maximum survives. Each step retires one index → O(n) time, O(1) space. The transferable idea: two pointers works whenever one comparison PROVES an index can never appear in a better answer.`,isCaseBased:!1},{question:`Two Sum on an UNSORTED array vs Two Sum II on a sorted one — compare the standard approaches and their tradeoffs.`,answer:`Unsorted: one pass with an unordered_map of value → index; for each x check whether target − x was seen. O(n) time, O(n) space, order untouched — the default. Sorted (or sorting allowed): converging two pointers — O(n) time after O(n log n) sort, O(1) extra space. Tradeoffs to say out loud: the hashmap wins on time when data is unsorted (sorting just to two-pointer costs n log n); the two-pointer wins on space and enumerates pairs in order, which is why 3Sum builds on it (a hashmap version drowns in duplicate handling). If asked to return original indices after sorting, note you must carry (value, index) pairs.`,isCaseBased:!1},{question:`Sketch the standard 3Sum solution and justify its complexity. How do you avoid duplicate triplets?`,answer:`Sort: O(n log n). For each i, run converging two pointers on a[i+1..n−1] hunting sum = −a[i]: O(n) per i, so O(n²) total, dominating the sort. Space O(1) beyond output. Duplicates: after processing an i, skip while a[i] == a[i−1]; after finding a triplet, advance l past equal values and r past equal values. All three skips are needed — miss one and the output has repeats. Follow-up they like: why not hash? A hash-based pair lookup per i is also O(n²) but duplicate control gets ugly and constants are worse; sorted two-pointer is the expected answer.`,isCaseBased:!1},{question:`Walk me through why the variable-size sliding window is O(n) — the amortized argument, not hand-waving.`,answer:`Both pointers only ever move right. r moves exactly n times. l moves at most n times over the WHOLE run — it never resets. So total pointer movement ≤ 2n, and every iteration of the inner while does O(1) work tied to one l-move (one eviction). Charge each eviction to the index evicted: each index enters the window once and leaves at most once. The nested-loop appearance is misleading because the inner loop's total work across ALL outer iterations is bounded, not its per-iteration work. The word that earns points: amortized.`,isCaseBased:!1},{question:`Case: your sliding-window solution to "count subarrays with sum exactly k" passes the sample tests but fails hidden tests. The samples were all-positive arrays. Diagnose and fix.`,answer:`Diagnosis: the hidden tests contain negatives (or zeros). The window relies on monotonicity — extend right ⇒ sum grows, shrink left ⇒ sum falls. With negatives both directions are ambiguous, so the window misses valid subarrays. Fix: prefix sums + hashmap. Keep running sum run and a map of prefix-value counts seeded with {0: 1}; at each element, add count of (run − k) to the answer, then increment the count of run. O(n) time, O(n) space, correct for any signs. Sanity check to offer: [1, −1, 1, −1] with k = 0 → 4 subarrays. If the interviewer confirms all-positive input, the window IS the better answer: O(1) space.`,isCaseBased:!0},{question:`Case: you solved Maximum Subarray with a double loop over all subarrays — O(n²) — and it TLEs at n = 1e5. Fix it and convince me the fix is correct.`,answer:`Kadane, O(n). Define cur = best sum of a subarray ending exactly at i. Recurrence: cur = max(a[i], cur + a[i]) — the best run ending at i either extends the best run ending at i−1 or starts fresh; those are the only two shapes a subarray ending at i can have, so taking the max is exhaustive, not greedy hope. The intuition: a negative running sum is dead weight — attached to anything, it only lowers the total, so restart. The global optimum ends at SOME index, and best = max over all cur values catches it there. Track a[0] as the initial value (not 0) so all-negative arrays return the largest element. n = 1e5: ~1e5 operations vs ~5e9 — well inside any time limit. O(1) space.`,isCaseBased:!0},{question:`Extend Kadane to also return the START and END indices of the best subarray.`,answer:`Track three extra variables. A candidate start position: when cur restarts (a[i] alone beats cur + a[i]), set curStart = i. When best improves, record bestL = curStart, bestR = i. Same O(n)/O(1). The follow-ups this invites: ties (first vs last maximal subarray — decide via > vs >=) and the empty-array contract (define it, don't assume). Being able to reconstruct the WITNESS, not just the value, is a recurring DP skill — the same trick reappears in LIS and edit distance.`,isCaseBased:!1},{question:`What exactly must be true about a problem for the sliding-window pattern to apply? Give an example where it silently fails.`,answer:`Two requirements: (1) the answer is a CONTIGUOUS subarray/substring, and (2) the constraint is monotone under growth — extending a window can only move the measured quantity in one known direction, so validity tells you unambiguously whether to grow or shrink (sum with all-positive elements, count of distinct characters, count of violations). Silent failure: "subarray with sum = k" over integers with negatives — growth no longer implies a larger sum, the grow/shrink decision becomes ambiguous, and the window skips answers without any error. The repair there is prefix sums + hashmap. Saying requirement (2) unprompted is a strong signal.`,isCaseBased:!1},{question:`Case: a metrics service receives one reading per second and must report, on demand, the maximum sum over any k consecutive readings from the last hour. Design the hot path.`,answer:`Fixed-size window over a stream. Keep a ring buffer of the last k readings (O(k) memory), a running window sum, and a running max-of-sums. Per reading: sum += new − buffer[pos]; overwrite; update max — O(1) per event, no rescans. Two honest caveats to raise: (1) the running max answers "max so far", but "max over the last hour" needs expiring old window-sums — a monotonic deque over window sums gives O(1) amortized expiry (that pattern is formalized in the stacks/queues module); (2) floating-point drift if readings are floats — recompute the sum every N events or use integers (fixed-point). Naming the drift issue is what separates the answer from a LeetCode paste.`,isCaseBased:!0},{question:`You get q range-sum queries on an array. When is building a prefix array the right call, and when is it the wrong one?`,answer:`Prefix array: O(n) build, O(1) per query, O(n) space → total O(n + q) vs O(n·q) for rescanning. Right call whenever the array is immutable and q > 1 — and at q = 1 it ties the naive scan, so it is essentially never worse on reads. Wrong call when the array UPDATES between queries: one point update invalidates O(n) prefix entries, so interleaved updates and queries degenerate to O(n) per update. Then the answer is a Fenwick tree or segment tree — O(log n) for both operations (Level 3 here). Bonus point: 2D prefix sums give O(1) rectangle sums via inclusion-exclusion — same idea, one dimension up.`,isCaseBased:!1},{question:`Fast and slow pointers are also "two pointers" — how does that family differ from the converging kind, and where does it show up?`,answer:`Converging pointers start at opposite ends and move toward each other — they need random access and usually sorted data; the invariant is "the answer lies between them". Fast/slow pointers move in the SAME direction at different speeds (typically 2× vs 1×) — no sorting, no random access needed, which is why they are THE tool for singly linked lists: cycle detection (Floyd — if a cycle exists, fast laps slow and they meet), finding the middle in one pass, and finding the cycle entry. Same O(n)/O(1) profile. Full treatment in the linked-list module; the takeaway now is that "two pointers" names two distinct invariant families.`,isCaseBased:!1}],flashcards:[{front:`Trigger: "sorted array" + "find a pair"`,back:`Converging two pointers. Sum too small → l++, too big → r--. O(n), O(1) space. Each step provably retires one index.`},{front:`Container With Most Water — the rule and its one-line proof`,back:`Always move the SHORTER wall. Keeping it while width shrinks can never beat the area just measured (height stays capped by it).`},{front:`3Sum recipe`,back:`Sort → fix a[i] → converging two pointers on the suffix for −a[i] → skip duplicates at i, l, AND r. O(n²) total.`},{front:`Variable window invariant`,back:`Grow with r every step; while the window is invalid, shrink with l. Record the answer only when valid. Window is valid after every iteration.`},{front:`Why the variable window is O(n) despite a nested while`,back:`Each index enters once (r) and leaves at most once (l) → ≤ 2n total pointer moves. Amortized argument.`},{front:`Fixed window slide, one step right`,back:`sum += a[i] − a[i−k]. One enters, one leaves: O(1) per slide, O(n) total. Never re-sum k elements.`},{front:`Prefix sum definition + range formula`,back:`pre[0] = 0, pre[i] = sum of first i elements (n+1 slots). sum(a[l..r]) = pre[r+1] − pre[l]. Immutable arrays only.`},{front:`Subarray Sum Equals K recipe`,back:`Hashmap of prefix-value counts, seeded seen[0] = 1. At each element: count += seen[run − k], THEN seen[run]++. Works with negatives; windows don't.`},{front:`Kadane recurrence + intuition`,back:`cur = max(a[i], cur + a[i]); best = max(best, cur). A negative running sum is dead weight — restart. O(n), O(1).`},{front:`Kadane's classic bug`,back:`Initializing best to 0 → returns 0 on all-negative arrays. Initialize cur and best from a[0]; correct answer is the largest element.`}],mindmapMarkdown:`- Array Patterns: Two Pointers, Sliding Window, Prefix Sums & Kadane
  - Converging two pointers
    - needs sorted data — comparison retires an index
    - sum small → l++ · sum big → r--
    - Two Sum II · 3Sum = fix i, two-pointer the rest, O(n²)
    - Container: move the shorter wall (never beats measured area)
  - Fast/slow pointers → linked lists (Floyd), next family
  - Sliding window
    - fixed k: sum += enter − leave, O(1) per slide
    - variable: grow r, shrink l while invalid (no-repeat substring)
    - each index enters once, leaves once → O(n) amortized
    - needs monotone constraint — negatives break sum windows
  - Prefix sums
    - pre[0] = 0 · pre[i] = sum of first i
    - sum(l..r) = pre[r+1] − pre[l], O(1) per query
    - Subarray Sum = K: count seen[run − k], seed seen[0] = 1
    - hashmap bridge → hashing module
  - Kadane (Maximum Subarray)
    - cur = max(a[i], cur + a[i]) — extend or restart
    - negative running sum = dead weight · init from a[0]
  - Triggers
    - sorted + pair → two pointers
    - contiguous + constraint → window
    - range sum → prefix
    - max subarray → Kadane`};export{e as default};