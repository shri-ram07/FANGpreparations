var e={id:`dbms-l0-dbms-er-keys`,subjectId:`dbms`,level:0,title:`DBMS, ER Modeling & Keys`,whyItMatters:`"Design the schema for X" appears in nearly every backend and data round, and the follow-ups never change: which key, why that key, what happens on delete. This module builds one Zomato-style schema from ER boxes down to runnable CREATE TABLE statements — and every keys concept you will ever be asked lives inside it.`,estMinutes:50,sections:[{type:`intuition`,title:`Why not just files?`,md:`Your first instinct is right: data is just files. One CSV per thing. It genuinely works — until the day two processes write at once.

- **Concurrent access**: two services append to the same CSV → interleaved half-lines, lost updates. A DBMS runs writes inside *transactions* and schedules them so concurrent users cannot corrupt each other.
- **Crash recovery**: a process dying mid-write leaves a half-written file, forever. A DBMS logs the change *before* touching the data, so after a crash it replays or undoes cleanly.
- **Constraints**: nothing stops a CSV row from naming user 999 who does not exist. A DBMS enforces the rules (unique, not null, must-reference) in one place, for every app.
- **Query language**: "top 5 restaurants by average rating this month" is a custom script per question with files. SQL states *what* you want; the DBMS picks *how* (which index, which order).
- One sentence to keep: a DBMS is the code you would otherwise rewrite, badly, inside every application.`},{type:`intuition`,title:`The 3-schema architecture: menu, recipe book, kitchen`,md:`A restaurant shows diners a menu, the chef works from the recipe book, and the kitchen decides which shelf everything sits on. Rearranging shelves changes zero menus. Databases copy this exactly.

- **External schema** — what each app or user sees: *views*. The delivery app sees orders without payment columns; analytics sees aggregates only.
- **Conceptual schema** — the one logical design: tables, columns, keys, constraints. Everything in this module lives here.
- **Internal schema** — how it is physically stored: files, pages, indexes, sort order on disk.
- **Physical data independence**: change the internal layer (add an index, move to faster storage) and the conceptual layer is untouched — no app changes. This is why a DBA can tune at 2am without breaking anyone.
- **Logical data independence**: change the conceptual layer (split a table in two) and views can be rewritten to present the old shape — apps still survive.`},{type:`intuition`,title:`ER modeling: draw the business before you type SQL`,md:`Before tables, draw boxes. An **ER diagram** (entity–relationship) is the business on one page. Our running example, start to finish: a Zomato-style food delivery app.

- **Entity** — a thing worth tracking, drawn as a box: *User, Restaurant, Dish, Order, Review*.
- **Attribute** — a fact about an entity: a user has an email, phone, name. The identifying attribute gets underlined — it will become the key.
- **Relationship** — a verb connecting entities: a User *places* an Order; a Restaurant *offers* Dishes; an Order *contains* Dishes; a User *reviews* a Restaurant.
- Relationships can carry their own attributes: *contains* needs a quantity — the quantity belongs to the (order, dish) pair, not to either alone.
- The diagram is cheap to change; a production schema is not. Argue about the boxes first.`},{type:`intuition`,title:`Cardinalities — and why M:N forces a junction table`,md:`**Cardinality** = how many of each side a relationship allows. Only three shapes exist.

- **1:1** — one user has one settings row. Rare; often it is really one table split in two.
- **1:N** — one restaurant offers many dishes; one user places many orders. The workhorse.
- **M:N** — one order contains many dishes AND one dish appears in many orders. Both directions are "many".
- M:N cannot be a plain column: a foreign key cell holds exactly **one** value. \`dish_id\` inside orders cannot hold three dishes; \`order_id\` inside dishes would pin each dish to a single order forever. Stuffing a comma-separated list into one cell is the file-system disease sneaking back in.
- The fix: a **junction table** — \`order_items\`, one row per (order, dish) pair. The unrepresentable M:N becomes two ordinary 1:N relationships, and the relationship's own attributes (quantity, price) finally have a home.`},{type:`note`,md:"ER → relational, the mapping rules: 1. **Strong entity** → its own table; identifying attribute → primary key. 2. **1:N relationship** → foreign key on the N side (dishes carries `restaurant_id`; never the reverse). 3. **1:1** → foreign key on either side plus UNIQUE — put it on the side that can be optional. 4. **M:N** → junction table holding FKs to both sides, composite primary key of the two FKs, relationship attributes as columns. 5. **Multivalued attribute** (a user's several delivery addresses) → its own child table with an FK back. 6. **Weak entity** — one that cannot exist without its owner — → table whose key *includes* the owner's key. `order_items` is exactly that shape: it means nothing without its order."},{type:`intuition`,title:`Keys, all of them, on one table`,md:`One running example: **users(user_id, email, phone, name)**. Every key term is a statement about this table.

- **Super key** — *any* column set that uniquely identifies a row: {user_id}, {email}, {email, name}, {user_id, phone}… dozens exist. Uniqueness is the only requirement, minimality is not.
- **Candidate key** — a *minimal* super key: drop any column and uniqueness dies. Here: {user_id}, {email}, {phone}. Note {email, name} is out — email alone already works.
- **Primary key** — the ONE candidate you elect. Choice criteria: **minimal** (fewest columns), **stable** (the value never changes), **never reused** (a retired value is never handed to a new row). Email fails stable — people change emails. Phone fails reused — telecoms recycle numbers. So \`user_id\` wins.
- The losing candidates do not vanish — they stay as **UNIQUE constraints** (alternate keys), still guarding against duplicates.
- **Composite key** — a key spanning 2+ columns: \`order_items(order_id, dish_id)\`. Neither column alone is unique; the pair is.`},{type:`note`,md:`**Foreign key** = a column whose value must match a primary-key (or UNIQUE) value in the parent table — or be NULL. The guarantee it buys is **referential integrity**: no reference ever points at a row that does not exist. What happens when the parent is deleted is your choice, declared once: **RESTRICT / NO ACTION** — refuse the delete while children exist (the safe default); **CASCADE** — delete the children too (dishes go down with their restaurant); **SET NULL** — keep the child, blank the reference (needs a nullable column: "order kept, courier field cleared"). Pick per relationship, deliberately — the default you never chose is still a choice.`},{type:`visual`,component:`PointerBoxDiagram`,props:{title:`Foreign keys are arrows between rows`,notice:`Each FK value is an arrow that must land on an existing parent row. The last frame shows the disease FKs exist to prevent.`,leftLabel:`orders rows (FK columns)`,rightLabel:`parent rows`,frames:[{note:`Order 101 lands. Its user_id = 7 and restaurant_id = 3 are foreign keys — each value is an arrow into a parent table, and the DBMS checks both arrows land before accepting the row.`,stack:[{name:`order 101 · user_id`,value:`7`,to:`u7`},{name:`order 101 · restaurant_id`,value:`3`,to:`r3`}],heap:[{id:`u7`,value:`user 7 · Rohan`,label:`users row`},{id:`r3`,value:`restaurant 3 · Biryani House`,label:`restaurants row`}]},{note:`Order 102 arrives: different user, same restaurant. Two arrows now converge on restaurant 3 — this is 1:N at row level: one parent, many children pointing at it.`,stack:[{name:`order 101 · user_id`,value:`7`,to:`u7`},{name:`order 101 · restaurant_id`,value:`3`,to:`r3`},{name:`order 102 · user_id`,value:`9`,to:`u9`},{name:`order 102 · restaurant_id`,value:`3`,to:`r3`}],heap:[{id:`u7`,value:`user 7 · Rohan`,label:`users row`},{id:`u9`,value:`user 9 · Meera`,label:`users row`},{id:`r3`,value:`restaurant 3 · Biryani House`,label:`restaurants row`}]},{note:`DELETE restaurant 3. With ON DELETE RESTRICT the DBMS counts incoming arrows, finds two, and refuses the delete. Referential integrity in one line: no arrow may ever point at nothing.`,stack:[{name:`order 101 · user_id`,value:`7`,to:`u7`},{name:`order 101 · restaurant_id`,value:`3`,to:`r3`},{name:`order 102 · user_id`,value:`9`,to:`u9`},{name:`order 102 · restaurant_id`,value:`3`,to:`r3`}],heap:[{id:`u7`,value:`user 7 · Rohan`,label:`users row`},{id:`u9`,value:`user 9 · Meera`,label:`users row`},{id:`r3`,value:`restaurant 3 · Biryani House`,label:`DELETE refused: 2 children`}]},{note:`Now imagine plain files (or FK checks switched off): the delete just happens. Restaurant 3 is gone, but both orders still say restaurant_id = 3 — dangling references, orphan rows. Every report joining orders to restaurants silently loses them. This frame is exactly what a foreign key constraint makes impossible.`,stack:[{name:`order 101 · user_id`,value:`7`,to:`u7`},{name:`order 101 · restaurant_id`,value:`3`,to:`r3`,danger:!0},{name:`order 102 · user_id`,value:`9`,to:`u9`},{name:`order 102 · restaurant_id`,value:`3`,to:`r3`,danger:!0}],heap:[{id:`u7`,value:`user 7 · Rohan`,label:`users row`},{id:`u9`,value:`user 9 · Meera`,label:`users row`},{id:`r3`,value:`restaurant 3 · deleted`,freed:!0}]}]}},{type:`intuition`,title:`Natural vs surrogate keys: the interview debate`,md:`A **natural key** is a real-world identifier (email, ISBN, PAN). A **surrogate key** is a meaningless auto-number (\`user_id\`). Interviewers want you to argue both sides.

- Natural pro: already there, human-readable, blocks duplicate entities by itself — no extra column.
- Natural con: reality changes. Emails change, numbers get recycled, government IDs get typo-fixed. A primary key's value is copied into every child table's FK — one change ripples through the whole database.
- Surrogate pro: stable forever, never reused, small and cheap to join on.
- Surrogate con: enforces nothing by itself — two rows for the same real person, different ids. You must ADD \`UNIQUE(email)\` anyway.
- The answer that wins the round: **surrogate primary key + UNIQUE constraints on the natural candidates.** Stability from the surrogate, duplicate-protection from the naturals.
- Where natural keys still win: codes that are stable *by construction* — ISO country codes, currency codes — and junction tables, whose composite of two surrogate FKs is itself natural.`},{type:`code`,lang:`sql`,title:`The Zomato schema — every idea above, runnable`,code:`PRAGMA foreign_keys = ON;   -- SQLite ships with FK checks OFF; turn them on

CREATE TABLE users (
  user_id INTEGER PRIMARY KEY,             -- surrogate key: meaningless, stable
  email   TEXT NOT NULL UNIQUE,            -- candidate key, kept as a constraint
  phone   TEXT UNIQUE,                     -- another candidate key
  name    TEXT NOT NULL
);

CREATE TABLE restaurants (
  restaurant_id INTEGER PRIMARY KEY,
  name          TEXT NOT NULL,
  city          TEXT NOT NULL,
  rating        REAL CHECK (rating BETWEEN 0 AND 5)
);

CREATE TABLE dishes (                      -- 1:N — one restaurant, many dishes
  dish_id       INTEGER PRIMARY KEY,
  restaurant_id INTEGER NOT NULL
    REFERENCES restaurants(restaurant_id) ON DELETE CASCADE,
  name          TEXT NOT NULL,
  price         REAL NOT NULL CHECK (price > 0)
);

CREATE TABLE orders (
  order_id      INTEGER PRIMARY KEY,
  user_id       INTEGER NOT NULL
    REFERENCES users(user_id) ON DELETE RESTRICT,
  restaurant_id INTEGER NOT NULL
    REFERENCES restaurants(restaurant_id) ON DELETE RESTRICT,
  placed_at     TEXT NOT NULL DEFAULT (datetime('now')),
  status        TEXT NOT NULL DEFAULT 'placed'
);

CREATE TABLE order_items (                 -- junction: orders M:N dishes
  order_id   INTEGER NOT NULL
    REFERENCES orders(order_id) ON DELETE CASCADE,
  dish_id    INTEGER NOT NULL REFERENCES dishes(dish_id),
  quantity   INTEGER NOT NULL CHECK (quantity > 0),
  unit_price REAL NOT NULL,                -- price frozen at order time
  PRIMARY KEY (order_id, dish_id)          -- composite key
);

CREATE TABLE reviews (
  review_id     INTEGER PRIMARY KEY,
  user_id       INTEGER NOT NULL
    REFERENCES users(user_id) ON DELETE CASCADE,
  restaurant_id INTEGER NOT NULL
    REFERENCES restaurants(restaurant_id) ON DELETE CASCADE,
  rating        INTEGER NOT NULL CHECK (rating BETWEEN 1 AND 5),
  body          TEXT,
  UNIQUE (user_id, restaurant_id)          -- one review per user per restaurant
);`,annotations:{1:`SQLite quirk worth knowing: FK enforcement is opt-in, per connection. Forgetting this line is how orphan rows happen "with FKs declared".`,4:`INTEGER PRIMARY KEY in SQLite auto-assigns ids. Surrogate key: carries no meaning, so it never needs to change and is never reused.`,5:`The candidate keys that lost the primary-key election stay as UNIQUE constraints — they still block duplicate accounts.`,20:`CASCADE because a dish is meaningless without its restaurant: parent gone → children go too. Deliberate, per relationship.`,30:`RESTRICT because order history is money and audit trail: deleting a restaurant with past orders must fail loudly, not silently erase records.`,40:`Not redundancy: dishes.price is the price NOW, unit_price is the price WHEN ORDERED — two different facts. Menus change; invoices must not.`,41:`Composite key: the (order, dish) PAIR is unique. Each column alone repeats freely — that is what makes it a junction table.`,52:`A business rule as a constraint: one review per user per restaurant. The DBMS enforces it for every app, forever.`}},{type:`note`,md:"Make this real: [Open the SQL playground](/playground/sql), paste the schema, insert a user, a restaurant, a dish, an order and its order_items — then try to break it. Insert an order with `restaurant_id = 999` (refused), delete a restaurant that has orders (refused), delete one that only has dishes (dishes cascade away). Ten minutes of trying to corrupt your own schema teaches referential integrity better than any diagram."}],quiz:[{question:`Two services append order rows to the same shared CSV. Occasionally a line comes out as two half-rows merged together. Which DBMS feature is the direct fix?`,options:[{text:`A query language`,explanation:`SQL helps you ASK questions — it does nothing about two writers colliding.`},{text:`Indexes`,explanation:`Indexes speed up reads. The corruption here happens on write.`},{text:`Concurrency control — transactions isolate simultaneous writers`,explanation:`Correct. Interleaved partial writes are exactly what transaction scheduling prevents: each write lands whole or not at all, even under concurrency.`},{text:`The 3-schema architecture`,explanation:`That insulates apps from storage changes — a different problem entirely.`}],correct:2},{question:`The DBA adds an index and moves a table to a new on-disk layout. No application code changes. Which principle made that safe?`,options:[{text:`Physical data independence — the internal schema changed, the conceptual schema did not`,explanation:`Correct. Apps talk to tables and views, not to files and pages. Storage can be rebuilt underneath them.`},{text:`Logical data independence`,explanation:`That covers changes to the CONCEPTUAL schema (splitting a table) hidden behind views. Here only storage changed.`},{text:`Referential integrity`,explanation:`That is about FK arrows landing on real rows — unrelated to storage layout.`},{text:`Normalization`,explanation:`Normalization removes redundancy in the logical design. Index changes are physical.`}],correct:0},{question:`Why can the M:N "order contains dishes" NOT be stored as a dish_id column inside orders?`,options:[{text:`Foreign keys cannot reference the dishes table`,explanation:`FKs can reference any table with a primary or unique key. The problem is arity, not the target.`},{text:`An FK column holds exactly one value, but one order needs many dishes (and one dish, many orders)`,explanation:`Correct. Both directions are "many", so no single column on either side can hold the relationship. That is what forces the junction table.`},{text:`It can — store comma-separated dish ids in one cell`,explanation:`That kills joins, FK checking, and indexing in one stroke — the file-system disease inside a database.`},{text:`Orders cannot have two foreign keys`,explanation:`It already has two (user_id, restaurant_id). Multiple FKs per table are routine.`}],correct:1},{question:`In users(user_id, email, phone, name), the set {email, name} is a…`,options:[{text:`Candidate key`,explanation:`Candidates must be minimal. Since email alone is unique, {email, name} carries a useless column.`},{text:`Composite primary key`,explanation:`A primary key is the one ELECTED candidate. This set is not even a candidate, let alone elected.`},{text:`Super key but not a candidate key`,explanation:`Correct. It identifies rows uniquely (email alone does), but it is not minimal — drop name and uniqueness survives. Minimality is what candidates add.`},{text:`Not a key at all`,explanation:`It does uniquely identify rows — any superset of a unique column is a super key.`}],correct:2},{question:`Phone numbers are unique per user. Why are they still a poor primary key?`,options:[{text:`Users change numbers, and telecoms recycle them to new people — a PK must be stable and never reused`,explanation:`Correct. A changed PK ripples into every child table FK; a recycled one silently glues a new person to an old person's history.`},{text:`TEXT columns cannot be primary keys`,explanation:`They can. The objection to phone is about its lifecycle, not its type.`},{text:`Phone numbers are too long to index`,explanation:`Ten-odd characters index fine. Length is not the issue.`},{text:`Natural keys are always forbidden as primary keys`,explanation:`Too absolute — ISO country codes make fine natural PKs because they are stable by construction.`}],correct:0},{question:`orders.user_id is declared ON DELETE RESTRICT. What happens when you DELETE a user who has orders?`,options:[{text:`The user and all their orders are deleted`,explanation:`That is CASCADE — a different, deliberate choice.`},{text:`The DELETE fails with an error — the orders keep their parent`,explanation:`Correct. RESTRICT refuses to remove a parent while children reference it. Loud failure beats silent data loss.`},{text:`The orders remain, with user_id set to NULL`,explanation:`That is SET NULL — and it would fail here anyway, since user_id is NOT NULL.`},{text:`The user is deleted and the orders silently become orphans`,explanation:`Orphans are precisely what every FK option exists to prevent — no setting produces this.`}],correct:1},{question:`PRIMARY KEY (order_id, dish_id) on order_items enforces exactly what?`,options:[{text:`order_id must be unique in the table`,explanation:`That would allow only one item per order — the composite exists precisely to avoid this.`},{text:`dish_id must be unique in the table`,explanation:`Then each dish could ever be ordered once, across all orders. The pair is what is unique.`},{text:`Both columns must each be individually unique`,explanation:`That is two separate UNIQUE constraints — far stricter than one composite key.`},{text:`The same dish cannot appear twice in one order; each column alone may repeat freely`,explanation:`Correct. The PAIR is unique. Want the same dish twice? Bump quantity — that is why the column exists.`}],correct:3},{question:`Your team debates email vs an auto-increment id as the users primary key. The strongest argument FOR the surrogate id is…`,options:[{text:`Surrogate ids prevent duplicate accounts`,explanation:`They prevent nothing — two rows for the same person get two ids happily. You still need UNIQUE(email).`},{text:`PK values are copied into every child table's FK — emails change, and that change would ripple through the whole database`,explanation:`Correct. Stability is the killer argument: a surrogate never changes and is never reused, so FKs never need repair.`},{text:`Emails cannot be indexed`,explanation:`False — the UNIQUE constraint on email is itself backed by an index.`},{text:`SQL requires primary keys to be integers`,explanation:`False — text and composite PKs are legal everywhere. The preference is practical, not syntactic.`}],correct:1}],interviewQuestions:[{question:`What does a DBMS actually give you over storing data in files? Name the big four.`,answer:`(1) Concurrency control — many readers and writers at once without corruption, via transactions and isolation. (2) Crash recovery — changes are logged before data is touched, so a crash mid-write is replayed or undone, never half-applied. (3) Integrity constraints — unique, not-null, foreign keys enforced in ONE place for every application, instead of re-validated (inconsistently) in each app. (4) A declarative query language plus an optimizer — you state what you want, it picks the access path. Bonus points: add access control and data independence. The compressed version: a DBMS is the infrastructure code every app would otherwise rewrite, badly.`,isCaseBased:!1},{question:`Explain the 3-schema architecture. Why can a DBA add an index without any application changing?`,answer:`Three layers: external (per-app views — each consumer sees only its slice), conceptual (the single logical design — tables, keys, constraints), internal (physical storage — files, pages, indexes). Two insulation guarantees fall out: physical data independence — internal changes (new index, new storage layout) never touch the conceptual layer, so apps that talk to tables are untouched; and logical data independence — conceptual changes (splitting a table) can be hidden behind rewritten views so external consumers survive. The index question is physical independence verbatim: an index changes HOW rows are found, not WHAT the tables look like.`,isCaseBased:!1},{question:`Orders and dishes are M:N. Walk me through why a plain foreign key fails and what the junction table looks like.`,answer:`A foreign key cell holds exactly one value. Putting dish_id in orders caps every order at one dish; putting order_id in dishes pins each dish to one order forever; a comma-separated list in one cell destroys joins, FK checking, and indexing. So M:N gets a junction table: order_items(order_id FK, dish_id FK, quantity, unit_price) with PRIMARY KEY (order_id, dish_id). That decomposes the M:N into two 1:N relationships (orders 1:N order_items, dishes 1:N order_items) and — the point candidates miss — gives the relationship's own attributes a home: quantity belongs to the PAIR, not to the order or the dish alone.`,isCaseBased:!1},{question:`Define super key, candidate key, primary key, and composite key — one running example.`,answer:`Take users(user_id, email, phone, name). Super key: any column set that uniquely identifies a row — {user_id}, {email}, {email, name}; uniqueness required, minimality not. Candidate key: a minimal super key — remove any column and uniqueness breaks: {user_id}, {email}, {phone}. Primary key: the one candidate elected to identify rows, chosen for minimality, stability, and never-reused values — user_id here; the losing candidates remain as UNIQUE constraints (alternate keys). Composite key: any key spanning multiple columns — order_items(order_id, dish_id), where neither column alone is unique but the pair is. The hierarchy in one line: every candidate is a super key, the primary is one chosen candidate.`,isCaseBased:!1},{question:`What are your criteria for choosing a primary key?`,answer:`Four tests. Not null — always present, no exceptions. Minimal — the fewest columns that stay unique. Stable — the value never changes, because PK values are copied into every referencing table's FK and a change ripples everywhere. Never reused — a retired value must not be assigned to a new row, or old references silently attach to the wrong entity (recycled phone numbers are the classic failure). Most natural identifiers fail stability or reuse, which is why the practical default is a surrogate id — with UNIQUE constraints keeping the natural candidates honest.`,isCaseBased:!1},{question:`Natural vs surrogate keys — argue both sides, then give your ruling.`,answer:`Natural (email, ISBN, PAN): pro — already exists, human-readable, blocks duplicate entities by itself. Con — reality changes: emails change, numbers recycle, IDs get typo-corrected, and every change ripples into all child FKs. Surrogate (auto id): pro — stable forever, never reused, compact joins. Con — semantically empty: it prevents no duplicates, so you must add UNIQUE on the natural candidates anyway; also leaks meaningless ids into URLs and APIs. Ruling: surrogate primary key plus UNIQUE constraints on natural candidates — stability from one, duplicate-protection from the other. Exceptions where natural wins: codes stable by construction (ISO country/currency codes) and junction tables, whose composite of two FKs is already natural.`,isCaseBased:!1},{question:`Walk through the ON DELETE choices in the Zomato schema and justify each.`,answer:`dishes.restaurant_id → CASCADE: a dish is meaningless without its restaurant; parent gone, children follow. orders.user_id and orders.restaurant_id → RESTRICT: order history is money and audit trail — deleting a parent with orders must fail loudly, never silently erase financial records; real deletion becomes a soft-delete (an is_active flag) instead. order_items.order_id → CASCADE: items are fragments of their order. reviews → CASCADE on both FKs: user deletion (think GDPR erasure) should take their reviews along. The meta-answer interviewers want: there is no universally correct option — CASCADE for owned fragments, RESTRICT for records with legal or financial weight, SET NULL when the child outlives the reference — and the default you never chose is still a choice.`,isCaseBased:!1},{question:`Case: extend the Zomato schema for delivery. Requirements: delivery partners exist; each order gets at most one partner; a partner handles at most one active order at a time; customers rate the delivery separately from the restaurant.`,answer:`New entity → new table: delivery_partners(partner_id INTEGER PRIMARY KEY, name, phone TEXT UNIQUE, is_active). Order-to-partner is 1:N over time (a partner serves many orders across history) — so FK on the N side: add partner_id INTEGER REFERENCES delivery_partners(partner_id) to orders, NULLABLE because an order exists before assignment; ON DELETE SET NULL so history survives a partner leaving. "One ACTIVE order at a time" is not a plain FK constraint — enforce with a partial unique index: CREATE UNIQUE INDEX one_active_per_partner ON orders(partner_id) WHERE status NOT IN ('delivered', 'cancelled') — SQLite and Postgres both support this. Delivery rating: it describes the delivery of one order, 1:1 with a delivered order — either columns on orders (delivery_rating CHECK BETWEEN 1 AND 5) or a delivery_reviews table with order_id as both PK and FK, which keeps orders lean. Tradeoffs to say aloud: nullable FK vs a separate assignments junction table — the junction wins if reassignment history matters (partner cancels, another picks up); columns-on-orders wins for simplicity if only the final state matters.`,isCaseBased:!0},{question:`Case: design the schema for a movie-ticket booking app (BookMyShow-style) — users book seats for showtimes of movies in theatres.`,answer:`Method first: nouns → entities: users, movies, theatres, screens, showtimes, bookings, seats. Relationships and cardinalities: theatre 1:N screens; screen 1:N showtimes; movie 1:N showtimes; user 1:N bookings; booking M:N seats — but scoped: a seat is booked FOR a showtime. Tables: users(user_id PK, email UNIQUE), movies(movie_id PK, title), theatres(theatre_id PK, name, city), screens(screen_id PK, theatre_id FK), seats(seat_id PK, screen_id FK, row_label, seat_number, UNIQUE(screen_id, row_label, seat_number)), showtimes(showtime_id PK, screen_id FK, movie_id FK, starts_at), bookings(booking_id PK, user_id FK, showtime_id FK, status), booking_seats(booking_id FK, showtime_id, seat_id, PRIMARY KEY (showtime_id, seat_id)) — that composite PK IS the business rule: one seat sells once per showtime; double-booking becomes a constraint violation, not an app-level race to lose. Close with the extension hook: prices vary per showtime → price on showtimes, amount frozen on bookings — same "snapshot the price" move as order_items.unit_price.`,isCaseBased:!0},{question:`dishes already has a price column. Why does order_items store unit_price again — is that not redundancy?`,answer:`No — they are two different facts that happen to share a name. dishes.price is the price NOW (menu state, mutable). order_items.unit_price is the price WHEN THE ORDER WAS PLACED (historical fact, immutable). If order_items only referenced dishes.price, every menu update would silently rewrite past invoices — old orders would total differently each time you queried them. Redundancy means storing the SAME fact twice; a snapshot stores a fact at a point in time. The general rule worth quoting: reference mutable data for the present, copy it for the past. Same reason an invoice PDF embeds the amount instead of linking to the product page.`,isCaseBased:!1},{question:`Case: a prod dashboard shows reviews for restaurants that no longer exist. The schema declares foreign keys. How did this happen, and what is your fix — data and prevention?`,answer:`Diagnose: declared is not enforced. Usual suspects: (1) SQLite — PRAGMA foreign_keys is OFF per connection unless the app sets it; (2) MySQL — the table was created with an engine or mode that ignores FK clauses; (3) constraints were dropped for a bulk load or migration and never re-enabled; (4) deletes ran through a path that bypasses the DB's checks (raw dump edits, a replica promoted mid-migration). Find the orphans: SELECT r.* FROM reviews r LEFT JOIN restaurants t ON t.restaurant_id = r.restaurant_id WHERE t.restaurant_id IS NULL. Fix the data — a business decision, not a technical one: delete the orphaned reviews, or resurrect/tombstone the missing restaurants if the reviews have value. Prevent: re-enable enforcement and re-add the constraint (which will refuse to attach until orphans are gone — that is the feature), decide ON DELETE per relationship (CASCADE here), and prefer soft-deletes for entities with history. The senior-sounding closer: constraints that are declared but unenforced are worse than none — they buy false confidence.`,isCaseBased:!0},{question:`What is the difference between a UNIQUE NOT NULL column and the primary key? Are they not the same thing?`,answer:`Functionally close, not identical. A table has exactly ONE primary key but may have many UNIQUE NOT NULL columns (alternate keys). The PK is the default target of foreign keys and the declared row identity. In several engines it is also physical: InnoDB clusters the table on the PK — rows are stored in PK order, so PK choice affects locality and every secondary index carries the PK as its row pointer (a fat PK bloats all indexes). Semantically: the PK says "this is what a row IS"; UNIQUE says "this must not repeat". Interview one-liner: every PK is a UNIQUE NOT NULL constraint, but not every UNIQUE NOT NULL constraint gets elected.`,isCaseBased:!1}],flashcards:[{front:`DBMS over raw files — the big four`,back:`Concurrency control (safe simultaneous writes), crash recovery (log first, write after), constraints enforced in one place, declarative query language + optimizer.`},{front:`3-schema architecture`,back:`External (per-app views) → conceptual (tables, keys, constraints) → internal (files, pages, indexes). Each layer insulates the one above.`},{front:`Physical vs logical data independence`,back:`Physical: storage/index changes never touch the logical design. Logical: table redesigns can hide behind rewritten views. Both mean: apps survive.`},{front:`Cardinalities + the M:N rule`,back:`1:1, 1:N (FK on the N side), M:N. An FK cell holds ONE value, so M:N forces a junction table — the M:N becomes two 1:N's.`},{front:`Super key vs candidate key`,back:`Super: ANY uniquely-identifying column set. Candidate: a MINIMAL one — drop any column and uniqueness dies. Every candidate is a super key.`},{front:`Primary key choice criteria`,back:`Not null, minimal, stable (value never changes — FKs copy it everywhere), never reused (recycled values corrupt history). Losing candidates → UNIQUE constraints.`},{front:`Composite key example`,back:`order_items PRIMARY KEY (order_id, dish_id): the PAIR is unique, each column alone repeats. Doubles as the junction table's identity.`},{front:`Foreign key + referential integrity`,back:`FK value must match a parent PK/UNIQUE value (or be NULL). Guarantee: no reference ever points at a nonexistent row — no orphans.`},{front:`ON DELETE options`,back:`RESTRICT: refuse while children exist (safe default). CASCADE: children deleted too (owned fragments). SET NULL: child kept, reference blanked (needs nullable FK).`},{front:`Natural vs surrogate — the winning answer`,back:`Surrogate PK (stable, never reused) + UNIQUE on natural candidates (blocks duplicates). Natural PKs OK for stable-by-construction codes (ISO codes) and junction tables.`}],mindmapMarkdown:`- DBMS, ER Modeling & Keys
  - Why a DBMS
    - Concurrent writes without corruption
    - Crash recovery: log first
    - Constraints in ONE place
    - SQL + optimizer
  - 3-schema architecture
    - External: per-app views
    - Conceptual: tables, keys, rules
    - Internal: files, pages, indexes
    - Physical / logical independence
  - ER model
    - Entity → table
    - Attribute → column
    - Relationship + cardinality
    - 1:1 / 1:N / M:N
    - M:N → junction (order_items)
  - Keys (users example)
    - Super: any unique set
    - Candidate: minimal super key
    - Primary: stable, never reused
    - Composite: (order_id, dish_id)
    - Losers → UNIQUE constraints
  - Foreign keys
    - Arrow must land on a parent
    - RESTRICT / CASCADE / SET NULL
    - Orphan rows = broken integrity
  - Natural vs surrogate
    - Surrogate PK + UNIQUE naturals
    - Natural OK: fixed codes, junctions
  - Zomato schema
    - users · restaurants · dishes
    - orders · order_items · reviews
    - unit_price = snapshot, not redundancy`};export{e as default};