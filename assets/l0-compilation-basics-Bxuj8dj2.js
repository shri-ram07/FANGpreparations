var e={id:`cpp-l0-compilation-basics`,subjectId:`cpp`,level:0,title:`What g++ Actually Does — Compilation, main & I/O`,whyItMatters:`Your first hour of C++ ends with an error Python never showed you: "undefined reference". Knowing WHICH of g++'s three machines rejected your code is the difference between a 30-second fix and a lost evening — and interviewers use exactly this to separate "wrote some C++" from "understands C++".`,estMinutes:45,sections:[{type:`intuition`,title:`Python reads your recipe aloud. C++ builds a machine first.`,md:`Python is a chef cooking while reading your recipe line by line. C++ is a factory: you hand in the whole recipe, it builds a vending machine, and THEN you press the button.

- Python: run the source directly. Errors appear when the bad line executes.
- C++: **compile** the source into an **executable** — a standalone machine-code file — then run that.
- Payoff: the machine is fast. No interpreter sits between your code and the CPU.
- Cost: a build step that can fail in confusing ways. This module makes those failures boring.
- \`g++\` runs the whole build. It is really three machines in a trench coat.`},{type:`intuition`,title:`The three machines inside g++`,md:`Photocopier, translator, bookbinder — in that order.

- **Preprocessor** (photocopier): dumb find-and-replace on text. \`#include <iostream>\` literally pastes that file's tens of thousands of lines into yours. Zero understanding involved.
- The pasted result is a **translation unit** — one .cpp plus everything it included, ready for real compilation.
- **Compiler** (translator): turns each translation unit into an **object file** (\`main.o\`) — machine code with holes where other files' functions will go.
- **Linker** (bookbinder): joins all the .o files plus libraries, fills every hole, binds one executable.
- Each .cpp is compiled **separately**. Only the linker ever sees the whole program. That one fact explains 90% of weird build errors.`},{type:`visual`,component:`PointerBoxDiagram`,props:{title:`The build pipeline, step by step`,notice:`Step through: one source file becomes an executable — and see WHERE each kind of error kills the build.`,leftLabel:`your files`,rightLabel:`build artifacts`,frames:[{note:`You write main.cpp. One file, human-readable. Nothing else exists yet — g++ is about to run three machines over it.`,stack:[{name:`main.cpp`}],heap:[]},{note:`Stage 1 — PREPROCESSOR. Pure find-and-replace: every #include line is replaced by that file's full text. Your 20 lines become one giant "translation unit".`,stack:[{name:`main.cpp`,to:`tu`}],heap:[{id:`tu`,value:`translation unit`,label:`~40k lines`}]},{note:`Stage 2 — COMPILER. Translates the translation unit into machine code: main.o. A missing semicolon dies HERE — with a file name and a line number.`,stack:[{name:`main.cpp`,to:`obj`}],heap:[{id:`obj`,value:`main.o`,label:`object file`}]},{note:`Subtlety: the compiler TRUSTS declarations. Calling a declared-but-undefined helper() still compiles — main.o just records an IOU: "need helper() later".`,stack:[{name:`main.cpp`,to:`obj`}],heap:[{id:`obj`,value:`main.o`,label:`IOU: helper()`}]},{note:`Stage 3 — LINKER, unhappy path. No .o file or library defines helper(), so the IOU bounces: "undefined reference to helper()". No line number — source code is long gone.`,stack:[{name:`main.o`,to:`fail`,danger:!0}],heap:[{id:`fail`,value:`no executable`,label:`link failed`}]},{note:`Happy path: hand the linker helper.o too. It joins all .o files plus the standard library into ONE executable. ./main now runs; its exit code is whatever main returns.`,stack:[{name:`main.o`,to:`exe`},{name:`helper.o`,to:`exe`},{name:`libstdc++ (auto)`,to:`exe`}],heap:[{id:`exe`,value:`main (executable)`,label:`runnable`}]}]}},{type:`note`,md:`Error triage — read the stage straight off the message:
- Missing semicolon, typo, unknown name → **compiler** error. Comes with file and line number. Fix that line.
- \`undefined reference to 'foo()'\` → **linker** error. Everything compiled; a called function's *definition* was never handed to the linker. No line number — source lines no longer exist at link time. Fix the build: add the missing .cpp/.o or library, or write the missing body.
- Rule of thumb: *"was not declared"* = compiler (add the \`#include\`). *"undefined reference"* = linker (add the definition).`},{type:`code`,lang:`cpp`,title:`The canonical main.cpp — read every line`,code:`#include <iostream>   // pasted in, full text, by the preprocessor

int main() {
    int age;
    std::cout << "age? ";
    std::cin >> age;
    std::cout << "next year: " << age + 1 << '\\n';
    return 0;          // exit code: 0 means success
}`,annotations:{1:`Not an import. The preprocessor replaces this line with iostream's entire text before compilation starts.`,3:`The one function the OS calls. int, because the return value becomes the program's exit code.`,5:`No newline needed here: cin is tied to cout — reading flushes the pending prompt so it always appears first.`,6:`>> reads the next whitespace-separated token and converts it to the target's type (int here).`,7:`Chaining: << writes, then returns the stream itself, which feeds the next <<. Evaluates left to right.`,8:`Shells, scripts and CI read this number. main is special: omitting it implies return 0 — write it anyway.`}},{type:`note`,md:"Why `int main`: the return value is your program's **exit code** — the one number the outside world sees after the process dies. `0` = success, anything else = failure. Shells check it (`echo $?`), CI pipelines pass or fail on it, scripts chain with it (`a && b` runs `b` only if `a` returned 0). That contract is why `void main` is wrong C++."},{type:`intuition`,title:`cout is a mailbox with a bag: newline vs endl`,md:"`cout` doesn't hand each character to the screen. It drops letters into a bag — the **buffer**, a chunk of memory holding pending output — and mails the whole bag occasionally.\n\n- **Flush** = force the bag to be delivered right now.\n- `'\\n'` drops a newline into the bag. Cheap.\n- `std::endl` drops a newline AND flushes. The delivery trip is the expensive part.\n- Flushing every line of a million-line loop can slow output several times over — a classic competitive-programming time-limit killer.\n- Default habit: `'\\n'`. The buffer flushes itself when full, at normal program exit, and before `cin` reads.\n- Reach for `endl` — or `std::cerr`, which is unbuffered — only when you must SEE output immediately, e.g. hunting a crash."},{type:`code`,lang:`cpp`,title:`Core types and sizeof`,code:`#include <cstdint>    // int64_t lives here
#include <iostream>

int main() {
    int       a = 42;             // usually 4 bytes
    long long b = 4000000000LL;   // 8 bytes
    int64_t   c = b;              // exactly 64 bits, everywhere
    double    d = 3.14;           // 8 bytes, ~15-16 significant digits
    char      e = 'A';            // 1 byte — really the small integer 65
    bool      f = true;           // 1 byte, not 1 bit

    std::cout << sizeof(a) << ' ' << sizeof(b) << ' ' << sizeof(c) << '\\n';
    std::cout << sizeof(d) << ' ' << sizeof(e) << ' ' << sizeof(f) << '\\n';
    return 0;                     // prints: 4 8 8, then 8 1 1
}`,annotations:{5:`2,147,483,647 is int's ceiling (~2.1e9). One step past it is undefined behavior, not a bigger number.`,6:`The LL suffix marks the literal itself as long long — plain 4000000000 does not fit in an int.`,7:`int64_t is guaranteed EXACTLY 64 bits on every platform; long long is guaranteed AT LEAST 64. Interchangeable in practice.`,10:`A full byte, not a bit — memory is addressed per byte, so nothing can be smaller.`,12:`sizeof is a compile-time operator: size of the TYPE in bytes. It never runs any code.`}},{type:`intuition`,title:`Why competitive programmers default to long long`,md:`Python ints grow forever. C++ ints are fixed-size boxes — pour in too much and it spills.

- \`int\`: 32 bits, tops out at 2,147,483,647 (~2.1 × 10⁹).
- Typical problem: 10⁵ values, each up to 10⁹. Their sum is ~10¹⁴ — an \`int\` accumulator is garbage long before the loop ends.
- Worse: signed overflow is **undefined behavior** — the compiler may assume it never happens and "optimize" your safety checks away. Not a wrong number, a wrong *program*.
- \`long long\`: 64 bits, ~9.2 × 10¹⁸. Covers almost every contest computation.
- Tradeoff: 8 bytes instead of 4 — double the memory traffic, slightly more cache pressure. For contest sizes this almost never matters; wrong answers always do.`},{type:`intuition`,title:`Coming from Python: what will bite`,md:'- **Braces and parens, not indentation.** `if (x > 0) { ... }` — indentation is for humans; the compiler reads `{ }`.\n- **`else if`, not `elif`.** And idiomatic C++ writes `&&`, `||`, `!` rather than `and`, `or`, `not`.\n- **`++i` and `--i` exist** and are the standard loop step.\n- **Integer division truncates.** `7 / 2` is `3` — and it truncates *toward zero*: `-7 / 2` is `-3`, while Python\'s `-7 // 2` floors to `-4`.\n- **Overflow is real.** Python silently promotes to big integers; C++ never does. Every arithmetic line carries a "does this fit?" question.\n- **Types are fixed at declaration.** After `int x`, writing `x = "hi"` is a compile error, not a rebind.'},{type:`code`,lang:`cpp`,title:`The differences, executable`,code:`#include <iostream>

int main() {
    for (int i = 1; i <= 3; ++i) {
        if (i % 2 == 1 && i != 3) {
            std::cout << "odd: " << i << '\\n';
        } else if (i == 2) {
            std::cout << "two\\n";
        }
    }

    int a = 7 / 2;         // 3   — the .5 is gone
    int b = -7 / 2;        // -3  — toward zero, NOT floor
    double c = 7.0 / 2;    // 3.5 — one double makes it real division

    int big = 2000000000;
    long long ok = 1LL * big + big;   // 4000000000 — math done in 64-bit
    // int bad = big + big;           // undefined behavior: int overflow

    std::cout << a << ' ' << b << ' ' << c << ' ' << ok << '\\n';
    return 0;
}`,annotations:{4:`Parens around the condition, braces around the body, ++i as the step. Indentation is decoration here.`,7:`"else if" is two keywords — there is no elif. That typo is every Python dev's first compiler error.`,12:`int / int stays int: the fraction is silently discarded. No warning, no error.`,13:`Truncation toward ZERO. Python floors (-7 // 2 == -4). Ported algorithms break exactly here.`,17:`1LL * big promotes the whole calculation to long long BEFORE the add. Overflow is prevented in the operands.`,18:`Both operands are int, so the add happens in int and overflows — the destination type cannot save it.`}},{type:`code`,lang:`cpp`,title:`The rest of the loop family: while, do-while, switch, break, continue`,code:`#include <iostream>

int main() {
  int x, sum = 0;
  while (std::cin >> x) {        // runs until input ends or fails
    if (x < 0) continue;         // skip negatives, keep looping
    if (x == 0) break;           // 0 = stop entirely
    sum += x;
  }

  int tries = 0;
  do {                           // body runs AT LEAST once
    ++tries;
  } while (tries < 3);

  switch (sum % 3) {             // integral values only
    case 0: std::cout << "divisible"; break;
    case 1: std::cout << "one over"; break;   // delete this break -> falls into default
    default: std::cout << "two over"; break;
  }
  std::cout << ' ' << tries << '\\n';   // "... 3"
  return 0;
}`,annotations:{5:`The idiomatic read-loop: cin >> x is truthy while reads succeed. This one line replaces Python's "for line in sys.stdin".`,6:`continue = jump to the next loop check. break = leave the loop for good. Same words as Python, same meaning.`,12:`do-while checks the condition AFTER the body — guaranteed first run. Rare, but interviewers like asking when it fits (retry prompts).`,17:`Every case needs its break. Omit it and execution FALLS THROUGH into the next case — a classic C++ bug Python can't even express.`}},{type:`code`,lang:`bash`,title:`The happy path — memorize this command`,code:`# the one command to memorize
g++ -std=c++17 -O2 -Wall main.cpp -o main

./main       # run the machine you just built
echo $?      # the exit code: whatever main returned`,annotations:{2:`-std=c++17: language version. -O2: optimize. -Wall: the common warning set — a free bug catcher, never build without it. -o main: name the output (default is a.out).`,4:`Windows PowerShell spelling: .\\main.exe — same binary idea, different shell.`,5:`0 here means your return 0 made the full round trip: main → OS → shell. Windows: $LASTEXITCODE.`}},{type:`note`,md:"The five-line recap:\n- `#include` pastes text (preprocessor) → each .cpp becomes a translation unit → compiler makes a .o → linker joins .o files + libraries into the executable.\n- Compiler errors carry file:line (syntax, unknown names). `undefined reference` = linker: a definition never arrived.\n- `main` returns `int` because the exit code is the program's public verdict: 0 = success.\n- Print with `'\\n'`; `endl` = newline + flush, slow in loops. Default to `long long` in contests — `int` dies at ~2.1 × 10⁹, and signed overflow is undefined behavior.\n- The command: `g++ -std=c++17 -O2 -Wall main.cpp -o main`."}],quiz:[{question:"g++ prints: undefined reference to `solve()`. Which stage failed, and what does it mean?",options:[{text:`Compiler — there is a syntax error inside solve()`,explanation:`Syntax errors die at compile time WITH a file and line number. This message has neither — it is not from the compiler.`},{text:`Linker — solve() was declared and called, but no object file or library defines it`,explanation:`Correct. Compilation trusted the declaration and left an IOU; the linker found nobody to pay it.`},{text:`Preprocessor — a missing #include`,explanation:`A missing include shows up as a COMPILER error like "solve was not declared" — a different message at a different stage.`},{text:`Runtime — solve() crashed while running`,explanation:`The link failed, so no executable was ever produced. Nothing ran at all.`}],correct:1},{question:`What does #include <iostream> literally do?`,options:[{text:`Tells the linker to link the iostream library`,explanation:`Linking happens at stage 3. #include is consumed at stage 1, before compilation even starts.`},{text:`Imports a compiled module, like Python's import`,explanation:`No module is loaded. Classic #include is dumber than that — it is pure text substitution.`},{text:`Pastes the entire text of the iostream header into your file`,explanation:`Correct. The preprocessor is a find-and-replace machine; your small file becomes a huge translation unit.`}],correct:2},{question:`The difference between std::endl and '\\n' is…`,options:[{text:`None — they are identical`,explanation:`Both emit a newline, but endl does one extra, expensive thing: it flushes the buffer.`},{text:`endl prints a newline AND flushes the output buffer`,explanation:`Correct. The flush forces pending output to be delivered now — useful when debugging, slow inside loops.`},{text:`'\\n' only works on Linux`,explanation:`'\\n' is fully portable — the stream library handles platform line endings for you.`}],correct:1},{question:`int x = 7 / 2; — what is x?`,options:[{text:`3.5`,explanation:`Both operands are int, so the division is integer division — no fraction survives.`},{text:`3`,explanation:`Correct. int / int truncates toward zero. The .5 is silently discarded.`},{text:`4`,explanation:`There is no rounding — truncation simply drops the fractional part.`},{text:`Compile error`,explanation:`Perfectly legal C++ — which is exactly what makes it a silent trap.`}],correct:1},{question:`You sum 100,000 array values, each up to 10⁹, into an int. What happens?`,options:[{text:`Fine — int grows as needed, like Python`,explanation:`C++ integers are fixed-width boxes. Nothing grows; excess spills.`},{text:`The sum overflows int — signed overflow is undefined behavior`,explanation:`Correct. The sum reaches ~10¹⁴, far past int's ~2.1 × 10⁹ ceiling. Use long long for the accumulator.`},{text:`The compiler refuses to build it`,explanation:`The compiler cannot know your runtime values — it builds happily and the program misbehaves later.`}],correct:1},{question:`main returns 0. Who actually reads that value?`,options:[{text:`Nobody — it is decorative`,explanation:`It is a live contract: entire CI systems and shell scripts branch on this number.`},{text:`The OS and shell — it is the process exit code; scripts and CI branch on it`,explanation:`Correct. 0 = success, nonzero = failure. echo $? shows it; a && b depends on it.`},{text:`It is printed to the screen after the program ends`,explanation:`Nothing is printed. The value is stored by the OS, visible only if you ask (echo $?).`}],correct:1},{question:`In g++ -std=c++17 -O2 -Wall main.cpp -o main, what does -Wall do?`,options:[{text:`Enables all optimizations`,explanation:`Optimization is -O2 (or -O3). -Wall is about diagnostics, not speed.`},{text:`Enables the common set of compiler warnings`,explanation:`Correct. A free bug catcher — it flags uninitialized variables, suspicious comparisons and more, at zero runtime cost.`},{text:`Links all available libraries`,explanation:`Library linking uses -l flags at the link stage. -Wall never touches the linker.`}],correct:1}],interviewQuestions:[{question:`Walk me through everything g++ does when you run: g++ main.cpp helper.cpp -o app.`,answer:`Per file: the preprocessor expands every #include and #define, producing one translation unit per .cpp. The compiler then translates each translation unit independently into an object file (main.o, helper.o) — machine code plus a symbol table listing what it defines and what it still needs. Finally the linker joins the object files with the standard library, matches every "still needs" against a "defines", and writes one executable, app. Key sentence: **compilation is per-file and trusts declarations; only the link step sees the whole program.**`,isCaseBased:!1},{question:`Why is "undefined reference" a linker error and not a compiler error?`,answer:`Because the compiler only ever sees one translation unit. A declaration is a promise: "this function exists somewhere". The compiler accepts the promise, emits a call to a named symbol, and moves on — it cannot know whether some other .cpp defines it. Only the linker, holding ALL object files and libraries, can discover the promise was never kept. That is also why the error has no line number: at link time there is no source code left, only symbols.`,isCaseBased:!1},{question:`Case: you refactor — solve() moves from main.cpp into solver.cpp. Now g++ main.cpp -o app fails with "undefined reference to solve()". A teammate says "just add a header". Will that fix it?`,answer:`No — headers fix *declarations*, and this is a missing *definition*. The declaration is exactly why main.cpp still COMPILES: the compiler trusts it. The link fails because no object file handed to g++ contains solve()'s body. Fix the build, not the source: g++ main.cpp solver.cpp -o app — or compile each file to a .o and link them, which is what real build systems do so unchanged files are not recompiled. Tradeoff worth naming: the one-command build is simple but recompiles everything every time; per-file .o + link scales, and automating that dependency dance is the entire reason Make and CMake exist.`,isCaseBased:!0},{question:`Case: your program crashes somewhere. You sprinkle std::cout << "checkpoint A\\n" logs, rerun — but the last few checkpoints never print, even though you are certain that code executed. What is going on?`,answer:`Buffering. cout collects output in a memory buffer, and '\\n' does not flush it. The crash killed the process with the final checkpoints still sitting undelivered in the buffer — so the log lies about where the crash happened. Fixes: log with std::cerr (unbuffered, appears immediately), or use std::endl to force a flush per line. Tradeoff: flushing every line is slow, which is exactly why cout buffers in the first place — so this is a debugging mode, not a shipping default. This bug has burned every C++ engineer once; interviewers use it to separate "used cout" from "knows what cout does".`,isCaseBased:!0},{question:`What exactly is a translation unit, and what practical consequences does it have?`,answer:`One .cpp file after preprocessing: the file itself plus the full pasted text of everything it included. It is the unit the compiler works on — compiled alone, into one object file, knowing nothing about other units. Consequences: (1) headers must carry declarations so every unit that calls a function knows its signature; (2) each definition must live in exactly one unit, or the linker reports a duplicate symbol — which is why function bodies generally stay out of headers; (3) touching a header recompiles every unit that includes it, which is why large projects obsess over include hygiene and build times.`,isCaseBased:!1},{question:`Why does main return int, and what happens to the value?`,answer:`It becomes the process exit code — the single number the OS keeps after your program terminates. Convention: 0 = success; nonzero = failure, with the value free to encode which failure. Consumers: shells (echo $?, a && b), CI pipelines (any nonzero fails the job), and any program that spawned yours. This contract is why void main is non-standard C++: the outside world expects an int. main is also the one function where the return may be omitted — the standard inserts return 0 — but writing it keeps intent visible.`,isCaseBased:!1},{question:`int vs long long vs int64_t — how do you choose?`,answer:`int when values provably fit in ±2.1 × 10⁹ AND memory matters — huge arrays, where 4 bytes vs 8 halves cache traffic. long long the moment any sum, product or count can exceed that — in contests that is "almost always", so long long is the standard default there. int64_t (from <cstdint>) when the width must be bit-exact: file formats, network protocols, serialized data shared across platforms — because long long only promises AT LEAST 64 bits while int64_t promises exactly 64. Tradeoffs to say out loud: 64-bit types double memory traffic and can genuinely slow array-heavy code, but a silent overflow costs infinitely more than a cache miss.`,isCaseBased:!1},{question:`Case: contest solution passes samples, fails big hidden tests. The suspicious line: int area = n * m; with n, m up to 100000. Diagnose it.`,answer:`n * m reaches 10¹⁰. The multiplication happens in int BEFORE any assignment, overflows past ~2.1 × 10⁹, and signed overflow is undefined behavior — typically a wrapped negative number, on a bad day something stranger from the optimizer. Crucially, changing the destination does NOT help: long long area = n * m; is equally broken, because the math already happened in int. Fixes: declare n and m as long long from the start (the reliable contest habit), or force promotion in the expression: 1LL * n * m. General rule: overflow is prevented in the OPERANDS, never in the destination.`,isCaseBased:!0},{question:`How does std::cout << a << " " << b actually chain? What makes that syntax work?`,answer:`operator<< takes the stream and a value, writes the value, and returns the same stream by reference. The expression evaluates left to right: (cout << a) yields cout back, which becomes the left operand of << " ", and so on. The same trick powers cin >> x >> y. Two things interviewers fish for: returning the stream by reference is the design choice that enables fluent chaining (the same pattern as builder APIs), and the stream also converts to bool — false once input fails — which is why while (cin >> x) is the idiomatic read-until-EOF loop.`,isCaseBased:!1},{question:`Why does -7 / 2 give -3 in C++ but -7 // 2 give -4 in Python — and when does the difference bite?`,answer:`C++ integer division truncates toward zero (drop the fraction); Python floor-divides (round toward minus infinity). They agree on positives and split on negatives: C++ says -3, Python -4. The remainder splits with it: in C++ -7 % 2 == -1 (sign follows the dividend), in Python it is 1 (sign follows the divisor). It bites when porting Python algorithms — hash bucketing, cyclic indexing, calendar math — where any x % n with a possibly-negative x needs ((x % n) + n) % n in C++. The rule to state: same symbols, different rounding contract; audit every division and mod the moment negative values are possible.`,isCaseBased:!1}],flashcards:[{front:`The three build stages, in order`,back:`Preprocessor (pastes #includes → translation unit) → compiler (translation unit → .o object file) → linker (joins .o files + libraries → executable).`},{front:`What #include <iostream> literally does`,back:`Text paste. The preprocessor replaces the line with the header's full contents — no importing, no linking, just find-and-replace.`},{front:`"undefined reference to f()" means…`,back:`Linker error: f was declared and called, but no object file or library given to the linker DEFINES it. Add the missing .cpp/.o or library.`},{front:`Compiler error vs linker error — the instant tell`,back:`Has file + line number → compiler (syntax, unknown names). No line, says "undefined reference" → linker (missing definition).`},{front:`'\\n' vs std::endl`,back:`Both print a newline; endl ALSO flushes the buffer. Flushing per line is slow — default to '\\n', use endl/cerr only when you must see output NOW.`},{front:`What main's return value is for`,back:`The process exit code. 0 = success, nonzero = failure. Shells ($?), scripts (a && b) and CI all read it.`},{front:`int vs long long ceilings`,back:`int: ~2.1 × 10⁹ (32-bit). long long: ~9.2 × 10¹⁸ (64-bit). Contest default: long long — sums and products blow past int constantly. int64_t = exactly 64 bits.`},{front:`Integer division: the Python trap`,back:`int / int truncates toward zero: 7/2 == 3, -7/2 == -3. Python floors: -7 // 2 == -4. The sign of % differs too.`},{front:`The build-and-run command`,back:`g++ -std=c++17 -O2 -Wall main.cpp -o main — language version, optimize, all common warnings, output name. Then ./main.`}],mindmapMarkdown:`- What g++ Actually Does — Compilation, main & I/O
  - Build pipeline
    - preprocessor: #include = text paste
    - translation unit → compiler → .o
    - linker: joins .o + libraries → executable
  - Error triage
    - compiler error: syntax, has file:line
    - undefined reference: linker, no line
  - main
    - returns int = exit code, 0 success
    - shell reads it: echo $?
  - I/O
    - cin >> / cout <<, chaining via returned stream
    - '\\n' vs endl: endl also flushes
    - flush per line = slow loops
  - Types
    - int ~±2.1e9, long long ~±9.2e18
    - int64_t: exactly 64 bits (<cstdint>)
    - sizeof: bytes, compile-time
    - contest default: long long
  - vs Python
    - braces, else if, ++/--
    - int/int truncates toward zero
    - overflow real: UB, no bigints
  - g++ -std=c++17 -O2 -Wall main.cpp -o main`};export{e as default};