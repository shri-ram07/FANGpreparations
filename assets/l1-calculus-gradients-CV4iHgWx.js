var e={id:`math-l1-calculus-gradients`,subjectId:`math`,level:1,title:`Derivatives, Gradients and Descent`,whyItMatters:`Training a model is one idea repeated: find the slope, step downhill. The gradient is what "downhill" means when there are a million parameters instead of one.`,assumes:[`You have read *Vectors and the Dot Product*`,`You know that a slope is rise over run`],estMinutes:18,sections:[{type:`intuition`,title:`One derivative, then many at once`,md:`A **derivative** answers one question: if I nudge this input a little, how much does the output move? It is the slope of the function at a point.

With several inputs, hold all but one fixed and ask the same question — that is a **partial derivative**. Do it for every input and collect the answers into a vector, and you have the **gradient**.

The gradient has two properties that make training possible. It points in the direction of **steepest increase**, so the negative gradient is downhill. And its **length** is how steep that is, which is why a flat region gives small steps and a cliff gives large ones without anyone arranging it.`},{type:`math`,intro:`The gradient, and the update rule that uses it. The gradient is just the partial derivatives stacked into a vector. η is the learning rate: how far to move along that direction, since the gradient tells you which way and how steep but not how far to trust it.`,latex:[`\\nabla f = \\left[\\frac{\\partial f}{\\partial x_1}, \\;\\dots,\\; \\frac{\\partial f}{\\partial x_n}\\right]`,`\\theta \\leftarrow \\theta - \\eta \\nabla f(\\theta)`]},{type:`code`,lang:`python`,title:`A gradient, by hand and by measurement`,code:`import math
f  = lambda x, y: x*x + 3*y*y
gx = lambda x, y: 2*x            # d/dx  by hand
gy = lambda x, y: 6*y            # d/dy  by hand

print('f(3,2) = %.1f   gradient = (%.1f, %.1f)' % (f(3,2), gx(3,2), gy(3,2)))

h = 1e-5
print('measured df/dx = %.6f   df/dy = %.6f'
      % ((f(3+h,2) - f(3-h,2)) / (2*h), (f(3,2+h) - f(3,2-h)) / (2*h)))

step = 0.01
along = (f(3 + step*6/math.hypot(6,12), 2 + step*12/math.hypot(6,12)) - f(3,2))
print('moving 0.01 along the gradient changes f by %.6f' % along)
print('predicted: 0.01 * |gradient| = %.6f' % (step * math.hypot(6,12)))

# ---- real output ----
# f(3,2) = 21.0   gradient = (6.0, 12.0)
# measured df/dx = 6.000000   df/dy = 12.000000
# moving 0.01 along the gradient changes f by 0.134424
# predicted: 0.01 * |gradient| = 0.134164`,annotations:{3:`The y term has a 3 in front, so its derivative is 6y against 2x. The gradient at (3,2) is (6, 12) — twice as steep in y as in x, which is exactly the ill-conditioning that motivates momentum and Adam.`,11:`The central difference agrees to six decimals. This is gradient checking: slow, since it needs two evaluations per input, but the definitive test of a hand-derived gradient.`,16:`0.134424 measured against 0.134164 predicted. The gradient's LENGTH is the rate of change in its own direction, and the small gap is the curvature — the gradient is a local linear approximation and 0.01 is not infinitesimal.`}},{type:`code`,lang:`python`,title:`Descent, and what the unequal steepness does to it`,code:`x, y, lr = 3.0, 2.0, 0.1
print(' step        x         y         f')
for t in range(6):
    print(' %4d %9.4f %9.4f %9.4f' % (t, x, y, f(x, y)))
    x -= lr * gx(x, y)
    y -= lr * gy(x, y)

# ---- real output ----
#  step        x         y         f
#     0    3.0000    2.0000   21.0000
#     1    2.4000    0.8000    7.6800
#     2    1.9200    0.3200    3.9936
#     3    1.5360    0.1280    2.4084
#     4    1.2288    0.0512    1.5178
#     5    0.9830    0.0205    0.9676`,annotations:{5:`Subtract the gradient times the learning rate from each coordinate. That is the entire algorithm, and everything in the optimizers module is a variation on this line.`,13:`y falls from 2.0 to 0.8 in one step while x moves only 3.0 to 2.4 — the steeper direction moves faster, automatically, because the gradient is larger there.`,18:`By step 5, y is essentially solved at 0.0205 while x is still at 0.9830 and has a long way to go. The shared learning rate is now doing nothing useful for y and is too small for x, which is exactly the conflict momentum and Adam exist to resolve.`}},{type:`note`,label:`The chain rule is the whole of backprop`,md:`A neural network is functions inside functions. The **chain rule** says the derivative of a composition is the product of the local derivatives: to differentiate f(g(x)), multiply the outer slope by the inner slope.

That is all backpropagation is — the chain rule applied systematically to a computation graph, walking backwards and multiplying local derivatives as it goes.

Two consequences follow immediately from it being a **product**. If the local slopes are consistently below 1, the product goes to zero with depth: **vanishing gradients**. If consistently above 1, it explodes. Neither is a bug in the algorithm; both are what multiplication does.

The two local rules worth memorising: a **multiplication** node passes the *other* factor, and an **addition** node passes the incoming gradient through unchanged.`},{type:`note`,label:`Convexity, and why it does not save you`,md:`A **convex** function has one minimum, and gradient descent on it is guaranteed to find that minimum. Linear and logistic regression are convex, which is why they are reliable and why their solutions are reproducible.

Neural network losses are **not convex** — they have many local minima, saddle points and flat regions, and no guarantee applies.

In practice this matters less than it sounds. In high dimensions, **saddle points vastly outnumber local minima**, because a local minimum requires the curvature to be positive in *every* one of a million directions at once, and that is unlikely. Saddles slow descent down rather than trapping it, and momentum escapes them.

The empirical finding is that most minima a large network reaches have similar loss, so which one you land in matters far less than the fact that you can reach one at all.`},{type:`note`,label:`Second derivatives: what they buy and what they cost`,md:`The **Hessian** is the matrix of second derivatives — how the slope itself changes, i.e. curvature. Its eigenvalues tell you the shape of the bowl in each direction, and the ratio of largest to smallest is the **condition number**, which is exactly what makes the ravine in the descent example above hard.

Newton's method uses that curvature to jump straight to the minimum of the local quadratic approximation, converging in far fewer steps.

It is unusable at scale. The Hessian of a model with **n** parameters has **n²** entries — for a million parameters that is a trillion numbers, and inverting it is worse. So deep learning uses first-order methods and approximates the useful part of the curvature information: **Adam's** per-coordinate scaling is a crude diagonal approximation to exactly this, which is why it helps on badly conditioned problems.`}],quiz:[{question:`What are the two properties of the gradient that make descent work?`,options:[{text:`It is always positive and always small`,explanation:`Neither is true.`},{text:`It points in the direction of steepest increase, and its length is how steep that is`,explanation:`Correct — so the negative gradient is downhill, and step size adapts to steepness automatically.`},{text:`It is constant across the function`,explanation:`It varies at every point.`},{text:`It equals the function value`,explanation:`Unrelated quantities.`}],correct:1},{question:`Moving 0.01 along the gradient changed f by 0.134424 while 0.01·‖∇f‖ predicted 0.134164. What is the gap?`,options:[{text:`A floating-point error`,explanation:`It is far larger than floating-point noise.`},{text:`Curvature — the gradient is a local LINEAR approximation and 0.01 is not infinitesimal`,explanation:`Correct, and it is exactly the information second-order methods use.`},{text:`The gradient was computed wrongly`,explanation:`It matched central differences to six decimals.`},{text:`The learning rate`,explanation:`No learning rate is involved in this measurement.`}],correct:1},{question:`In the descent trace, y went 2.0 → 0.8 in one step while x went 3.0 → 2.4. Why?`,options:[{text:`Different learning rates were used`,explanation:`The same 0.1 was applied to both.`},{text:`The gradient is 6y against 2x, so the steeper direction moves faster automatically`,explanation:`Correct — the gradient at (3,2) is (6, 12).`},{text:`y was updated twice`,explanation:`Each coordinate is updated once per step.`},{text:`A rounding artefact`,explanation:`The values are exact.`}],correct:1},{question:`By step 5, y is at 0.0205 and x is at 0.9830. What does that illustrate?`,options:[{text:`The algorithm has converged`,explanation:`x is nowhere near zero.`},{text:`A shared learning rate is now useless for y and too small for x — the conflict momentum and Adam resolve`,explanation:`Correct. Unequal curvature across directions is the core optimisation problem.`},{text:`The learning rate is too large`,explanation:`Nothing has diverged; it is too small for x.`},{text:`The gradient was computed wrongly`,explanation:`It matches the analytic derivative exactly.`}],correct:1},{question:`Why do vanishing and exploding gradients both follow from the chain rule?`,options:[{text:`They are bugs in backprop implementations`,explanation:`They are what the correct algorithm does.`},{text:`The chain rule multiplies local derivatives, and a product of numbers consistently below or above 1 goes to zero or infinity`,explanation:`Correct — it is arithmetic, which is why the fixes are structural.`},{text:`Because of the learning rate`,explanation:`The learning rate scales the result but does not cause the compounding.`},{text:`Only vanishing follows; exploding is separate`,explanation:`They are the same mechanism in opposite directions.`}],correct:1},{question:`Why is the non-convexity of neural network losses less alarming than it sounds?`,options:[{text:`Because the losses are actually convex`,explanation:`They are genuinely not.`},{text:`Saddle points vastly outnumber local minima in high dimensions, and saddles slow descent rather than trapping it`,explanation:`Correct — a local minimum needs positive curvature in every one of a million directions at once.`},{text:`Because Adam guarantees a global minimum`,explanation:`No optimizer guarantees that here.`},{text:`Because the minima are all identical`,explanation:`They are not identical, only similar in loss.`}],correct:1}],interviewQuestions:[{question:`What is a gradient and why does gradient descent work?`,answer:`The vector of partial derivatives — one per input, each asking how the output moves if you nudge that input alone. Two properties make it useful: it points in the direction of steepest increase, so the negative gradient is downhill, and its length is how steep that direction is. The second property matters more than people expect, because it means the step size adapts to the terrain for free: a flat region produces small steps and a steep one large steps, without anyone arranging it. The update is θ ← θ − η∇f, and the learning rate exists because the gradient tells you which way and how steep but not how far the linear approximation stays valid.`,isCaseBased:!1},{question:`How do you verify a gradient you derived by hand?`,answer:`Central differences: (f(x+h) − f(x−h)) / 2h, compared against the analytic value. In the worked example that agreed to six decimal places on both partials. Central rather than forward differences because the error is O(h²) instead of O(h), and h around 1e-5 in double precision — larger and truncation error dominates, smaller and floating-point cancellation does. Compare relative rather than absolute error, and check a random sample of parameters rather than all of them, since it costs two forward passes each and is far too slow to train with. It is a verification tool, not an alternative to backprop.`,isCaseBased:!0},{question:`Explain the chain rule's role in backpropagation.`,answer:`Backprop is the chain rule applied systematically to a computation graph, in reverse. The derivative of a composition is the product of the local derivatives, so the backward pass starts at the loss and multiplies by each operation's local derivative as it walks back. The two local rules worth having memorised are that a multiplication node passes the other factor and an addition node passes the gradient through unchanged. The important structural consequence is that the result is a product — which is why gradients vanish when the local slopes are consistently below 1 and explode when above, and why ReLU with slope exactly 1 and residual connections with an unmultiplied path are the standard fixes.`,isCaseBased:!1},{question:`Why do we use first-order methods rather than Newton's method?`,answer:`Cost. Newton's method uses the Hessian, the matrix of second derivatives, to jump straight to the minimum of the local quadratic approximation — far fewer steps, and it handles ill-conditioning naturally. But the Hessian of a model with n parameters has n² entries, so a million-parameter model would need a trillion numbers before you even try to invert it. So deep learning uses gradients and approximates the useful part of the curvature: Adam's per-coordinate scaling is essentially a crude diagonal approximation to the Hessian, which is exactly why it helps on badly conditioned problems. L-BFGS sits between them and is used where the parameter count is modest.`,isCaseBased:!1},{question:`Does non-convexity matter in practice?`,answer:`Less than the theory suggests. Neural network losses are genuinely non-convex, so no convergence guarantee applies, but the high-dimensional geometry is forgiving: saddle points vastly outnumber local minima, because a local minimum requires positive curvature in every one of a million directions simultaneously and that is very unlikely. Saddles slow descent rather than trapping it, and momentum gets through them. The empirical finding that matters is that most minima a large network reaches have similar loss, so which one you land in is less important than being able to reach one. Where it does bite is reproducibility — different seeds give different solutions, so results have to be reported over several runs.`,isCaseBased:!0},{question:`What is the condition number and why does it hurt?`,answer:`The ratio of the largest to the smallest eigenvalue of the Hessian — how much more curved the loss is in the steepest direction than the flattest. It hurts because the learning rate is shared: it must be small enough for the steepest direction to remain stable, and that same value makes progress along the flat direction glacial. The descent trace shows the shape of it even in two dimensions — at step 5, y is essentially solved at 0.0205 while x is still at 0.9830 and the shared rate is now useless for one and too small for the other. Momentum accumulates along the flat direction and Adam rescales per coordinate; both are answers to this one problem.`,isCaseBased:!0},{question:`What is a directional derivative, and how does it relate to the gradient?`,answer:`The rate of change along a chosen unit direction, and it is the dot product of the gradient with that direction. That single fact is what proves the gradient points steepest uphill: a dot product is maximised when the two vectors are aligned, so the direction of greatest increase is the gradient's own direction, and the rate along it is the gradient's length. The worked example checks this numerically — moving 0.01 along the gradient changed f by 0.134424 against a predicted 0.01 × 13.4164 = 0.134164, with the small gap being curvature. Directions orthogonal to the gradient give zero, which is what a contour line is.`,isCaseBased:!1},{question:`Why is the learning rate needed at all if the gradient already has a magnitude?`,answer:`Because the gradient is only a local linear approximation, and its magnitude tells you the rate of change right at that point, not how far that rate remains accurate. The worked measurement shows the discrepancy appearing at a step of just 0.01. Take a step scaled purely by the gradient and on a steep region you overshoot badly, which is exactly what happens past the stability limit of 2 over the curvature. The learning rate is the trust parameter: how far to believe the linear approximation. That is also why schedules exist — early on the model is far from a minimum and large steps are useful, and later small ones are needed to settle.`,isCaseBased:!1}],flashcards:[{front:`Gradient`,back:`The vector of partial derivatives. Points steepest UPHILL, and its LENGTH is how steep — so step size adapts to terrain for free.`},{front:`The update rule`,back:`θ ← θ − η∇f. The learning rate exists because the gradient says which way and how steep, not how far to trust it.`},{front:`Gradient checking`,back:`(f(x+h) − f(x−h)) / 2h with h ≈ 1e-5. Matched 6.000000 and 12.000000 exactly. O(h²) error; too slow to train with.`},{front:`The curvature gap`,back:`Moving 0.01 along ∇f changed f by 0.134424; 0.01·‖∇f‖ predicted 0.134164. The gap IS the curvature.`},{front:`What the descent trace shows`,back:`y: 2.0 → 0.0205 in five steps while x is still 0.9830. The shared learning rate is now useless for one and too small for the other.`},{front:`The chain rule`,back:`Backprop IS this, applied to a graph in reverse. Multiplication node passes the other factor; addition passes the gradient through.`},{front:`Why gradients vanish/explode`,back:`The chain rule gives a PRODUCT. Below 1 compounds to zero, above 1 to infinity. Arithmetic, not a bug.`},{front:`Non-convexity in practice`,back:`Saddles vastly outnumber local minima — a minimum needs positive curvature in EVERY direction at once. Saddles slow descent, not trap it.`}],mindmapMarkdown:`- Derivatives and gradients
  - Derivative
    - nudge the input, how much does the output move
    - partial: hold the others fixed
    - gradient: all the partials, stacked
  - Two properties
    - points steepest UPHILL
    - length = how steep
    - so step size adapts to terrain for free
  - Verification
    - central difference (f(x+h)-f(x-h))/2h, h ~ 1e-5
    - matched 6.000000 and 12.000000
    - moving 0.01 along grad: 0.134424 vs 0.134164
    - the gap IS the curvature
  - Descent
    - theta <- theta - eta * grad
    - y 2.0 -> 0.0205 while x still 0.9830
    - shared LR: useless for one, too small for the other
    - -> momentum, Adam
  - Chain rule
    - backprop IS this, on a graph, in reverse
    - multiply node passes the OTHER factor
    - add node passes the gradient through
    - it is a PRODUCT -> vanish or explode
  - Convexity
    - convex = one minimum, guaranteed (linear/logistic)
    - NN losses are not
    - but saddles >> local minima in high dimensions
    - most reachable minima have similar loss
  - Second order
    - Hessian = curvature; condition number = the ravine
    - n^2 entries -> unusable at scale
    - Adam is a crude diagonal approximation to it`};export{e as default};