var e={id:`dl-l0-perceptron-mlp`,subjectId:`dl`,level:0,title:`From Perceptron to MLP: Why Non-Linearity`,whyItMatters:`Every neural network is layers stacked on layers. Without one specific ingredient between them, that entire stack collapses back into a single linear layer — and the algebra showing it is three lines long.`,assumes:[`You know what a weighted sum is`,`You have seen matrix multiplication`],estMinutes:20,sections:[{type:`intuition`,title:`A perceptron draws one straight line`,md:`A **perceptron** takes inputs, multiplies each by a weight, adds a bias, and fires if the total clears zero. That is the whole model.

Geometrically it draws **one straight line** through the input space and calls everything on one side 1 and everything on the other 0. Its capacity is exactly that: one line.

So it can learn any pattern that a single line separates, and nothing else. The next snippet finds the wall.`},{type:`code`,lang:`python`,title:`A perceptron on AND, then on XOR`,code:`X = [(0,0), (0,1), (1,0), (1,1)]

def train(targets):
    w, b = [0.0, 0.0], 0.0
    for _ in range(50):
        for (x1, x2), t in zip(X, targets):
            p = 1 if w[0]*x1 + w[1]*x2 + b > 0 else 0
            e = t - p
            w[0] += 0.1*e*x1; w[1] += 0.1*e*x2; b += 0.1*e
    return w, b, [1 if w[0]*a + w[1]*c + b > 0 else 0 for a, c in X]

for name, t in [('AND', [0,0,0,1]), ('XOR', [0,1,1,0])]:
    w, b, preds = train(t)
    acc = sum(int(p == q) for p, q in zip(preds, t)) / 4
    print('%s  w=%s b=%.1f  preds=%s  accuracy=%.2f' % (name, [round(v,2) for v in w], b, preds, acc))

# ---- real output ----
# AND  w=[0.2, 0.1] b=-0.2  preds=[0, 0, 0, 1]  accuracy=1.00
# XOR  w=[-0.1, 0.0] b=0.1  preds=[1, 1, 0, 0]  accuracy=0.50`,annotations:{9:`The perceptron rule: nudge the weights by the error times the input. On separable data this provably converges; the guarantee simply does not apply otherwise.`,18:`AND is solved exactly — one line with (1,1) on one side and the other three on the other.`,19:`XOR after the same 50 epochs sits at 50%, which is chance. It is not undertrained: no line exists that puts (0,1) and (1,0) together while separating them from (0,0) and (1,1). Draw the four points and try.`}},{type:`intuition`,title:`Stacking layers does not help, and here is why`,md:`The obvious fix is to stack a second layer on the first. It does nothing.

Two linear layers apply **W₂(W₁x)**, and matrix multiplication is associative, so that equals **(W₂W₁)x** — a single matrix, therefore a single linear layer, therefore still one straight line.

A hundred layers collapse the same way. Depth without something non-linear between the layers buys exactly zero extra capacity, and the next snippet is that claim as arithmetic.`},{type:`code`,lang:`python`,title:`Two layers collapsing into one matrix`,code:`import numpy as np

A = np.array([[1.76, 0.4], [0.98, 2.24], [1.87, -0.98]])   # layer 2 (3x2)
B = np.array([[0.95, -0.15, -0.1], [0.41, 0.14, 1.45]])    # layer 1 (2x3)
v = np.array([1.0, 2.0, 3.0])

print('A @ B =', np.round(A @ B, 4).tolist())
print('two layers  A(Bv) =', np.round(A @ (B @ v), 4).tolist())
print('one layer  (AB)v  =', np.round((A @ B) @ v, 4).tolist())

# ---- real output ----
# A @ B = [[1.836, -0.208, 0.404], [1.8494, 0.1666, 3.15], [1.3747, -0.4177, -1.608]]
# two layers  A(Bv) = [2.632, 11.6326, -4.2847]
# one layer  (AB)v  = [2.632, 11.6326, -4.2847]`,annotations:{7:`A @ B is computed once and is a plain 3×3 matrix. Whatever the two layers were meant to represent is now a single linear map.`,13:`Identical to the last decimal, and not by luck — associativity makes it an identity, not an approximation. This is why an activation function is not a tuning detail but the thing that makes depth mean anything.`}},{type:`note`,label:`The fix, in one word`,md:`Put a **non-linear function** between the layers. Then W₂·f(W₁x) cannot be folded into one matrix, because f is not a matrix and there is nothing to multiply through.

The usual choice is **ReLU**: \`f(z) = max(0, z)\`. It is barely non-linear — two straight pieces joined at zero — and that is enough. The bend is what breaks associativity.

A network of straight pieces can bend the decision boundary anywhere, given enough pieces. That is the entire content of depth.`},{type:`code`,lang:`python`,title:`XOR solved by hand with two hidden units`,code:`def relu(z):
    return max(0.0, z)

for (x1, x2), t in zip(X, [0, 1, 1, 0]):
    h1 = relu(x1 + x2 - 0.0)   # fires when at least one input is on
    h2 = relu(x1 + x2 - 1.0)   # fires only when both are on
    out = h1 - 2 * h2
    print('x=(%d,%d)  h=(%.0f, %.0f)  out=%.0f  target=%d' % (x1, x2, h1, h2, out, t))

# ---- real output ----
# x=(0,0)  h=(0, 0)  out=0  target=0
# x=(0,1)  h=(1, 0)  out=1  target=1
# x=(1,0)  h=(1, 0)  out=1  target=1
# x=(1,1)  h=(2, 1)  out=0  target=0`,annotations:{5:`Both hidden units compute the SAME weighted sum x1 + x2. Only the bias differs, which is enough to make them fire on different regions — that is what a bias is for.`,8:`The output layer subtracts twice h2, so the "both on" case is cancelled back down. XOR is exactly "at least one, but not both", and these three lines say precisely that.`,14:`All four correct, with weights chosen by hand rather than trained. The point is capacity: two hidden units are enough for XOR, which no single perceptron can reach at all.`}},{type:`note`,label:`What "universal approximation" does and does not promise`,md:`The universal approximation theorem says an MLP with **one hidden layer** and enough units can approximate any continuous function to any accuracy. It sounds like it ends the discussion about depth. It does not.

What it omits: **how many** units — the bound can be exponential in the input dimension — and whether gradient descent can **find** those weights. Existence is not reachability.

Depth is the practical answer to both. Deep networks reuse features across layers, so representing the same function takes far fewer parameters than a single enormous hidden layer, and they train better in practice. The theorem tells you the ceiling exists; it says nothing about the stairs.`}],quiz:[{question:`Why can a single perceptron not learn XOR?`,options:[{text:`XOR needs more training epochs`,explanation:`It sat at 50% after 50 epochs and will stay there forever — this is not a training issue.`},{text:`A perceptron draws one straight line, and no line separates {(0,1),(1,0)} from {(0,0),(1,1)}`,explanation:`Correct. Its capacity is exactly one linear boundary.`},{text:`The learning rate was too high`,explanation:`No learning rate produces a boundary that does not exist.`},{text:`XOR has too few examples`,explanation:`All four possible inputs were shown; the data is complete.`}],correct:1},{question:`Two linear layers applied in sequence gave [2.632, 11.6326, −4.2847], identical to one matrix A@B applied once. Why is that exact rather than approximate?`,options:[{text:`The numbers happened to be small`,explanation:`It holds for any matrices and any input.`},{text:`Matrix multiplication is associative, so A(Bv) = (AB)v is an identity`,explanation:`Correct — which is why stacking linear layers adds no capacity at all.`},{text:`NumPy caches the result`,explanation:`The two expressions are computed independently.`},{text:`Rounding to four decimals hid a difference`,explanation:`There is no difference to hide; associativity is exact.`}],correct:1},{question:`What does a non-linearity between layers actually do?`,options:[{text:`It speeds up training`,explanation:`It may or may not; that is not its purpose.`},{text:`It makes W₂·f(W₁x) unfoldable into one matrix, so depth adds capacity`,explanation:`Correct. f is not a matrix, so there is nothing to multiply through.`},{text:`It normalises the activations`,explanation:`That is what normalisation layers do, and it is a different job.`},{text:`It prevents overfitting`,explanation:`It increases capacity, which if anything makes overfitting easier.`}],correct:1},{question:`In the hand-built XOR network, both hidden units compute x1 + x2. What makes them different?`,options:[{text:`Different weights`,explanation:`The weights are identical — both are 1 and 1.`},{text:`Different biases: 0 versus −1, so one fires when at least one input is on and the other only when both are`,explanation:`Correct. The bias shifts where the ReLU bend sits, which is exactly what a bias is for.`},{text:`Different activation functions`,explanation:`Both use ReLU.`},{text:`One is in a deeper layer`,explanation:`They are in the same layer, side by side.`}],correct:1},{question:`ReLU is max(0, z) — two straight pieces. How can something that simple be enough?`,options:[{text:`It is not; smooth activations are required in practice`,explanation:`ReLU is the most widely used activation in deep learning.`},{text:`The bend at zero breaks associativity, and enough straight pieces can approximate any bend`,explanation:`Correct. Non-linearity does not have to be elaborate; it has to exist.`},{text:`ReLU is secretly non-linear everywhere`,explanation:`It is linear on each side of zero; only the junction is non-linear.`},{text:`Because it is unbounded above`,explanation:`Unboundedness matters for gradients, not for the collapse argument.`}],correct:1},{question:`The universal approximation theorem says one hidden layer suffices. Why is deep learning deep, then?`,options:[{text:`The theorem is wrong`,explanation:`It is correct; it just promises less than it appears to.`},{text:`It promises existence, not the unit count or that gradient descent can find those weights — depth reuses features and needs far fewer parameters`,explanation:`Correct. The bound can be exponential in the input dimension.`},{text:`One hidden layer cannot represent XOR`,explanation:`It can — the worked example above has exactly one hidden layer.`},{text:`Deep networks are faster to evaluate`,explanation:`Not generally, and that is not the reason.`}],correct:1}],interviewQuestions:[{question:`Why do neural networks need activation functions?`,answer:`Because without them, depth is free of charge and worth nothing. Two linear layers compute W₂(W₁x), and matrix multiplication is associative, so that is exactly (W₂W₁)x — one matrix, one linear layer, one straight decision boundary, no matter how many layers you stack. An activation breaks that: W₂·f(W₁x) cannot be folded, because f is not a matrix. It does not need to be elaborate — ReLU is two straight pieces joined at zero, and that single bend is enough to make the stack strictly more expressive than any one layer.`,isCaseBased:!1},{question:`Explain the XOR problem and its significance.`,answer:`XOR is true when exactly one input is on. Plot the four points and no straight line separates the two true cases from the two false ones, so a perceptron cannot represent it — trained on all four examples for 50 epochs it lands at 50% accuracy, which is chance, and would stay there forever. It matters historically because Minsky and Papert made this point in 1969 and it contributed to the first AI winter. It matters technically because the fix is the whole architecture of modern networks: a hidden layer with a non-linearity. Two ReLU units solve XOR by hand, using the same weights with different biases.`,isCaseBased:!0},{question:`What does the universal approximation theorem actually guarantee?`,answer:`That a network with one hidden layer and enough units can approximate any continuous function on a compact domain to arbitrary accuracy. What it does not guarantee is the part that matters in practice: how many units — the bound can grow exponentially with input dimension — and whether gradient descent can find those weights from a random start. It is an existence result about representation, not a statement about learnability. Depth is the practical answer, because deep networks compose features and reach the same functions with far fewer parameters and better optimisation behaviour.`,isCaseBased:!1},{question:`Why go deep instead of wide?`,answer:`Parameter efficiency and feature reuse. There are functions a deep network represents with a number of units polynomial in depth that a shallow one needs exponentially many to match — the composition itself is where the leverage is. Practically, layers form a hierarchy: early layers learn edges, later ones parts, later still objects, and each level is built from the one below rather than learned from scratch. Wide-and-shallow also tends to memorise rather than generalise. The counterweights are that depth brings vanishing gradients and optimisation difficulty, which is why residual connections and normalisation exist at all.`,isCaseBased:!1},{question:`A colleague builds a 10-layer network with no activation functions and it underperforms logistic regression. Explain what happened.`,answer:`It is logistic regression, with more parameters and a harder optimisation problem. Ten stacked linear layers collapse to one linear map by associativity, so the model class is identical to a single linear layer — it cannot represent anything logistic regression cannot. It underperforms rather than matching because the redundant parameterisation makes the loss surface worse conditioned and initialisation scale compounds across ten multiplications. The fix is one line: put a non-linearity between the layers. It is worth confirming by counting how much of the ten-layer product varies at all during training.`,isCaseBased:!0},{question:`Can you use a linear activation in the output layer?`,answer:`Yes, and for regression you should — the output needs to take any real value, so squashing it is actively harmful. The collapse argument applies to hidden layers, where the point is to add capacity between transformations; the output layer is chosen to match the target's range. Classification wants sigmoid for a single binary output or softmax over classes, both of which produce probabilities. In practice the activation often lives inside the loss function instead, since combining softmax with cross-entropy in one operation is numerically far more stable than doing them separately.`,isCaseBased:!1},{question:`What is a bias term for, concretely?`,answer:`It moves the decision boundary off the origin. Without a bias, w·x = 0 always passes through the origin, so an entire family of correct boundaries is unreachable. The XOR example shows it doing exactly this: both hidden units compute the identical weighted sum x1 + x2, and only the bias differs — 0 versus −1 — which is what makes one fire when at least one input is on and the other only when both are. Same weights, different biases, two genuinely different features. It also matters for where the ReLU bend sits, since the bias is what positions the kink relative to the data.`,isCaseBased:!1},{question:`How would you explain a neural network to a non-technical stakeholder?`,answer:`I would start from what it does, not what it is: it learns to draw a boundary between examples. A simple model can only draw a straight line, which is fine for easy separations and useless for anything folded — and I would use XOR or a real example from their domain to show a case where no straight line works. A neural network stacks simple steps so the boundary can bend as much as the data requires, and it finds those bends by adjusting itself against examples where the answer is known. Then I would be explicit about the cost: it needs a lot of examples, and it can tell you what but rarely why.`,isCaseBased:!0}],flashcards:[{front:`Perceptron capacity`,back:`One straight line through the input space. Solves AND at 100%, sits at 50% on XOR forever.`},{front:`Why stacking linear layers fails`,back:`A(Bv) = (AB)v exactly — associativity. Two layers gave [2.632, 11.6326, −4.2847] identical to one matrix. Depth without non-linearity adds zero capacity.`},{front:`What an activation does`,back:`Makes W₂·f(W₁x) unfoldable, because f is not a matrix. The single bend in ReLU is enough.`},{front:`XOR by hand`,back:`h1 = ReLU(x1+x2), h2 = ReLU(x1+x2−1), out = h1 − 2h2. Same weights, different biases.`},{front:`What a bias is for`,back:`Moves the boundary off the origin, and positions where the ReLU bend sits. In the XOR net it is the ONLY difference between the two hidden units.`},{front:`Universal approximation`,back:`One hidden layer with enough units approximates any continuous function. Says nothing about how many units or whether SGD can find them.`},{front:`Why deep, not wide`,back:`Feature reuse: the same function needs far fewer parameters. Wide-and-shallow tends to memorise.`},{front:`Output-layer activation`,back:`Linear for regression, sigmoid/softmax for classification — and usually fused into the loss for numerical stability.`}],mindmapMarkdown:`- Perceptron to MLP
  - Perceptron
    - weighted sum + bias, fire if > 0
    - capacity = ONE straight line
    - AND: 100%. XOR: 50% forever
  - Why stacking alone fails
    - A(Bv) = (AB)v, associativity
    - verified identical to 4 decimals
    - 100 layers collapse the same way
  - The fix
    - a non-linearity between layers
    - ReLU = max(0, z), two straight pieces
    - the bend breaks associativity
  - XOR by hand
    - h1 = ReLU(x1+x2), h2 = ReLU(x1+x2-1)
    - out = h1 - 2*h2
    - same weights, DIFFERENT BIASES
  - Universal approximation
    - one hidden layer suffices, in principle
    - silent on unit count (can be exponential)
    - silent on whether SGD finds it
    - depth = feature reuse, fewer parameters`};export{e as default};