var e={id:`cpp-l2-stl`,subjectId:`cpp`,level:2,title:`STL Mastery — the DSA Toolkit`,whyItMatters:`DSA rounds in C++ are open-book — the book is the STL. Interviewers watch which container you grab and whether "amortized O(1)" comes out of you without flinching. This module is every container, its price list, and the three traps (max-heap default, iterator invalidation, multiset erase) that silently fail otherwise-correct solutions.`,estMinutes:70,sections:[{type:`note`,md:"Snippet convention: to stay short, STL snippets here assume `#include <bits/stdc++.h>` and `using namespace std;` — standard competitive-programming shorthand. In production code, include precisely (`<vector>`, `<algorithm>`, …) and skip the using-directive."},{type:`intuition`,title:`Stop building tools. Grab the toolbox.`,md:`A mechanic does not forge a wrench before fixing your car. The STL (Standard Template Library) is C++'s wrench wall, and DSA interviews assume you grab the right one blind.

- Three interlocking parts: **containers** (objects that own elements — vector, map, set…), **iterators** (bookmarks into containers), and **algorithms** (sort, lower_bound…) that run on any container through its iterators.
- Interviewers grade the grab: reaching for \`map\` when \`unordered_map\` was right costs a log factor and a follow-up question.
- Hand-rolling a structure the STL ships is a red flag — unless the interviewer explicitly asks you to build it.`},{type:`intuition`,title:`vector — the apartment that moves`,md:`A vector is a family in an apartment. **size** = people living there; **capacity** = rooms. New kid, no room left? Rent a bigger apartment (usually 2× the rooms), carry every belonging over, abandon the old one.

- Contiguous memory: elements sit side by side. Indexing is O(1) and the CPU cache loves it.
- \`push_back\` with spare capacity: drop the item in the next free room. O(1).
- \`push_back\` at full capacity: allocate bigger, copy ALL n elements, free the old buffer. O(n) — but rare, and that rarity is the amortized story below.
- Everyone who wrote down the old address — pointers, iterators, references — now points at an abandoned building.`},{type:`code`,lang:`cpp`,title:`vector essentials — the 90% container`,code:`vector<int> v;                 // size 0, capacity 0
for (int i = 1; i <= 5; i++)
    v.push_back(i * 10);       // v: 10 20 30 40 50
v.reserve(100);                // capacity >= 100 NOW; size still 5
v[2] = 33;                     // O(1), NO bounds check
v.at(2) = 30;                  // bounds-checked: bad index throws out_of_range
int last = v.back();           // 50
v.pop_back();                  // drop the last: O(1). v: 10 20 30 40`,annotations:{3:`Capacity jumps 1 → 2 → 4 → 8 as pushes overflow it (GCC doubles; MSVC grows 1.5×). Each overflow = new buffer + copy everything.`,4:`Know the final size in advance? reserve once — zero reallocations, zero invalidations during the pushes.`,5:`[] is fast and trusting: v[50] here reads or writes garbage — silently. Use it when the index is provably valid.`,6:`.at() costs a branch, buys a loud exception. Glue code: .at(). Hot loops with proven indices: [].`}},{type:`visual`,component:`PointerBoxDiagram`,props:{title:`push_back at full capacity, frame by frame`,notice:`Watch the saved iterator. It never moves — the vector moves out from under it.`,leftLabel:`vector internals`,rightLabel:`heap buffers`,frames:[{note:`size 2, capacity 2 — the buffer is full. An iterator it was saved earlier and points into it.`,stack:[{name:`vec.data`,to:`buf1`},{name:`it`,to:`buf1`},{name:`size`,value:`2`},{name:`capacity`,value:`2`}],heap:[{id:`buf1`,value:`[10, 20]`,label:`cap 2`}]},{note:`push_back(30): no room. The vector allocates a NEW buffer (capacity 4) and copies both elements into it.`,stack:[{name:`vec.data`,to:`buf2`},{name:`it`,to:`buf1`},{name:`size`,value:`3`},{name:`capacity`,value:`4`}],heap:[{id:`buf1`,value:`[10, 20]`,label:`old cap 2`},{id:`buf2`,value:`[10, 20, 30, _]`,label:`cap 4`}]},{note:`Old buffer freed. it still points there — dereferencing it is undefined behavior. This is ITERATOR INVALIDATION.`,stack:[{name:`vec.data`,to:`buf2`},{name:`it`,to:`buf1`,danger:!0},{name:`size`,value:`3`},{name:`capacity`,value:`4`}],heap:[{id:`buf1`,value:`[10, 20]`,freed:!0},{id:`buf2`,value:`[10, 20, 30, _]`,label:`cap 4`}]}]}},{type:`note`,md:`The amortized argument, interview-ready: capacity doubles on every overflow, so the copies form a geometric series — reaching size n copied at most 1 + 2 + 4 + … + n/2 < n elements in total. n pushes, under 2n units of work → **amortized O(1)** per push. Any *individual* push can still cost O(n). Say both halves out loud.`},{type:`intuition`,title:`string — a container wearing a text costume`,md:"`std::string` is essentially `vector<char>` with text conveniences bolted on. Everything vector taught you transfers.\n\n- **Mutable**, unlike Python: `s[0] = 'F'` edits in place; `s += \"x\"` appends with the same amortized-doubling trick.\n- Text extras: `substr(pos, len)`, `find` (returns `string::npos` when absent), comparisons in dictionary order.\n- A real container means every algorithm applies: `sort`, `reverse`, `unique` — straight on the characters.\n- Name-drop for bonus points: short strings often skip the heap entirely — small-string optimization (SSO)."},{type:`code`,lang:`cpp`,title:`string in five moves`,code:`string s = "faang";
s += " prep";                    // appends in place -- mutable, unlike Python
s[0] = 'F';                      // direct write: "Faang prep"
string cut = s.substr(6, 4);     // "prep" -- substr(pos, len) returns a copy
size_t pos = s.find("an");       // 2 -- string::npos when absent
sort(s.begin(), s.end());        // algorithms just work: string IS a container`,annotations:{2:`Same growth machinery as vector: capacity doubling, so += per character is amortized O(1). No Python-style O(n²) trap.`,5:`find returns size_t. The idiom is: if (pos != string::npos). Testing pos > 0 or pos != -1 by habit is a classic bug.`}},{type:`intuition`,title:`pair — two values, stapled`,md:'A `pair` staples two values into one unit: (distance, node) for Dijkstra, (score, index) for sorting without losing identity.\n\n- Access: `.first` and `.second` — or unpack both at once with **structured bindings**: `auto [d, node] = p` (C++17).\n- The free superpower: pairs compare **lexicographically** — by first, ties broken by second. A `vector<pair>` sorts with no comparator at all.\n- Interview move: "sort by X, then by Y" — build pairs, call sort, done.'},{type:`code`,lang:`cpp`,title:`pair, unpacked`,code:`pair<int, string> p = {7, "amy"};
cout << p.first << ' ' << p.second;    // 7 amy
auto [score, name] = p;                // structured bindings (C++17)

vector<pair<int, int>> pts = {{3, 9}, {1, 5}, {3, 2}};
sort(pts.begin(), pts.end());          // {1,5} {3,2} {3,9}`,annotations:{3:`Two named locals in one line. Cleaner than .first/.second sprinkled through the code — use it on map iteration too.`,6:`Lexicographic order for free: by first, ties by second. This is why "sort by X then Y" answers are usually just pairs + sort.`}},{type:`intuition`,title:`map vs unordered_map — the librarian and the valet`,md:'`map` is a librarian: every book filed in order on the shelves — a **red-black tree**, a self-balancing binary tree. Finding one means walking the catalog: **O(log n)**. But the shelf IS sorted: reading left to right is free ordered output, and "first key after K" is one walk.\n\n- `unordered_map` is a valet with a numbered lot: **hash** the key (crunch it into a slot number), jump straight to the slot. **Average O(1)**.\n- Ask the valet for cars in alphabetical order: blank stare. Hashing scatters keys on purpose — there is no order to give.\n- Hash worst case is **O(n)**: many keys crammed into one slot (bad or adversarial hashing).\n- Pick `map` for sorted iteration, range queries, lower_bound. Pick `unordered_map` for raw lookup speed.\n- On the chart below: `map` lives on the O(log n) curve, `unordered_map` on the flat O(1) line. At a million items that gap is ~20 steps vs ~1.'},{type:`visual`,component:`BigOGrowthChart`,props:{}},{type:`code`,lang:`cpp`,title:`Both maps, side by side`,code:`map<string, int> rank;             // red-black tree: keys always sorted
rank["zed"] = 3;
rank["amy"] = 1;
for (auto& [name, r] : rank)
    cout << name << ' ' << r << ' ';  // amy 1 zed 3 -- sorted, guaranteed

unordered_map<string, int> freq;   // hash table: no order, average O(1)
freq["apple"]++;                   // missing key? [] inserts 0 first, then ++
if (freq.count("kiwi"))            // membership check WITHOUT inserting
    freq["kiwi"]++;`,annotations:{1:`Every insert rebalances the tree: O(log n) per operation, and an in-order walk is free sorted output.`,8:`operator[] on a missing key default-constructs the value and inserts it. Perfect for counting; a landmine inside read-only checks.`,9:`count/find read without inserting. On a const map, operator[] does not even compile — precisely because it might insert.`}},{type:`note`,md:'Custom types as keys — what each side demands:\n\n- `map`/`set` key → provide `operator<` (a strict weak ordering). The tree only ever asks one question: is a before b?\n- `unordered_map`/`unordered_set` key → provide a hash (specialize `std::hash` or pass a functor) AND `operator==`. The table needs "which bucket?" and "same key?".\n- Forget either and you get a page-long template error. Read only its first line — it names the missing piece.'},{type:`intuition`,title:`set and multiset — the ordered bouncers`,md:'A `set` is `map` minus the values: sorted unique keys, O(log n) insert/erase/find. A `multiset` is the same club, but duplicates are welcome.\n\n- Both keep sorted order, so both answer lower_bound/upper_bound — the edge over unordered_set.\n- multiset powers "sorted bag" problems: sliding-window medians, score books with repeats.\n- THE trick: `ms.erase(value)` deletes **every** copy of value. `ms.erase(ms.find(value))` deletes exactly **one**. Sliding windows evict one element at a time — use the second form or the window silently corrupts.'},{type:`code`,lang:`cpp`,title:`The multiset erase trick`,code:`set<int> s = {30, 10, 20, 20};   // stored as {10, 20, 30}: sorted, dup dropped
bool has = s.count(20);          // 1 -- O(log n) membership

multiset<int> a = {4, 7, 7, 7, 9};
a.erase(7);                      // ALL 7s gone -> {4, 9}

multiset<int> b = {4, 7, 7, 7, 9};
b.erase(b.find(7));              // exactly ONE 7 gone -> {4, 7, 7, 9}`,annotations:{5:`erase(value) means "erase every element equal to value". In a sliding window this evicts three when you meant one.`,8:`find returns an iterator to one occurrence; erasing an iterator removes only that node. The multiset interview trick — memorize it.`}},{type:`intuition`,title:`stack and queue — discipline, not structure`,md:"Both are **adapters**: thin wrappers (over deque by default) that expose a discipline and hide everything else.\n\n- `stack` — plates: **LIFO** (last in, first out). push, top, pop. Powers: matched brackets, undo, monotonic-stack patterns.\n- `queue` — a ticket line: **FIFO** (first in, first out). push, front, pop. Powers: BFS.\n- No iteration, no indexing, no begin(). Needing to peek at the middle means you picked the wrong tool — the restriction is the design.\n- Gotcha: `pop()` returns **void**. Read `top()` / `front()` first, then pop."},{type:`code`,lang:`cpp`,title:`stack and queue in eight lines`,code:`stack<char> st;             // LIFO adapter (wraps a deque)
st.push('a');
st.push('b');
char t = st.top();          // 'b' -- last in, first out
st.pop();                   // removes 'b'. Returns VOID

queue<int> q;               // FIFO adapter
q.push(1);
q.push(2);
int f = q.front();          // 1 -- first in, first out
q.pop();                    // removes 1, also returns void`,annotations:{1:`Adapters expose a discipline, not a data structure: the tiny API is the feature. Wrong access pattern = compile error, not a slow program.`,5:`pop() returning void surprises everyone exactly once. The idiom: top() to read, pop() to drop — two calls.`}},{type:`intuition`,title:`priority_queue — the emergency room`,md:`An ER does not treat in arrival order — most urgent first. A \`priority_queue\` always serves the "largest" element, whenever it arrived.

- Under the hood: a **binary heap** stored in a vector. push O(log n), pop O(log n), top O(1).
- **Default is a MAX-heap.** Python's heapq is a min-heap — porting a solution without flipping is the classic silent wrong-answer bug.
- The min-heap incantation: \`priority_queue<int, vector<int>, greater<int>>\`. Memorize the whole line.
- Powers: k-largest/k-smallest, Dijkstra, merge k sorted lists, two-heap running median.`},{type:`code`,lang:`cpp`,title:`Max by default, min by incantation, custom by decltype`,code:`priority_queue<int> mx;                // MAX-heap by default. Yes, max.
mx.push(3); mx.push(7); mx.push(1);
int big = mx.top();                    // 7

priority_queue<int, vector<int>, greater<int>> mn;   // the min-heap trick
mn.push(3); mn.push(7); mn.push(1);
int small = mn.top();                  // 1

auto cmp = [](const pair<int,int>& a, const pair<int,int>& b) {
    return a.first > b.first;          // "a sorts after b" -> smallest .first on top
};
priority_queue<pair<int,int>, vector<pair<int,int>>, decltype(cmp)> pq(cmp);`,annotations:{1:`The single most common C++ interview slip. If your heap answers look inverted, this line is why.`,5:`greater<int> flips every comparison, turning the max-heap into a min-heap. Same O(log n) costs.`,12:`priority_queue takes the comparator TYPE as a template argument — hence decltype(cmp) — plus the instance in the constructor. sort, by contrast, just takes the lambda.`}},{type:`intuition`,title:`deque — the train of cars`,md:`A vector is one long rail coach — more room means building a longer coach and moving every seat. A \`deque\` (double-ended queue) is a train: fixed-size chunks of memory linked by a small index. More room? Couple on another car.

- **push_front AND push_back are O(1)** — vector charges O(n) for anything at the front.
- Indexing stays O(1): two hops — which car, which seat.
- Growth never mass-copies existing elements the way vector reallocation does.
- The price: elements are not contiguous. Iteration is slightly slower, and you cannot hand it to code expecting one flat array.
- Reach for it when both ends are hot: BFS queues (it backs \`std::queue\`), sliding-window maximum (the monotonic deque pattern).`},{type:`intuition`,title:`Iterators — bookmarks with expiry dates`,md:`An iterator is a bookmark into a container. \`begin()\` marks the first element; \`end()\` marks **one past the last** — every range is half-open: [begin, end).

- Half-open is a feature: empty range = begin == end, and size = end − begin. No off-by-ones.
- Bookmarks expire. **Invalidation** = the container changed and the iterator now points at memory that moved or died.
- vector: a reallocating push_back kills EVERY iterator, pointer, and reference. erase kills everything from the erased spot onward.
- map/set: erase kills only the erased element's iterator — tree nodes never move.
- Rule: after any mutation, treat old iterators as dead unless you know the specific rule that saves them.`},{type:`code`,lang:`cpp`,title:`The erase-while-iterating idiom`,code:`vector<int> v = {1, 2, 3, 4};
for (auto it = v.begin(); it != v.end(); ++it)
    cout << *it << ' ';       // end() is one PAST the last -- never dereference it

for (auto it = v.begin(); it != v.end(); ) {
    if (*it % 2 == 0)
        it = v.erase(it);     // erase hands back the next valid iterator
    else
        ++it;
}                             // v is now {1, 3}`,annotations:{3:`Half-open [begin, end): the loop runs exactly size times, and an empty vector never enters it. Dereferencing end() is undefined behavior.`,5:`No ++ in the for header — advancing is decided per branch. Copying the ++ up there skips the element after every erased one.`,7:`After erase, it and everything past it are dead. Re-arming from the return value is the only correct pattern.`}},{type:`intuition`,title:`sort and the two bounds`,md:"- `sort(v.begin(), v.end())` — O(n log n) introsort, on any half-open random-access range.\n- Custom order = pass a lambda. The comparator answers ONE question: should a come before b? Return `true` only for *strictly* before — never for equal elements.\n- `lower_bound(begin, end, x)` — iterator to the first element **>= x**. `upper_bound` — first element **> x**. Binary search: O(log n) on a sorted range.\n- Between them: `upper − lower` = how many copies of x. All equal elements live in [lower, upper).\n- On `set`/`map`, call the MEMBER versions: `s.lower_bound(x)`. The free function compiles there but degrades to O(n)."},{type:`code`,lang:`cpp`,title:`sort with a lambda, bounds on vector and set`,code:`struct Task { int pri; string name; };
vector<Task> ts = {{2, "ship"}, {1, "fix"}, {2, "doc"}};
sort(ts.begin(), ts.end(), [](const Task& x, const Task& y) {
    return x.pri < y.pri;     // true = x strictly before y. NEVER <=
});

vector<int> v = {10, 20, 20, 30};
auto lo = lower_bound(v.begin(), v.end(), 20);   // first element >= 20
auto hi = upper_bound(v.begin(), v.end(), 20);   // first element >  20
int cnt = hi - lo;                               // 2 -- how many 20s
int idx = lo - v.begin();                        // 1 -- iterator -> index

set<int> st = {10, 20, 30};
auto it = st.lower_bound(25);                    // *it == 30, tree walk: O(log n)`,annotations:{4:`Comparator contract: strict weak ordering. Returning true for equal elements (<=) is undefined behavior — real crashes in real interviews, not a style nit.`,8:`lower_bound/upper_bound demand a SORTED range. On unsorted data they return confident garbage.`,14:`lower_bound(st.begin(), st.end(), 25) compiles but steps node by node: O(n). Tree containers ship member versions that walk the tree — always use those.`}},{type:`note`,md:"The cost sheet — recite it before choosing:\n\n- `vector` — push_back amortized O(1) · index O(1) · insert/erase in the middle O(n) · unsorted find O(n)\n- `deque` — push/pop at BOTH ends O(1) · index O(1)\n- `map` / `set` / `multiset` — insert, erase, find all O(log n) · iteration comes out sorted\n- `unordered_map` / `unordered_set` — insert, find average O(1), worst O(n) · no order\n- `priority_queue` — push, pop O(log n) · top O(1)\n- `stack` / `queue` — push, pop, top/front all O(1)\n- `sort` O(n log n) · `lower_bound`/`upper_bound` on a sorted random-access range O(log n)"},{type:`note`,md:`Coming attraction: the plan's "implement vector from scratch" practical lives in the move-semantics module, where you build **MiniVec**. This module's reallocation story becomes code YOU write — allocate, move, free — plus the Rule of 5 that keeps it from double-freeing. Everything here about capacity and invalidation is the spec for that build.`}],quiz:[{question:`v has size 4, capacity 4. You call v.push_back(9). What happens to an iterator saved before the call?`,options:[{text:`Still valid — push_back only adds at the end`,explanation:`Adding at the end IS the trigger: capacity was full, so a new buffer was allocated and every element moved.`},{text:`Invalidated — the vector reallocated and every element moved to a new buffer`,explanation:`Correct. Full capacity forces reallocation: all iterators, pointers, and references into the old buffer are dead.`},{text:`Valid until you erase something`,explanation:`erase is a different invalidation rule. Reallocation on push_back already killed it.`},{text:`Only end() changes`,explanation:`With spare capacity that intuition is close — but capacity was FULL, so everything moved.`}],correct:1},{question:`Why is push_back "amortized O(1)" rather than plain O(1)?`,options:[{text:`Because it is secretly O(log n)`,explanation:`No logarithm involved — the occasional cost is linear, not logarithmic.`},{text:`Marketing — it is just O(1)`,explanation:`A reallocating push copies all n elements: genuinely O(n) for that one call.`},{text:`Occasional pushes cost O(n) (realloc + copy), but doubling makes n pushes cost O(n) total — O(1) averaged`,explanation:`Correct. Copies form a geometric series: 1 + 2 + 4 + … + n/2 < n. Rare expensive pushes, cheap on average.`}],correct:2},{question:`You must print all user scores in alphabetical username order, after many inserts. Which container?`,options:[{text:`map<string, int> — the tree keeps keys sorted; iterating IS the sorted output`,explanation:`Correct. O(log n) per insert, and in-order iteration is free sorted output every time.`},{text:`unordered_map — lookups are faster`,explanation:`Faster lookups, but no order: every print would need copying keys out and sorting — O(n log n) each time.`},{text:`vector<pair<string, int>> kept unsorted`,explanation:`Insert is cheap but each print needs a full sort, and lookups are O(n). Wrong tool for repeated ordered output.`}],correct:0},{question:`priority_queue<int> pq; pq.push(3); pq.push(7); pq.push(1); — what does pq.top() return?`,options:[{text:`1`,explanation:`That is min-heap thinking — Python heapq habits. C++ needs greater<int> to behave that way.`},{text:`3`,explanation:`Insertion order is irrelevant — a heap reorders on every push.`},{text:`7`,explanation:`Correct. C++ priority_queue is a MAX-heap by default: largest on top.`}],correct:2},{question:`multiset<int> ms = {5, 5, 9}; ms.erase(5); — what is ms.size()?`,options:[{text:`1 — erase(value) removes every element equal to the value`,explanation:`Correct. Both 5s vanish, leaving {9}. To remove one copy: ms.erase(ms.find(5)).`},{text:`2 — it removes a single 5`,explanation:`That is what erase(ms.find(5)) does. The by-value overload erases ALL equal elements.`},{text:`3 — erase needs an iterator, so nothing happens`,explanation:`The by-value overload exists and works — too well: it takes every copy.`}],correct:0},{question:`On a set<int> s, why call s.lower_bound(x) instead of lower_bound(s.begin(), s.end(), x)?`,options:[{text:`The free function does not compile on sets`,explanation:`It compiles fine — which is exactly why this trap catches people.`},{text:`They are identical in cost`,explanation:`Same result, wildly different cost. Only one walks the tree.`},{text:`The member descends the tree in O(log n); the free function must step through bidirectional iterators — O(n)`,explanation:`Correct. Binary search needs random-access jumps; set iterators can only step one node at a time.`}],correct:2},{question:`unordered_map<string,int> m is empty. You run: if (m["k"] == 7) { ... }. What is m.size() afterwards?`,options:[{text:`0 — a comparison does not modify the map`,explanation:`operator[] is not a plain read: on a missing key it inserts before handing you anything to compare.`},{text:`1 — operator[] inserted "k" with value 0 before the comparison ran`,explanation:`Correct. The check is false AND the map grew. Use m.count("k") or m.find("k") for read-only checks.`},{text:`It throws because "k" is missing`,explanation:`That is .at() behavior. operator[] never throws for missing keys — it inserts.`}],correct:1},{question:`You will push_back exactly 1,000,000 ints. Best single line before the loop?`,options:[{text:`v.resize(1000000);`,explanation:`resize sets SIZE: a million zeros exist now, and your push_backs append AFTER them — 2 million elements. Classic bug.`},{text:`v.reserve(1000000);`,explanation:`Correct. Capacity rises, size stays 0: one allocation, zero reallocations, zero invalidations during the loop.`},{text:`Nothing — push_back is O(1) anyway`,explanation:`Amortized O(1) still means ~20 reallocations copying ~2 million elements in total — and every saved iterator dies along the way.`}],correct:1}],interviewQuestions:[{question:`Walk me through exactly what happens when push_back is called on a vector whose size equals its capacity.`,answer:`Four steps: (1) allocate a new, larger buffer — typically 2× capacity (GCC) or 1.5× (MSVC); (2) move the existing n elements across — copy if the element type's move constructor is not noexcept, since the vector must keep its strong exception guarantee; (3) construct the new element at the end; (4) destroy the old elements and free the old buffer. Consequences to name unprompted: this one call is O(n), and every iterator, pointer, and reference into the vector is invalidated. Then land the punchline: doubling makes the O(n) case geometrically rare, so push_back is amortized O(1).`,isCaseBased:!1},{question:`Prove push_back is amortized O(1). Why does capacity double instead of growing by a fixed 10 slots?`,answer:`With doubling, reaching size n means copies happened at sizes 1, 2, 4, …, n/2 — a geometric series summing to under n. Total work for n pushes < 2n → O(1) average per push. With +10 growth, reallocation happens every 10 pushes and each copies everything so far: 10 + 20 + 30 + … ≈ n²/20 → amortized O(n) per push. The principle: growth must be multiplicative for the series to telescope. Tradeoff worth naming: doubling can waste up to half the allocated memory — some implementations pick 1.5× to trade a few extra copies for tighter memory.`,isCaseBased:!1},{question:`Case: a teammate stores int* p = &v[0], then push_backs in a loop, then dereferences p. It crashes — but only sometimes, and only in production. Diagnose.`,answer:`Textbook iterator/pointer invalidation. When a push_back exceeds capacity, the vector reallocates and the old buffer is freed — p dangles. "Sometimes" is the fingerprint: if the vector happened to have spare capacity, no reallocation occurred and p stayed valid; different input sizes in production crossed a capacity boundary that tests never hit. Fixes, in preference order: store an index instead of a pointer (indices survive reallocation); reserve() the final size before the loop so no reallocation happens; or take the pointer only after all growth is done. Close with the general rule: never hold pointers/iterators/references across a vector mutation.`,isCaseBased:!0},{question:`map vs unordered_map — give the full tradeoff, and a case where the "slower" map is the right call.`,answer:`map: red-black tree — O(log n) insert/erase/find, keys always sorted, so free ordered iteration, lower_bound/upper_bound, and range queries; iterators stay valid except the erased one. unordered_map: hash table — average O(1), worst O(n) on collisions, no order, and rehashing invalidates iterators. Memory: the hash table generally spends more on buckets. map wins whenever order is part of the job: leaderboards printed in order, "first key after K", range sums — and when worst-case latency matters, since O(log n) is a hard bound while a hash table can degrade (hash-flooding on adversarial keys). unordered_map wins pure hot-path lookups: caches, frequency counts, seen-sets.`,isCaseBased:!1},{question:`Case: design the containers for a live leaderboard — update any player's score, show the top 10, and report a player's current rank.`,answer:`Two structures, kept in sync. (1) unordered_map<playerId, score> for O(1) score lookup on update. (2) An ordered structure on scores for rank queries: a multiset<score> (duplicates — ties — must survive, which is exactly why set is wrong) or a map<score, count>. Update = erase old score with ms.erase(ms.find(old)) — the erase-ONE trick, since erase(value) would wipe every tied player — then insert the new score. Top 10 = iterate from the ordered end. Exact rank = distance from the end, O(n) worst case in a multiset — say that honestly, then name the upgrade: an order-statistics tree (policy tree / Fenwick over score buckets) makes rank O(log n). A priority_queue alone fails here: no random erase, no rank queries.`,isCaseBased:!0},{question:`You want a custom struct as a key. What must you provide for map, and what for unordered_map — and why does each need what it needs?`,answer:`map needs operator< (or a comparator type) forming a strict weak ordering — the red-black tree navigates purely by "is a before b?", and even equality is derived as !(a<b) && !(b<a). unordered_map needs two things: a hash function (specialize std::hash or pass a functor as a template argument) to pick the bucket, and operator== to confirm the match inside the bucket — hashes collide, so equality is the tiebreaker. Quality point: the hash should mix all fields that participate in equality; hashing only one field of a two-field key is legal but silently degrades toward O(n) buckets.`,isCaseBased:!1},{question:`Why does priority_queue<int, vector<int>, greater<int>> produce a MIN-heap? Explain what the comparator actually means.`,answer:`The comparator defines the ordering, and top() is the element that sorts LAST in that ordering. Default less<int> puts small elements first, so the largest is last → top → max-heap. greater<int> reverses the order: large first, smallest last → top → min-heap. Same logic for custom comparators: return a.first > b.first and the smallest .first surfaces on top. The mnemonic worth saying: the priority_queue comparator feels inverted versus sort — sort's comparator says who comes first in output, pq's effectively says who is buried deepest.`,isCaseBased:!1},{question:`When does deque beat vector, and what does deque give up?`,answer:`deque wins when both ends are hot: push_front and push_back are both O(1) (vector's front insert is O(n)), and growth never mass-relocates existing elements, so there is no vector-style reallocation spike. Classic uses: the backbone of std::queue for BFS, and the monotonic deque in sliding-window maximum. It gives up contiguity: elements live in linked fixed-size chunks, so iteration is slightly slower cache-wise, and you cannot pass its storage to APIs expecting one flat array. Default rule: vector unless the front is genuinely hot.`,isCaseBased:!1},{question:`Show the correct way to erase elements from a vector while looping over it, and explain why the naive loop is broken.`,answer:`The idiom: for (auto it = v.begin(); it != v.end(); ) { if (bad) it = v.erase(it); else ++it; }. Two load-bearing details: erase invalidates the erased iterator and everything after it, but returns a fresh iterator to the next element — so you re-arm from the return value; and ++it must live only on the else branch, otherwise you skip the element that slid into the erased slot. The naive for(...; ++it) with erase inside either skips elements or hits undefined behavior. Bonus: for bulk removal, name the erase-remove idiom — v.erase(remove_if(...), v.end()) — one O(n) pass instead of repeated O(n) erases.`,isCaseBased:!1},{question:`v[i] vs v.at(i) — and what surprising thing does operator[] do on a map?`,answer:`On vector: [] is unchecked — out-of-range is undefined behavior (garbage read or crash, no diagnosis), while .at() bounds-checks and throws std::out_of_range. Tradeoff: [] in hot loops where the index is provably valid; .at() at trust boundaries and glue code. On map/unordered_map, operator[] is a different beast entirely: a missing key is default-constructed and INSERTED, then the reference returned. That makes counting one-liners (freq[x]++) elegant, and read-only code subtly wrong — a lookup that mutates. It is also why const maps have no operator[]: use .at(), .find(), or .count() to read.`,isCaseBased:!1},{question:`A candidate writes sort(v.begin(), v.end(), [](int a, int b){ return a <= b; }) and it crashes on large inputs. Why?`,answer:`sort requires a strict weak ordering, and <= violates it: for equal elements it returns true both ways, so cmp(a,b) and cmp(b,a) are both true — the algorithm can no longer tell "before" from "equal". std::sort exploits the contract for speed (e.g., running pointers past pivots without bounds checks), so a broken comparator walks iterators off the buffer: undefined behavior, typically a segfault that only shows on inputs large enough to hit the affected code paths. Fix: strictly less-than, always. Same contract governs comparators for map, set, lower_bound, and priority_queue.`,isCaseBased:!1}],flashcards:[{front:`push_back on a full vector — the four steps`,back:`Allocate bigger buffer (≈2×) → move/copy all n elements → construct the new one → free the old buffer. O(n) for that call; every iterator/pointer/reference invalidated.`},{front:`Why push_back is amortized O(1)`,back:`Doubling makes copies a geometric series: 1+2+4+…+n/2 < n total. n pushes < 2n work → O(1) average. Individual push can still be O(n).`},{front:`map vs unordered_map, one breath`,back:`map: red-black tree, O(log n), keys sorted → range queries, ordered iteration. unordered_map: hash, average O(1), worst O(n), no order.`},{front:`Custom key requirements`,back:`map/set: operator< (strict weak ordering). unordered_map/set: hash function + operator==.`},{front:`vector iterator invalidation rules`,back:`Reallocating push_back: ALL iterators/pointers/references die. erase: everything from the erased position onward dies — re-arm with it = v.erase(it).`},{front:`lower_bound vs upper_bound`,back:`lower_bound: first element >= x. upper_bound: first element > x. upper − lower = count of x. Sorted range required. On set/map: use the MEMBER versions.`},{front:`Erase ONE from a multiset`,back:`ms.erase(value) removes ALL equal elements. ms.erase(ms.find(value)) removes exactly one. Sliding windows need the second.`},{front:`The min-heap incantation`,back:`priority_queue<int, vector<int>, greater<int>>. Default priority_queue is a MAX-heap (Python heapq is min — do not port blind).`},{front:`sort comparator contract`,back:`Return true iff a goes strictly before b — a strict weak ordering. Using <= is undefined behavior (real crashes). Lambda for sort; type + instance (decltype) for priority_queue.`},{front:`v[i] vs v.at(i) · map operator[]`,back:`vector: [] unchecked (UB out of range), .at() throws. map/unordered_map: [] INSERTS a default value on a missing key — use count/find/.at() for read-only.`}],mindmapMarkdown:`- STL Mastery — the DSA Toolkit
  - vector
    - push_back: amortized O(1) via doubling
    - realloc invalidates ALL iterators
    - reserve · [] unchecked vs at()
  - string — a real container, mutable
  - pair — sorts by first, then second
  - map vs unordered_map
    - tree O(log n) sorted vs hash avg O(1), worst O(n)
    - custom key: operator< vs hash + ==
  - set / multiset
    - erase(find(x)) removes exactly ONE
  - adapters: stack LIFO · queue FIFO · pop() returns void
  - priority_queue
    - MAX-heap by default
    - greater<int> → min-heap
  - deque — O(1) at both ends
  - iterators
    - [begin, end) half-open
    - it = v.erase(it)
  - algorithms
    - sort + lambda: strict <, never <=
    - lower_bound: first >= x
    - member lower_bound on set/map
  - MiniVec → built in the move-semantics module`};export{e as default};