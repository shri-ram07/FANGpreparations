var e={id:`metrics-l1-metric-learning-losses`,subjectId:`metrics`,level:1,title:`Contrastive and Triplet Loss`,whyItMatters:`Face recognition and semantic search cannot use a fixed set of classes — new faces and new documents appear constantly. These losses learn a distance instead, and the same idea powers every embedding model you will meet.`,assumes:[`You know Euclidean distance between two points`,`You have seen a Python list and a for loop`],estMinutes:20,sections:[{type:`intuition`,title:`What metric learning is for`,md:`A classifier needs a fixed set of classes. Face recognition cannot have one — new people keep arriving, and you cannot retrain for each.

**Metric learning** sidesteps it. Instead of learning "which class is this?", learn an **embedding** — a vector per item — such that **similar things land close together and different things land far apart**.

Then recognition is just a distance check against stored embeddings, and a new face needs no retraining at all.

Two losses do this. Both need a **margin**: how much separation is enough.`},{type:`math`,intro:`Both losses, and note the difference in what they take. Contrastive works on a PAIR with a label saying whether they match. Triplet works on three items at once — an anchor, a positive of the same identity, and a negative of another — and only the gap between the two distances matters.`,latex:[`L_{\\text{contrastive}} = y\\,d^2 \\;+\\; (1-y)\\,\\max(0,\\; m - d)^2`,`L_{\\text{triplet}} = \\max\\bigl(0,\\; d(a,p) - d(a,n) + m\\bigr)`]},{type:`code`,lang:`python`,title:`Both halves of contrastive loss, across five distances`,code:`margin = 1.5

def contrastive(d, same):
    if same:
        return d ** 2
    return max(0.0, margin - d) ** 2

print('   d   same-pair   different-pair')
for d in [0.0, 0.5, 1.0, 1.5, 3.0]:
    print('%4.1f %11.3f %16.3f' % (d, contrastive(d, True), contrastive(d, False)))

# ---- real output ----
#    d   same-pair   different-pair
#  0.0       0.000            2.250
#  0.5       0.250            1.000
#  1.0       1.000            0.250
#  1.5       2.250            0.000
#  3.0       9.000            0.000`,annotations:{4:`For a matching pair the loss is simply the squared distance, so it pulls them together and never stops pulling — there is no distance small enough to satisfy it except zero.`,6:`For a non-matching pair, the hinge. Once d reaches the margin the loss is exactly 0 and that pair stops contributing.`,14:`Read the two columns as opposing forces. At d = 0 a matching pair is free and a non-matching pair pays the maximum 2.250.`,17:`At d = 1.5 the roles have swapped: the non-matching pair is satisfied and contributes nothing, while the matching pair now pays 2.250. Beyond the margin the different-pair column stays at 0 forever — pushing them further apart buys nothing.`}},{type:`code`,lang:`python`,title:`Triplet loss cares about the gap, not the distances`,code:`margin = 1.5

def triplet(d_ap, d_an):
    return max(0.0, d_ap - d_an + margin)

print('near positive, far negative :', triplet(0.5, 3.0))
print('near positive, near negative:', triplet(0.5, 1.0))
print('both far, gap still 2.5     :', triplet(20.0, 22.5))
print('both far, gap only 1.0      :', triplet(20.0, 21.0))

# ---- real output ----
# near positive, far negative : 0.0
# near positive, near negative: 1.0
# both far, gap still 2.5     : 0.0
# both far, gap only 1.0      : 0.5`,annotations:{4:`Only the DIFFERENCE d(a,p) − d(a,n) appears. The absolute distances never enter the formula.`,11:`Rows 1 and 3 both score 0.0 — one has distances of 0.5 and 3.0, the other 20.0 and 22.5. Wildly different scales, identical loss, because both gaps exceed the margin.`,13:`That is the crucial difference from contrastive loss: triplet imposes no opinion about how far apart things should be in absolute terms, only that the right one is closer by at least the margin.`}},{type:`note`,label:`The consequence: hard mining is not optional`,md:`Both losses go **exactly zero** once a pair or triplet is comfortably right. That is the design — stop pushing on what is already settled.

But it means most randomly drawn triplets teach the model **nothing at all**, and a batch of zeros produces a gradient of zero. Training stalls not because the model is good but because you sampled badly.

So **hard mining** — deliberately selecting triplets the model currently gets wrong — is a structural requirement, not an optimisation. The next snippet measures how bad the problem is.`},{type:`code`,lang:`python`,title:`How many random triplets teach nothing?`,code:`import random

random.seed(0)
margin = 0.2
zero = 0
for _ in range(10000):
    d_ap = random.uniform(0.0, 1.4)
    d_an = random.uniform(0.6, 2.0)
    if max(0.0, d_ap - d_an + margin) == 0.0:
        zero = zero + 1
print('triplets with exactly zero loss: %d out of 10000' % zero)
print('fraction that teach nothing    : %.2f' % (zero / 10000))

# ---- real output ----
# triplets with exactly zero loss: 7386 out of 10000
# fraction that teach nothing    : 0.74`,annotations:{7:`A partly-trained model: positives usually closer than negatives, but the ranges overlap. This is a realistic mid-training state, not a pathological one.`,13:`7,386 of 10,000 — nearly three quarters of randomly drawn triplets produce exactly zero loss and zero gradient.`,14:`So roughly 74% of the compute in a naive training step is spent on triplets that cannot teach anything. That is what mining exists to recover.`}},{type:`note`,label:`Three mining strategies`,md:`- **Hardest negative** — the closest wrong item in the batch. Strongest signal, and the least stable: a single mislabelled example becomes the hardest negative for everything and drags training into collapse.
- **Semi-hard** — a negative further than the positive but still inside the margin. The FaceNet choice, and the usual right answer: real signal without the instability.
- **Batch-all** — average over every non-zero triplet in the batch. Cheap to implement and dilutes the signal with easy triplets.

The practical shape is **batch-hard within a large batch**: sample P identities × K images, then mine inside that batch, which is why embedding models train with unusually large batches.`},{type:`note`,label:`Contrastive or triplet, and what came after`,md:`**Contrastive** is simpler and needs only a pair, but its absolute distance target is a strong assumption — it insists matching pairs go to distance 0 and non-matching to the margin, whatever the geometry wants.

**Triplet** only constrains the ordering, which is a weaker and usually more realistic requirement — but it needs mining and is fiddlier to train.

Both were largely superseded by **InfoNCE**, which compares one positive against many negatives at once and so gets a useful gradient from a batch without explicit mining. Same idea; better-conditioned objective.`}],quiz:[{question:`Why can face recognition not be treated as ordinary classification?`,options:[{text:`Faces are too high-dimensional`,explanation:`Dimensionality is handled fine by a CNN.`},{text:`The set of classes is open — new people keep appearing, and you cannot retrain per person`,explanation:`Correct. Metric learning replaces "which class" with "how far", so a new identity needs only a stored embedding.`},{text:`There is not enough labelled data`,explanation:`There are large face datasets; the problem is structural.`},{text:`Classification cannot handle images`,explanation:`It handles them very well when the classes are fixed.`}],correct:1},{question:`In contrastive loss, what happens to a non-matching pair once d exceeds the margin?`,options:[{text:`The loss keeps decreasing`,explanation:`It is exactly 0 and cannot decrease further.`},{text:`The loss is exactly 0, so pushing them further apart buys nothing`,explanation:`Correct — at margin 1.5, d = 1.5 and d = 3.0 both give 0.000.`},{text:`The loss becomes negative`,explanation:`max(0, ·) prevents that.`},{text:`It switches to the matching-pair formula`,explanation:`The label decides which branch applies, not the distance.`}],correct:1},{question:`Triplet loss gave 0.0 for distances (0.5, 3.0) and also for (20.0, 22.5). Why?`,options:[{text:`Both were clipped by max(0, ·) after rounding`,explanation:`Both are genuinely at or below zero before clipping — that is the point.`},{text:`Only the gap d(a,p) − d(a,n) appears, and both gaps exceed the margin`,explanation:`Correct. Absolute distances never enter, so 0.5-vs-3.0 and 20-vs-22.5 are equivalent to the loss.`},{text:`The second triplet is invalid`,explanation:`It is perfectly valid; the distances are just larger.`},{text:`Because the margin is 1.5 in both cases`,explanation:`True, but the reason is that the loss is a function of the gap alone.`}],correct:1},{question:`7,386 of 10,000 random triplets gave exactly zero loss. What follows?`,options:[{text:`The model has converged`,explanation:`The distances were drawn from overlapping ranges, i.e. a partly-trained model.`},{text:`Roughly 74% of a naive training step does no work, so hard mining is structural rather than an optimisation`,explanation:`Correct. A batch of zeros gives a zero gradient and training stalls for sampling reasons, not model reasons.`},{text:`The margin should be reduced`,explanation:`A smaller margin makes MORE triplets satisfied and the problem worse.`},{text:`The random seed was unlucky`,explanation:`The proportion is a property of the overlapping distributions.`}],correct:1},{question:`What is the risk of always mining the hardest negative?`,options:[{text:`It is too slow`,explanation:`Within a batch it is cheap.`},{text:`A single mislabelled example becomes the hardest negative for everything and can collapse training`,explanation:`Correct, which is why semi-hard mining is the usual default.`},{text:`It produces zero gradients`,explanation:`It produces the largest gradients — that is the appeal and the danger.`},{text:`It requires a larger margin`,explanation:`The margin is independent of the mining strategy.`}],correct:1},{question:`What does triplet loss assume that contrastive loss does not?`,options:[{text:`Nothing — triplet assumes more`,explanation:`It assumes strictly less: only an ordering.`},{text:`Less. Triplet constrains only the ordering; contrastive insists on absolute target distances`,explanation:`Correct. Contrastive wants matching pairs at 0 and non-matching at the margin, whatever the geometry wants.`},{text:`That embeddings are normalised`,explanation:`Normalisation is a common choice for both, not an assumption of either.`},{text:`That classes are fixed`,explanation:`Neither assumes a fixed class set — that is the whole point.`}],correct:1}],interviewQuestions:[{question:`Why use metric learning instead of a classifier?`,answer:`Because the class set is open. Face recognition, speaker identification and semantic search all keep acquiring new identities or documents, and a softmax over a fixed set cannot absorb one without retraining. Metric learning produces an embedding where distance means similarity, so adding a new identity is storing one vector. It also handles the one-shot case — recognising someone from a single reference image — which classification fundamentally cannot.`,isCaseBased:!1},{question:`Contrastive or triplet — what is the real difference?`,answer:`What they constrain. Contrastive takes a pair and imposes absolute targets: matching pairs toward distance 0, non-matching out to the margin. Triplet takes three items and constrains only the ordering — the positive must be closer than the negative by at least the margin — so absolute distances never enter. That showed up directly in the numbers: triplet gave 0.0 for distances of (0.5, 3.0) and also for (20.0, 22.5), because both gaps clear the margin. The weaker constraint is usually more realistic, at the cost of needing mining.`,isCaseBased:!0},{question:`Why is hard negative mining necessary rather than merely helpful?`,answer:`Because the loss is exactly zero for a satisfied triplet, and a batch of zeros produces a zero gradient. On a realistically partly-trained model, 7,386 of 10,000 randomly drawn triplets gave exactly zero loss — so about three quarters of the compute does nothing and training appears to stall for reasons unrelated to the model. Mining is what recovers that. The standard approach is batch-hard within a large P×K batch, which is why embedding models train with unusually large batch sizes.`,isCaseBased:!0},{question:`How do you choose the margin?`,answer:`It interacts with whether embeddings are normalised. If they are L2-normalised onto the unit sphere, distances are bounded by 2 and a margin around 0.2–0.5 is typical; unnormalised, the scale is arbitrary and the margin has to be tuned alongside it. Too small and almost every triplet is satisfied, so the gradient vanishes; too large and nothing is ever satisfied, so the model chases an unreachable separation. Watching the fraction of non-zero triplets during training is the practical diagnostic.`,isCaseBased:!1},{question:`What replaced these losses, and why?`,answer:`InfoNCE, and the contrastive-learning family built on it. Instead of one positive and one negative, it compares one positive against many negatives simultaneously via a softmax over similarities, so a large batch supplies a useful gradient without explicit mining — the easy negatives contribute little automatically. It also gives a cleaner probabilistic interpretation. The same shift shows up in supervised face recognition as ArcFace and CosFace, which put an angular margin into a softmax rather than working with triplets at all.`,isCaseBased:!1},{question:`Your triplet model collapses — every embedding converges to the same point. Diagnose it.`,answer:`Collapse means the model found the degenerate solution where all distances are zero, which satisfies the positive term and leaves only a constant. Usual causes: hardest-negative mining amplifying a mislabelled example until it dominates, a learning rate high enough to overshoot into the degenerate basin early, or a margin so large that no arrangement satisfies it so the model gives up. Fixes are semi-hard rather than hardest mining, normalising embeddings so distances are bounded, warming up the learning rate, and monitoring the fraction of active triplets — collapse shows up there before it shows up in the loss.`,isCaseBased:!0},{question:`How would you evaluate an embedding model?`,answer:`Not by the loss, which is uninterpretable and goes to zero as triplets are satisfied. For verification, ROC or the equal error rate over pairwise distances — at what threshold does a same-identity pair get separated from a different-identity pair. For identification, recall@k against a gallery, which is what retrieval actually does. And I would check the distance distributions directly: the overlap between same-pair and different-pair histograms is the picture that tells you whether the embedding works, and where the threshold should sit.`,isCaseBased:!1},{question:`Why do embedding models train with such large batches?`,answer:`Because the negatives come from within the batch. A larger batch means a larger pool to mine hard or semi-hard negatives from, and with InfoNCE it directly means more negatives per positive in the denominator, which is what makes the task hard enough to teach anything. With a small batch, most in-batch negatives are trivially easy and the effective signal collapses. It is why contrastive pretraining runs at batch sizes in the thousands, and why memory banks and momentum encoders exist — they simulate a large batch without paying for one.`,isCaseBased:!1}],flashcards:[{front:`Why metric learning`,back:`The class set is open. Learn an embedding where distance means similarity, so a new identity is one stored vector and no retraining.`},{front:`Contrastive loss`,back:`y·d² + (1−y)·max(0, m − d)². Matching pairs pulled to 0; non-matching pushed to the margin and then ignored.`},{front:`Triplet loss`,back:`max(0, d(a,p) − d(a,n) + m). Only the GAP matters — (0.5, 3.0) and (20.0, 22.5) both score 0.0.`},{front:`The contrastive table`,back:`At margin 1.5: d = 0 gives same 0.000 / different 2.250; d = 1.5 gives 2.250 / 0.000. Opposing forces that swap at the margin.`},{front:`Why mining is structural`,back:`7,386 of 10,000 random triplets give exactly zero loss on a partly-trained model — 74% of the compute teaches nothing.`},{front:`The three mining strategies`,back:`Hardest negative (strongest, unstable — one bad label collapses it), semi-hard (FaceNet, the usual answer), batch-all (cheap, diluted).`},{front:`Contrastive vs triplet, precisely`,back:`Contrastive imposes ABSOLUTE target distances. Triplet constrains only the ORDERING — a weaker and usually more realistic requirement.`},{front:`What superseded both`,back:`InfoNCE: one positive against many negatives at once, so a large batch gives a useful gradient without explicit mining.`}],mindmapMarkdown:`- Contrastive & triplet loss
  - Why
    - open class set: new faces keep arriving
    - learn a DISTANCE, not a class
    - new identity = one stored vector
  - Contrastive (a pair)
    - y d^2 + (1-y) max(0, m-d)^2
    - margin 1.5: d=0 -> 0.000 / 2.250
    - d=1.5 -> 2.250 / 0.000
    - imposes ABSOLUTE target distances
  - Triplet (anchor, positive, negative)
    - max(0, d_ap - d_an + m)
    - only the GAP matters
    - (0.5, 3.0) and (20.0, 22.5) both -> 0.0
    - constrains ORDERING only
  - Mining is structural
    - loss is exactly 0 once satisfied
    - 7386/10000 random triplets teach nothing (74%)
    - hardest (unstable), semi-hard (FaceNet), batch-all
    - batch-hard within a large P x K batch
  - After
    - InfoNCE: one positive vs many negatives
    - no explicit mining needed`};export{e as default};