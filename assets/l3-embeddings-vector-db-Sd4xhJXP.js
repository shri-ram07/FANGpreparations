var e={id:`genai-l3-embeddings-vector-db`,subjectId:`genai`,level:3,title:`Vector Search at Scale`,whyItMatters:`Comparing a query against a million 768-dimensional vectors is 65 queries per second on one core. Every vector database exists to break that, and every one of them does it by giving up exactness in a way you should be able to quantify.`,assumes:[`You have read *Embeddings*, so you know what cosine similarity is`,`You know what big-O notation means`],estMinutes:20,sections:[{type:`intuition`,title:`The exact answer does not scale`,md:`Semantic search is: embed the query, compare it against every stored vector, return the closest. The comparison is a dot product, and a dot product is cheap.

The problem is that there are a lot of them. **Exact search is O(n·d)** per query — a million documents at 768 dimensions is 768 million multiply-accumulates, every time anyone searches.

So every vector database trades exactness for speed: **approximate nearest neighbour** search returns *probably* the closest vectors, in a fraction of the time. The number that matters is **recall@k** — what fraction of the true top-k the index actually returned — and it is a dial rather than a fixed property.`},{type:`code`,lang:`python`,title:`Where brute force stops working`,code:`d = 768
for n in [1e3, 1e5, 1e6, 1e8]:
    ops = n * d
    ms = ops / 50e9 * 1000                     # ~50 GFLOP/s, one core
    print('n=%12s  %.2e ops  ~%8.2f ms/query  ~%9.1f queries/sec'
          % (f'{int(n):,}', ops, ms, 1000/ms))

for n in [1e6, 1e8]:
    print('n=%12s  fp32 %6.1f GB   int8 %5.1f GB   PQ at 96 bytes %5.1f GB'
          % (f'{int(n):,}', n*d*4/1e9, n*d/1e9, n*96/1e9))

# ---- real output ----
# n=       1,000  7.68e+05 ops  ~    0.02 ms/query  ~  65104.2 queries/sec
# n=     100,000  7.68e+07 ops  ~    1.54 ms/query  ~    651.0 queries/sec
# n=   1,000,000  7.68e+08 ops  ~   15.36 ms/query  ~     65.1 queries/sec
# n= 100,000,000  7.68e+10 ops  ~ 1536.00 ms/query  ~      0.7 queries/sec
# n=   1,000,000  fp32    3.1 GB   int8   0.8 GB   PQ at 96 bytes   0.1 GB
# n= 100,000,000  fp32  307.2 GB   int8  76.8 GB   PQ at 96 bytes   9.6 GB`,annotations:{7:`At 1,000 documents brute force runs 65,000 queries a second. Do not build an index — you do not have a problem yet, and an exact answer is worth more than a fast one.`,9:`At a million it is 65 queries a second on one core. Parallelism buys a factor of tens, not thousands, so this is where the index becomes necessary.`,10:`At 100 million, 1.5 seconds per query. And 307 GB just to hold the vectors in fp32 — the memory becomes the binding constraint before the compute does.`,13:`Which is why quantisation appears here too. Product quantisation compresses each 768-dimensional vector to 96 bytes, a 32x reduction, at a real but usually acceptable recall cost.`}},{type:`note`,label:`The three index families`,md:`**Flat** — no index, compare against everything. Exact, and the right answer below roughly 100,000 vectors. It is also the baseline every other option must be measured against.

**IVF (inverted file)** — cluster the vectors, and at query time search only the few clusters nearest the query. Fast to build, memory-light, and it fails on boundary cases: a query sitting between two clusters may miss neighbours in the one it did not probe. \`nprobe\` is the dial — more clusters searched, higher recall, slower.

**HNSW (hierarchical navigable small world)** — a multi-layer graph where each node links to its near neighbours, with sparse long-range links in the upper layers. Search descends from a coarse layer to a fine one, following edges greedily. It is the best recall-versus-speed trade available and it is the default in most vector databases. The costs are build time and memory — at 100 million vectors the graph alone adds 12.8 GB on top of the vectors.

**Product quantisation** is orthogonal to all three: split each vector into sub-vectors, replace each with a codebook entry, and compare in compressed space. It combines with IVF or HNSW rather than competing with them.`},{type:`note`,label:`The dials, and what they cost`,md:`Every ANN index exposes the same trade in different clothing. HNSW has **M** (edges per node — more means better recall and more memory) and **efSearch** (how wide the search beam is — more means better recall and more time). IVF has **nlist** and **nprobe**.

**Recall@k is measurable and should be measured.** Take a sample of queries, compute the exact top-k with a flat index, and compare. Anything above 0.95 is usually fine for retrieval feeding a language model, because the reranker and the model both tolerate a little noise in the candidate list.

Two operational facts that decide architectures more often than the algorithm does:

**Deletion is hard in a graph index.** HNSW does not support true deletion — removed nodes are tombstoned and the graph degrades until it is rebuilt. A high-churn corpus is a genuine argument for IVF or for periodic rebuilds.

**Filtered search is the real difficulty.** "Nearest neighbours where tenant = X and date > Y" is not what these indexes are built for. Pre-filtering then searching breaks the graph's connectivity; searching then post-filtering can return nothing. Every vector database solves this differently and badly, and it is the first thing to test against your actual query shape.`},{type:`code`,lang:`python`,title:`Why reranking is affordable and one-stage retrieval is not`,code:`N, shortlist = 1_000_000, 50
print('bi-encoder over %s docs   : 1 query embedding + one ANN lookup' % f'{N:,}')
print('cross-encoder over %s docs: %s forward passes  <- impossible'
      % (f'{N:,}', f'{N:,}'))
print('cross-encoder over the top %d : %d forward passes  <- trivial'
      % (shortlist, shortlist))

# ---- real output ----
# bi-encoder over 1,000,000 docs   : 1 query embedding + one ANN lookup
# cross-encoder over 1,000,000 docs: 1,000,000 forward passes  <- impossible
# cross-encoder over the top 50 : 50 forward passes  <- trivial`,annotations:{2:`A bi-encoder embeds the query and the documents separately, so every document embedding is computed once, offline, and the query cost is one embedding plus a lookup.`,3:`A cross-encoder feeds the query and document through the model TOGETHER, so it sees their interaction and is markedly more accurate — but it cannot precompute anything, so it needs one forward pass per candidate.`,5:`Which is the entire architecture of a good retrieval system: a cheap bi-encoder to get 50 candidates from a million, then an expensive cross-encoder to order those 50. Accuracy where it is affordable, speed where it is not.`}},{type:`note`,label:`What embeddings alone cannot do`,md:`**Exact matches.** Product codes, error numbers, personal names, version strings — embeddings are built to blur exactly the distinctions these depend on. **Hybrid search**, combining the dense vector score with BM25 keyword matching, is not an optimisation but a repair, and it is standard for a reason.

**Recency and other metadata.** A vector has no notion of "the latest one". That has to come from filtering or from a scoring function applied afterwards.

**Negation.** "Documents not about pricing" embeds close to documents about pricing, because the meaningful words dominate the vector.

And the two operational traps: **the query and the documents must be embedded by the same model**, and re-embedding a corpus after a model upgrade is a migration, not a config change. Some models also require an asymmetric prefix — the query and the passage are embedded with different instructions — and getting that wrong degrades retrieval silently.`}],quiz:[{question:`At what scale does brute-force vector search stop working?`,options:[{text:`Around a thousand documents`,explanation:`At 1,000 it runs 65,000 queries a second.`},{text:`Around a million — 15.36 ms per query, or 65 queries a second on one core`,explanation:`Correct, and parallelism buys tens, not thousands.`},{text:`It never stops working`,explanation:`At 100 million it is 1.5 seconds per query.`},{text:`It depends only on dimension`,explanation:`Cost is n×d; n is what grows.`}],correct:1},{question:`At 100 million vectors, fp32 storage is 307.2 GB. What does that tell you?`,options:[{text:`You need a faster CPU`,explanation:`Memory, not compute, is the binding constraint at that point.`},{text:`Memory becomes the constraint before compute does, which is why product quantisation (96 bytes per vector, 9.6 GB) exists`,explanation:`Correct — a 32x reduction at a real but usually acceptable recall cost.`},{text:`The dimension should be reduced`,explanation:`A valid option, but PQ addresses it without changing the model.`},{text:`Brute force is fine at that scale`,explanation:`1.5 seconds per query is not fine.`}],correct:1},{question:`What is the characteristic failure of an IVF index?`,options:[{text:`It is too slow to build`,explanation:`It builds faster than HNSW.`},{text:`A query sitting between clusters may miss neighbours in the cluster it did not probe`,explanation:`Correct, and nprobe is the dial that trades recall for speed.`},{text:`It uses too much memory`,explanation:`It is the memory-light option.`},{text:`It cannot handle deletions`,explanation:`That is HNSW's weakness.`}],correct:1},{question:`Why is HNSW the default in most vector databases?`,options:[{text:`It is exact`,explanation:`It is approximate like the others.`},{text:`It gives the best recall-versus-speed trade available, at the cost of build time and memory — 12.8 GB of graph at 100M vectors`,explanation:`Correct, and its real weakness is deletion and filtered search.`},{text:`It uses the least memory`,explanation:`It uses the most of the three.`},{text:`It handles filtered queries natively`,explanation:`Filtering is precisely where graph indexes struggle.`}],correct:1},{question:`Why can a cross-encoder rerank 50 candidates but not search a million documents?`,options:[{text:`It is less accurate at scale`,explanation:`It is more accurate; the problem is cost.`},{text:`It processes query and document TOGETHER, so nothing precomputes — a million documents means a million forward passes`,explanation:`Correct, while a bi-encoder computes every document embedding once, offline.`},{text:`It requires a GPU`,explanation:`So does the bi-encoder for embedding.`},{text:`It cannot handle long documents`,explanation:`Both have length limits.`}],correct:1},{question:`Why is hybrid search with BM25 standard rather than optional?`,options:[{text:`It is faster`,explanation:`It adds a second search path.`},{text:`Embeddings blur exactly the distinctions that product codes, error numbers and version strings depend on — it is a repair, not an optimisation`,explanation:`Correct, and negation and recency are the other things vectors cannot express.`},{text:`BM25 is more accurate in general`,explanation:`It is worse semantically; the two are complementary.`},{text:`It reduces index size`,explanation:`It increases it.`}],correct:1}],interviewQuestions:[{question:`How does a vector database work?`,answer:`It stores embeddings and finds the nearest ones to a query, and the whole engineering problem is that exact search is O(n·d) — a million 768-dimensional vectors is 768 million multiply-accumulates per query, which is about 65 queries a second on one core. So every vector database trades exactness for speed with an approximate nearest-neighbour index. HNSW builds a multi-layer graph of near-neighbour links and searches greedily from coarse to fine; IVF clusters the vectors and probes only the nearest clusters; product quantisation compresses each vector so comparisons happen in a smaller space. The number that matters is recall@k against a flat baseline, and it is a dial rather than a property.`,isCaseBased:!1},{question:`When do you actually need one?`,answer:`Later than people build them. At a thousand documents brute force runs 65,000 queries a second and gives exact answers, so an index is pure complexity — a NumPy array and a matrix multiply is genuinely the right architecture. At a hundred thousand it is 1.54 ms a query, still comfortable. Around a million it becomes 15 ms and 65 queries a second on one core, and that is where an index earns its keep. Above ten million, memory starts binding before compute — 100 million fp32 vectors is 307 GB — and quantisation becomes necessary rather than optional. I would start flat and measure, because the exact answer is worth something and an approximate index is a permanent source of subtle bugs.`,isCaseBased:!0},{question:`HNSW or IVF?`,answer:`HNSW by default: it gives the best recall-versus-speed trade available and it is the default in most vector databases for that reason. The costs are build time, memory — at 100 million vectors the graph alone is 12.8 GB on top of the vectors — and, more importantly, that it does not support true deletion. Removed nodes are tombstoned and the graph degrades until rebuilt, so a high-churn corpus is a genuine argument for IVF. IVF is also the right answer when memory is tight or when you need fast index construction. In practice both are usually combined with product quantisation, so the choice is about the graph-versus-clustering structure rather than the compression.`,isCaseBased:!1},{question:`Explain the bi-encoder / cross-encoder split.`,answer:`A bi-encoder embeds the query and each document independently, so every document embedding is computed once offline and a search is one query embedding plus an index lookup. A cross-encoder feeds query and document through the model together, so it can see their interaction and is markedly more accurate — but nothing precomputes, so scoring a million documents means a million forward passes, which is impossible. That is exactly why retrieval systems are two-stage: a cheap bi-encoder plus ANN index to get 50 candidates out of a million, then a cross-encoder to order those 50, which is 50 forward passes and trivial. Accuracy where it is affordable, speed where it is not.`,isCaseBased:!0},{question:`What is the hardest operational problem with vector search?`,answer:`Filtered search — "nearest neighbours where tenant = X and date > Y". These indexes are not built for it. Pre-filtering and then searching breaks the graph's connectivity, because the surviving nodes may not be reachable from each other; searching and then post-filtering can return nothing at all if the filter is selective. Every vector database solves it differently and none solve it well, so it is the first thing I would test against the actual query shape rather than trusting a benchmark. The usual workarounds are partitioning the index by the high-cardinality filter, over-fetching before filtering, or maintaining separate indexes per tenant — all of which are architecture decisions, not configuration.`,isCaseBased:!0},{question:`Why is hybrid search standard?`,answer:`Because embeddings are built to blur exactly the distinctions some queries depend on. A product code, an error number, a personal name or a version string is a token whose value is its exactness, and a dense vector deliberately maps similar things together. Combining the dense score with BM25 keyword matching — usually via reciprocal rank fusion — recovers that, and it is a repair rather than an optimisation. The same limitation covers negation, where "documents not about pricing" embeds close to documents about pricing, and recency, where a vector has no notion of the latest one and the ordering has to come from metadata.`,isCaseBased:!1},{question:`How do you measure whether your index is good enough?`,answer:`Recall@k against a flat baseline, which is directly measurable: take a sample of real queries, compute the exact top-k with brute force, and see what fraction the index returned. Above 0.95 is usually fine for retrieval feeding a language model, because a reranker and the model both tolerate a little noise in the candidate list. But recall@k is an intermediate metric, and the one that matters is end-to-end: whether the answer was right. I would build a small evaluation set of question-and-expected-source pairs, measure recall of the correct source, and use that to tune chunking and the index dials — chunking usually moves it more than the index does.`,isCaseBased:!0},{question:`What happens when you upgrade the embedding model?`,answer:`It is a migration, not a configuration change. Query and document embeddings must come from the same model — vectors from two different models are not comparable at all — so the entire corpus has to be re-embedded and the index rebuilt, and for a large corpus that is a real project with a cost and a cutover plan. The usual approach is to build the new index alongside the old one, evaluate both on the same query set, and switch atomically. Two related traps: some models require an asymmetric prefix, embedding the query and the passage with different instructions, and getting that wrong degrades retrieval silently; and embedding dimension changes force a schema change in the store.`,isCaseBased:!1}],flashcards:[{front:`The scaling problem`,back:`Exact search is O(n·d). At d=768: 1,000 docs → 65,000 q/s; 1M → 65 q/s; 100M → 0.7 q/s.`},{front:`Memory binds first`,back:`100M fp32 vectors = 307.2 GB. int8 = 76.8 GB. Product quantisation at 96 bytes = 9.6 GB, a 32x cut.`},{front:`IVF`,back:`Cluster, probe only the nearest clusters. Cheap to build, memory-light. Fails on queries between clusters; nprobe is the dial.`},{front:`HNSW`,back:`Multi-layer near-neighbour graph, searched coarse to fine. Best recall/speed trade, the default. 12.8 GB of graph at 100M vectors.`},{front:`HNSW's real weakness`,back:`No true deletion — nodes are tombstoned and the graph degrades until rebuilt. High churn is an argument for IVF.`},{front:`Bi vs cross encoder`,back:`Bi: encode separately, precompute offline, one lookup. Cross: encode TOGETHER, more accurate, one forward pass per candidate.`},{front:`The two-stage architecture`,back:`Bi-encoder gets 50 from 1,000,000. Cross-encoder orders those 50. A million forward passes is impossible; 50 is trivial.`},{front:`What vectors cannot do`,back:`Exact matches (product codes, names, versions), recency, and negation. Hybrid BM25 is a REPAIR, not an optimisation.`}],mindmapMarkdown:`- Vector search at scale
  - The problem
    - exact search is O(n*d)
    - d=768: 1,000 -> 65,000 q/s
    - 1M -> 65 q/s; 100M -> 0.7 q/s
    - 100M fp32 = 307 GB: memory binds first
  - Index families
    - flat: exact, right below ~100k, the baseline
    - IVF: cluster + probe; nprobe is the dial
      - fails on queries between clusters
    - HNSW: layered near-neighbour graph
      - best recall/speed, the default
      - 12.8 GB graph at 100M
      - NO true deletion - tombstones, then rebuild
    - product quantisation: 768 dims -> 96 bytes
      - orthogonal, combines with either
  - The hard part
    - FILTERED search
    - pre-filter breaks graph connectivity
    - post-filter can return nothing
    - test it against your real query shape
  - Two-stage retrieval
    - bi-encoder: precompute offline, one lookup
    - cross-encoder: together, accurate, no precompute
    - 1,000,000 passes impossible; 50 trivial
  - What vectors cannot do
    - exact matches -> hybrid BM25 (a repair)
    - recency -> metadata
    - negation -> embeds close to the thing negated
    - same model for query and docs, always`};export{e as default};