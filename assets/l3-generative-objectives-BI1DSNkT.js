var e={id:`metrics-l3-generative-objectives`,subjectId:`metrics`,level:3,title:`Generative Objectives: Reconstruction, ELBO and GANs`,whyItMatters:`A classifier is shown the right answer and asked to match it. A model that generates new faces has no right answer — there is no single correct new face. Three objectives exist to train without one, and each is strange in a different way.`,assumes:[`You have met squared error and cross-entropy`,`You know that a log of a small number is a large negative number`],estMinutes:22,sections:[{type:`intuition`,title:`There is no target to compare against`,md:`Supervised learning has a label. Generation does not: asked for "a face", any of a billion outputs is correct, so there is nothing to subtract from.

Three objectives get around this, each by changing the question.

**Reconstruction** manufactures a target — compress the input, rebuild it, and compare against the input itself. **The ELBO** adds a second term that forces the compressed codes into a known shape you can sample from. **GANs** abandon comparison entirely and train a second network to tell real from fake, using its opinion as the loss.`},{type:`code`,lang:`python`,title:`Reconstruction loss on four pixels`,code:`x  = [0.9, 0.1, 0.8, 0.2]     # the real image
xh = [0.7, 0.3, 0.75, 0.35]   # what came back out of the bottleneck

errs = [(a - b) ** 2 for a, b in zip(x, xh)]
print('per-pixel squared error:', [round(e, 4) for e in errs])
print('reconstruction loss    :', round(sum(errs) / len(errs), 5))

# ---- real output ----
# per-pixel squared error: [0.04, 0.04, 0.0025, 0.0225]
# reconstruction loss    : 0.02625`,annotations:{2:`The bottleneck is the whole design: the network must squeeze the image through a code smaller than the image, so it cannot simply copy and has to learn what matters.`,6:`Ordinary MSE, with the input serving as its own label. That is the trick — no human annotation is needed anywhere.`,10:`Pixels 1 and 2 contribute 0.04 each while pixel 3 contributes 0.0025. Squared error means the loss is dominated by the few worst pixels, which is why autoencoder outputs come out blurry: an average grey is the safest way to avoid a large squared miss.`}},{type:`note`,label:`Why reconstruction alone cannot generate anything`,md:`A trained autoencoder rebuilds inputs well and still cannot produce a new image. Nothing in the objective says anything about codes it was never given.

The learned codes end up scattered wherever training happened to put them, with **holes between them**. Pick a random point in that space and decode it and you get noise, because that point corresponds to no training image and the decoder was never asked to make it mean anything.

To sample, you need the code space to have a **known shape**. That is exactly what the second ELBO term buys.`},{type:`math`,intro:`The ELBO, written as the thing you minimise. The first term is the reconstruction above. The second is the KL divergence between the code distribution the encoder produced — a Gaussian with mean μ and spread σ — and the standard normal you intend to sample from later. For that pair the KL has a closed form, which is the second line.`,latex:[`\\mathcal{L} = \\underbrace{\\mathbb{E}\\bigl[-\\log p(x \\mid z)\\bigr]}_{\\text{rebuild it}} \\;+\\; \\underbrace{D_{KL}\\bigl(q(z \\mid x)\\,\\|\\,\\mathcal{N}(0,1)\\bigr)}_{\\text{keep codes tidy}}`,`D_{KL} = \\tfrac{1}{2}\\bigl(\\mu^2 + \\sigma^2 - 1 - 2\\log\\sigma\\bigr)`]},{type:`code`,lang:`python`,title:`What the KL term actually punishes`,code:`import math

def kl(mu, sd):
    return 0.5 * (mu * mu + sd * sd - 1 - 2 * math.log(sd))

for mu, sd in [(0.0, 1.0), (0.5, 0.8), (0.0, 2.0), (0.0, 0.1), (3.0, 1.0)]:
    print('mu=%.1f  sigma=%.1f  ->  KL = %.4f' % (mu, sd, kl(mu, sd)))

# ---- real output ----
# mu=0.0  sigma=1.0  ->  KL = 0.0000
# mu=0.5  sigma=0.8  ->  KL = 0.1681
# mu=0.0  sigma=2.0  ->  KL = 0.8069
# mu=0.0  sigma=0.1  ->  KL = 1.8076
# mu=3.0  sigma=1.0  ->  KL = 4.5000`,annotations:{9:`Exactly 0 at μ = 0, σ = 1 and nowhere else. KL is zero only when the two distributions are identical, so this term has a single target and no way to cheat toward it.`,12:`σ = 0.1 costs 1.8076 — more than σ = 2.0 costs. Shrinking the spread is punished harder than widening it, and that asymmetry is the whole point: a near-zero σ is a plain autoencoder pretending to be a VAE.`,13:`μ = 3 costs 4.5, and it grows as μ². Codes cannot drift off to their own private corner of the space, which is what closes the holes and makes sampling work.`}},{type:`note`,label:`The tension, and the two ways it fails`,md:`The two terms pull against each other. Reconstruction wants every image to have its own distinct, precise code; the KL wants all codes squashed into one standard normal blob. A VAE is the compromise, and both extremes are real failure modes.

**Posterior collapse** — the KL term wins. Codes become indistinguishable, the decoder learns to ignore them, and the model produces the dataset average for every input. Watch for the KL term going to ~0 while reconstruction stops improving.

**Blurry samples** — reconstruction wins, but the fuzziness of sampling a code from a distribution rather than using a point means the decoder must produce something plausible for a whole neighbourhood. Averaging over a neighbourhood is what blur is. β-VAE exposes the trade directly by weighting the KL term.`},{type:`intuition`,title:`GANs: no reference at all`,md:`Both objectives so far compare an output to something. A GAN compares to nothing.

Instead, a second network — the **discriminator** — is trained to output the probability that an input is real. The **generator** is trained to make that probability high. Neither ever sees a target image for the thing it produced.

The loss is therefore not a fixed function: it is another network's current opinion, and it moves as that network learns. That is why GAN training is a game rather than a descent, and why it can oscillate instead of converging.`},{type:`code`,lang:`python`,title:`The discriminator loss, and why the obvious generator loss fails`,code:`def d_loss(d_real, d_fake):
    return -(math.log(d_real) + math.log(1 - d_fake))

print('discriminator winning (0.9, 0.1):', round(d_loss(0.9, 0.1), 4))
print('discriminator at chance (0.5, 0.5):', round(d_loss(0.5, 0.5), 4))

print('\\n  D    minimax slope   non-saturating slope   ratio')
for D in [0.05, 0.1, 0.5, 0.9]:
    print('%.2f %13.4f %20.4f %8.2f' % (D, 1 / (1 - D), 1 / D, (1 / D) * (1 - D)))

# ---- real output ----
# discriminator winning (0.9, 0.1): 0.2107
# discriminator at chance (0.5, 0.5): 1.3863
#
#   D    minimax slope   non-saturating slope   ratio
# 0.05        1.0526              20.0000    19.00
# 0.10        1.1111              10.0000     9.00
# 0.50        2.0000               2.0000     1.00
# 0.90       10.0000               1.1111     0.11`,annotations:{2:`Reward the discriminator for calling real data real and fake data fake. Nothing here references a target image — only two probabilities.`,5:`1.3863 is 2·ln(2), the loss when the discriminator is guessing. That is the value a healthy GAN's discriminator loss should sit near: it means the generator is good enough that real and fake are indistinguishable.`,13:`At D = 0.05 the generator is losing badly — and the original minimax loss log(1 − D) gives it a slope of 1.05, almost nothing. It has the least gradient exactly when it most needs one.`,16:`Swapping to −log D gives 20.0 at the same point, 19× the signal. This one-line change is what made GAN training practical; both losses have the same optimum, they just differ in where the gradient lives.`}},{type:`visual`,component:`Plot`,props:{title:`Gradient available to the generator, by how badly it is losing`,notice:`The two curves cross at D = 0.5 and are mirror images. The minimax loss is flat exactly where the generator is worst — slope 1.05 at D = 0.05 — so early training barely moves. The non-saturating loss puts its steepest gradient there instead, 20.0 at the same point. Same optimum, opposite gradient profile, and the reason the non-saturating form is what everyone actually uses.`,kind:`line`,xLabel:`D(G(z)) — discriminator's probability the fake is real`,yLabel:`gradient magnitude`,series:[{name:`minimax log(1−D)`,points:[[.05,1.0526],[.1,1.1111],[.2,1.25],[.3,1.4286],[.5,2],[.7,3.3333],[.8,5],[.9,10]]},{name:`non-saturating −log D`,points:[[.05,20],[.1,10],[.2,5],[.3,3.3333],[.5,2],[.7,1.4286],[.8,1.25],[.9,1.1111]]}]}},{type:`note`,label:`Which objective, and what replaced them`,md:`**Reconstruction** when you want compression, denoising or anomaly detection — a large reconstruction error is itself the useful signal. **VAE** when you need a code space you can sample from smoothly and can accept blur. **GAN** when sharpness is the product and you can tolerate unstable training and **mode collapse**, where the generator finds one convincing output and stops varying.

There is no loss curve to read in a GAN. Falling generator loss can mean a winning generator or a failing discriminator, so evaluation moves outside the objective entirely — FID and Inception Score compare feature statistics of real and generated sets.

Diffusion models now dominate image generation, and the reason is partly this: their objective is a plain regression on added noise, so training is stable descent, not a game.`}],quiz:[{question:`Why does generation need different objectives from supervised learning?`,options:[{text:`Generative models are larger`,explanation:`Size is incidental; the objective problem is structural.`},{text:`There is no single correct output to subtract from — asked for "a face", a billion outputs are equally right`,explanation:`Correct. Every objective here is a way of training without a target.`},{text:`Labels are too expensive`,explanation:`Even unlimited labels would not define one correct new face.`},{text:`Gradients cannot be computed for images`,explanation:`They can, and are, throughout.`}],correct:1},{question:`Why do autoencoder reconstructions come out blurry?`,options:[{text:`The bottleneck is too small`,explanation:`A wider bottleneck reduces blur but does not explain its character.`},{text:`Squared error is dominated by the worst pixels, so an average grey is the safest way to avoid a large miss`,explanation:`Correct — 0.04 versus 0.0025 in the worked example shows how one bad pixel outweighs several good ones.`},{text:`The decoder has too few layers`,explanation:`Depth changes capacity, not the incentive the loss creates.`},{text:`Pixels are averaged during encoding`,explanation:`Nothing in an autoencoder averages pixels directly.`}],correct:1},{question:`A trained autoencoder rebuilds inputs perfectly. Why can it still not generate a new image?`,options:[{text:`It needs more training`,explanation:`More training on the same objective changes nothing about unseen codes.`},{text:`Nothing in the objective constrains codes it was never given, so the code space has holes and a random point decodes to noise`,explanation:`Correct. That is precisely what the KL term is added to fix.`},{text:`The decoder is not invertible`,explanation:`Invertibility is not required for sampling.`},{text:`Reconstruction loss is not differentiable`,explanation:`MSE is perfectly differentiable.`}],correct:1},{question:`KL cost 1.8076 at σ = 0.1 but only 0.8069 at σ = 2.0. Why is that asymmetry deliberate?`,options:[{text:`It is an artefact of the log term with no purpose`,explanation:`The −2 log σ term is what creates it, and the effect is the point.`},{text:`A near-zero σ is a plain autoencoder pretending to be a VAE, so shrinking the spread must be punished harder`,explanation:`Correct. Collapsing σ removes the fuzziness that makes the code space samplable.`},{text:`Large σ is harmless`,explanation:`It is penalised too, just less.`},{text:`Because μ was 0 in both cases`,explanation:`True, which is exactly what isolates the σ effect.`}],correct:1},{question:`A GAN's discriminator loss settles near 1.3863. What does that tell you?`,options:[{text:`The discriminator has failed and training should be restarted`,explanation:`It is the healthy value, not a failure.`},{text:`It equals 2·ln(2) — the discriminator is guessing, meaning real and fake are indistinguishable`,explanation:`Correct, and it is the value a well-behaved GAN converges toward.`},{text:`The generator has collapsed to one output`,explanation:`Mode collapse is not visible in this number.`},{text:`The learning rate is too high`,explanation:`The value says nothing about the learning rate.`}],correct:1},{question:`At D = 0.05 the minimax generator loss has slope 1.05 and the non-saturating form 20.0. Why does that matter?`,options:[{text:`It makes the non-saturating loss converge to a different optimum`,explanation:`Both share the same optimum; only the gradient profile differs.`},{text:`The minimax loss is flattest exactly when the generator is losing worst, so early training barely moves`,explanation:`Correct — that is why the non-saturating form is universally used.`},{text:`A larger slope always trains faster`,explanation:`Not in general; what matters is having gradient where you need it.`},{text:`It prevents mode collapse`,explanation:`Mode collapse is a separate failure and this change does not fix it.`}],correct:1}],interviewQuestions:[{question:`Explain the VAE loss.`,answer:`Two terms. Reconstruction — encode the input to a distribution, sample a code from it, decode, and measure how far the result is from the input, so the input acts as its own label. And a KL divergence pushing that encoder distribution toward a standard normal. The second term is what makes sampling possible: a plain autoencoder scatters codes wherever training put them, leaving holes that decode to noise, whereas the KL forces them into a known shape you can draw from. The two terms genuinely fight — reconstruction wants distinct precise codes, KL wants them all squashed together — and a VAE is that compromise, which is why its samples are blurry.`,isCaseBased:!1},{question:`What is posterior collapse and how do you detect it?`,answer:`It is the KL term winning outright. The encoder outputs nearly the same distribution for every input, the codes carry no information, the decoder learns to ignore them, and the model produces something close to the dataset average for everything. The diagnostic is watching the two loss terms separately: KL heading toward zero while reconstruction stops improving is the signature. Fixes are KL annealing — start the KL weight at zero and ramp it — free-bits, which floors the KL per dimension so it cannot be driven to zero, or weakening the decoder so it cannot afford to ignore the code.`,isCaseBased:!0},{question:`Why is GAN training unstable?`,answer:`Because the loss is not a fixed function — it is another network's current opinion, and it moves as that network learns. You are looking for a Nash equilibrium between two players rather than descending a fixed landscape, so the pair can oscillate indefinitely without either being wrong. Two specific failures follow: the discriminator winning too completely, which flattens the generator's gradient, and mode collapse, where the generator finds one output that reliably fools the current discriminator and stops varying. Mitigations are the non-saturating loss, gradient penalties or spectral normalisation to keep the discriminator from getting too strong, and balancing update counts between the two.`,isCaseBased:!1},{question:`Why is the non-saturating generator loss used instead of the original minimax form?`,answer:`Gradient placement. Minimising log(1 − D(G(z))) gives the generator a slope of 1/(1 − D), which at D = 0.05 is about 1.05 — almost flat exactly when the generator is losing badly and needs the strongest signal. Maximising log D(G(z)) instead gives 1/D, which is 20.0 at the same point, roughly nineteen times the signal. The two have the same optimum; they differ only in where the gradient lives, and early in training the generator is always in the region where minimax provides nothing. It is a one-line change that made GAN training practical.`,isCaseBased:!0},{question:`How do you evaluate a generative image model?`,answer:`Not from the loss — in a GAN the loss is meaningless in isolation, since a falling generator loss can mean a winning generator or a failing discriminator. The standard is FID: pass real and generated images through an Inception network, fit a Gaussian to each set of features, and measure the distance between them, so it captures both quality and diversity, with lower better. Inception Score is the older alternative and is blind to mode collapse against the real distribution. Precision and recall for generative models split the two concerns — realism versus coverage — which is more diagnostic. And human evaluation remains the reference, because FID is known to disagree with perceived quality in specific regimes.`,isCaseBased:!1},{question:`Your VAE produces blurry images. What do you do?`,answer:`First accept that some blur is intrinsic: the decoder must produce something plausible for a whole neighbourhood of codes, and averaging over a neighbourhood is what blur is. Then the levers. Lower the KL weight — β-VAE exposes this trade directly — at the cost of a less samplable code space. Swap the pixel-wise reconstruction term for a perceptual loss computed in feature space, since squared error over pixels is what rewards the safe grey average. Increase latent capacity. Or use a VQ-VAE, which replaces the continuous Gaussian code with discrete codebook entries and sidesteps the averaging. If sharpness is genuinely the product, a VAE may simply be the wrong family.`,isCaseBased:!0},{question:`Why have diffusion models displaced GANs?`,answer:`Mostly because of the objective. A diffusion model is trained to predict the noise added to an image at a known timestep, which is a plain regression with a fixed target — stable descent, not a two-player game. So there is no adversarial instability and no mode collapse: the training objective covers the whole data distribution by construction, whereas a GAN generator is rewarded only for fooling the current discriminator. The cost is sampling, which needs many sequential denoising steps rather than one forward pass, and a great deal of recent work is about reducing that step count.`,isCaseBased:!1},{question:`When would you still choose reconstruction loss on its own?`,answer:`When the error is the product rather than the samples. Anomaly detection is the clearest case: train an autoencoder on normal data only, and anything that reconstructs badly is unlike what it was trained on, so the reconstruction error is directly the anomaly score. Denoising and compression are the other two — there the target genuinely exists and the model is not being asked to generate anything new. The moment you need to sample, plain reconstruction is the wrong objective, because it says nothing about codes the model was never given.`,isCaseBased:!1}],flashcards:[{front:`Why generation needs new objectives`,back:`No single correct output to subtract from. Reconstruction manufactures a target, the ELBO adds a shape constraint, GANs replace comparison with a second network.`},{front:`Reconstruction loss`,back:`MSE with the input as its own label. Four pixels: errors [0.04, 0.04, 0.0025, 0.0225], loss 0.02625.`},{front:`Why autoencoders are blurry`,back:`Squared error is dominated by the worst pixels, so an average grey is the safest way to avoid a large miss.`},{front:`Why reconstruction cannot generate`,back:`Nothing constrains codes the model was never given. The code space has holes; a random point decodes to noise.`},{front:`The KL term`,back:`½(μ² + σ² − 1 − 2 log σ). Exactly 0 at μ=0, σ=1. σ=0.1 costs 1.8076 — more than σ=2.0 at 0.8069.`},{front:`Posterior collapse`,back:`KL wins: codes become indistinguishable, decoder ignores them, output is the dataset average. Watch KL → 0 while reconstruction stalls.`},{front:`Healthy GAN discriminator loss`,back:`≈1.3863 = 2·ln(2) — the discriminator is guessing, so real and fake are indistinguishable.`},{front:`Non-saturating generator loss`,back:`At D=0.05, minimax slope 1.05 vs −log D slope 20.0 — 19× the signal exactly when the generator is losing. Same optimum, different gradient profile.`}],mindmapMarkdown:`- Generative objectives
  - The problem
    - no single correct output to subtract from
  - Reconstruction
    - input is its own label, MSE through a bottleneck
    - four pixels: [0.04, 0.04, 0.0025, 0.0225] -> 0.02625
    - blurry because squared error fears the worst pixel
    - CANNOT generate: code space has holes
    - still right for anomaly detection / denoising
  - ELBO (VAE)
    - rebuild + KL(q || N(0,1))
    - KL = 0.5(mu^2 + sd^2 - 1 - 2 log sd)
    - 0 only at mu=0, sd=1
    - sd=0.1 costs 1.8076 > sd=2.0 at 0.8069
    - mu=3 costs 4.5, grows as mu^2
    - failure 1: posterior collapse (KL wins)
    - failure 2: blur (averaging over a neighbourhood)
  - GAN
    - loss is another network's opinion, so it moves
    - healthy D loss ~ 1.3863 = 2 ln 2
    - minimax slope 1.05 at D=0.05 -> flat when losing
    - non-saturating -log D gives 20.0, 19x
    - mode collapse; evaluate with FID, not the loss
  - After
    - diffusion: plain regression on noise, stable descent`};export{e as default};